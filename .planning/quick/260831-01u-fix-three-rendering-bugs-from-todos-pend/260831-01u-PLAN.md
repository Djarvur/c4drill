---
phase: quick-260831-01u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - internal/graph/graph.go
  - internal/view/view.go
  - internal/view/scope.go
  - internal/render/converter.go
  - internal/render/converter_test.go
  - cmd/c4drill/root.go
  - cmd/c4drill/root_test.go
  - cmd/c4drill/testdata/deepcross.toml
  - cmd/c4drill/testdata/deepcross.dot
  - cmd/c4drill/testdata/nolabels.dot
  - cmd/c4drill/testdata/nolabels.expanded.dot
  - README.adoc
  - skill/SKILL.md
  - plugins/c4drill/skills/c4drill-toml/SKILL.md
  - plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md
autonomous: true
requirements: [BUG-1-ROOT-COMPACT, BUG-2-NO-LABELS-EDGE-ONLY, BUG-3-EDGE-MERGE-PRE-FLAG]
must_haves:
  truths:
    - "Non-expanded root (C1) diagram is compact again: it depicts only top-level units, author-expanded units' visible subunits, boundary nodes, and CTX-02 deep-link chain paths — never whole sibling subtrees or the entire model (proven on the new deepcross fixture)"
    - "C2/C3 drill-down views keep WRAP-01/02 ancestor wrapper clusters and all committed wrap/invariance tests stay green"
    - "--no-labels suppresses ONLY edge label text; node, cluster (boundary + wrapper + expanded), and legend labels remain visible"
    - "Formatting flags (--no-labels, --no-colors, --no-styles, --no-rank, --no-length, --plain, and compositions) never change edge count, endpoints, or multiplicity thickness — only styling"
    - "All golden fixtures pass: nolabels goldens re-baselined to the new semantics, new deepcross.dot golden pins the compact root"
  artifacts:
    - path: "cmd/c4drill/testdata/deepcross.toml"
      provides: "Fixture reproducing the reported shape: deep nesting, cross-container links, external actors deep-linking into nested subunits, properties.expanded"
    - path: "cmd/c4drill/testdata/deepcross.dot"
      provides: "Compact-root golden pinning the fixed C1 output (canonical, DI-1 comparison)"
    - path: "internal/graph/builder.go"
      provides: "Edge-label-only NoLabels guards; unique flag-independent Edge identity source"
    - path: "internal/render/converter.go"
      provides: "Converter uses builder-assigned edge names (no label-derived names); node/cluster labels survive --no-labels"
    - path: "internal/graph/builder_test.go"
      provides: "Compact-root invariant test, edge-labels-only NoLabels tests, flag-invariant edge topology test"
  key_links:
    - from: "internal/graph/builder.go (createEdge)"
      to: "internal/render/converter.go (createEdge/CreateEdgeByName)"
      via: "builder-assigned Edge.Name used verbatim as cgraph edge name"
      pattern: "CreateEdgeByName\\(edge\\.Name"
    - from: "cmd/c4drill/root.go:112"
      to: "internal/render/converter.go createEdge (635-639) + internal/graph/builder.go createEdge (1390-1398)"
      via: "--no-labels threads to RenderOpts.NoLabels; only edge-label emission sites consult it"
      pattern: "NoLabels"
    - from: "internal/view/scope.go (GenerateC1View/ensureDeepLinkChain)"
      to: "internal/graph/builder.go (buildC1ViewGraph/buildCluster)"
      via: "C1 root renders chain paths without unfolding whole sibling subtrees"
      pattern: "UnfoldChain|VisiblePaths"
---

<objective>
Fix the three rendering bugs captured in .planning/todos/pending/ (2026-08-30), in severity order:

1. **BUG-1-ROOT-COMPACT** (regression, most severe): the non-expanded root diagram prints the whole model instead of a compact C1 view (55 → 260 titles on the reporter's model; reported culprit commit b2447da).
2. **BUG-2-NO-LABELS-EDGE-ONLY**: `--no-labels` suppresses ALL labels; it must suppress only edge labels (semantics locked by the planning context — option 1 of the todo: narrow semantics, keep the flag name).
3. **BUG-3-EDGE-MERGE-PRE-FLAG**: formatting flags change which arrows merge — edge identity must be computed from pre-flag, model-derived attributes so flags stay pure presentation switches.

Purpose: the default render is corrupted (Bug 1), and two presentation flags silently alter graph semantics (Bugs 2/3). All three shipped in the v1.21.0..v1.22.0 window; the fixture corpus lacks the structures that trigger them, so new fixtures and golden pins are part of every fix.

Output: green `go test ./... -count=1` with new RED→GREEN tests per bug, re-baselined nolabels goldens, new deepcross fixture + compact-root golden, updated docs (README.adoc + three SKILL.md copies).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-30-fix-root-diagram-bloated-by-ancestor-wrapping.md
@.planning/todos/pending/2026-08-30-fix-no-labels-to-suppress-only-edge-labels.md
@.planning/todos/pending/2026-08-30-fix-edge-merge-to-compare-pre-flag-attributes.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-01-SUMMARY.md
@.planning/STATE.md

Source (read once each; line numbers verified on HEAD 98186a0):
@internal/graph/builder.go
@internal/graph/graph.go
@internal/view/scope.go
@internal/render/converter.go
@cmd/c4drill/root.go
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from HEAD; no exploration needed. -->

From internal/graph/builder.go — dispatch (BuildGraph, lines 56-62):
```go
if v.Level != view.LevelC1 && v.ExpandedUnit != "" {
    buildBoundaryViewGraph(v, g)   // C2/C3 — WRAP-01/02 wrapping lives here
} else {
    buildC1ViewGraph(v, g)         // ROOT diagram — the Bug 1 surface
}
```

From internal/graph/builder.go — edge dedup (D-01/D-02, pair-based, PRE-flag):
```go
edgeKey := path + "->" + link.Peer            // resolved views (C1/C2/C3)
if v.AllExpanded { edgeKey += ":" + link.Technology + ":" + link.Description }  // COMPAT-02
// markSeen(seen, edgeKey) dedups; pairCounts >= 2 → penWidth 2.0 (D-04)
```

From internal/graph/builder.go — LBL-01 suppression sites (Bug 2 targets):
lines 197-202 (ensureWrapperCluster), 291-299 (buildBoundaryCluster),
404-409 (buildNestedCluster), 464-486 (buildNode), 843-848 (buildCluster),
1390-1398 (createEdge edge label — THIS ONE STAYS).

From internal/render/converter.go — the Bug 3 mechanism:
```go
// createEdge, lines 612-627:
edgeName := edge.Source + "_to_" + edge.Target
if edge.Label != nil && edge.Label.Description != "" {
    edgeName += "_" + sanitizeForName(edge.Label.Description)  // flag-suppressible!
}
e, err := cg.CreateEdgeByName(edgeName, tail, head)
// CreateEdgeByName passes create-flag 1 to cgraph agedge = FIND-OR-CREATE:
// a name collision between the same endpoints returns the EXISTING edge
// → the second builder edge silently merges into the first.
```

From internal/render/converter.go — remaining Bug 2 targets:
setNodeLabel lines 410-415 (`if opts.NoLabels { cn.SetLabel("") }`),
setClusterLabel lines 526-527 (`if opts.NoLabels { subgraph.SetLabel("") }`),
createEdge lines 635-639 (edge label suppression — STAYS).

From cmd/c4drill/root.go — flag threading (LBL-02):
lines 333-338 (processView) and 386-391 (processExpandedView) set
v.NoLabels = noLabels AFTER view generation, BEFORE graph build.
Flag registration + help text: line 112-113.

From internal/view/scope.go — Bug 1 suspects:
ensureDeepLinkChain (374-426): registers deep-link chain entries + sets
UnfoldChain on the depicted ancestor when !IsExpanded.
buildC1ViewGraph (builder.go 257-277): `(entry.IsExpanded || entry.UnfoldChain) && len(Subunits) > 0`
→ buildCluster → children with subunits recurse via buildNestedCluster (WHOLE subtree).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-1 — Restore the compact C1 root (repro fixture, bisection, fix, golden pin)</name>
  <files>cmd/c4drill/testdata/deepcross.toml, cmd/c4drill/testdata/deepcross.dot, internal/graph/builder_test.go, internal/view/scope.go, internal/graph/builder.go, cmd/c4drill/root_test.go</files>
  <behavior>
    - The new compact-root invariant test builds the C1 view of the deepcross fixture and asserts the root graph depicts ONLY: top-level units, the author-expanded unit's direct visible subunits, external/boundary nodes, and (where a deep link fires CTX-02) the chain path to the true target — with an exact expected node count derived by hand from the fixture. On unfixed HEAD this test MUST fail (RED).
    - The invariant test additionally asserts no whole sibling subtree appears in the root: no node whose path is a non-chain descendant deeper than the compact set above.
    - All committed WRAP tests (TestBoundaryEntryWrappedInAncestorChain, TestFullyExternalBoundaryStaysTopLevel, TestWrapperClustersHaveNoExploreURL, TestBoundaryViewNodeSetInvariant, TestEdgeEndpointsUnchangedByWrapping) and all CTX-02 tests from phase 37 stay green after the fix.
    - TestDeeplinkRootCompactGolden locks the root DOT of the deepcross fixture against testdata/deepcross.dot (canonical order-insensitive comparison, same helpers as TestPlainFlagC1Golden).
  </behavior>
  <action>
    TDD: write the fixture + failing invariant test FIRST, commit RED, then diagnose, fix, commit GREEN.

    1. Create `cmd/c4drill/testdata/deepcross.toml` reproducing the reported shape (todo card: "cross-container links + expanded: [actors, yic]" — 55 vs 260 titles, root ≈ expanded). Model shape requirements: at least two top-level systems with 3-level nesting (system → container → component), external person/actor units linking (including linkFrom) to DEEP subunits of those systems, at least one cross-container link between subunits of different systems, and `expanded` in [properties] naming one nested-heavy system. Vary ingredients until the bug reproduces (see step 2) — the fixture corpus is proven blind to this regression, so reproducing it IS the deliverable.

    2. EMPIRICAL BISECTION — do not trust the todo's b2447da attribution: static analysis of b2447da shows it touches ONLY buildBoundaryViewGraph (C2/C3 dispatch), while the root diagram renders through buildC1ViewGraph, which is behaviorally identical between v1.21.0 and v1.22.0 for no-flag renders (verified: the v1.21.0..v1.22.0 builder diff for C1 paths is flag-threading only, and scope.go is untouched). Prime alternative suspect: CTX-02 (commit c065c07, phase 37, v1.21.0) — ensureDeepLinkChain marks collapsed ancestors UnfoldChain and buildCluster/buildNestedCluster then unfolds ENTIRE sibling subtrees into the root, not just the chain path. Procedure: build the fixture render at HEAD and at v1.21.0 (and, if needed, walk further back via `git bisect` between v1.20.0-era tags and HEAD), counting root DOT node statements + `subgraph cluster_` occurrences per commit (`c4drill testdata/deepcross.toml -f dot -o tmpdir`, grep the root .dot). The commit that inflates the count is the culprit. Record the finding in the summary — it corrects or confirms the todo's attribution.

    3. Fix whatever the bisect identifies, under these PINNED constraints: (a) the non-expanded root stays a compact C1 — chain insertion may unfold ONLY the chain path to the true link target, never sibling subtrees (if CTX-02 is the culprit, the fix is in how buildCluster/buildNestedCluster or the C1 view renders UnfoldChain entries: render exclusively the VisiblePaths chain entries under the unfolded ancestor, or register the chain as wrapper-style clusters containing only chain nodes — preserve the CTX-02 contract that edges reach the TRUE target and phase-37 CTX-02 tests stay green); (b) WRAP-01/02 C2/C3 wrapping is untouched; (c) node IDs, edge endpoints, and dedup keys stay untouched (WRAP-03 lock). If the bisect instead implicates a 38 commit, fix that hunk directly.

    4. Re-baseline nothing blindly: existing goldens covered C1/expanded and passed post-38 (38-01 census) — if any now fail, the compact-root fix changed legitimately pinned output; re-baseline ONLY with a eyeball-check of the diff recorded in the summary.

    5. Generate `cmd/c4drill/testdata/deepcross.dot` from the FIXED binary (root diagram, `-f dot`) and add TestDeeplinkRootCompactGolden in cmd/c4drill/root_test.go following the TestPlainFlagC1Golden pattern (generateFixtureOutput + readOutputFile + canonical comparison).

    Per the todo's item 4 (re-run of the 93.8 Mpt² comparison): out of scope here — the reporter's model is not in the repo; the fixture-based invariant test is the in-repo proxy. Note this in the summary.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestBoundary|TestWrapper|TestEdgeEndpoints|TestDeeplinkRootCompact|TestUnfold' -count=1 && go test ./cmd/c4drill/ -run 'TestDeeplinkRootCompactGolden' -count=1 && go test ./... -count=1</automated>
  </verify>
  <done>deepcross fixture reproduces the bloat on the pre-fix commit (documented), the invariant test is green on the fixed HEAD, bisect culprit recorded, deepcross.dot golden committed and passing, full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-2 — Narrow --no-labels to edge labels only</name>
  <files>internal/graph/builder.go, internal/graph/builder_test.go, internal/graph/graph.go, internal/view/view.go, internal/render/converter.go, internal/render/converter_test.go, cmd/c4drill/root.go, cmd/c4drill/root_test.go, cmd/c4drill/testdata/nolabels.dot, cmd/c4drill/testdata/nolabels.expanded.dot, README.adoc, skill/SKILL.md, plugins/c4drill/skills/c4drill-toml/SKILL.md, plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md</files>
  <behavior>
    - Under --no-labels, nodes carry full labels (name, technology, description, icons 🔍/📖) — flip TestNoLabelsNodesAreBareShapes (builder_test.go:4265) to assert labels PRESENT; it must fail before the fix (RED).
    - Under --no-labels, all cluster kinds carry labels: wrapper clusters (ensureWrapperCluster), boundary cluster (buildBoundaryCluster), expanded/nested clusters (buildNestedCluster/buildCluster) — flip TestNoLabelsClustersUnlabelled (builder_test.go:4309) accordingly.
    - Under --no-labels, edges carry NO label (Technology/Description dropped) — TestNoLabelsEdgesHaveNoLabel (builder_test.go:4296) stays green unchanged.
    - Legend stays under --no-labels (LBL-03 pin unchanged) — TestNoLabelsLegendStays stays green.
    - E2E: TestNoLabelsAllGenerationsAndFormats / TestNoLabelsComposesWithPlainAndSwitches / TestNoLabelsOptIn assert edge-label markup ABSENT while node/cluster label markup PRESENT across C1/drill-down/--expanded × dot/svg/html.
  </behavior>
  <action>
    TDD: flip the builder tests first (RED), then implement (GREEN).

    1. builder.go — REMOVE the NoLabels guards so node/cluster label construction is unconditional again: ensureWrapperCluster (197-202 — restore `label = wrapperLabel(path, v)` unconditionally), buildBoundaryCluster (291-299), buildNestedCluster (404-409), buildNode (464-486 — delete the NoLabels early-return branch), buildCluster (843-848). KEEP the edge-label guard in createEdge (1390-1398) — edge labels are the one thing --no-labels still suppresses. Rewrite the LBL-01 comments at each site to state the new semantics (edge labels only) — these comments are the living decision record superseding the phase-38 LBL-01 pin (LBL-03 legend pin is unaffected and stays).

    2. converter.go — setNodeLabel (410-415): delete the NoLabels empty-label branch (keep the nil-Label guard and the Plain branch). setClusterLabel (526-527): delete the NoLabels branch (keep Plain + nil guards, keep the URL emission). createEdge (635-639): KEEP the NoLabels → SetLabel("") defense-in-depth branch, update its comment. Defense-in-depth at the edge site stays per the original LBL-01 layering.

    3. Docs on the types: graph.go RenderOpts.NoLabels field comment (lines 54-57) and view.go's NoLabels field comment — "edge label text only; node/cluster/legend labels unaffected".

    4. cmd/c4drill/root.go:112-113 — help text: "Suppress edge label text only: nodes, clusters and legend keep their labels". Threading (333-338, 386-391) unchanged.

    5. Re-baseline goldens: regenerate nolabels.dot and nolabels.expanded.dot from the fixed binary (they were additive 38-04 goldens; their content now changes by design — node/cluster labels return, edge labels stay gone). Update the "shape-only" wording in the golden test comments.

    6. Update KEY-03 E2E assertions in root_test.go (TestNoLabelsAllGenerationsAndFormats, TestNoLabelsComposesWithPlainAndSwitches, TestNoLabelsOptIn): edge-label absence checks stay, node/cluster-label absence checks invert to presence.

    7. Docs, all four files (keep the three SKILL.md copies in sync — 37-06 precedent): README.adoc (--no-labels mentions) and skill/SKILL.md + plugins/c4drill/skills/c4drill-toml/SKILL.md + plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md lines ~1006-1017: replace "shape-only: no element label text" with edge-label-only semantics; keep the note that it composes with --plain and that the legend stays.

    Note for Task 3: after this change --no-labels still nils edge.Label in the builder, so converter-level edge identity must not depend on label content — that is exactly Task 3's fix; do not pre-empt it here beyond keeping the existing edge sites intact.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestNoLabels' -count=1 && go test ./internal/render/ -count=1 && go test ./cmd/c4drill/ -run 'TestNoLabels' -count=1 && go test ./... -count=1</automated>
  </verify>
  <done>--no-labels mutes only edge labels everywhere (builder + converter), nolabels goldens re-baselined and passing, KEY-03 E2E green with the new semantics, docs updated in all four files, full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BUG-3 — Flag-invariant edge identity (pre-flag merge key, no label-derived names)</name>
  <files>internal/graph/builder.go, internal/graph/builder_test.go, internal/render/converter.go, internal/render/converter_test.go</files>
  <behavior>
    - New invariant test (e.g. TestEdgeTopologyFlagInvariant): for a fixture model with (a) two links between the same pair differing in technology+description, (b) two links same pair same description different technology, (c) links with distinct colors/kinds on one pair, (d) a mirror-synthesized linkFrom pair — assert the emitted DOT edge-statement multiset (ordered endpoint pairs) is IDENTICAL across: no flags, --no-labels, --no-colors, --no-styles, --plain, and --no-labels+--no-colors — for the expanded view AND the C1 view. On HEAD this FAILS (RED) for at least the expanded same-description case: converter.go builds edgeName from edge.Label.Description, and CreateEdgeByName is find-or-create, so same-description parallel edges collapse into one drawn edge; under --no-labels every label is nil so ALL parallel edges on a pair collapse to `src_to_dst`.
    - The same test asserts penwidth multiplicity display is identical across flag sets (flags never change which pairs show thickness 2.0).
  </behavior>
  <action>
    TDD: invariant test first (RED), then the identity fix (GREEN).

    1. Pin the canonical merge semantics (this decides todo item 2 — keep, do not change): the builder's pair-based key `path + "->" + link.Peer` (D-01/D-02) in resolved views and the tech+description key in expanded mode (COMPAT-02) REMAIN the merge semantics — they are computed purely from model data before any flag is consulted, which is exactly the "pre-flag attributes" invariant the todo demands; flag-suppressible attributes (color/label/style) deliberately do NOT belong in the key. Post-merge thickness/multiplicity display stays as-is (D-04, AGG aggregates, goldens pin it). Update the D-01/D-02 comment blocks at builder.go:1225-1229 and 1271-1276 to state the invariant explicitly: "format-disabling flags never change edge topology — only styling".

    2. Fix the identity mechanism: add a `Name` field to graph.Edge (graph.go) and have the builder populate it with a unique, model-derived, flag-independent cgraph-safe identifier at Edge construction — format `"{source}_to_{target}_{n}"` where n is a per-(source,target) sequence number from the pre-dedup link walk (unit paths contain dots; that is fine, graphviz quotes identifiers — but sanitize to [A-Za-z0-9_] using the existing sanitizeForName helper logic moved/reused for the path segments). In converter.go createEdge (612-627), replace the label-derived edgeName with the builder-supplied edge.Name verbatim (keep a sanitize pass for hand-built graphs whose Name is empty: fall back to the current construction). Every builder Edge then maps to its own cgraph edge regardless of which flags suppressed which attributes — this also future-proofs Task 2's semantics, where --no-labels nils edge labels permanently.

    3. Audit the upstream layers named in the todo for flag-independence and note the reconciliation in the summary: validator mirror synthesis (internal/validator/index.go populateIncomingLinks — Mirror-flagged copies are excluded from pair counting, WR-02 — pre-flag by construction) and view-layer resolution (scope.go ResolvedLinks — pre-flag by construction). No changes expected there; the audit closes the todo's "second comparison layer" question.

    4. Re-baseline any goldens whose DOT text changes from the edge renaming — first diff and eyeball the canonical comparison output (edge names are usually not emitted in DOT text, so expect zero-delta; if delta appears, verify it is only naming/attribute-carrier changes and record it).

    5. Keep TestBuildEdgesPairCollapse, TestBuildEdgesPenwidth*, TestPairAggregation, TestBuildEdgesExpandedExemption green — the fix must not alter merge outcomes, only drawn-edge identity.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestEdgeTopologyFlagInvariant|TestBuildEdges|TestPairAggregation' -count=1 && go test ./internal/render/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <done>Edge topology (count, endpoints, multiplicity) is bit-identical across all flag compositions on C1 and expanded views (pinned by test), converter edge names come from the builder and never from flag-suppressible label content, merge semantics documented as pair-based pre-flag, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model file → cgraph identifiers | Author-controlled unit paths/labels flow into graphviz edge/node names and attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1-01 | Tampering | converter.go createEdge name construction | mitigate | Builder-assigned Edge.Name sanitized to [A-Za-z0-9_] (reuse sanitizeForName); no raw author text interpolated into cgraph names |
| T-Q1-02 | Tampering (aliasing) | CreateEdgeByName find-or-create semantics | mitigate | Unique per-edge names eliminate silent edge-object reuse (the Bug 3 merge itself is a graph-construction integrity bug) |
| T-Q1-03 | Information disclosure | label content in DOT/SVG | accept | Unchanged from status quo — labels already render escaped via SetLabel/SetLabelHTML paths (T-37-07 escaping); this plan alters which labels appear, not the escaping |
| T-Q1-SC | Tampering | npm/pip/cargo installs | accept | None — no package installs in this plan; stdlib + existing go-graphviz only |
</threat_model>

<verification>
- `go test ./... -count=1` fully green after each task (repo TDD norm: RED commit → GREEN commit per bug).
- Bug 1: deepcross fixture demonstrably bloats the root on the pre-fix commit and is compact on HEAD; deepcross.dot golden passes; WRAP + CTX-02 suites green.
- Bug 2: nolabels goldens re-baselined; node/cluster labels survive --no-labels in all generations and formats; edge labels do not; legend stays.
- Bug 3: emitted DOT edge multisets identical across flag matrices; no label-derived edge names remain in converter.go (grep `sanitizeForName(edge.Label.Description)` returns nothing).
- Docs: `grep -rn "no-labels" README.adoc skill/SKILL.md plugins/c4drill/skills/c4drill-toml/SKILL.md plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md` shows edge-label-only wording everywhere.
</verification>

<success_criteria>
1. Non-expanded root is a compact C1 on the repro fixture, pinned by a committed golden and an exact node-set invariant test; regression attribution corrected or confirmed by recorded bisection evidence.
2. --no-labels = edge labels only, everywhere (builder, converter, help text, docs ×4, goldens).
3. Formatting flags provably never change edge topology; edge identity is model-derived and flag-independent.
4. Full suite green; every fix landed test-first (RED→GREEN commits per repo norm).
</success_criteria>

<output>
Create `.planning/quick/260831-01u-fix-three-rendering-bugs-from-todos-pend/260831-01u-SUMMARY.md` when done, recording: the Bug 1 bisect verdict (confirms or corrects the b2447da attribution), the canonical edge-merge-key reconciliation (todo Bug 3 item 2), the LBL-01 supersession, and any golden re-baseline diffs eyeballed.
</output>
