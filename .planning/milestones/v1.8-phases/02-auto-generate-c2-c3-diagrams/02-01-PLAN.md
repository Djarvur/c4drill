---
phase: 02-auto-generate-c2-c3-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - internal/view/scope_test.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements: [VIEW-03, VIEW-04, VIEW-05]
must_haves:
  truths:
    - "A top-level unit listed in properties.expanded with NO subunits renders as a plain collapsed node in C1 — no empty cluster box (D-07)"
    - "Any unit with subunits gets a sub-diagram file, C1 boxes included: {basename}/boxname.svg and {basename}/system/cbox.svg (D-01/D-02/D-03)"
    - "properties.expanded expands a top-level unit in C1 even without per-unit self-reference (D-05); entries matching no top-level unit are silently ignored (D-06)"
    - "A per-unit expanded container renders its components as a cluster inside its system's C2 diagram (D-04 second half)"
    - "Linked actors (personExternal) appear as external boundary nodes in C2 views (D-08)"
    - "Phase 1 WR-01 behavior intact: C2/C3 boundary-cluster branch (builder.go:30-55) untouched; penwidth tests stay green"
  artifacts:
    - path: "internal/graph/builder.go"
      provides: "D-07 guard in C1 branch"
      contains: "len(entry.Unit.Subunits) > 0"
    - path: "internal/graph/builder_test.go"
      provides: "D-07 RED/GREEN test + D-04 second-half C2 cluster test"
      contains: "func TestBuildGraphExpandedEmptyUnitRendersPlainNode"
    - path: "internal/view/scope_test.go"
      provides: "D-05 OR / D-06 silent-ignore / D-08 actor boundary tests"
      contains: "func TestGenerateC2View_ActorBoundaryFromSubunitLinks"
    - path: "cmd/c4drill/root_test.go"
      provides: "D-01/D-02/D-03 box + containerBox sub-diagram file test"
      contains: "func TestFullPipeline_BoxWithSubunitsGeneratesSubDiagram"
  key_links:
    - from: "internal/graph/builder.go BuildGraph C1 branch (:68)"
      to: "buildNode (plain node fallback when guard false)"
      via: "two-condition expansion guard"
      pattern: "IsExpanded && len\\(entry\\.Unit\\.Subunits\\) > 0"
    - from: "internal/view/scope.go GenerateC1View (:120)"
      to: "isExpandedInC1 (:182-188)"
      via: "OR semantics (properties.expanded ∪ unit.Expanded self-ref)"
      pattern: "isExpandedInC1"
    - from: "cmd/c4drill/root.go collectExpandableUnitPaths (:170)"
      to: "internal/output/writer.go dotted-path layout (:37-47)"
      via: "{basename}/{unit}.{format} file naming (D-03)"
      pattern: "strings.ReplaceAll\\(unitPath"
---

<objective>
Refine the already-implemented C2/C3 sub-diagram generation: implement the single production change D-07 (expanded-but-empty top-level unit renders as a plain node, not an empty cluster box) and lock the seven confirmed behaviors (D-01..D-06, D-08) with regression tests, closing the Wave 0 test gaps identified in RESEARCH.md and 02-PATTERNS.md.

Purpose: VIEW-03/VIEW-04/VIEW-05 are implemented and tested in v1.8; this phase makes the confirmed behavior explicit (box sub-diagram file test, actor boundary node in C2, OR/silent-ignore expansion semantics, per-unit expanded clusters in C2/C3) and fixes the one genuine refinement (D-07) at its single agreed point — the C1 branch of BuildGraph (internal/graph/builder.go:68).

Output: One-condition guard change in internal/graph/builder.go; 4 new test functions across internal/graph/builder_test.go, internal/view/scope_test.go, cmd/c4drill/root_test.go; SUMMARY at .planning/phases/02-auto-generate-c2-c3-diagrams/02-01-SUMMARY.md.

This is a REFINEMENT phase — the auto-detect, view generation, and navigation machinery already exist and pass tests. Do NOT restructure; do NOT touch internal/view/scope.go (Phase 1 WR-01 penwidth constraint) or internal/graph/builder.go:30-55 (C2/C3 boundary-cluster branch).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auto-generate-c2-c3-diagrams/02-CONTEXT.md
@.planning/phases/02-auto-generate-c2-c3-diagrams/02-RESEARCH.md
@.planning/phases/02-auto-generate-c2-c3-diagrams/02-PATTERNS.md
@.planning/phases/01-fix-c1-view-scoping/01-CONTEXT.md
@internal/graph/builder.go
@internal/graph/builder_test.go
@internal/view/scope_test.go
@cmd/c4drill/root_test.go

Refinement over the existing v1.8 implementation — use the file:line refs in 02-PATTERNS.md; do NOT re-read whole files. The pattern map contains the exact current state of the C1 branch, the test analog shapes, and conventions.

<interfaces>
Current state of the exact code to modify (verified this session):

// BuildGraph C1 branch — builder.go:56-76. The ONLY production edit is line 68:
for _, key := range v.UnitOrder {
    if v.VisiblePaths[key] { continue }            // :63 — unchanged
    entry := v.Units[key]
    if entry.IsExpanded {                          // :68 — D-07 target
        cluster := buildCluster(entry)
        g.Clusters = append(g.Clusters, cluster)
    } else {
        node := buildNode(entry)                   // :72 — plain node fallback
        g.Nodes = append(g.Nodes, node)
    }
}
// D-07 change: `if entry.IsExpanded && len(entry.Unit.Subunits) > 0`.
// Do NOT touch the C2/C3 branch (builder.go:30-55) — its `if entry.IsExpanded` at :45
// renders per-unit expanded clusters inside the boundary cluster (D-04 second half).

// buildNode [+] indicator — builder.go:249-252: `if entry.HasSubunits && !entry.IsExpanded`
// — the D-07 node (no subunits → HasSubunits false) renders WITHOUT "[+]". No change needed.

// isExpandedInC1 — scope.go:182-188 (OR semantics, D-05/D-06, READ-ONLY):
func isExpandedInC1(m *parser.Model, unit *model.Unit, unitPath string) bool {
    if slices.Contains(m.Properties.Expanded, unitPath) { return true }
    return slices.Contains(unit.Expanded, unitPath)
}
// isUnitExpanded(unit, unitPath) = slices.Contains(unit.Expanded, unitPath) — scope.go:176, builder.go:332.
// C2 view children expand when the PARENT's Expanded list contains the CHILD KEY (scope.go:414:
// IsExpanded: isUnitExpanded(systemUnit, name)) — fixture "app": {Expanded: []string{"api"}}.

// buildCluster — builder.go:265-311: cluster ID = "cluster_" + entry.FullPath ("cluster_app.api" for C2).
// Boundary cluster for C2/C3 = g.Clusters[0]; sub-clusters land in g.Clusters[0].Clusters.

// IsExternalType — view.go:74-79: true for TypePersonExternal / TypeSystemExternal /
// TypeDbExternal / TypeQueueExternal (NOT plain TypePerson). D-08 fixture MUST use TypePersonExternal.

// Writer layout — writer.go:37-47 (D-03): C2/C3 relPath = filepath.Join(basename,
// strings.ReplaceAll(unitPath, ".", string(filepath.Separator)) + "." + format).
// basename = TOML filename stem ("expanded.toml" → "expanded").

// Validator box rules (fixture constraints): TypeBox contains C1 types only (rules.go:57, 236-243);
// TypeContainerBox contains C2 types only (rules.go:59, 262+); TypeContainerBox = "containerBox" (model/unit.go:24).

// Test analog locations (02-PATTERNS.md has full bodies):
//   TestBuildGraphClusters — builder_test.go:216-251 (D-07 test shape, inverted)
//   TestFullPipeline_NestedWithExpanded — root_test.go:334-390 (box file test shape)
//   TestGenerateC2View_ExternalBoundaryFromSubunitLinks — scope_test.go:664-691 (actor test shape)
//   TestGenerateC1View_IsExpandedWhenUnitExpandsSelf — scope_test.go:240-258 (self-ref OR side, exists)
//   TestGenerateC2View_IsExpandedForChildUnits — scope_test.go:690-715 (C2 view-layer expansion, exists)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED→GREEN): D-07 — expanded-but-empty top-level unit renders as plain node</name>
  <files>internal/graph/builder_test.go, internal/graph/builder.go</files>
  <read_first>
    internal/graph/builder_test.go (TestBuildGraphClusters :216-251 — exact analog to mirror and invert; package is graph_test, t.Parallel, require/assert conventions)
    internal/graph/builder.go (C1 branch :56-76 — the only production edit point; do NOT touch :30-55)
    .planning/phases/02-auto-generate-c2-c3-diagrams/02-PATTERNS.md (builder.go + builder_test.go sections: D-07 guard shape, conventions)
  </read_first>
  <behavior>
    - Test 1 (D-07): model with Properties.Expanded = ["app"], unit "app" TypeSystem with NO Subunits → GenerateC1View → BuildGraph → g.Clusters has 0 entries, g.Nodes has 1 entry with ID "app".
    - Test 2 (no-regression): existing TestBuildGraphClusters (expanded "app" WITH subunits) still yields 1 cluster "cluster_app" with 2 nodes.
  </behavior>
  <action>
    RED — add `TestBuildGraphExpandedEmptyUnitRendersPlainNode` to internal/graph/builder_test.go (external package graph_test, t.Parallel, numbered comment "// D-07: ..." per CONVENTIONS traceability). Fixture mirrors TestBuildGraphClusters (:216-251) with one difference: `Properties: model.Properties{Name: "Test", Expanded: []string{"app"}}` and unit "app" `{Type: model.TypeSystem, Name: "App"}` with NO Subunits map and NO per-unit Expanded. Drive through the public API exactly like the analog: `v := view.GenerateC1View(m)` then `g := graph.BuildGraph(v)`. Assert `require.Len(t, g.Clusters, 0)`, `require.Len(t, g.Nodes, 1)`, `assert.Equal(t, "app", g.Nodes[0].ID)` — this MUST fail against current code (entry.IsExpanded is true via properties.expanded → buildCluster produces 1 empty cluster, 0 nodes). Run and confirm failure, then commit `test(02-01): add failing test for expanded-empty unit as plain node`.

    GREEN — edit internal/graph/builder.go line 68: `if entry.IsExpanded` → `if entry.IsExpanded && len(entry.Unit.Subunits) > 0`. This is the ONLY production change in this plan. Do NOT touch the C2/C3 branch (:30-55, `if entry.IsExpanded` at :45) — Phase 1's WR-01 penwidth behavior depends on that branch staying untouched. Do NOT edit isExpandedInC1/GenerateC1View (scope.go stays read-only; the view-layer IsExpanded semantics asserted by TestGenerateC1View_IsExpandedWhenUnitExpandsSelf :240 remain valid — D-07 is graph-layer only). Run the test to green, then commit `feat(02-01): render expanded-empty units as plain nodes`.

    REFACTOR — none expected (one-condition guard). If the full-suite run in the verify step surfaces a regression, fix in builder.go only, commit `refactor(02-01): ...` with explanation.
  </action>
  <verify>
    <automated>go test ./internal/graph/ -run 'TestBuildGraphExpanded' -count=1 && go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - RED observed: before the guard edit, `go test ./internal/graph/ -run 'TestBuildGraphExpanded' -count=1` fails (1 cluster produced, 0 nodes).
    - GREEN observed: same command passes after the one-line edit; `go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1` green.
    - builder.go:68 contains `entry.IsExpanded && len(entry.Unit.Subunits) > 0`; builder.go:45 unchanged (still plain `if entry.IsExpanded`).
    - TestBuildGraphClusters and TestGenerateC1View_IsExpandedWhenUnitExpandsSelf still pass (view-layer IsExpanded semantics NOT changed).
    - Both gate commits present in git log: `test(02-01):` then `feat(02-01):` in that order.
  </acceptance_criteria>
  <done>
    Expanded-but-empty top-level unit renders as a plain node in C1 (0 clusters, 1 node "app"); the one-condition guard at builder.go:68 is the only production change; RED and GREEN commits exist in order.
  </done>
</task>

<task type="auto">
  <name>Task 2: Lock VIEW-05 semantics — OR precedence (D-05), silent ignore (D-06), per-unit expanded clusters in C2/C3 (D-04 second half)</name>
  <files>internal/view/scope_test.go, internal/graph/builder_test.go</files>
  <read_first>
    internal/view/scope_test.go (TestGenerateC1View_IsExpandedWhenUnitExpandsSelf :240-258 — existing self-ref lock; TestGenerateC2View_IsExpandedForChildUnits :690-715 — existing C2 view-layer expansion lock; package is view_test)
    internal/graph/builder_test.go (TestBuildGraphClusters :216-251 — cluster assertion pattern; boundary-cluster shape from tests at :853/:1393)
    internal/view/scope.go (:176-188 — isUnitExpanded/isExpandedInC1 semantics the tests lock)
  </read_first>
  <action>
    Pure test additions — no production changes. In internal/view/scope_test.go (package view_test, t.Parallel):

    1. `TestGenerateC1View_IsExpandedOrSemantics` (D-05 full OR): single model with TWO top-level units — "sysprops" `{Type: model.TypeSystem, Name: "Sys", Subunits: {"api": {Type: model.TypeContainer}}}` expanded ONLY via `Properties: model.Properties{Name: "Test", Expanded: []string{"sysprops"}}` (no per-unit Expanded), and "sysself" `{Type: model.TypeSystem, Name: "Self", Expanded: []string{"sysself"}, Subunits: {"api": {Type: model.TypeContainer}}}` expanded ONLY via self-reference. GenerateC1View → `assert.True(t, v.Units["sysprops"].IsExpanded)` (properties side) AND `assert.True(t, v.Units["sysself"].IsExpanded)` (self-ref side, complementing the existing test at :240).

    2. `TestGenerateC1View_SilentlyIgnoresUnknownExpandedEntries` (D-06): model with `Properties: model.Properties{Name: "Test", Expanded: []string{"bogus"}}` where "bogus" matches no top-level unit, and "app" `{Type: model.TypeSystem, Subunits: {"api": {Type: model.TypeContainer}}}`. GenerateC1View → `require.NotNil(t, v)` (no error path — D-06) and `assert.False(t, v.Units["app"].IsExpanded)` (the non-matching entry has no effect; "app" renders collapsed).

    In internal/graph/builder_test.go (package graph_test):

    3. `TestBuildGraphC2ExpandedContainerRendersCluster` (D-04 second half at the BUILD layer — the builder.go:45 branch is currently unasserted): model with system "app" `{Type: model.TypeSystem, Expanded: []string{"api"}, Subunits: {"api": {Type: model.TypeContainer, Name: "API", Subunits: {"auth": {Type: model.TypeComponent}, "store": {Type: model.TypeComponent}}}}}`. `v := view.GenerateC2View(m, "app")`, `g := graph.BuildGraph(v)`. Assert: `require.Len(t, g.Clusters, 1)` (boundary cluster only), `require.Len(t, g.Clusters[0].Clusters, 1)`, `assert.Equal(t, "cluster_app.api", g.Clusters[0].Clusters[0].ID)`, `assert.Len(t, g.Clusters[0].Clusters[0].Nodes, 2)` (components rendered inside the expanded container's cluster). Comment "// D-04: per-unit expanded renders clusters in C2/C3 views". This test also guards Task 1's edit scope: it fails if the C2/C3 branch is ever altered.
  </action>
  <verify>
    <automated>go test ./internal/view/ -run 'TestGenerateC1View_IsExpanded|TestGenerateC1View_Silently' -count=1 && go test ./internal/graph/ -run 'TestBuildGraphC2Expanded' -count=1 && go test ./internal/view/ ./internal/graph/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All three new test functions exist and pass; the two targeted -run commands are green.
    - No production file modified in this task (git status shows only scope_test.go/builder_test.go changes).
    - Existing TestGenerateC1View_IsExpandedWhenUnitExpandsSelf (:240) untouched and green — D-05 OR is locked by the NEW test complementing it, not by editing it.
    - Commit `test(02-01): lock C1 expansion OR/silent-ignore semantics and C2 per-unit clusters` (single commit for all three tests, or split into `test(02-01):` commits per file — either is fine).
  </acceptance_criteria>
  <done>
    OR expansion (D-05), silent ignore (D-06), and the per-unit-expanded C2 cluster (D-04 second half) each have a passing test; no production file modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock D-01/D-02/D-03 box sub-diagram files and D-08 actor boundary nodes in C2</name>
  <files>cmd/c4drill/root_test.go, internal/view/scope_test.go</files>
  <read_first>
    cmd/c4drill/root_test.go (TestFullPipeline_NestedWithExpanded :334-390 — inline-TOML + NewRootCmd + FileExists pattern; note NO t.Parallel and the //nolint:paralleltest directive; TestExpandedUnits :483-507 — existing C2/C3 file coverage that must not be duplicated)
    internal/view/scope_test.go (TestGenerateC2View_ExternalBoundaryFromSubunitLinks :664-691 — exact D-08 analog)
    internal/view/view.go (IsExternalType :74-79 — true for TypePersonExternal only)
    .planning/phases/02-auto-generate-c2-c3-diagrams/02-PATTERNS.md (root_test.go + scope_test.go sections)
  </read_first>
  <action>
    Pure test additions — no production changes.

    1. cmd/c4drill/root_test.go — `TestFullPipeline_BoxWithSubunitsGeneratesSubDiagram` (package main; NO t.Parallel; top-of-function comment `//nolint:paralleltest // go-graphviz WASM engine has concurrency issues`; clone the harness from :334-390: t.TempDir, os.WriteFile(testPath, content, 0o600), NewRootCmd, cmd.SetOut/SetErr, PersistentFlags().Set("output", outputDir), cmd.SetArgs([]string{testPath}), cmd.Execute() require.NoError). TOML fixture (basename "boxtest" from filename "boxtest.toml"): `[properties] name = "Box Test"`; `[boxname] type = "box" name = "Box"` with child `[boxname.child] type = "system" name = "Child"` (box contains C1 types only — validator rules.go:57/236); `[system] type = "system"` with `[system.cbox] type = "containerBox" name = "Containers"` with child `[system.cbox.comp] type = "container" name = "Container"` (containerBox contains C2 types — rules.go:59/262). NO per-unit expanded anywhere — auto-detect (collectExpandableUnitPaths, root.go:170) fires on len(Subunits) > 0 alone (D-01 uniform rule). Assertions: `assert.FileExists(t, filepath.Join(outputDir, "boxtest.svg"))` (C1), `assert.FileExists(t, filepath.Join(outputDir, "boxtest", "boxname.svg"))` (C2 for the box — D-01), `assert.FileExists(t, filepath.Join(outputDir, "boxtest", "system", "cbox.svg"))` (C3 for the containerBox — D-02 deep box parity, dotted-path layout D-03). Comment "// D-01/D-02/D-03: uniform auto-detect incl. boxes; unit-key file naming".

    2. internal/view/scope_test.go — `TestGenerateC2View_ActorBoundaryFromSubunitLinks` (package view_test, t.Parallel; clone :664-691): model with top-level `"webUser": {Type: model.TypePersonExternal, Name: "Web User"}` and system "system" `{Type: model.TypeSystem, Name: "System", Subunits: {"api": {Type: model.TypeContainer, Name: "API", Links: []model.Link{{Peer: "webUser"}}}}}`. `v := view.GenerateC2View(m, "system")` → `require.NotNil(t, v)`, `assert.Contains(t, v.Units, "webUser")`, `assert.True(t, v.Units["webUser"].IsExternal)`. Comment "// D-08: actors are NOT filtered from deeper views — personExternal renders as external boundary node in C2". Use TypePersonExternal, NOT TypePerson (IsExternalType returns false for TypePerson — view.go:74-79).
  </action>
  <verify>
    <automated>go test ./cmd/c4drill/ -run 'TestFullPipeline_Box' -count=1 && go test ./internal/view/ -run 'TestGenerateC2View_Actor' -count=1 && go test ./cmd/c4drill/ ./internal/view/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both new test functions exist and pass; the two targeted -run commands are green.
    - TestFullPipeline_BoxWithSubunitsGeneratesSubDiagram asserts all three files (C1, box C2, containerBox C3); file paths use the unit-key convention (no display names).
    - TestGenerateC2View_ActorBoundaryFromSubunitLinks asserts webUser in v.Units with IsExternal true.
    - No production file modified; no t.Parallel in the root_test.go addition (render/WASM constraint).
    - Commit `test(02-01): lock box sub-diagram generation and actor boundary nodes in C2`.
  </acceptance_criteria>
  <done>
    Box C2 and containerBox C3 sub-diagram files asserted at unit-key paths (D-01/D-02/D-03); personExternal actor asserted as external boundary node in the C2 view (D-08); no production file modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file → parser/validator | UNCHANGED this phase — existing typed ParseError/ValidationErrors pipeline rejects before view/graph; zero new input surface |
| view → graph builder | The D-07 guard is a slice-length check on already-validated model data inside BuildGraph's C1 branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | DoS | builder.go C1 branch guard (:68) | mitigate | `len(entry.Unit.Subunits) > 0` is a plain slice-length read on units already validated by the parser/validator; nil-Unit entries cannot reach it — the C1 branch already dereferences entry.IsExpanded and buildCluster dereferences entry.Unit at the same site (existing nil-guard convention unchanged); VisiblePaths nil-map read at :63 untouched |
| T-02-02 | Tampering | test files only | accept | All three test additions construct in-memory models or inline TOML in t.TempDir; no new file/network/env input surface; fixtures conform to validator type-nesting rules |
| T-02-03 | DoS | C2/C3 boundary-cluster branch (builder.go:30-55) | mitigate | Explicitly out of scope for every task; guarded by regression run of TestBuildEdgesPenwidthC2C3CollapsedPairs / TestBuildEdgesPenwidthLinkFromContributions in the full suite |
| T-02-SC | Tampering | package installs | accept | Zero new dependencies (RESEARCH verified against go.mod — stdlib + testify v1.11.1 only); no package-manager commands in this plan — package legitimacy gate not applicable |

Note: ASVS L1 — no BLOCKING threats. RESEARCH Security Domain confirms no new threat surface (V5 input validation pipeline unchanged; guard is structural).
</threat_model>

<verification>
- RED gate: `go test ./internal/graph/ -run 'TestBuildGraphExpanded' -count=1` fails before Task 1's guard edit, passes after.
- Per-task commits: `go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1` (02-VALIDATION.md sampling rate).
- Wave gate: `go test -v -race ./...` + `mise lint` (golangci-lint).
- WR-01 regression spot check: `go test ./internal/graph/ -run 'TestBuildEdgesPenwidth' -count=1` green — proves the C2/C3 branch and Phase 1 edge work untouched.
- Scope guard: `git diff internal/view/scope.go` empty at plan end (scope.go must stay read-only).
</verification>

<success_criteria>
- `go test -v -race ./...` green; `mise lint` clean.
- D-07: expanded-but-empty top-level unit renders as plain node (no empty cluster box) — TestBuildGraphExpandedEmptyUnitRendersPlainNode passes; C1 view-layer IsExpanded semantics unchanged (TestGenerateC1View_IsExpandedWhenUnitExpandsSelf green).
- D-01/D-02/D-03: box and containerBox sub-diagram files asserted at unit-key paths (TestFullPipeline_BoxWithSubunitsGeneratesSubDiagram).
- D-04 second half: per-unit expanded container renders components cluster inside C2 (TestBuildGraphC2ExpandedContainerRendersCluster).
- D-05/D-06: OR expansion and silent-ignore locked at view layer (TestGenerateC1View_IsExpandedOrSemantics, TestGenerateC1View_SilentlyIgnoresUnknownExpandedEntries).
- D-08: personExternal actor boundary node in C2 locked (TestGenerateC2View_ActorBoundaryFromSubunitLinks).
- TDD gate: `git log --oneline --grep="^test(02-01)"` and `--grep="^feat(02-01)"` each return a commit; RED precedes GREEN.
- No change to internal/view/scope.go or builder.go:30-55.
</success_criteria>

<output>
Create `.planning/phases/02-auto-generate-c2-c3-diagrams/02-01-SUMMARY.md` when done
</output>
