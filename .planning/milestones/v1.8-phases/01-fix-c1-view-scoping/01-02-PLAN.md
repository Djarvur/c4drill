---
phase: 01-fix-c1-view-scoping
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - internal/view/scope.go
  - internal/view/scope_test.go
  - internal/graph/builder_test.go
autonomous: true
requirements: [VIEW-01]
must_haves:
  truths:
    - "GenerateExpandedView creates no boundary nodes for undefined peers — the validator is the single gatekeeper (D-12)"
    - "A link's length (minlen) takes effect only when BOTH drawn endpoints are the link's original units; resolved edges carry no minlen (D-13)"
    - "Expanded-mode DOT output still emits minlen and penwidth 2.0 (COMPAT-02)"
  artifacts:
    - path: "internal/view/scope.go"
      provides: "addExternalBoundaryNodes + addExternalBoundaryNodesRecursive deleted; GenerateExpandedView sets AllExpanded: true; Length gates at all 6 resolved-link synthesis sites"
      contains: "AllExpanded: true"
    - path: "internal/view/scope_test.go"
      provides: "Inverted expanded-boundary test (D-12)"
      contains: "TestGenerateExpandedView_SkipsExternalBoundaryNodesForLinkedUnits"
    - path: "internal/graph/builder_test.go"
      provides: "MinLen gating tests + expanded DOT baseline guard"
      contains: "func TestBuildGraphResolvedEdgeMinLen"
  key_links:
    - from: "GenerateExpandedView (scope.go:14)"
      to: "BuildExpandedGraph (builder.go:125)"
      via: "AllExpanded flag consumed by buildEdges dedup key + penwidth branch"
      pattern: "AllExpanded"
    - from: "resolveAndAddBoundary / resolveBoundaryNodeLinks / addResolvedCrossLink(s)"
      to: "createEdge MinLen"
      via: "Length dropped from synthesized links when either endpoint resolved (D-13)"
      pattern: "link.Peer == resolved"
---

<objective>
Implement D-12 (remove legacy `addExternalBoundaryNodes` + `addExternalBoundaryNodesRecursive`), activate the D-02 mode discriminator (`GenerateExpandedView` sets `View.AllExpanded = true`), and implement D-13 (minlen takes effect only when both drawn endpoints are the link's original units) at all six resolved-link synthesis sites in `internal/view/scope.go`.

Purpose: VIEW-01 purity (C1 can never be polluted by the legacy recursive boundary path) + COMPAT-02 (expanded mode stays byte-identical) + D-13 minlen semantics.
Output: Cleaned `GenerateExpandedView`, deleted dead functions, conditional `Length` copying in `resolveAndAddBoundary`, `resolveBoundaryNodeLinks`, `addResolvedCrossLink`/`addResolvedCrossLinkFrom`, and RED→GREEN tests.

Refinement of the existing v1.8 implementation — DELTA edits at the exact line refs below, not rewrites.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-c1-view-scoping/01-CONTEXT.md
@.planning/phases/01-fix-c1-view-scoping/01-RESEARCH.md
@.planning/phases/01-fix-c1-view-scoping/01-PATTERNS.md
@internal/view/scope.go
@internal/view/scope_test.go
@internal/graph/builder_test.go

<interfaces>
Current state of the exact code to modify (internal/view/scope.go):

// GenerateExpandedView — scope.go:14-52. View literal at :19-25 (fields Level, Title, Edges,
// UnitOrder, Units). Call at :48-49: `// Add external boundary nodes for referenced units not in the model` / `addExternalBoundaryNodes(v, m)` — DELETE the call, and add `AllExpanded: true` to the literal (D-02). Update the doc comment (drop the "It includes external boundary nodes..." sentence, :13).

// addExternalBoundaryNodes — scope.go:154-174 — DELETE entire function.
// addExternalBoundaryNodesRecursive — scope.go:178-215 — DELETE entire function.
// createExternalBoundaryNode — scope.go:220-245 — KEEP (called by addC1BoundaryNodes :295/:324 and addResolvedBoundaryNode :642).

// resolveAndAddBoundary — scope.go:278-363. sourceAncestor := top-level of path (:280-283).
//   Outgoing copy block :301-311 and incoming copy block :330-340 each build a model.Link{Peer, Technology, Description, Style, Arrow, Rank, LabelPosition, Color, Length: link.Length} appended to sourceEntry.ResolvedLinks / .ResolvedLinksFrom.

// resolveBoundaryNodeLinks — scope.go:652-711. Outgoing resolved-link copy :669-679 (fields incl. `Length: link.Length` :678); incoming copy :693-703 (Length :702). resolvedPeer := resolveToViewAncestor(v, link.Peer); copies appended only when resolvedPeer != "" && resolvedPeer != path.

// addResolvedCrossLink — scope.go:831-855; addResolvedCrossLinkFrom — scope.go:859-883.
//   Signature: func addResolvedCrossLink(v *View, subunitEntry *Entry, sourcePath string, link model.Link)
//   resolvedPeer := resolveToViewAncestor(v, link.Peer); skip when "" or == sourcePath; dedup against existing peers; copy block :844-854 (Length :853).
//   Call sites: resolveSubunitCrossLinks :765 (direct links on the subunit — original source IS fullPath) and resolveDescendantCrossLinks :805 (descendant links — original source is the recursion path `fullPath`, currently NOT passed).

// D-13 rule: keep link.Length in a synthesized link ONLY when (original source unit == drawn source) AND (original peer == drawn peer). Resolved edges carry Length 0 → MinLen 0 at builder.go:454 (no builder change needed; renderer serializes MinLen>0 only — converter.go:474-476).

// populateIncomingLinks (internal/validator/index.go:53-80) mirrors Links into LinksFrom with all attrs preserved — mirrors are NOT authored links (relevant to D-05 counting in plan 01-01, and to which Length copies matter here).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Failing tests for expanded cleanup and minlen gating</name>
  <files>internal/view/scope_test.go, internal/graph/builder_test.go</files>
  <read_first>
    internal/view/scope_test.go (TestGenerateExpandedView_AddsExternalBoundaryNodesForLinkedUnits :784-818 — the test to INVERT per D-12)
    internal/graph/builder_test.go (TestBuildGraphEdgeLength :928-1023 — MinLen assert pattern; TestBuildExpandedGraphRealToml :698-739 — real-TOML fixture pattern with validator.Validate)
  </read_first>
  <action>
    Write failing tests:
    1. internal/view/scope_test.go — INVERT the D-12 test: rename `TestGenerateExpandedView_AddsExternalBoundaryNodesForLinkedUnits` (:784) to `TestGenerateExpandedView_SkipsExternalBoundaryNodesForLinkedUnits`; keep the fixture (api → handler → link Peer "cloudstorage"); replace the two asserts at :802-804 (`assert.Contains(t, v.Units, "cloudstorage")` + `assert.True(t, v.Units["cloudstorage"].IsExternal)`) with `assert.NotContains(t, v.Units, "cloudstorage")`. Update the "Test 4" comment to reflect the new contract (expanded view no longer synthesizes boundary nodes — validator rejects undefined peers upstream, internal/validator/rules.go:26).
    2. internal/graph/builder_test.go — new `TestBuildGraphResolvedEdgeMinLen` (t.Parallel, D-13), using view.GenerateC1View + graph.BuildGraph:
       - subtest "resolved edge drops minlen": model webUser (TypePersonExternal, Links: [{Peer: "system.api.handler", Length: 2}]) + system (TypeSystem with nested Subunits api.handler — system NOT expanded); require.Len(t, g.Edges, 1); assert.Zero(t, g.Edges[0].MinLen) — the edge webUser→system is resolved, so the authored length must NOT apply.
       - subtest "direct pair keeps minlen": model app (Links: [{Peer: "db", Length: 2}]) + db; require.Len(t, g.Edges, 1); assert.Equal(t, 2, g.Edges[0].MinLen) — both endpoints original.
    3. internal/graph/builder_test.go — new `TestBuildExpandedGraphBaselineDOT` (t.Parallel, COMPAT-02 guard per RESEARCH Pitfall 2): mirror the fixture pattern of TestBuildExpandedGraphRealToml (:698-739): parse "../../cyp-auth-infra/saira-20260320.c2.full.toml" (exists in-repo, has 5 `length` attrs), validator.Validate → require.Empty(valErrors), then GenerateExpandedView → BuildExpandedGraph → RenderDOT: assert Contains "minlen=", assert Contains "penwidth=2", and assert every g.Edges[i].PenWidth == 2.0 (expanded mode keeps v1.7 2.0 prominence).
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/graph/ -run 'TestGenerateExpandedView_SkipsExternal|TestBuildGraphResolvedEdgeMinLen|TestBuildExpandedGraphBaselineDOT' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - RED: the inverted test fails (cloudstorage IS still added today); TestBuildGraphResolvedEdgeMinLen fails (MinLen 2 on resolved edge); TestBuildExpandedGraphBaselineDOT fails (GenerateExpandedView does not yet set AllExpanded → post-plan-01-01 penwidth branch emits penwidth=1 for single edges).
    - Existing tests untouched except the renamed/inverted one.
    - Commit: `test(01-02): add failing tests for minlen gating and expanded cleanup`
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Remove legacy boundary nodes, activate AllExpanded, gate minlen at all synthesis sites</name>
  <files>internal/view/scope.go</files>
  <read_first>
    internal/view/scope.go (GenerateExpandedView :14-52, addExternalBoundaryNodes :154-174, addExternalBoundaryNodesRecursive :178-215, resolveAndAddBoundary :278-363, resolveBoundaryNodeLinks :652-711, resolveSubunitCrossLinks :750-787, resolveDescendantCrossLinks :794-827, addResolvedCrossLink :831-855, addResolvedCrossLinkFrom :859-883)
  </read_first>
  <action>
    Per D-12, D-02, D-13:
    1. D-12: delete the call at :49 and BOTH functions (addExternalBoundaryNodes :154-174, addExternalBoundaryNodesRecursive :178-215). KEEP createExternalBoundaryNode (:220-245). Update GenerateExpandedView's doc comment: remove the "It includes external boundary nodes for units referenced by links but not defined." sentence.
    2. D-02: add `AllExpanded: true` to the View literal in GenerateExpandedView (:19-25) with no comment needed (field doc comment lives on View).
    3. D-13 C1 sites — resolveAndAddBoundary outgoing copy (:301-311) and incoming copy (:330-340): set `Length: 0` by default, but `Length: link.Length` ONLY when `path == sourceAncestor && link.Peer == resolved` (both drawn endpoints are the link's original units). `sourceAncestor` is the variable computed at :280-283; plan 01-03 replaces it with deepest-visible resolution and will update this gate's first operand.
    4. D-13 C2/C3 sites — resolveBoundaryNodeLinks outgoing copy (:669-679) and incoming copy (:693-703): `Length: 0` unless `link.Peer == resolvedPeer` (source of a boundary-node link is the boundary entry itself — never resolved; only the peer side can resolve). Keep `Length: link.Length` when unchanged.
    5. D-13 C2/C3 sites — addResolvedCrossLink (:831-855) and addResolvedCrossLinkFrom (:859-883): add a parameter `originalSource string` after `sourcePath`; gate `Length: 0` unless `originalSource == sourcePath && link.Peer == resolvedPeer`, else `link.Length`. Update call sites: resolveSubunitCrossLinks direct-link calls (:765, :770) pass `fullPath` as originalSource (source IS the subunit); resolveDescendantCrossLinks calls (:805, :810) pass `fullPath` — the current recursion path of the descendant unit (source is NOT original → Length always dropped there).
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/graph/ -run 'TestGenerateExpandedView_SkipsExternal|TestBuildGraphResolvedEdgeMinLen|TestBuildExpandedGraphBaselineDOT' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - GREEN: all three new tests pass.
    - `grep -v '^\s*//' internal/view/scope.go | grep -c "addExternalBoundaryNodes"` == 0 (both functions and the call gone; comments may remain mentioning the name).
    - scope.go:49 area: GenerateExpandedView no longer calls addExternalBoundaryNodes; its literal contains AllExpanded: true.
    - addResolvedCrossLink/addResolvedCrossLinkFrom signatures include originalSource; both call sites updated.
    - Commit: `feat(01-02): remove legacy boundary nodes and gate minlen to original pairs`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression — C2/C3 cross-subunit and expanded baseline</name>
  <files>internal/view/scope.go</files>
  <read_first>
    internal/view/scope_test.go (TestGenerateC2View_ExternalBoundaryFromSubunitLinks :623, TestGenerateC3View_ExternalBoundaryFromSubunitLinks :539)
    internal/graph/builder_test.go (TestBuildExpandedGraphRealToml :698)
  </read_first>
  <action>
    No production changes expected. Verify Pitfall 1 (C2/C3 cross-subunit edge synthesis must not regress — the Length gate touches copy blocks inside those passes without changing resolution semantics):
    1. Run the full suite; pay attention to C2/C3 boundary tests (TestGenerateC2View_ExternalBoundaryFromSubunitLinks, TestGenerateC3View_ExternalBoundaryFromSubunitLinks) and the C2/C3 edge-synthesis integration tests (TestIntegrationC2ViewNestedSystem, TestIntegrationC3ViewNestedContainer — internal/view/integration_test.go).
    2. Confirm TestBuildExpandedGraphRealToml still passes (expanded mode minlen untouched).
    3. If any failure is a genuine regression, fix scope.go; otherwise commit nothing.
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1 && go test -v -race ./...</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (`go test -v -race ./...`); `mise lint` clean.
    - C2/C3 boundary-node and cross-subunit edge tests unchanged and green (Pitfall 1: no resolution-semantics drift).
    - If and only if code changed: commit `refactor(01-02): regression fixes`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file → parser/validator | UNCHANGED — validator (internal/validator/rules.go:26 ValidateReferences) remains the single gatekeeper for undefined peers (D-12 basis) |
| view synthesis (scope.go) → graph builder | Internal transform; synthesized model.Link values consumed by buildEdges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | DoS | resolveAndAddBoundary / resolveBoundaryNodeLinks / addResolvedCrossLink(s) Length gates | mitigate | Gate operates on existing int fields (Length) and string equality only — no new parsing; nil-source guard `sourceEntry != nil` and existing `resolvedPeer == ""` skips retained |
| T-01-05 | Tampering | undefined-peer handling | accept | Removal of addExternalBoundaryNodes* cannot introduce new input acceptance — the code path was unreachable for valid models (D-12 rationale); validator unchanged |
| T-01-06 | DoS | GenerateExpandedView | mitigate | AllExpanded is a constant literal `true` — no user input reaches the flag; downstream branches are boolean-only |
| T-01-SC | Tampering | package installs | accept | Zero new dependencies; no package-manager commands in this plan |

Note: ASVS L1 — no BLOCKING threats. CLI/parser/validator untouched (RESEARCH Security Domain).
</threat_model>

<verification>
- RED gate: the three new/updated tests fail before Task 2, pass after.
- Per-task commits: `go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1`.
- Wave gate: `go test -v -race ./...` + `mise lint`.
- COMPAT-02: TestBuildExpandedGraphBaselineDOT (minlen + penwidth=2 in expanded DOT) + TestBuildExpandedGraphRealToml both green.
</verification>

<success_criteria>
- `go test -v -race ./...` green; `mise lint` clean.
- TestGenerateExpandedView_SkipsExternalBoundaryNodesForLinkedUnits, TestBuildGraphResolvedEdgeMinLen (both subtests), TestBuildExpandedGraphBaselineDOT pass.
- scope.go contains no live reference to addExternalBoundaryNodes (grep gate: 0 non-comment matches).
- Expanded-mode DOT still contains `minlen=` (COMPAT-02).
</success_criteria>

<output>
Create `.planning/phases/01-fix-c1-view-scoping/01-02-SUMMARY.md` when done
</output>
