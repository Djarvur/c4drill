# v1.12 Requirements Archive — C4D DSL Alternative

> Archived 2026-08-17 at milestone close. Requirements source: `phases/35-.../35-CONTEXT.md` decisions D-01..D-35 (the top-level REQUIREMENTS.md was archived with v1.11; Phase 35 requirements lived in the phase CONTEXT.md per roadmap note).

## Outcome Summary

All 35 requirements (D-01..D-35) satisfied. Verification: 24/24 truths (3 gap-fix closures at close), UAT 12/12, security 30/30 threats closed (ASVS 1). Full suite 16/16 packages green.

## Traceability Table

| Requirement | Source Plan | Status | Outcome |
|-------------|------------|--------|---------|
| D-01..D-09, D-12, D-18, D-20 | 35-01 | ✅ SATISFIED | Core C4D grammar (units, nesting, fields, edges, properties, literals, comments); pigeon v1.3.0 pinned (deviation from planned v1.0.0 documented — `-nolint` flag); committed generated parser; `*parser.ParseError` contract |
| D-16, D-17 | 35-02 | ✅ SATISFIED | TOML `[[unit.X.use]]` sugar + `[[template.X.use]]` body uses; recursive Expand with cycle detection + maxTemplateDepth=100; v1.10 template-nesting deferral lifted (HS-1 every level) |
| D-13, D-14, D-15, D-19 | 35-03 | ✅ SATISFIED | template/use(3 positions)/include-once grammar + both list forms; reserved-word hard errors with Levenshtein suggestions (19 keywords) |
| D-23, D-33 | 35-04 | ✅ SATISFIED | Deterministic canonical field order (now incl. width/height post-fix); compact-leaf C4D style; idempotency fixpoint tests |
| D-02, D-10, D-11, D-21, D-26 | 35-05 | ✅ SATISFIED | `c4d.Parse` → `*parser.Model`; inference parity; duplicate-edge hard error; mixed .c4d/.toml include dispatch |
| D-22, D-26 | 35-06 | ✅ SATISFIED | Canonical-equivalent round-trip contract over 29-fixture corpus + render equivalence; CR-01/02/03/05 defect classes closed by representability gate + escaped-quoted fallback + quoted TOML keys |
| D-24, D-25, D-27, D-28, D-29, D-30 | 35-07 | ✅ SATISFIED | Validate-first convert (D-24), fresh-parse emission, extension dispatch hard-errors, swapped-ext + `-o` placement, `--follow-includes` graph migration; CR-06 `-o` structure preservation fixed for relative entry paths; write gate (re-parse + CanonicalEqual) added |
| D-31, D-32, D-33 | 35-08 | ✅ SATISFIED | gofmt-style `fmt` both formats; comment + blank-line preservation; author key order; `--check` CI gate; semantic safety gate (re-parse + DeepEqual before write) |
| D-34, D-35 | 35-09 | ✅ SATISFIED | README.adoc C4D Format section + convert/fmt CLI reference; 12 render-parity-enforced twins; skill extended in place (name kept) with all plugin copies synced |

## Changed Requirements

- **D-20 (pigeon pin):** v1.3.0 instead of planned v1.0.0 — v1.0.0 lacks the `-nolint` flag the lint gate requires. Intent (exact pin + go.sum provenance) unchanged.
- **D-23 (canonical field set):** plan-pinned order omitted width/height; at verification this contradicted the phase goal's full-parity claim — resolved by implementing parity (FieldKey + emitters + canonical form) rather than accepting an override.
- **D-22 (round-trip contract):** contract text unqualified; corpus did not cover the corrupting input classes. Closed by loud representability errors + a re-parse/equality write gate (stronger than the literal contract).

## Deferred / Non-Blocking at Close

- WR-03: duplicate `properties {}` blocks last-win in C4D (warning)
- WR-04: skill/SKILL.md type-inference table drift vs `DefaultTypeForParent` (warning, optional fix)
- WR-05: quoted edge-label whitespace trimmed (warning)
- canonsrc.go:549-551 stale "pipe-safe quoting" comment (doc nuance, behaviorally moot post-gate)

See `35-VERIFICATION.md` §Anti-Patterns and the v1.12 MILESTONES.md entry.
