---
phase: 30-relative-peer-resolution
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - cmd/c4drill/root.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements: [ERGO-01, ERGO-02]
user_setup: []

must_haves:
  truths:
    - "peer.Resolve is invoked in cmd/c4drill/root.go between parser.ParseFile and validator.Validate"
    - "A model with a bare peer that resolves renders without validation errors (the validator sees the absolute peer)"
    - "A model with an unresolvable bare peer causes the CLI to exit non-zero with a clear error message naming the peer and host (no panic)"
    - "Existing corpus models render identically to before (the Resolve call is a no-op for the rendered output of every existing fixture)"
  artifacts:
    - path: "cmd/c4drill/root.go"
      provides: "Stage 1.6 peer.Resolve call wired between Parse and Validate"
      contains: "peer.Resolve"
    - path: "cmd/c4drill/root_test.go"
      provides: "integration test: pipeline ordering + CLI unresolvable-peer error path"
      contains: "TestPipelineResolveBeforeValidate"
  key_links:
    - from: "cmd/c4drill/root.go"
      to: "internal/peer.Resolve"
      via: "import + invocation in the Parse→Validate gap"
      pattern: "peer\\.Resolve\\(m\\)"
    - from: "cmd/c4drill/root_test.go"
      to: "cmd/c4drill/testdata/peer_walkup.toml"
      via: "fixture-driven pipeline test parses + resolves + validates"
      pattern: "peer_walkup\\.toml"
---

<objective>
Wire `peer.Resolve` into the CLI pipeline (`cmd/c4drill/root.go`) between `parser.ParseFile` and `validator.Validate`, and add the integration tests proving (a) the pipeline ordering makes bare peers resolvable before the validator sees them, and (b) the CLI exits cleanly with a clear error on an unresolvable bare peer.

Purpose: Plan 01 built the resolver; Plan 02 connects it to the CLI so users can actually author bare peers. This is glue code (single function call + error wrap + integration tests), so it is a standard `type: execute` plan, not TDD — the resolver's behavior is already covered by Plan 01's unit tests.

Output: Modified `cmd/c4drill/root.go` (one new import + ~4-line staging insertion) + new `cmd/c4drill/root_test.go` (or extension of an existing test file) with two integration tests.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-relative-peer-resolution/30-CONTEXT.md
@.planning/phases/30-relative-peer-resolution/30-RESEARCH.md
@.planning/phases/30-relative-peer-resolution/30-PATTERNS.md
@.planning/phases/30-relative-peer-resolution/30-01-PLAN.md

<interfaces>
<!-- The pipeline staging the executor modifies. Extracted from cmd/c4drill/root.go:111-123. -->

From cmd/c4drill/root.go:111-123 (current Parse→Validate staging):
```go
// Stage 1: Parse
m, err := parser.ParseFile(inputPath)
if err != nil {
    return fmt.Errorf("parse: %w", err)
}

// Stage 2: Validate
valErrors := validator.Validate(m)
if err != nil) {
    validator.ReportErrors(valErrors, cmd.OutOrStderr())
    return errValidationFailed
}
```

Target (insert Stage 1.6 between Parse and Validate):
```go
// Stage 1: Parse
m, err := parser.ParseFile(inputPath)
if err != nil {
    return fmt.Errorf("parse: %w", err)
}

// Stage 1.6: Resolve relative peers (Phase 30; runs after future template.Expand, before humanize + Validate)
if err := peer.Resolve(m); err != nil {
    return fmt.Errorf("resolve peers: %w", err)
}

// Stage 2: Validate
valErrors := validator.Validate(m)
```

From internal/peer/resolve.go (Plan 01's output — the function being wired in):
```go
package peer

// Resolve rewrites every Link.Peer from relative bare to absolute dotted path.
// Returns *ResolveError naming peer + host if a bare peer cannot be resolved.
func Resolve(m *parser.Model) error
```

Module path (go.mod line 1): `github.com/Djarvur/c4drill` — so the import is `github.com/Djarvur/c4drill/internal/peer`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire peer.Resolve into the CLI pipeline</name>
  <files>cmd/c4drill/root.go</files>
  <behavior>
    - The CLI pipeline runs Parse → peer.Resolve → Validate in that order.
    - An unresolvable bare peer causes `runRoot` to return a non-nil error wrapping the ResolveError (via `fmt.Errorf("resolve peers: %w", err)`), so the CLI exits non-zero with a clear message — no panic, no silent render of a broken model.
    - Existing corpus models (valid.toml, expanded.toml, multilevel.toml) still parse + resolve + validate + render identically (Resolve is a no-op for their peer sets — verified by Plan 01's TestResolveCorpusByteIdentical).
  </behavior>
  <action>
    Read `cmd/c4drill/root.go` (full file, focus on lines 1-20 for imports and 95-150 for the runRoot staging). Add the import `"github.com/Djarvur/c4drill/internal/peer"` to the import block, placed to match the existing alphabetical/grouping convention (the existing `internal/parser` and `internal/validator` imports show the group — peer goes alongside them alphabetically: parser, peer, validator or parser, validator, peer depending on the existing grouping; match what's there).

    Insert the Stage 1.6 block between the existing Stage 1 (Parse, ending around line 115 with the closing `}` of the error check) and Stage 2 (Validate, around line 118). The exact insertion (per the &lt;interfaces&gt; block above):
    ```
    // Stage 1.6: Resolve relative peers (Phase 30; runs after future template.Expand, before humanize + Validate)
    if err := peer.Resolve(m); err != nil {
        return fmt.Errorf("resolve peers: %w", err)
    }
    ```
    Use the same error-wrapping idiom as the surrounding Parse (`return fmt.Errorf("parse: %w", err)`) and Validate stages.

    Do NOT reorder existing stages. Do NOT add a humanize call (Phase 29 owns that). Do NOT add a template.Expand call (Phase 31 owns that). Phase 30 inserts ONLY the peer.Resolve call.

    CONCURRENCY NOTE: A background agent is concurrently executing Phase 29, which edits `internal/parser/parser.go` and `internal/model/unit.go` (adds Humanize) — NOT `cmd/c4drill/root.go`. This plan's edit to `cmd/c4drill/root.go` (adding the peer.Resolve call + import) does NOT conflict with Phase 29's edits. If Phase 29 has ALSO added a humanize staging call to root.go by the time this plan executes, place the peer.Resolve call BEFORE the humanize call (pipeline order XC-01: peer.Resolve runs before humanize). Read root.go fresh at execution time to see the current state.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go build ./cmd/c4drill/ && grep -c "peer.Resolve(m)" cmd/c4drill/root.go | grep -q "^1$" && echo "wired"</automated>
  </verify>
  <done>
    - `cmd/c4drill/root.go` imports `github.com/Djarvur/c4drill/internal/peer`
    - `cmd/c4drill/root.go` contains exactly one `peer.Resolve(m)` invocation between ParseFile and Validate, wrapped with `fmt.Errorf("resolve peers: %w", err)`
    - `go build ./cmd/c4drill/` succeeds
    - Existing corpus models still render: `go test ./...` green (Plan 01's tests + any existing cmd/c4drill tests)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pipeline integration tests (ordering + CLI error path)</name>
  <files>cmd/c4drill/root_test.go</files>
  <behavior>
    - TestPipelineResolveBeforeValidate: parse `peer_walkup.toml` (created in Plan 01), call `peer.Resolve`, call `validator.Validate`, assert validation returns no errors (proving the validator saw the rewritten absolute peers). Also assert at least one peer in the fixture was a bare name pre-resolve (sanity check the fixture exercises the feature).
    - TestCLIUnresolvablePeerExits: invoke the CLI (or the runRoot function directly) on `peer_unresolvable.toml` (created in Plan 01); assert the result is a non-nil error whose message contains "resolve peers" and the unresolvable peer name (e.g. "nonexistent"); assert no panic.
    - TestCLICorpusRendersUnchanged: (optional but recommended) invoke the CLI on each existing corpus fixture (valid.toml, expanded.toml, multilevel.toml) and assert it succeeds without error — proving the Resolve call is a no-op for the rendered output of every existing model (ERGO-02 end-to-end acceptance, complementing Plan 01's peer-set unit test).
  </behavior>
  <action>
    Check whether `cmd/c4drill/root_test.go` exists. If it does, EXTEND it (add the three test functions); if not, CREATE it with `package c4drill_test` (black-box, matching the parser_test.go idiom) importing `testing`, `os`, `path/filepath`, `github.com/Djarvur/c4drill/internal/parser`, `github.com/Djarvur/c4drill/internal/peer`, `github.com/Djarvur/c4drill/internal/validator`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`.

    For TestPipelineResolveBeforeValidate: read `peer_walkup.toml` via `os.ReadFile(filepath.Join("testdata", "peer_walkup.toml"))` (test is in cmd/c4drill/, so `testdata/` is the direct subdir `cmd/c4drill/testdata/`); `parser.Parse(data)`; collect the pre-resolve bare-peer count (walk the tree, count `Link.Peer` values with no "." — assert >= 1 to confirm the fixture exercises the feature); `peer.Resolve(m)` (assert no error); `validator.Validate(m)` (assert returns nil or empty — no validation errors).

    For TestCLIUnresolvablePeerExits: the cleanest approach is to call `peer.Resolve` directly on a parsed `peer_unresolvable.toml` and assert the error (unit-level). For the true CLI path, either (a) invoke the cobra root command via `rootCmd.Execute()` with args pointing at the fixture and capture the error, or (b) if the runRoot function is not easily testable in isolation, assert at the Resolve-call level and note that the wiring test (Task 1's grep + build) covers the call-site. Prefer (a) if `rootCmd` is accessible from the test package; fall back to (b). Read `cmd/c4drill/root.go` to determine the testability of runRoot/rootCmd.

    For TestCLICorpusRendersUnchanged: loop over the THREE cmd/c4drill/testdata corpus fixtures that have peers — `valid.toml`, `expanded.toml`, `multilevel.toml` (do NOT include `invalid.toml` — it is a validator error-path fixture). For each: read via `os.ReadFile(filepath.Join("testdata", fix))`; `parser.Parse`; `peer.Resolve`; `validator.Validate`; assert validation returns no errors (proving the Resolve call is a no-op for the rendered output of every existing model). This is the end-to-end backward-compat proof complementing Plan 01's parser-corpus peer-set unit test. NOTE: these fixtures' bare peers (`user`, `external`, `externalSys`) all reference top-level units → identity rewrite → validation passes unchanged.

    Run `go test ./cmd/c4drill/ -x` — all new tests MUST pass. Commit `test(30-02): add pipeline integration tests for peer resolution`.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run "TestPipelineResolveBeforeValidate|TestCLIUnresolvablePeerExits|TestCLICorpusRendersUnchanged" -x</automated>
  </verify>
  <done>
    - `cmd/c4drill/root_test.go` (new or extended) contains TestPipelineResolveBeforeValidate, TestCLIUnresolvablePeerExits, TestCLICorpusRendersUnchanged
    - TestPipelineResolveBeforeValidate passes (validator sees absolute peers post-resolve)
    - TestCLIUnresolvablePeerExits passes (CLI/Resolve returns error naming the peer, no panic)
    - TestCLICorpusRendersUnchanged passes for valid.toml, expanded.toml, multilevel.toml
    - `go test ./...` full suite green
    - Commit `test(30-02): add pipeline integration tests for peer resolution` made
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation → runRoot | User-supplied file path crosses here; existing flag validation (root.go:96-98) gates the format arg; `args[0]` is the input path. Phase 30 adds no new boundary — it inserts a call into the existing Parse→Validate staging. |
| peer.Resolve error → CLI exit | The ResolveError is wrapped via `fmt.Errorf("resolve peers: %w", err)` and returned; the CLI's existing error-reporting path surfaces it. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Denial of Service | CLI on pathologically deep model | accept | The resolver's walk depth is bounded by model nesting (see T-30-02 in Plan 01). The CLI already handles parser/validator errors gracefully; the Resolve error path follows the same `fmt.Errorf + return` idiom. No new DoS surface. |
| T-30-06 | Information Disclosure | CLI error output | accept | The "resolve peers" error names the peer and host unit (author diagnostic data). Consistent with existing parser/validator error verbosity. No PII. |

**Summary:** No new threats beyond Plan 01's analysis. The wiring change adds one function call following the existing error-handling idiom. All dispositions ACCEPT.
</threat_model>

<verification>
- [ ] `go build ./cmd/c4drill/` succeeds
- [ ] `cmd/c4drill/root.go` contains exactly one `peer.Resolve(m)` call between Parse and Validate
- [ ] `go test ./cmd/c4drill/ -run "TestPipelineResolveBeforeValidate|TestCLIUnresolvablePeerExits|TestCLICorpusRendersUnchanged" -x` passes
- [ ] `go test ./...` full suite green
- [ ] `go vet ./...` clean
- [ ] Existing corpus fixtures (valid.toml, expanded.toml, multilevel.toml) render without error end-to-end
</verification>

<success_criteria>
- peer.Resolve is wired into the CLI pipeline in the correct position (Parse → Resolve → Validate)
- An unresolvable bare peer produces a clean non-zero CLI exit with a diagnostic naming the peer and host (no panic, no silent broken render)
- Every existing corpus model renders identically to v1.9 (ERGO-02 end-to-end backward-compat)
- The pipeline ordering (XC-01: peer.Resolve after Parse, before Validate) is enforced by an integration test that would fail if Resolve were removed or reordered
</success_criteria>

<output>
Create `.planning/phases/30-relative-peer-resolution/30-02-SUMMARY.md` when done.
</output>
