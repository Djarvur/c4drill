---
phase: 32-include-directive-multi-file
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/parser/parser.go
  - internal/parser/parser_test.go
autonomous: true
requirements: [INC-01]
must_haves:
  truths:
    - "A model with one or more [[include]] array-of-tables extracts them into Model.Includes ([]IncludeDirective) in document order, each carrying Path and Once fields"
    - "A model with [[include]] produces ZERO phantom units — 'include' never appears in Model.UnitOrder or Model.Units (consumes the Phase 31 BC-1 skip; this plan does NOT re-touch captureDefinitionOrder)"
    - "A model with no [[include]] leaves Model.Includes nil/empty — existing single-file models parse identically (no regression)"
    - "Model.Includes is tagged toml:\"-\" (populated by explicit extraction in Parse, matching the UnitOrder convention at parser.go:39) and rawMap[\"include\"] is deleted before the unit loop so the directive never enters m.Units"
  artifacts:
    - path: "internal/parser/parser.go"
      provides: "IncludeDirective type (Path string, Once bool) + Model.Includes field + [[include]] rawMap extraction in Parse"
      contains: "Includes []IncludeDirective"
    - path: "internal/parser/parser_test.go"
      provides: "TestParseIncludesExtracted + TestParseNoIncludes (extraction + no-regression)"
      contains: "TestParseIncludesExtracted"
  key_links:
    - from: "internal/parser/parser.go Parse rawMap"
      to: "Model.Includes"
      via: "properties-style marshal/unmarshal extraction + delete(rawMap, \"include\")"
      pattern: "rawMap\\[\"include\"\\]"
---

<objective>
Land the parser-side half of the include feature: extract `[[include]]` array-of-tables into a new `Model.Includes []IncludeDirective` field in `parser.Parse`, mirroring the existing `properties` extraction at `parser.go:68-77`. This plan CONSUMES the BC-1 reserved-table skip that Phase 31 Plan 1 already lands in `captureDefinitionOrder` — it does NOT re-touch `captureDefinitionOrder` (concurrency safety: Phase 28 is editing parser.go concurrently; the diff here is confined to the `Model` struct and one new extraction block in `Parse`).

Purpose: this is the foundation Plan 02 (`internal/include/Resolve`) consumes. Without `Model.Includes`, the resolver has nothing to walk. It is parser-only — no resolution, no merge, no recursion. Fully testable in isolation via inline TOML strings (no multi-file fixtures needed here; those live with Plan 02 under `internal/include/testdata/`).

Output: Modified `internal/parser/parser.go` (`IncludeDirective` type + `Model.Includes` field + extraction block in `Parse`), extended `internal/parser/parser_test.go` (two new tests).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-include-directive-multi-file/32-CONTEXT.md
@.planning/phases/32-include-directive-multi-file/32-RESEARCH.md
@.planning/phases/32-include-directive-multi-file/32-PATTERNS.md
@.planning/phases/32-include-directive-multi-file/32-VALIDATION.md

@internal/parser/parser.go
@internal/parser/errors.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/parser/parser.go (current Model struct, lines 35-42 — Phase 31 Plan 1 adds Templates/Instantiations alongside these; this plan adds Includes):
```go
type Model struct {
    Properties model.Properties `toml:"properties"`
    UnitOrder  []string
    Units      map[string]*model.Unit
}
```
NOTE: by the time Phase 32 executes, Phase 31 Plan 1 has landed `Templates map[string]*TemplateDef` and `Instantiations []Instantiation` (both `toml:"-"`). Add `Includes []IncludeDirective` (`toml:"-"`) alongside them.

From internal/parser/parser.go (current properties extraction, lines 68-77 — the analog to mirror):
```go
if props, ok := rawMap["properties"]; ok {
    propsData, err := toml.Marshal(props)
    if err != nil {
        return nil, &ParseError{Message: "failed to marshal properties", Cause: err}
    }
    if err := toml.Unmarshal(propsData, &m.Properties); err != nil {
        return nil, wrapDecodeError(err)
    }
}
```

From internal/parser/parser.go (properties skip in captureDefinitionOrder, lines 127-130 — DO NOT TOUCH; Phase 31 Plan 1 extends this to skip include/template/use):
```go
// Skip [properties] section
if len(parts) == 1 && parts[0] == "properties" {
    continue
}
```

From internal/parser/parser.go (unit loop, lines 80-93 — keyed on UnitOrder; the delete(rawMap, "include") guarantees the directive never reaches here even if the skip were absent):
```go
for _, name := range unitOrder {
    value, ok := rawMap[name]
    if !ok { continue }
    // ... parseUnitWithOrder ...
    m.Units[name] = unit
}
```

From internal/parser/errors.go (ParseError shape, lines 13-22 — the error type for any extraction failure):
```go
type ParseError struct {
    Message string
    Line    int
    Context string
    Cause   error
}
```
</interfaces>
</context>

<feature>
  <name>IncludeDirective extraction (parser-side)</name>
  <files>internal/parser/parser.go, internal/parser/parser_test.go</files>
  <behavior>
    Parsing a TOML document with `[[include]]` array-of-tables routes them into `Model.Includes` and creates no phantom units.
    Cases:
    - Input: doc with two `[[include]]` blocks (`path="a.toml" once=true` then `path="b.toml"`) + hand-authored `[user]` → `Model.Includes` has len 2 in document order with `Includes[0].Path=="a.toml"`, `Includes[0].Once==true`, `Includes[1].Path=="b.toml"`, `Includes[1].Once==false`; `Model.UnitOrder` contains "user" only (NOT "include"); `Model.Units` has only "user".
    - Input: doc with a single `[[include]] path="x.toml"` (no `once`) → `Model.Includes` has len 1 with `Once==false`; "include" not in `UnitOrder`.
    - Input: existing testdata/valid.toml (no `[[include]]`) → `Model.Includes` is nil/empty; `Model.UnitOrder`/`Model.Units`/`Model.Properties` identical to current behavior (no regression).
  </behavior>
  <implementation>
    RED: write the two failing tests first (TestParseIncludesExtracted, TestParseNoIncludes) using inline `parser.Parse([]byte(...))`. GREEN: implement (1) `IncludeDirective` struct (`Path string toml:"path"`, `Once bool toml:"once"`) near the `Model` struct; (2) `Includes []IncludeDirective` field on `Model` tagged `toml:"-"`; (3) a rawMap extraction block in `Parse` mirroring the properties extraction at `parser.go:68-77`, placed AFTER the properties block and BEFORE the unit loop at `parser.go:80`, followed by `delete(rawMap, "include")`. REFACTOR: none expected.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing parser tests for [[include]] extraction (RED)</name>
  <files>internal/parser/parser_test.go</files>
  <behavior>
    - TestParseIncludesExtracted: parse an inline TOML doc with `[properties]`, a hand-authored `[user]` person unit, and two `[[include]]` blocks (`path="a.toml" once=true` then `path="b.toml"`); assert `got.Includes` has len 2; `Includes[0].Path == "a.toml"` and `Includes[0].Once == true`; `Includes[1].Path == "b.toml"` and `Includes[1].Once == false`; assert `got.UnitOrder == []string{"user"}` (NOT "include"); assert `got.Units` has only key "user".
    - TestParseIncludesOnceDefaultsFalse: parse a doc with one `[[include]] path="x.toml"` (no `once` key); assert `got.Includes` has len 1 with `Once == false` (zero-value default).
    - TestParseNoIncludes: parse existing `../../testdata/valid.toml`; assert `got.Includes` is nil or len 0; assert `got.UnitOrder`, `got.Units`, `got.Properties` unchanged from current behavior (no regression for single-file models).
  </behavior>
  <action>
    Add the three tests to internal/parser/parser_test.go following the existing idiom (package parser_test, t.Parallel(), testify require/assert). Use inline `parser.Parse([]byte("..."))` for the two include-extraction cases — the TOML content is small (properties + one user unit + one or two [[include]] blocks). For TestParseNoIncludes, reuse the existing `os.ReadFile("../../testdata/valid.toml")` pattern (mirror TestParseValidProperties at parser_test.go:15-28). Define a local const for the repeated "user" key if it reads cleaner. Do NOT modify parser.go in this task — the tests MUST fail to compile (Model.Includes does not exist yet) to prove they exercise new behavior. Run `go test ./internal/parser/ -run 'TestParseIncludes|TestParseNoIncludes'` and confirm the compilation failure mentions `got.Includes` (unknown field).
  </action>
  <read_first>
    - internal/parser/parser_test.go (the existing test idiom — TestParseValidProperties at lines 15-28 is the closest analog)
    - internal/parser/parser.go (the Model struct at lines 35-42 and the properties extraction at lines 68-77 that this plan mirrors)
    - .planning/phases/32-include-directive-multi-file/32-PATTERNS.md (File: internal/parser/parser_test.go section — the analog excerpt)
  </read_first>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/parser/ -run 'TestParseIncludes|TestParseNoIncludes' 2>&1 | grep -v '^#' | tail -15</automated>
  </verify>
  <done>
    Three named tests exist in parser_test.go and FAIL to compile because `Model.Includes` does not exist yet (RED). TestParseNoIncludes may compile+pass already if it only references existing Model fields — if so, that is expected and confirms the test is well-formed; the RED proof comes from the two Include-extraction tests failing on `got.Includes`. The inline TOML in the two include tests is valid TOML (verifiable by the compiler error being "unknown field Includes" rather than a TOML decode error).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement IncludeDirective type, Model.Includes field, and rawMap extraction (GREEN)</name>
  <files>internal/parser/parser.go</files>
  <behavior>
    - All three tests from Task 1 now PASS.
    - `Model` has an `Includes []IncludeDirective` field tagged `toml:"-"` (populated by explicit extraction in Parse, matching UnitOrder's `toml:"-"` convention at parser.go:39).
    - `IncludeDirective` is a struct with `Path string toml:"path"` and `Once bool toml:"once"`.
    - `Parse` extracts `rawMap["include"]` into `&m.Includes` via the same marshal/unmarshal pattern used for properties at parser.go:68-77, then `delete(rawMap, "include")` to guarantee the directive never enters the unit loop.
    - No other behavior changes — `captureDefinitionOrder` is NOT modified (the [[include]] skip lands in Phase 31 Plan 1; this plan consumes it).
  </behavior>
  <action>
    (1) Define the `IncludeDirective` struct in internal/parser/parser.go near the existing `Model` struct (place it just above or below `Model`): `type IncludeDirective struct { Path string \`toml:"path"\`; Once bool \`toml:"once"\` }`. (2) Add `Includes []IncludeDirective \`toml:"-"\`` to the `Model` struct (parser.go:35-42), alongside wherever Phase 31 Plan 1 added `Templates`/`Instantiations` — if Phase 31 has not landed yet, add it directly after `Units`. (3) In `Parse` (parser.go:47-96), immediately AFTER the properties extraction block (lines 68-77) and BEFORE the unit loop (line 80), add an extraction block mirroring properties: `if inc, ok := rawMap["include"]; ok { incData, err := toml.Marshal(inc); if err != nil { return nil, &ParseError{Message: "failed to marshal include", Cause: err} }; if err := toml.Unmarshal(incData, &m.Includes); err != nil { return nil, wrapDecodeError(err) }; delete(rawMap, "include") }`. The `delete` is belt-and-suspenders: it guarantees that even if the Phase 31 skip were somehow absent, the unit loop would not pick up "include" (the loop is keyed on `unitOrder` from captureDefinitionOrder, not rawMap, but delete removes any path). Do NOT enable `DisallowUnknownFields()` (STATE.md DI-1 / BC-3). Do NOT modify captureDefinitionOrder or isBuiltinField.
  </action>
  <read_first>
    - internal/parser/parser.go (full Parse at lines 47-96; the Model struct at 35-42; the properties extraction at 68-77 to mirror exactly)
    - internal/parser/errors.go (ParseError + wrapDecodeError — the error path the extraction reuses)
    - .planning/phases/32-include-directive-multi-file/32-PATTERNS.md (File: internal/parser/parser.go — Change 1 and Change 2 excerpts)
    - .planning/phases/32-include-directive-multi-file/32-RESEARCH.md (Pattern 1: Reserved-table extraction mirrors properties — the exact target code)
  </read_first>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/parser/ -x 2>&1 | grep -v '^#' | tail -20 && golangci-lint run ./internal/parser/ 2>&1 | tail -5</automated>
  </verify>
  <done>
    `go test ./internal/parser/` is fully green (all three new tests pass, all existing tests pass — no regression). `Model.Includes` is a non-nil slice when `[[include]]` tables exist; nil/empty otherwise. The `delete(rawMap, "include")` line is present in Parse. `golangci-lint run ./internal/parser/` and `go vet ./internal/parser/` are clean. captureDefinitionOrder and isBuiltinField are UNCHANGED (verified by `git diff internal/parser/parser.go` showing no edits in the lines 100-157 or 309-316 regions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML input file → parser | Local user-authored .toml content crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Parse on malformed [[include]] tables | mitigate | go-toml/v2 returns errors (no panic); the extraction reuses the existing marshal/unmarshal error path (parser.go:68-77 analog) which surfaces failures as `*ParseError` via `wrapDecodeError`. A malformed `[[include]]` (e.g. missing `path`) surfaces as a clear decode error, not silent acceptance. |
| T-32-02 | Tampering | Path traversal via crafted include `path` | accept (for this plan) | This plan only EXTRACTS the path string into `IncludeDirective.Path`; it does not read the file. File reading + canonicalization is Plan 02's concern (RESEARCH Security Domain: `filepath.Clean`/`Abs` canonicalize for correctness, not as a security boundary; C4Drill is author-controlled local tooling). |
| T-32-SC | Tampering | package installs | accept | This plan installs ZERO packages (stdlib + already-pinned go-toml/v2 only — verified against go.mod in RESEARCH.md). No slopcheck vector. |
</threat_model>

<verification>
- `go test ./internal/parser/ -x` — all parser tests green (3 new + all existing, no regression)
- `go test ./...` — full suite green (parser change has no downstream effect yet; Plan 02 wires consumption)
- `golangci-lint run ./internal/parser/` — clean
- `go vet ./internal/parser/` — clean
- `git diff internal/parser/parser.go` confined to the Model struct + the new extraction block in Parse (no edits to captureDefinitionOrder lines 100-157 or isBuiltinField lines 309-316 — concurrency safety with Phase 28/31)
</verification>

<success_criteria>
- `IncludeDirective` type + `Model.Includes` field (`toml:"-"`) landed in parser.go
- `Parse` extracts `rawMap["include"]` into `m.Includes` and deletes it from rawMap
- A model with `[[include]]` blocks has them in `Model.Includes` (document order, Path/Once populated) and ZERO phantom "include" units in UnitOrder/Units
- A model without `[[include]]` parses identically to before (no regression)
- captureDefinitionOrder and isBuiltinField are UNCHANGED (Phase 31 Plan 1 owns the skip)
- Plan 02 can consume `Model.Includes` directly
</success_criteria>

<output>
Create `.planning/phases/32-include-directive-multi-file/32-01-SUMMARY.md` when done
</output>
