---
phase: 29-optional-name-humanization
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - README.md
  - skill/SKILL.md
autonomous: true
requirements:
  - ERGO-03
  - ERGO-04
  - ERGO-05

must_haves:
  truths:
    - "README.md TOML Format section documents that `name` is OPTIONAL and shows the humanize rule with a before/after example (localIDP → \"Local IDP\", sessionManager → \"Session Manager\")"
    - "README.md documents the dumb-split behavior and the acronym escape hatch (gRPC → \"Grpc\"; set explicit name = for acronyms)"
    - "README.md documents that explicit name = always wins (backward-compat)"
    - "skill/SKILL.md Required Fields / Unit Definition section reflects that `name` is optional (not Required) with the humanize behavior and escape hatch"
    - "No existing doc content is removed or contradicted; the change is purely additive (new Optional Name subsection + Required→Optional relabel where `name` is described)"
  artifacts:
    - README.md (modified — new Optional Name subsection under TOML Format; relabel `name` annotations)
    - skill/SKILL.md (modified — Required Fields + Unit Definition sections updated)
  key_links:
    - "README.md Optional Name example renders the same display name the parser produces (consistent with Plan 01's model.Humanize contract)"
---

<objective>
Document the optional-`name` ergonomics feature shipped in Plan 01 so users discover it. Update the two user-facing doc surfaces (README.md TOML Format section and skill/SKILL.md schema reference) to mark `name` as optional, explain the dumb camelCase humanization, show a before/after example, and document the explicit-`name`-wins escape hatch (especially for acronyms like `gRPC`).

Purpose: A feature ships only when users can find and use it. The original ergonomic todo explicitly lists README + skill/SKILL.md updates as part of every accepted ergonomics change ("Validation/docs" section). This plan depends on Plan 01 so the docs accurately describe the shipped behavior, not a hypothesis.

Output: README.md with a new "Optional Name" subsection; skill/SKILL.md with `name` relabeled from Required to Optional and a short humanize note. Both changes are additive — no existing content removed.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-optional-name-humanization/29-CONTEXT.md
@.planning/phases/29-optional-name-humanization/29-RESEARCH.md
@.planning/phases/29-optional-name-humanization/29-01-SUMMARY.md

<interfaces>
<!-- Doc structure the executor edits. Extracted from the files. -->

README.md structure (relevant sections):
  line 34: ## TOML Format
  line 36: ### Minimal Example
  line 51: ### Properties Section
  line 63: ### Unit Types
  line 138: #### Box (Grouping Container)
  line 147: #### Reference (External Documentation URL)   <-- Phase 28 territory; do NOT touch
  line 164: ### Nesting (C2/C3 Diagrams)
  line 190: ### Links (Relationships)
  >>> INSERT new "### Optional Name (Humanization)" subsection AFTER ### Unit Types
      (after line ~163, before ### Nesting) so it sits with the other unit-definition topics.

skill/SKILL.md structure (relevant sections):
  line 40: ### Required Fields        <-- contains `name = "Display Name"  # Required`
  line 74: ## Schema Reference
  line 92: ### Unit Definition         <-- `name = "Display Name"  # Required: Display name`
  line 114: #### reference (...)        <-- Phase 28 territory; do NOT touch
  >>> RELABEL `name` from Required to Optional in both Required Fields and Unit Definition;
      add a short humanize note + escape-hatch example.

The model.Humanize contract (from Plan 01, must match docs exactly):
  localIDP       -> "Local IDP"
  sessionManager -> "Session Manager"
  linuxSystem    -> "Linux System"
  gRPC           -> "Grpc"          (acronym NOT preserved — escape with explicit name =)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Document optional name + humanization in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full TOML Format section, lines 34-190; note the existing example style and comment conventions like `# Required:` and `# Optional:`)
    - .planning/phases/29-optional-name-humanization/29-01-SUMMARY.md (the shipped behavior — docs must match exactly)
    - .planning/phases/29-optional-name-humanization/29-RESEARCH.md §Code Examples (the 9-row reference table — pick the user-facing examples)
  </read_first>
  <action>
    In README.md, under `## TOML Format`, add a new subsection `### Optional Name (Humanization)` immediately AFTER the `### Unit Types` section (i.e., after the last `####` unit-type subsection, before `### Nesting (C2/C3 Diagrams)`). The subsection MUST contain:

    1. A one-sentence intro: the `name` field is optional; when omitted, the display name is derived from the last segment of the unit's identifier via a dumb camelCase split.
    2. A before/after TOML example showing the same unit with and without `name`:
       ```toml
       # Explicit name (always wins — use this for acronyms or custom labels)
       [linuxSystem.localIDP]
       name = "My Custom Name"

       # Name omitted — humanized from the last path segment "localIDP"
       [linuxSystem.sessionManager]
       # displays as "Session Manager"
       ```
    3. A short "Humanization rules" list: splits camelCase boundaries and Title-cases each word; works on the LAST path segment only (so `[linuxSystem.localIDP]` → "Local IDP", not "Linux System Local IDP"). Give 3-4 examples: `sessionManager` → "Session Manager", `localIDP` → "Local IDP", `linuxSystem` → "Linux System".
    4. An "Acronyms" note: acronym preservation is intentionally NOT supported (dumb split); `gRPC` humanizes to "Grpc". To preserve an acronym or set any custom label, set `name =` explicitly — explicit name always wins.
    5. A backward-compat note: existing models with explicit `name =` are unaffected.

    Additionally, scan the `### Unit Types` section's per-type examples (lines ~72-163) and the `### Minimal Example` (lines 36-49): where a comment says `# Required: Display name` on a `name =` line, RELABEL it to `# Optional: defaults to humanized identifier`. Do NOT remove the `name =` lines from those examples (they remain valid, just no longer required). Do NOT touch the `#### Reference` subsection (line 147) — that is Phase 28's territory and is being edited concurrently.

    Keep the writing style consistent with the existing README (short paragraphs, TOML code blocks, inline `# comments`).
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -c "Optional Name" README.md &amp;&amp; grep -c "Humanization" README.md &amp;&amp; grep -ci "acronym" README.md</verify>
  <acceptance_criteria>
    - README.md contains a `### Optional Name (Humanization)` heading under `## TOML Format`
    - The subsection includes a before/after TOML example with both an explicit-name and an omitted-name unit
    - The subsection states the last-segment-only rule with at least the `localIDP` → "Local IDP" and `sessionManager` → "Session Manager" examples
    - The subsection documents the dumb-split acronym behavior (`gRPC` → "Grpc") and the explicit-`name` escape hatch
    - At least one former `# Required: Display name` (or `# Required:` name) comment in the Unit Types / Minimal Example sections is relabeled to indicate `name` is optional
    - The `#### Reference` subsection is UNCHANGED (verify: `git diff README.md` shows no edits near the Reference heading)
    - All examples in the doc match model.Humanize's actual output (cross-check against 29-01-SUMMARY.md or the reference table)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Update skill/SKILL.md: name is optional + humanize note</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (lines 40-46 Required Fields; lines 92-113 Unit Definition; note the existing comment style `# Required:` / `# Optional:`)
    - .planning/phases/29-optional-name-humanization/29-01-SUMMARY.md (shipped behavior)
    - README.md (the Optional Name subsection just written in the previous task — keep SKILL.md consistent with it)
  </read_first>
  <action>
    In skill/SKILL.md:

    1. In `### Required Fields` (line ~40): the `name = "Display Name"  # Required` entry is no longer accurate. RELABEL the comment to `# Optional: humanized from identifier if omitted` (keep the line, just fix the annotation). If the section lists fields that are genuinely still required (like nothing else — most fields are optional), leave those alone.

    2. In `### Unit Definition` (line ~92): the `name = "Display Name"  # Required: Display name` line (around line 99) — RELABEL to `# Optional: defaults to humanized last path segment`.

    3. Add a short note (2-4 sentences) under `### Unit Definition` (after the field table/list, before the next `####` subsection) titled `**Optional name humanization**` stating: when `name` is omitted, the display name is derived from the last segment of the unit's identifier via a dumb camelCase split (e.g. `localIDP` → "Local IDP", `sessionManager` → "Session Manager"). Acronyms are NOT preserved (`gRPC` → "Grpc") — set `name =` explicitly to override. Explicit `name =` always wins.

    4. Do NOT touch the `#### reference` subsection (line ~114) — Phase 28 territory.

    Keep the SKILL.md voice consistent with its existing schema-reference style (terse, field-oriented).
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -ci "humaniz\|Optional.*name\|name.*Optional" skill/SKILL.md &amp;&amp; ! grep -n "# Required: Display name" skill/SKILL.md</verify>
  <acceptance_criteria>
    - skill/SKILL.md no longer contains the literal `# Required: Display name` comment (it has been relabeled Optional)
    - skill/SKILL.md `### Unit Definition` contains a humanization note mentioning `localIDP` → "Local IDP" (or equivalent) and the gRPC/Grpc acronym escape hatch
    - The `#### reference` subsection is UNCHANGED (`git diff skill/SKILL.md` shows no edits near the reference heading)
    - The humanize examples in SKILL.md match model.Humanize's actual output and are consistent with README.md's Optional Name subsection
    - No existing field documentation is removed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No code, no config, no runtime behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Information Disclosure | N/A — docs contain no secrets/PII | accept | README and SKILL.md are public project docs with example identifiers only. |

**Net security impact: none.** Documentation-only.
</threat_model>

<verification>
1. `git diff --stat README.md skill/SKILL.md` — both files modified, changes localized to the intended sections
2. `grep -rn "Reference" README.md skill/SKILL.md | head` — Reference subsections still present and unmodified (Phase 28 concurrency safety)
3. Cross-check: every humanize example written in the docs matches a row in Plan 01's model.Humanize reference table (no invented outputs)
4. `go test ./...` — still green (docs change must not break anything; sanity check)
</verification>

<success_criteria>
- README.md has an Optional Name (Humanization) subsection with before/after example, last-segment rule, acronym escape hatch
- skill/SKILL.md relabels `name` from Required to Optional in both Required Fields and Unit Definition, with a humanize note
- Both docs are consistent with each other and with the shipped model.Humanize behavior from Plan 01
- No edits to the Phase 28 Reference subsections (concurrency-safe)
- ERGO-03/04/05 documented for users
</success_criteria>

<output>
Create `.planning/phases/29-optional-name-humanization/29-02-SUMMARY.md` when done.
</output>
