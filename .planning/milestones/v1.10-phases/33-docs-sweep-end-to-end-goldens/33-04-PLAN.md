---
phase: 33-docs-sweep-end-to-end-goldens
plan: 04
type: execute
wave: 2
depends_on:
  - "33-01"
  - "33-02"
files_modified:
  - cmd/c4drill/root_test.go
autonomous: true
requirements:
  - XC-01
  - XC-05

must_haves:
  truths:
    - "cmd/c4drill/root_test.go contains TestXC05_ComposedEquivSingleFile which renders skill/examples/09-composed/entry.toml AND skill/examples/09-composed/single-file-equivalent.toml through the full pipeline (ParseFile → include.Resolve → template.Expand → peer.Resolve → humanize → Validate → GenerateExpandedView → BuildExpandedGraph → RenderDOT), canonicalizes both DOT outputs with canonical.Canonical, and asserts equality (XC-05)"
    - "cmd/c4drill/root_test.go contains TestXC01_PipelineOrdering which renders skill/examples/09-composed/entry.toml (or a narrower crafted fixture) through the full pipeline and asserts the specific order-dependent behavior: a relative peer authored inside a template resolves to the INSTANTIATION-SITE sibling, not the template-site (XC-03), AND a template defined in an included file is visible to a [[use]] in the entry file (XC-02) — both only true if include runs before template.Expand runs before peer.Resolve (XC-01)"
    - "Both tests use the reusable canonical.Canonical helper from internal/testutil/canonical (Plan 01) for any DOT comparison — NEVER byte-exact require.Equal on raw DOT (STATE.md DI-1)"
    - "Both tests are skipped or fail-clearly if the v1.10 pipeline passes (include.Resolve/template.Expand/peer.Resolve) are not yet wired into root.go — so running Phase 33 before Phases 30-32 ship produces a clear 'pipeline not ready' signal, not a mysterious compile failure"
    - "The E2E tests exercise the REAL pipeline (calling through parser.ParseFile + the pipeline pass functions + validator + view + graph + render), NOT a reimplementation — they catch composition bugs a model-level deep-equal would miss (a templated unit's reference URL not rendering, a relative peer inside a template resolving to the wrong target)"
  artifacts:
    - cmd/c4drill/root_test.go (modified — 2 new E2E test functions)
  key_links:
    - "canonical.Canonical imported from github.com/Djarvur/c4drill/internal/testutil/canonical (Plan 01)"
    - "TestXC05 reads skill/examples/09-composed/{entry,single-file-equivalent}.toml (Plan 02)"
    - "TestXC01 reads skill/examples/09-composed/entry.toml (Plan 02, shared fixture per D-20 discretion lean)"
---

<objective>
Add the two end-to-end integration tests that prove the four v1.10 features compose correctly through the full pipeline. XC-05: the composed multi-file fixture (include + templates + relative-peer + reference) renders to identical DOT as its hand-expanded single-file equivalent (canonicalDOT comparison, STATE.md DI-1). XC-01: a behavioral test asserting the pipeline ordering is load-bearing — a relative peer inside a template resolves to the instantiation-site sibling (not the template-site), and a template defined in an included file is visible to a `[[use]]` — both only true when include → template.Expand → peer.Resolve runs in that order (D-20).

Purpose: This is the milestone's integration proof. Plan 32 deferred SVG goldens to Phase 33 expecting the reusable canonicalDOT helper (Plan 01); this plan uses that helper + the composed fixture (Plan 02) to assert the composition contract. Per D-20, the behavioral XC-01 test reuses the XC-05 composed fixture with a different assertion (the golden already implicitly enforces correct ordering — skip or reorder any pass and the multi-file templated relative-peer case breaks visibly). The two tests share the fixture but assert different properties for clarity.

Output: 2 new test functions in `cmd/c4drill/root_test.go`. No production code. The tests depend on Phases 30-32 having wired `include.Resolve`, `template.Expand`, `peer.Resolve` into the pipeline — execution of this plan MUST wait for those phases.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-VALIDATION.md

<interfaces>
<!-- The full pipeline the E2E tests must exercise. Verified read of cmd/c4drill/root.go + the pass packages. -->

// The pipeline (current state after Phase 30; Phases 31/32 add the missing passes):
//   parser.ParseFile(path) → *parser.Model, error
//   include.Resolve(m)     → *parser.Model, error    // Phase 32 — runs FIRST
//   template.Expand(m)     → *parser.Model, error    // Phase 31 — runs after include
//   peer.Resolve(m)        → error                   // Phase 30 — wired at root.go:122, runs after template.Expand
//   (humanize runs INSIDE parser.ParseFile at parser.go:203 per Phase 29; XC-04 may relocate it — verify)
//   validator.Validate(m)  → []error                 // root.go:127
//   view.GenerateExpandedView(m) → *view.ExpandedView
//   graph.BuildExpandedGraph(v) → *graph.Graph
//   render.RenderDOT(g)    → []byte, error

// The canonical helper (Plan 01, lives at internal/testutil/canonical):
import "github.com/Djarvur/c4drill/internal/testutil/canonical"
canon := canonical.Canonical(t, string(dotBytes))   // order-insensitive semantic form (DI-1)

// Existing E2E-golden precedent (internal/graph/builder_test.go:2720-2756 TestReference_BackwardCompat):
//   ParseFile → Validate → GenerateExpandedView → BuildExpandedGraph → RenderDOT → Canonical → require.Equal
// XC-05 follows this pattern but inserts include.Resolve + template.Expand + peer.Resolve between ParseFile and Validate.

// Test file + conventions (cmd/c4drill/root_test.go): package main, testify require/assert,
// fixtures via parser.ParseFile with relative paths from cmd/c4drill/ (e.g. "../../skill/examples/09-composed/entry.toml").
// Existing precedent reads "../../cmd/c4drill/testdata/multilevel.toml" from internal/graph — cross-dir reads are established.

// Phase-30-already-shipped signal: peer.Resolve is wired at root.go:122 (verified).
// Phase-31/32 NOT-yet-shipped signal: internal/template/ and internal/include/ do not exist yet (verified).
// This plan's tests reference include.Resolve + template.Expand — they will not compile until 31/32 ship.
// MUST declare the cross-phase execution dependency (this plan cannot execute until 30+31+32 ship).
</interfaces>
</context>

<feature>
  <name>XC-05 golden + XC-01 behavioral E2E tests (D-18, D-20)</name>
  <files>cmd/c4drill/root_test.go</files>
  <behavior>
    Two new test functions in `cmd/c4drill/root_test.go`, both exercising the full v1.10 pipeline:

    **`TestXC05_ComposedEquivSingleFile`** — renders the composed multi-file fixture (`skill/examples/09-composed/entry.toml`) AND its hand-expanded single-file equivalent (`skill/examples/09-composed/single-file-equivalent.toml`) through the complete pipeline: `ParseFile → include.Resolve → template.Expand → peer.Resolve → Validate → GenerateExpandedView → BuildExpandedGraph → RenderDOT`. Canonicalizes both DOT outputs with `canonical.Canonical` and asserts `require.Equal`. This proves include + templates + relative-peer + reference compose to an indistinguishable rendered result (XC-05). Uses canonicalDOT (DI-1) — NOT byte-exact — because go-graphviz emits map-order-dependent sibling order and layout geometry.

    **`TestXC01_PipelineOrdering`** — renders the composed fixture (or a narrower crafted input) and asserts the two order-dependent properties that ONLY hold when the pipeline runs include → template.Expand → peer.Resolve in order: (a) the relative peer authored inside the template (e.g. `peer = "${upstreamBus}"` resolving to `messageBus`) connects the instantiated unit to the instantiation-site's `messageBus`, NOT to a template-site fabrication (XC-03); (b) the template defined in `templates.toml` (reached via `[[include]]`) is visible to the `[[use]]` in `entry.toml` (XC-02). The assertion is behavioral: inspect the rendered DOT (or the validated model's links) for the specific edge that proves correct ordering. This detects pipeline reordering as a regression (XC-01) without a fragile source-scan (D-20).

    Both tests use `canonical.Canonical` for any DOT comparison. Both are robust to refactors that move pipeline calls into helper functions (they test behavior, not source layout — D-20 explicitly rejected the source-scan alternative).
  </behavior>
  <implementation>
    **Pre-condition gate (critical):** This plan CANNOT execute until Phases 30, 31, 32 have all shipped — the tests reference `include.Resolve` and `template.Expand` which do not exist until 32 and 31 land. The `depends_on: ["33-01","33-02"]` is WITHIN phase 33; the CROSS-PHASE dependency on 30/31/32 is recorded in STATE.md by the planner and must be honored by execute-phase. If execute-phase tries to run this plan before 31/32 ship, the test file will fail to compile (`undefined: include.Resolve` / `undefined: template.Expand`). The executor should verify `internal/template/` and `internal/include/` exist before starting; if not, STOP and report the blocker.

    **Pipeline invocation (XC-05):** Write a small helper in the test (or inline) that runs the full pipeline on a fixture path and returns the rendered DOT bytes:
    ```go
    func renderComposed(t *testing.T, path string) string {
        m, err := parser.ParseFile(path)
        require.NoError(t, err)
        // v1.10 pipeline (matches root.go ordering):
        m, err = include.Resolve(m)
        require.NoError(t, err)
        m, err = template.Expand(m)
        require.NoError(t, err)
        require.NoError(t, peer.Resolve(m))
        // humanize ran inside ParseFile (parser.go:203) — verify Phase 31's XC-04 did not relocate it;
        // if it did, add the explicit humanize call here per the relocated site.
        require.Empty(t, validator.Validate(m), "model should be valid after pipeline")
        v := view.GenerateExpandedView(m)
        g := graph.BuildExpandedGraph(v)
        dot, err := render.RenderDOT(g)
        require.NoError(t, err)
        return canonical.Canonical(t, string(dot))
    }
    ```
    The two test paths (`../../skill/examples/09-composed/entry.toml` and `../../skill/examples/09-composed/single-file-equivalent.toml`) both go through `renderComposed`; assert `require.Equal(t, renderComposed(t, multiFile), renderComposed(t, singleFile))`.

    **Verify the humanize site before finalizing (Open Question A2 from RESEARCH):** the executor reads `internal/parser/parser.go` (around line 200-203) and Phase 29's SUMMARY to confirm whether humanize still runs inside ParseFile or was relocated by Phase 31's XC-04 work. If relocated to a standalone pass (e.g. `humanize.Apply(m)`), add that call to `renderComposed` between peer.Resolve and Validate. If still inside ParseFile, no explicit call needed. The pipeline-ordering note in root.go's comments is the authoritative hint.

    **Behavioral assertion (XC-01):** Use the same `renderComposed` helper on `entry.toml`, then assert the order-dependent edge is present in the canonical output. The specific edge: the instantiated `platform.auth` unit's parametrized link (`peer = "${upstreamBus}"` with `upstreamBus = "messageBus"`) must render as an edge from `platform.auth` to `messageBus`. Assert this via `require.Contains(t, canon, `"platform.auth" -> "messageBus"`)` (or the canonical equivalent — check the canonical form's edge serialization in Plan 01's helper). If the pipeline were mis-ordered (e.g. peer.Resolve before template.Expand), the `${upstreamBus}` would not yet be substituted and the peer would resolve incorrectly or fail. Additionally assert the template-from-included-file case: the `[[use]]` in entry.toml instantiates `microservice` which is DEFINED in the included `templates.toml` — if include ran AFTER template.Expand, the `[[use]]` would fail with "unknown template". A successful render proves include ran first (XC-02).

    **Test isolation:** Use `//nolint:paralleltest` (go-graphviz WASM engine has concurrency issues — established convention in root_test.go and builder_test.go). Do NOT modify existing tests.

    Do NOT use byte-exact `require.Equal` on raw DOT — always canonicalize first (DI-1). Do NOT add a source-scan test for pipeline ordering (D-20 rejected it).
  </implementation>
</feature>

<tasks>

<task type="execute">
  <name>Add TestXC05_ComposedEquivSingleFile E2E golden test</name>
  <files>cmd/c4drill/root_test.go</files>
  <read_first>
    - cmd/c4drill/root_test.go (existing test style: package main, testify, //nolint:paralleltest, fixture paths via "../../...")
    - internal/graph/builder_test.go (lines 2720-2756 — TestReference_BackwardCompat, the E2E-golden precedent; lines 1235-1247 — the canonicalDOT call pattern)
    - internal/testutil/canonical/canonical.go (Plan 01 — the exported Canonical(t, dot) signature)
    - cmd/c4drill/root.go (lines 112-132 — the pipeline: ParseFile, peer.Resolve at :122, Validate at :127; confirm where include.Resolve and template.Expand will insert)
    - skill/examples/09-composed/entry.toml + single-file-equivalent.toml (Plan 02 — the fixture pair being compared)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md (§Code Examples — the E2E pipeline invocation; §Common Pitfalls Pitfall 2/3 execution-wait; Open Question A2 humanize site)
    - internal/parser/parser.go (lines 200-203 — verify humanize is still inside ParseFile, OR check Phase 31 SUMMARY if XC-04 relocated it)
    - .planning/STATE.md (DI-1 — canonicalDOT mandate)
  </read_first>
  <action>
    PRE-CONDITION CHECK: Before writing, confirm `internal/template/` and `internal/include/` packages exist (Phases 31 + 32 shipped). If they do NOT exist, STOP — this plan cannot execute until 30+31+32 ship. Report the blocker; do not write tests that will not compile.

    Add to `cmd/c4drill/root_test.go`:
    1. A test-local helper `renderThroughPipeline(t *testing.T, path string) string` that runs the full v1.10 pipeline on the fixture at `path` and returns the canonical DOT. The pipeline sequence MUST match root.go's ordering: `parser.ParseFile` → `include.Resolve` → `template.Expand` → `peer.Resolve` → (humanize, if not inside ParseFile — verify) → `validator.Validate` (assert empty) → `view.GenerateExpandedView` → `graph.BuildExpandedGraph` → `render.RenderDOT` → `canonical.Canonical`. Use `require.NoError`/`require.Empty` at each stage so failures point to the broken pass. Import `github.com/Djarvur/c4drill/internal/testutil/canonical`, plus the pipeline packages (`include`, `template`, `peer`, `parser`, `validator`, `view`, `graph`, `render`).
    2. `func TestXC05_ComposedEquivSingleFile(t *testing.T)` (with `//nolint:paralleltest` per go-graphviz convention). It calls `renderThroughPipeline` on BOTH `../../skill/examples/09-composed/entry.toml` and `../../skill/examples/09-composed/single-file-equivalent.toml`, then asserts `require.Equal(t, multiCanon, singleCanon, "XC-05: composed multi-file model must render identical to its hand-expanded single-file equivalent (canonicalDOT, DI-1)")`.

    Verify the humanize call site (Open Question A2): read parser.go around the Phase 29 hook; if humanize is inside ParseFile, no explicit call; if Phase 31's XC-04 relocated it to a standalone pass, add the call between peer.Resolve and Validate. Add a comment in the helper citing the humanize site.

    If the two fixtures do NOT canonicalize equal on first run, investigate whether the FIXTURE (Plan 02's single-file-equivalent.toml) is wrong OR the CODE is wrong before changing either (RESEARCH Pitfall 4). Fix the fixture side until they match — the single-file-equivalent is the "expected" golden.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; go test ./cmd/c4drill/ -run TestXC05_ComposedEquivSingleFile -x 2&gt;&amp;1 | tail -20</verify>
  <acceptance_criteria>
    - `grep -c 'func TestXC05_ComposedEquivSingleFile' cmd/c4drill/root_test.go` returns 1
    - `grep -c 'canonical.Canonical' cmd/c4drill/root_test.go` returns at least 2 (the helper canonicalizes; or the test calls it on both sides)
    - `grep -c 'include.Resolve\|template.Expand\|peer.Resolve' cmd/c4drill/root_test.go` returns at least 3 (the full pipeline is invoked)
    - The test does NOT use byte-exact `require.Equal` on raw DOT — only on canonical output
    - `go test ./cmd/c4drill/ -run TestXC05_ComposedEquivSingleFile -x` exits 0 (the composed fixture and its single-file equivalent canonicalize equal)
    - The test has `//nolint:paralleltest` (go-graphviz convention)
    - No existing test in root_test.go was modified
    - Commit message starts with `test(33-04):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Add TestXC01_PipelineOrdering behavioral test (XC-01, XC-02, XC-03)</name>
  <files>cmd/c4drill/root_test.go</files>
  <read_first>
    - cmd/c4drill/root_test.go (the renderThroughPipeline helper from the previous task — reuse it)
    - skill/examples/09-composed/entry.toml (Plan 02 — the fixture whose render proves ordering)
    - skill/examples/09-composed/templates.toml (the template defined in an INCLUDED file — XC-02 proof)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-20 behavioral test spec)
    - .planning/phases/31-template-expansion/31-CONTEXT.md (D-01/D-02 XC-03 instantiation-site resolution)
    - .planning/phases/32-include-directive-multi-file/32-CONTEXT.md (XC-02 template-isolation use case)
    - .planning/REQUIREMENTS.md (XC-01, XC-02, XC-03 full text)
  </read_first>
  <action>
    Add `func TestXC01_PipelineOrdering(t *testing.T)` (with `//nolint:paralleltest`) to `cmd/c4drill/root_test.go`. It reuses the `renderThroughPipeline` helper on `../../skill/examples/09-composed/entry.toml` and asserts the two order-dependent properties that ONLY hold when the pipeline runs include → template.Expand → peer.Resolve in the documented order:

    **Assertion A (XC-02 — include before template.Expand):** The `[[use]] template = "microservice"` in entry.toml succeeds in instantiating a template that is DEFINED in the included `templates.toml`. Proof: the render completed without "unknown template" error (the `require.NoError` inside `renderThroughPipeline` at the `template.Expand` step would have fired otherwise). To make this assertion EXPLICIT rather than implicit, the test can additionally assert the instantiated unit appears in the canonical output: `require.Contains(t, canon, "platform.auth")` (or the canonical edge form). If include ran AFTER template.Expand, the `[[use]]` would fail at Expand time.

    **Assertion B (XC-03 — template.Expand before peer.Resolve):** The instantiated `platform.auth` unit's parametrized link (`peer = "${upstreamBus}"` with `upstreamBus = "messageBus"`) renders as an edge connecting `platform.auth` to `messageBus` (the instantiation-site sibling/top-level unit), NOT to a template-site fabrication. Assert via `require.Contains(t, canon, <the canonical edge between platform.auth and messageBus>)` — inspect Plan 01's canonical edge serialization format (`attr\x00"platform.auth" -> "messageBus"\x00...`) to craft the correct substring. If peer.Resolve ran BEFORE template.Expand, the `${upstreamBus}` literal would not yet be substituted and peer resolution would fail or resolve wrongly.

    Add a clear comment block above the test explaining: this is the XC-01 behavioral ordering regression guard (D-20); it proves the pipeline order is load-bearing WITHOUT a fragile source-scan; reordering any pass breaks one of the two assertions. Cite XC-01, XC-02, XC-03.

    If the canonical edge substring assertion is brittle to craft, fall back to a model-level assertion: after the pipeline runs, inspect `m.Units["platform.auth"].Links` for a link whose Peer resolved to "messageBus" (proving the parametrized peer expanded then resolved correctly). Either assertion form is acceptable; the behavioral proof is what matters.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; go test ./cmd/c4drill/ -run TestXC01_PipelineOrdering -x 2&gt;&amp;1 | tail -20</verify>
  <acceptance_criteria>
    - `grep -c 'func TestXC01_PipelineOrdering' cmd/c4drill/root_test.go` returns 1
    - The test asserts BOTH order-dependent properties (include-before-expand via the successful render of an included template, AND expand-before-peerresolve via the parametrized peer resolving to the instantiation-site target)
    - The test cites XC-01, XC-02, XC-03 in a comment block explaining it is the behavioral ordering guard (D-20)
    - The test does NOT grep source files (no source-scan — D-20 rejected it)
    - `go test ./cmd/c4drill/ -run TestXC01_PipelineOrdering -x` exits 0
    - The test reuses the `renderThroughPipeline` helper (no duplicated pipeline logic)
    - No existing test modified
    - Commit message starts with `test(33-04):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Full-suite regression gate for Phase 33</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-VALIDATION.md (§Sampling Rate — full suite is the phase gate)
  </read_first>
  <action>
    Run the full test suite to confirm Phase 33's additions (helper extraction, fixtures loaded by tests, docs) introduce no regression. The fixtures themselves are data (no test runs them unless Plan 04's tests do), so this gate primarily confirms: (a) the canonical helper extraction didn't break internal/graph; (b) the new E2E tests pass; (c) no other package regressed.

    If the suite is green, no commit required (verification only). Note "full suite green, Phase 33 complete" in the plan SUMMARY. This is the phase gate per VALIDATION.md.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; go test ./... 2&gt;&amp;1 | tail -20</verify>
  <acceptance_criteria>
    - `go test ./...` exits 0 (full suite green — Phase 33 introduces no regression)
    - `go vet ./...` exits 0
    - If a commit is made, message starts with `test(33-04):`; if verification only, note in SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | These are test functions exercising the existing pipeline on committed fixtures. No new trust boundary, no production code change, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-T | Tampering | N/A — tests assert on trusted pipeline output | accept | The tests read committed fixtures and assert on rendered DOT; no untrusted input crosses a boundary. |
| T-33-04-SC | Tampering | N/A — zero new dependencies | accept | No package installs; imports are all internal (parser, include, template, peer, validator, view, graph, render, testutil/canonical). |

**Net security impact: none.** Test-only additions.
</threat_model>

<verification>
Phase-level checks (executor runs after all tasks):
1. `go test ./cmd/c4drill/ -run 'TestXC05_ComposedEquivSingleFile|TestXC01_PipelineOrdering' -x` — both new E2E tests pass
2. `go test ./...` — full suite green (no regression from helper extraction, fixture loading, or new tests)
3. `go vet ./...` — clean
4. `grep -c 'TestXC05_ComposedEquivSingleFile\|TestXC01_PipelineOrdering' cmd/c4drill/root_test.go` returns 2
5. `grep -c 'canonical.Canonical' cmd/c4drill/root_test.go` returns at least 2 (DI-1 honored — no byte-exact DOT comparison)
6. Cross-phase gate: confirm Phases 30, 31, 32 are all shipped (internal/peer, internal/template, internal/include all exist; root.go wires all three passes) before this plan is considered complete
</verification>

<success_criteria>
- XC-05: composed multi-file fixture renders identical (canonicalDOT) to its hand-expanded single-file equivalent (D-18)
- XC-01: behavioral test proves pipeline ordering is load-bearing (D-20) — detects reordering as a regression without a fragile source-scan
- Both tests use the reusable canonical.Canonical helper (Plan 01) — DI-1 honored
- Both tests exercise the REAL full pipeline (include → template.Expand → peer.Resolve → validate → views → render) — catch render-layer composition bugs
- XC-02 (template in included file visible to [[use]]) and XC-03 (relative peer in template resolves at instantiation site) are covered as the order-dependent assertions in TestXC01
- Cross-phase dependency honored: this plan executed only after Phases 30-32 shipped
- No production code modified
</success_criteria>

<output>
Create `.planning/phases/33-docs-sweep-end-to-end-goldens/33-04-SUMMARY.md` when done. Document: the humanize call-site resolution (Open Question A2 — inside ParseFile vs relocated), the canonical edge substring used in the XC-01 assertion, and confirm Phases 30-32 were all shipped before execution.
</output>
