---
phase: 33-docs-sweep-end-to-end-goldens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - skill/SKILL.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "README.md contains a '### Optional Type (Inference)' section documenting that `type` is omittable, with the full default-type-by-parent table (defaultTypeForParent, parser.go:250) AND the generic db/queue promotion-by-nesting-level table (inferGenericType, parser.go:276), plus a before/after example (DOC-01)"
    - "README.md contains sections documenting templates (### Templates), include (### Multi-File Composition (Include)), and relative-peer (### Relative Peer Resolution) — each with a syntax block + one short example + a pointer to the skill/examples/ fixture (DOC-02)"
    - "skill/SKILL.md contains the full type-inference rules (both tables) in its Schema Reference, replacing the partial 'Type defaults' note (DOC-01)"
    - "skill/SKILL.md contains per-feature schema sections for templates, include, and relative-peer — each with the full field table + validation rules + a pipeline-ordering note + interactions with other features (DOC-02)"
    - "The new README sections match the established style of the existing '#### Reference' and '### Optional Name (Humanization)' sections (D-19 fill-gaps, not rewrite)"
    - "The new SKILL.md sections match the established style of the existing '#### reference' and 'Optional name humanization' blocks (D-19 fill-gaps, not rewrite)"
    - "Phase 28 (reference) and Phase 29 (humanization) doc sections are NOT modified, moved, or rewritten — only the three missing features + DOC-01 are added (D-19)"
    - "A pipeline-ordering note appears in SKILL.md documenting: include.Resolve → template.Expand → peer.Resolve → humanize → validate → views → render, and why ordering is load-bearing (per D-19)"
  artifacts:
    - README.md (modified — 4 new sections: Optional Type, Templates, Multi-File Composition, Relative Peer Resolution)
    - skill/SKILL.md (modified — 4 new schema sections + expanded type-inference + pipeline-ordering note)
  key_links:
    - "README.md templates section points to skill/examples/06-templates.toml (Plan 02)"
    - "README.md include section points to skill/examples/08-include/ (Plan 02)"
    - "README.md relative-peer section points to skill/examples/07-relative-peer.toml (Plan 02)"
---

<objective>
Fill the documentation gaps (D-19) in `README.md` (user-facing) and `skill/SKILL.md` (agent-facing schema reference): add DOC-01 (type is omittable, with the full inference rules) and DOC-02 (templates, include, relative-peer — the three features Phases 28/29 did not document). Reference (Phase 28) and optional-name humanization (Phase 29) are already documented in both files and are NOT reworked.

Purpose: CONTEXT D-19 locks "fill gaps, don't rewrite" — Phase 28 + 29 executors established a consistent doc style (per-feature: syntax block + short example for README; full field table + validation rules for SKILL). Phase 33 matches that style for the three missing features plus DOC-01. This closes DOC-01 and DOC-02.

Output: 4 new sections in README.md (Optional Type Inference, Templates, Multi-File Composition, Relative Peer Resolution), 4 new schema sections + an expanded type-inference block + a pipeline-ordering note in skill/SKILL.md. No existing section is moved or rewritten.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md

<interfaces>
<!-- The exact code to transcribe for DOC-01. Verified read of internal/parser/parser.go. -->

// parser.go:250-272 — defaultTypeForParent (DEFAULT type by parent):
//   ""             → system            (C1 root)
//   system         → container         (C2)
//   box            → system            (C1 same-level grouping)
//   container      → component         (C3)
//   containerBox   → container         (C2 same-level grouping)
//   componentBox   → component         (C3 same-level grouping)
//   (other)        → system            (C1 fallback)

// parser.go:276-317 — inferGenericType (GENERIC db/queue promotion by nesting level):
//   parent "" or box         → db stays db,          queue stays queue          (C1)
//   parent system/containerBox → db → containerDb,    queue → containerQueue    (C2)
//   parent container/componentBox → db → componentDb, queue → componentQueue    (C3)

<!-- Existing README style anchors (verified read): -->
//   Line 147-162: "#### Reference (External Documentation URL)" — 1-paragraph intro + toml block + 1-line note. MATCH THIS.
//   Line 164-186: "### Optional Name (Humanization)" — intro + toml block + **Humanization rules:** bullets + **Acronyms:** + **Backward compatibility:**. MATCH THIS depth.
//   INSERTION POINT: after the Optional Name section (line ~186), before "### Nesting (C2/C3 Diagrams)" (line 188). The new "### Optional Type (Inference)" section sits naturally with Optional Name.

<!-- Existing SKILL.md style anchors (verified read): -->
//   Line 92-115: "### Unit Definition" — field-by-field toml block with inline comments.
//   Line ~116: "#### reference (External Documentation URL)" — sub-section.
//   Line ~133: "**Optional name humanization:**" paragraph.
//   Line ~138: "**Type defaults** (when `type` is omitted):" — PARTIAL 2-line note. EXPAND THIS into the full 2-table DOC-01 spec.
//   INSERTION POINTS: (1) expand the "Type defaults" note at ~line 138 into the full inference tables; (2) add new schema sections (#### templates, #### include, #### relative-peer) after the existing Unit Definition sub-sections, before "### Nesting".
</interfaces>
</context>

<feature>
  <name>Documentation gap-fill (D-19): DOC-01 + DOC-02</name>
  <files>README.md, skill/SKILL.md</files>
  <behavior>
    **README.md gains 4 sections** (matching the depth of the existing Reference/Optional-Name sections — intro paragraph + TOML syntax block + short example + pointer to the fixture):
    1. `### Optional Type (Inference)` — documents `type` is omittable. Contains BOTH inference tables (default-by-parent + generic-db/queue-promotion) transcribed from parser.go:250 and parser.go:276, plus a before/after example. Lands right after "### Optional Name (Humanization)".
    2. `### Templates` — documents `[template.*]` define + `[[use]]` instantiate. Syntax block + one short example + pointer to `skill/examples/06-templates.toml`. Lands in the TOML Format section (after Links or near Expanded Units — planner picks the most natural spot).
    3. `### Multi-File Composition (Include)` — documents `[[include]]`. Syntax block + short example + pointer to `skill/examples/08-include/`.
    4. `### Relative Peer Resolution` — documents bare-peer walk-up + absolute-fallback. Syntax block + the 4 resolution cases + pointer to `skill/examples/07-relative-peer.toml`.

    **skill/SKILL.md gains** (matching the existing schema-reference style):
    1. The partial `**Type defaults**` note (at ~line 138) is EXPANDED into the full DOC-01 spec: both inference tables with all rows, plus the before/after example.
    2. New schema sections `#### templates ([template.*] + [[use]])`, `#### include ([[include]])`, `#### relative-peer (bare peer resolution)` — each with the full field table + validation rules + interactions with other features.
    3. A `### Pipeline Ordering` note documenting `include.Resolve → template.Expand → peer.Resolve → humanize → validate → views → render` and why ordering is load-bearing (template peers must expand before relative resolution; include must resolve before templates defined in included files are visible).

    Phase 28 (reference) and Phase 29 (humanization) sections in BOTH files are NOT modified. The new content is ADDITIVE.
  </behavior>
  <implementation>
    **Style discipline (D-19):** Every new section matches the depth and tone of the existing Reference / Optional-Name sections. README sections are user-facing (syntax + example + fixture pointer, minimal prose). SKILL.md sections are agent-facing (full field tables + validation rules + interaction notes). Do NOT reorganize existing sections. Do NOT "improve" the reference or humanization sections while in there — D-19 is strict about fill-gaps-only.

    **DOC-01 transcription (both files):** The two tables in RESEARCH.md §Code Examples ("DOC-01 type-inference rules table") are the canonical content — transcribe them directly. The before/after example (verbose explicit types vs omitted types) is also in RESEARCH.md. Cite `parser.go:250` (defaultTypeForParent) and `parser.go:276` (inferGenericType) as the source of truth in a one-line footnote so the docs stay auditable against code.

    **Templates section content** (from 31-CONTEXT + REQUIREMENTS TMPL-01..10): `[template.<name>]` with `params = [...]` declaring all-required named params; `[[use]]` with `template`, `parent`, and the params as fields; `${param}` substitution into Name/Description/Technology/Reference/Link fields; fixed link count (no fan-out); subunit subtree (TMPL-04); all params required (TMPL-02/06, no defaults). Validation: missing param = hard error; duplicate unit path = hard error (TMPL-07).

    **Include section content** (from 32-CONTEXT + REQUIREMENTS INC-01..10): `[[include]]` with `path` (relative to including file) and `once = true` (optional); transitive includes; cycle detection fatal; flat merge (no namespacing); cross-file subunits (D-10); root-file-wins properties (INC-08). Validation: missing file = hard error (D-12); duplicate unit path across files = hard error.

    **Relative-peer section content** (from 30-CONTEXT + REQUIREMENTS ERGO-01/02): bare peer (no dot) resolves via walk-up (immediate parent's children → grandparent → ... → root, nearest-first); peer with a dot = absolute (resolved as-is); multiple matches at same depth = hard error; miss at root = hard error. The 4 cases (sibling, aunt, root, absolute-fallback) from the 07-relative-peer.toml fixture.

    **Pipeline-ordering note (SKILL.md only):** the fixed runtime order `include.Resolve → template.Expand → peer.Resolve → humanize → validate → views → render` and the two load-bearing facts: (a) include runs first so templates in included files are visible to `[[use]]` (XC-02); (b) template.Expand runs before peer.Resolve so relative peers authored in templates resolve at the instantiation site (XC-03).

    Insert new README sections in the TOML Format area (after Optional Name for the Type section; the three feature sections near Links/Expanded). Insert new SKILL.md sections in the Schema Reference (expand Type defaults in place; add the three feature sub-sections after the Unit Definition block).
  </implementation>
</feature>

<tasks>

<task type="execute">
  <name>README.md: add Optional Type (Inference) section (DOC-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 147-186 — the existing Reference + Optional Name sections; MATCH this style and depth)
    - README.md (lines 188-260 — the Nesting/Links area; pick the insertion point for the new sections)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md (§Code Examples — "DOC-01 type-inference rules table": the 2 tables + before/after example, already transcribed from parser.go:250/276)
    - internal/parser/parser.go (lines 250-317 — defaultTypeForParent + inferGenericType; confirm the tables in RESEARCH match the code exactly before transcribing)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-19 fill-gaps mandate)
  </read_first>
  <action>
    Add a new `### Optional Type (Inference)` section to README.md, inserted immediately AFTER the existing `### Optional Name (Humanization)` section (around line 186) and BEFORE `### Nesting (C2/C3 Diagrams)`. The section contains:
    - A 1-paragraph intro: "`type` is optional. When omitted, the type is inferred from the parent unit's type (or nesting level for generic `db`/`queue` types)."
    - **Default type by parent** table (7 rows: root→system, system→container, box→system, container→component, containerBox→container, componentBox→component, other→system) — transcribe from RESEARCH.md §Code Examples.
    - **Generic `db`/`queue` promotion by nesting level** table (3 rows: C1 unchanged, C2 → containerDb/containerQueue, C3 → componentDb/componentQueue) — transcribe from RESEARCH.md.
    - A before/after TOML example showing the same 3-unit hierarchy with explicit types (verbose) then omitted types (inferred) — transcribe from RESEARCH.md.
    - A one-line footnote: "Source: `defaultTypeForParent` and `inferGenericType` in `internal/parser/parser.go`."

    Match the style of the adjacent `### Optional Name (Humanization)` section (intro + table/example + rules). Do NOT modify the Optional Name section.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -n 'Optional Type\|defaultTypeForParent\|inferGenericType' README.md</verify>
  <acceptance_criteria>
    - `grep -c '### Optional Type' README.md` returns 1 (the new section exists)
    - `grep -c 'componentDb\|containerQueue' README.md` returns at least 2 (the generic promotion table is present)
    - `grep -c 'system.*container\|container.*component' README.md` returns at least 1 (the default-by-parent table is present)
    - The section appears between `### Optional Name (Humanization)` and `### Nesting` — verify with `grep -n '### Optional Name\|### Optional Type\|### Nesting' README.md` (Optional Type line number is between the other two)
    - The existing `### Optional Name (Humanization)` section is byte-identical (no modification) — `git diff README.md` shows only additions
    - Commit message starts with `docs(33-03):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>README.md: add Templates, Include, Relative Peer sections (DOC-02)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the Optional Type section you just added; the Links/Expanded/Styling area for insertion points)
    - skill/examples/06-templates.toml, 07-relative-peer.toml, 08-include/entry.toml (Plan 02 fixtures — the syntax blocks in the docs should be consistent with these)
    - .planning/phases/31-template-expansion/31-CONTEXT.md (D-03/D-04 syntax; the instantiation sketch in Specific Ideas)
    - .planning/phases/32-include-directive-multi-file/32-CONTEXT.md (D-09/D-10/D-11; the directive syntax sketch)
    - .planning/phases/30-relative-peer-resolution/30-CONTEXT.md (D-13..D-16; the 5 resolution traces)
    - .planning/REQUIREMENTS.md (TMPL-01..10, INC-01..10, ERGO-01/02 full text)
  </read_first>
  <action>
    Add 3 new sections to README.md in the TOML Format area (insert them in a natural reading order — e.g. after Links or near Expanded Units; the planner/executor picks the spot that reads best, likely grouping the "composition" features together):

    **`### Templates`** — 1-paragraph intro ("Define a parametrized unit template once, instantiate it N times with distinct parameter values"). A TOML syntax block showing `[template.microservice]` with `params = [...]`, `${param}` in fields, a `[[template.microservice.link]]`, and a subunit; then a `[[use]]` instantiation. A bullet list of key rules: all params required (no defaults), `${param}` substitutes into all string fields including reference, fixed link count (no fan-out), subunit subtree supported, forward references allowed. End with: "See `skill/examples/06-templates.toml` for a runnable example."

    **`### Multi-File Composition (Include)`** — 1-paragraph intro ("Assemble a diagram from multiple TOML files"). A TOML syntax block showing `[[include]] path = "auth.toml"` and `[[include]] path = "templates.toml" once = true`. Bullet list: paths relative to the including file, transitive includes, `once=true` dedup, flat merge (no namespacing), cross-file subunits, cycle detection fatal, missing file = hard error. End with: "See `skill/examples/08-include/` for a runnable multi-file example."

    **`### Relative Peer Resolution`** — 1-paragraph intro ("A bare `peer` value (no dot) resolves against the enclosing parent's ancestry"). A short TOML block + the 4 cases as a labeled list: sibling match, aunt (walk-up) match, root (top-level) match, absolute fallback (peer with a dot). Note: multiple matches at the same depth = error; miss at root = error. End with: "See `skill/examples/07-relative-peer.toml` for a runnable example demonstrating all four cases."

    Match the style of the existing `### Links (Relationships)` and `### Expanded Units (Drill-Down)` sections. Do NOT modify any existing section.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -n '### Templates\|### Multi-File Composition\|### Relative Peer' README.md</verify>
  <acceptance_criteria>
    - `grep -c '### Templates' README.md` returns 1
    - `grep -c '### Multi-File Composition' README.md` returns 1
    - `grep -c '### Relative Peer' README.md` returns 1
    - `grep 'skill/examples/06-templates.toml' README.md` returns at least 1 (fixture pointer)
    - `grep 'skill/examples/08-include' README.md` returns at least 1 (fixture pointer)
    - `grep 'skill/examples/07-relative-peer.toml' README.md` returns at least 1 (fixture pointer)
    - `grep '\[\[use\]\]\|\[\[include\]\]' README.md` returns at least 2 (syntax blocks present)
    - `git diff README.md` shows only additions (no existing section modified)
    - Commit message starts with `docs(33-03):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>skill/SKILL.md: expand Type defaults + add Pipeline Ordering note (DOC-01)</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (lines 92-160 — the Unit Definition block, the existing "#### reference" sub-section, the "Optional name humanization" paragraph, and the PARTIAL "**Type defaults** (when `type` is omitted):" note at ~line 138 that you will EXPAND)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md (§Code Examples — the 2 DOC-01 tables + before/after example)
    - internal/parser/parser.go (lines 250-317 — confirm tables match code)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-19; the pipeline-ordering note D-19 bullet)
  </read_first>
  <action>
    In `skill/SKILL.md` Schema Reference:

    (1) REPLACE the partial `**Type defaults** (when \`type\` is omitted):` note (around line 138, currently just 2 lines: root→system, inside system/box→container) with the FULL DOC-01 spec: a `**Type inference rules (when \`type\` is omitted):**` heading, the default-type-by-parent table (7 rows), the generic-db/queue-promotion table (3 rows), and the before/after example. Cite `parser.go:250` (defaultTypeForParent) and `parser.go:276` (inferGenericType) as source.

    (2) ADD a new `### Pipeline Ordering` section (in the Schema Reference, after the Unit Definition sub-sections, before Validation Rules or wherever it reads most naturally) documenting: the fixed runtime order `include.Resolve → template.Expand → peer.Resolve → humanize → validate → views → render`, and two load-bearing notes: (a) include runs first so templates defined in included files are visible to `[[use]]` (XC-02); (b) template.Expand runs before peer.Resolve so relative peers authored inside templates resolve at the instantiation site, not the template's lexical location (XC-03); humanize runs after expand (sees substituted names) and before validate (error messages show final names) (XC-04).

    Match the style of the existing "Optional name humanization" paragraph and the `#### reference` sub-section. Do NOT modify the "Optional name humanization" or `#### reference` blocks.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -n 'Type inference\|Pipeline Ordering\|include.Resolve\|template.Expand\|peer.Resolve' skill/SKILL.md</verify>
  <acceptance_criteria>
    - `grep -c '### Pipeline Ordering' skill/SKILL.md` returns 1
    - `grep -c 'include.Resolve' skill/SKILL.md` returns at least 1
    - `grep -c 'template.Expand' skill/SKILL.md` returns at least 1
    - `grep -c 'peer.Resolve' skill/SKILL.md` returns at least 1
    - `grep -c 'componentDb\|containerQueue' skill/SKILL.md` returns at least 2 (full generic-promotion table)
    - The partial 2-line "Type defaults" note is GONE (replaced by the full spec) — `grep -c 'Type defaults' skill/SKILL.md` returns 0 OR the heading is renamed to "Type inference rules"
    - `git diff skill/SKILL.md` shows the Type defaults block expanded (not deleted) + Pipeline Ordering added; the `#### reference` and "Optional name humanization" blocks unchanged
    - Commit message starts with `docs(33-03):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>skill/SKILL.md: add templates, include, relative-peer schema sections (DOC-02)</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (the Schema Reference structure — Unit Definition, Nesting, Link Syntax, Link Attributes; place the new sections consistently)
    - skill/SKILL.md (the Pipeline Ordering section you just added; reference it from the new feature sections' "interactions" notes)
    - skill/examples/06-templates.toml, 07-relative-peer.toml, 08-include/ (Plan 02 fixtures)
    - .planning/phases/31-template-expansion/31-CONTEXT.md, 32-CONTEXT.md, 30-CONTEXT.md (full field/rule specs)
    - .planning/REQUIREMENTS.md (TMPL/INC/ERGO full text)
  </read_first>
  <action>
    Add 3 new schema sections to `skill/SKILL.md` (in the Schema Reference, after the Link sections or grouped logically). Each matches the depth of the existing `#### reference` sub-section (field table + validation rules + interaction note):

    **`### Templates ([template.*] + [[use]])`** — full field table for `[template.<name>]` (`params` array, plus all Unit fields with `${param}` substitution), the `[[use]]` directive fields (`template`, `parent`, plus the template's params). Validation rules: all params required (TMPL-02/06), `${param}` substitutes into Name/Description/Technology/Reference/Color/Link fields (TMPL-03), fixed link count no fan-out (TMPL-03), subunit subtree (TMPL-04), duplicate unit path = hard error (TMPL-07), deep-copy correctness (TMPL-08). Interactions note: runs as the 2nd pipeline pass (see Pipeline Ordering); relative peers authored inside resolve at the instantiation site (XC-03); reference field parametrization composes with Phase 28 (TMPL-10).

    **`### Include ([[include]])`** — field table (`path` string, `once` bool). Validation rules: path relative to including file (INC-02), transitive includes (INC-03), cycle detection fatal (INC-04), diamond same-file dedup / cross-file collision hard error (D-11), `once=true` visited-set dedup (INC-06), flat merge no namespacing (INC-07), root-file-wins properties (INC-08), UnitOrder append (D-09), cross-file subunits (D-10), missing file = hard error (D-12). Interactions note: runs as the 1st pipeline pass so templates in included files are visible to `[[use]]` (XC-02).

    **`### Relative Peer Resolution`** — explanation of bare-vs-absolute gate (D-16): peer with a dot = absolute; bare = walk-up. The walk-up algorithm (D-13/D-14/D-15): immediate parent's children → grandparent → ... → root, nearest-first; first depth with exactly-one-match wins. Error cases: multiple matches at same depth = hard error; miss at root = hard error. Interactions note: runs as the 3rd pipeline pass after template.Expand (see Pipeline Ordering); templated units' relative peers resolve at the instantiation site (XC-03); absolute peers (with a dot) are always unchanged (ERGO-02 backward-compat).

    Match the style of the existing schema sub-sections. Do NOT modify existing sections.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; grep -n '### Templates\|### Include\|### Relative Peer' skill/SKILL.md</verify>
  <acceptance_criteria>
    - `grep -c '### Templates' skill/SKILL.md` returns 1
    - `grep -c '### Include' skill/SKILL.md` returns 1 (or the section heading names it clearly)
    - `grep -c '### Relative Peer' skill/SKILL.md` returns 1
    - `grep 'TMPL-0' skill/SKILL.md` returns at least 3 matches (validation rules cite the requirement IDs)
    - `grep 'INC-0' skill/SKILL.md` returns at least 3 matches
    - `grep 'ERGO-0' skill/SKILL.md` returns at least 2 matches
    - Each new section references the Pipeline Ordering section (cross-link)
    - `git diff skill/SKILL.md` shows only additions (no existing schema section modified)
    - Commit message starts with `docs(33-03):`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation edits only. No code, no config, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-I | Information Disclosure | N/A — docs contain no secrets | accept | Example URLs are example.com placeholders; no real system names or runbooks. |

**Net security impact: none.** Documentation only.
</threat_model>

<verification>
Phase-level checks (executor runs after all tasks):
1. `grep -c '### Optional Type\|### Templates\|### Multi-File Composition\|### Relative Peer' README.md` returns 4 (DOC-01 + DOC-02 README sections)
2. `grep -c '### Pipeline Ordering' skill/SKILL.md` returns 1
3. `grep -c '### Templates\|### Include\|### Relative Peer' skill/SKILL.md` returns 3 (DOC-02 SKILL sections)
4. `grep -c 'componentDb\|containerQueue' README.md skill/SKILL.md` returns at least 4 total (DOC-01 generic-promotion table in both files)
5. `git diff README.md skill/SKILL.md` — confirm only additions; the Phase 28 (reference) and Phase 29 (humanization) sections are byte-identical to before
6. (Optional) render README locally if there's a preview tool, else eyeball the markdown structure
</verification>

<success_criteria>
- DOC-01: both files document `type` is omittable with the full 2-table inference rules + before/after example
- DOC-02: both files document templates, include, relative-peer (reference + humanization already shipped by 28/29)
- D-19 honored: existing sections unchanged, new sections match established style
- Pipeline-ordering note in SKILL.md (D-19)
- Every new section cross-links to its skill/examples/ fixture (README) or the Pipeline Ordering section (SKILL)
</success_criteria>

<output>
Create `.planning/phases/33-docs-sweep-end-to-end-goldens/33-03-SUMMARY.md` when done. List the 4 README sections + 4 SKILL additions with their insertion points, and confirm `git diff` shows additions only (Phase 28/29 sections untouched).
</output>
