---
phase: 03-views-graphs
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [cmd/c4drill/main.go, internal/view/integration_test.go, internal/graph/integration_test.go]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "Full test suite passes with race detection"
    - "Test coverage >= 75% across all packages"
    - "Lint passes with 0 issues"
    - "Integration tests verify view-to-graph pipeline"
    - "CLI can exercise view and graph generation"
  artifacts:
    - path: "internal/view/integration_test.go"
      provides: "Integration tests for view generation with real model"
    - path: "internal/graph/integration_test.go"
      provides: "Integration tests for graph building with real views"
  key_links:
    - from: "internal/graph/integration_test.go"
      to: "internal/view"
      via: "import and GenerateC1View call"
      pattern: "view\\.GenerateC1View"
---

<objective>
Integration testing and quality verification for the views and graphs phase.

Purpose: Ensure view and graph packages work together correctly and meet quality gates.
Output: Integration tests, updated CLI for testing, verified quality metrics.
</objective>

<execution_context>
@/Users/nil/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-views-graphs/03-CONTEXT.md
@.planning/phases/03-views-graphs/03-RESEARCH.md

<interfaces>
<!-- Types from prior plans in this phase -->

From internal/view/view.go:
```go
type View struct { Level, Title, Units, Edges, Parent, ExpandedUnit }
type ViewUnit struct { Unit, FullPath, IsExpanded, IsExternal, HasSubunits }
func GenerateC1View(m *parser.Model) *View
func GenerateC2View(m *parser.Model, systemPath string) *View
func GenerateC3View(m *parser.Model, containerPath string) *View
```

From internal/graph/graph.go:
```go
type Graph struct { Title, Direction, EdgeStyle, Nodes, Edges, Clusters }
type Node struct { ID, Label, Shape, Style, IsExternal }
type Edge struct { Source, Target, Label, Style, ArrowHead }
type Cluster struct { ID, Label, Nodes, Style }
```

From internal/graph/builder.go:
```go
func BuildGraph(v *view.View) *Graph
```

From testdata/valid.toml (existing test fixture):
```toml
[properties]
name = "Test System"

[mainapp]
type = "system"
name = "Main Application"
description = "The main system"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create view package integration tests</name>
  <files>internal/view/integration_test.go</files>
  <behavior>
    - Test 1: GenerateC1View with multi-level model produces correct top-level view
    - Test 2: GenerateC2View with nested system produces correct container view
    - Test 3: GenerateC3View with nested container produces correct component view
    - Test 4: External boundary nodes appear when links reference out-of-scope units
    - Test 5: View respects per-unit expanded attribute
    - Test 6: FullPath correctly uses dotted notation for nested units
  </behavior>
  <action>
    Create internal/view/integration_test.go with integration tests using real model structures.

    Test scenarios:
    1. Create a model with nested units (system -> container -> component) and verify C1 view only shows top-level
    2. Create a model with expanded system and verify C2 view shows its containers
    3. Create a model with expanded container and verify C3 view shows its components
    4. Create a model with links to undefined units and verify external boundary nodes
    5. Create a model with unit.Expanded set and verify it's respected

    Use table-driven tests following patterns from existing test files.

    Use testdata/valid.toml or create new test fixtures as needed.
  </action>
  <verify>
    <automated>go test ./internal/view/... -v -run "Integration"</automated>
  </verify>
  <done>View integration tests pass with real model structures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create graph package integration tests</name>
  <files>internal/graph/integration_test.go</files>
  <behavior>
    - Test 1: BuildGraph from C1 view produces correct nodes and edges
    - Test 2: BuildGraph from C2 view produces correct clusters
    - Test 3: BuildGraph with external boundary nodes produces correct styling
    - Test 4: Full pipeline: model -> view -> graph produces valid structure
    - Test 5: Multiple links between same units produce separate edges
    - Test 6: [+] indicator appears on nodes with subunits
  </behavior>
  <action>
    Create internal/graph/integration_test.go with integration tests.

    Test scenarios:
    1. Generate C1 view from model, build graph, verify nodes have correct shapes
    2. Generate C2 view from model with expanded system, build graph, verify cluster exists
    3. Generate view with external boundary nodes, build graph, verify dashed border style
    4. Full pipeline test: parse -> validate -> generate view -> build graph
    5. Create model with multiple links between same units, verify separate edges

    Import view package and use GenerateC1View etc. to create views for testing.
  </action>
  <verify>
    <automated>go test ./internal/graph/... -v -run "Integration"</automated>
  </verify>
  <done>Graph integration tests pass with real view structures</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify quality gates</name>
  <files>internal/view/*.go, internal/graph/*.go</files>
  <action>
    Run full quality verification:

    1. Run all tests with race detection:
       `go test -v -race ./...`

    2. Run coverage check:
       `go test -cover ./...`

    3. Run lint:
       `mise run lint`

    4. Verify coverage >= 75% for view and graph packages

    Fix any issues found. Common fixes:
    - Missing t.Parallel() calls
    - Race conditions in tests
    - Coverage gaps needing additional test cases
  </action>
  <verify>
    <automated>mise run test && mise run lint && go test -cover ./... | grep -E "(view|graph).*[0-9]+%"</automated>
  </verify>
  <done>All tests pass, coverage >= 75%, lint clean</done>
</task>

<task type="auto">
  <name>Task 4: Update CLI to demonstrate view/graph generation</name>
  <files>cmd/c4drill/main.go</files>
  <action>
    Update cmd/c4drill/main.go to demonstrate view and graph generation:

    1. After validation passes, generate C1 view
    2. Build graph from view
    3. Print summary of generated structures (for now - Phase 4 will add DOT output)

    Example output:
    ```
    Generated C1 view: "System Name" with 5 units
    Built graph: 5 nodes, 4 edges
    ```

    This provides a manual verification point that the pipeline works.
    Full rendering output comes in Phase 4.
  </action>
  <verify>
    <automated>go run ./cmd/c4drill testdata/valid.toml 2>&1 | grep -E "(Generated|Built graph)"</automated>
  </verify>
  <done>CLI demonstrates view/graph generation working</done>
</task>

</tasks>

<verification>
- All tests pass with race detection enabled
- Coverage >= 75% for internal/view and internal/graph packages
- Lint passes with 0 issues
- Integration tests verify full pipeline
- CLI can exercise the view/graph generation
</verification>

<success_criteria>
- All QUAL requirements (QUAL-01 through QUAL-05) verified
- Phase 3 complete and ready for Phase 4 (Rendering)
- Full pipeline from model -> view -> graph works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-views-graphs/03-03-SUMMARY.md`
</output>
