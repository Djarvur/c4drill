---
phase: 08-all-expanded-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/view/scope.go
  - internal/graph/graph.go
  - internal/output/writer.go
autonomous: true
requirements:
  - EXPD-02
  - EXPD-04
must_haves:
  truths:
    - "All units in the model appear in the expanded view with proper nesting"
    - "Output file is named {basename}.expanded.{ext}"
    - "Cluster struct supports arbitrary nesting depth"
  artifacts:
    - path: "internal/view/scope.go"
      provides: "GenerateExpandedView function for all-expanded rendering"
      contains: "func GenerateExpandedView"
    - path: "internal/graph/graph.go"
      provides: "Cluster struct with nested cluster support"
      contains: "Clusters []*Cluster"
    - path: "internal/output/writer.go"
      provides: "WriteExpanded method for expanded output path"
      contains: "func.*WriteExpanded"
  key_links:
    - from: "internal/view/scope.go"
      to: "internal/graph/graph.go"
      via: "View struct consumed by BuildGraph"
      pattern: "GenerateExpandedView.*return.*View"
---

<objective>
Add core data structures and view generation for all-expanded mode.

Purpose: Enable recursive unit collection and nested cluster output path handling.
Output: GenerateExpandedView function, extended Cluster struct, WriteExpanded method.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-all-expanded-mode/08-CONTEXT.md
@.planning/phases/08-all-expanded-mode/08-RESEARCH.md

<interfaces>
From internal/view/view.go:
```go
type View struct {
    Level Level
    Title string
    Units map[string]*Entry
    Edges string
    Parent string
    ExpandedUnit string
}

type Entry struct {
    Unit *model.Unit
    FullPath string
    IsExpanded bool
    IsExternal bool
    HasSubunits bool
}
```

From internal/graph/graph.go:
```go
type Cluster struct {
    ID string
    Label *Label
    Nodes []*Node
    Style *NodeStyle
}
```

From internal/output/writer.go:
```go
func (w *Writer) Write(basename, unitPath, format string, data []byte) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GenerateExpandedView function to scope.go</name>
  <files>internal/view/scope.go</files>
  <behavior>
    - Test 1: GenerateExpandedView returns nil for nil model
    - Test 2: GenerateExpandedView includes all top-level units
    - Test 3: GenerateExpandedView recursively includes nested subunits
    - Test 4: GenerateExpandedView adds external boundary nodes for linked units
  </behavior>
  <action>
Add GenerateExpandedView function that creates a view containing ALL units in the model at all nesting levels.

Implementation approach (modified C1 pattern):
1. Create function `GenerateExpandedView(m *parser.Model) *View` following the GenerateC1View pattern
2. Use recursive helper `addUnitRecursive(v *View, path string, unit *model.Unit)` to traverse all units depth-first
3. For each unit, create Entry with:
   - FullPath: dotted path (e.g., "mainapp.api.handler")
   - IsExpanded: true if unit has subunits (always show nested structure)
   - HasSubunits: len(unit.Subunits) > 0
   - IsExternal: IsExternalType(unit.Type)
4. Call addExternalBoundaryNodes(v, m) to include linked external units
5. Set Level to LevelC1 and Title to m.Properties.Name (per locked decision)

Do NOT create a standalone different architecture - this follows the existing C1 view pattern exactly, just with recursive collection instead of top-level only.

Reference existing GenerateC1View (lines 13-40) for the pattern to follow.
  </action>
  <verify>
    <automated>go test ./internal/view/... -v -run TestGenerateExpandedView</automated>
  </verify>
  <done>GenerateExpandedView function exists and recursively collects all units with proper paths and metadata</done>
</task>

<task type="auto">
  <name>Task 2: Extend Cluster struct with nested Clusters field</name>
  <files>internal/graph/graph.go</files>
  <action>
Add `Clusters []*Cluster` field to the Cluster struct to support arbitrary nesting depth.

The current Cluster struct (lines 93-103) has:
- ID, Label, Nodes, Style

Add after Nodes field:
```go
// Clusters are nested clusters inside this cluster (for all-expanded mode).
Clusters []*Cluster
```

This is a non-breaking change - existing code that creates Clusters will simply have nil Clusters slice, which is safe. The builder.go code in the next plan will populate this field for all-expanded views.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/graph/... -v</automated>
  </verify>
  <done>Cluster struct has Clusters []*Cluster field, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add WriteExpanded method to writer.go</name>
  <files>internal/output/writer.go</files>
  <action>
Add WriteExpanded method that writes expanded diagram to `{basename}.expanded.{ext}` path.

Implementation:
1. Add method `func (w *Writer) WriteExpanded(basename, format string, data []byte) error`
2. Construct path as `fmt.Sprintf("%s.expanded.%s", basename, format)`
3. Join with baseDir: `filepath.Join(w.baseDir, relPath)`
4. Create parent directory if needed (same pattern as Write method)
5. Write file with same permissions

This follows the locked decision: output to output directory root with `{basename}.expanded.{ext}` naming, no subdirectory structure.
  </action>
  <verify>
    <automated>go test ./internal/output/... -v -run TestWriteExpanded</automated>
  </verify>
  <done>WriteExpanded method creates file at {basename}.expanded.{ext} in output directory</done>
</task>

</tasks>

<verification>
- GenerateExpandedView recursively collects all units (verified by unit tests)
- Cluster struct supports nesting (verified by compilation and existing tests)
- WriteExpanded creates correct output path (verified by unit test)
</verification>

<success_criteria>
- `go test ./internal/view/... -run TestGenerateExpandedView` passes
- `go test ./internal/graph/...` passes (no regression)
- `go test ./internal/output/... -run TestWriteExpanded` passes
- `go build ./...` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-all-expanded-mode/08-01-SUMMARY.md`
</output>
