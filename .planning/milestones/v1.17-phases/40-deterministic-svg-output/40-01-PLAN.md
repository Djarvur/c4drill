---
phase: 40-deterministic-svg-output
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [internal/validator/index.go, internal/validator/index_test.go, internal/render/deterministic_test.go]
autonomous: true
requirements: [REPRO-01, REPRO-02, REPRO-03]
user_setup: []

must_haves:
  truths:
    - "Rendering the issue #42 reproducer model twice in-process produces byte-identical SVG output, including the generated id=\"edge<N>\" group ids"
    - "Byte-equality across repeated renders holds for dot, svg, and html alike"
    - "The validator's synthesized LinksFrom mirror order is identical across repeated pipeline runs on the same model"
    - "No DOT-output change: no explicit edge ids are emitted into DOT (per D-04)"
  artifacts:
    - path: "internal/validator/index.go"
      provides: "Sorted-key iteration in populateIncomingLinks — mirror append order derived from model content, not map iteration (D-02)"
      contains: "sort"
    - path: "internal/render/deterministic_test.go"
      provides: "Byte-equality regression test built from the issue #42 reproducer, asserting dot/svg/html byte-equality across two in-process pipeline runs (D-05)"
      min_lines: 60
    - path: "internal/validator/index_test.go"
      provides: "Unit test pinning per-target mirror peer order across repeated BuildIndex + populateIncomingLinks runs"
      contains: "TestMirrorOrderDeterministicAcrossRuns"
  key_links:
    - from: "internal/validator/index.go (populateIncomingLinks)"
      to: "internal/graph/builder.go (buildEdges inLinks source)"
      via: "LinksFrom slice order feeds edge insertion order"
      pattern: "populateIncomingLinks"
    - from: "internal/render/deterministic_test.go"
      to: "render.Render / view.GenerateC1View / graph.BuildGraphWithPath"
      via: "full pipeline invoked twice per assertion"
      pattern: "GenerateC1View|BuildGraphWithPath"
---

<objective>
Make repeated renders byte-identical by fixing the root cause of issue #42: `populateIncomingLinks` (internal/validator/index.go:54) ranges over the `BuildIndex` map, so validator-synthesized `LinksFrom` mirrors land in run-dependent order, which permutes global edge insertion order and therefore GraphViz's `edge<N>` id numbering (D-01, D-02). Pin the fix with the D-05 TDD byte-equality regression test built from the issue #42 reproducer.

Purpose: Byte-stable output makes committed-diagram CI gates (`re-render` + `git diff --exit-code`) trustworthy (issue #42 motivation).

Output: Deterministic mirror synthesis in internal/validator/index.go, a validator mirror-order unit test, and internal/render/deterministic_test.go asserting dot/svg/html byte-equality across repeated renders. TDD RED on current code, GREEN after the fix.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-deterministic-svg-output/40-CONTEXT.md
@.planning/phases/40-deterministic-svg-output/40-RESEARCH.md
@.planning/phases/40-deterministic-svg-output/40-PATTERNS.md

@internal/validator/index.go
@internal/validator/index_test.go
@internal/render/integration_test.go
@internal/graph/builder.go
@cmd/c4drill/root.go

<interfaces>
<!-- Key signatures the executor needs (verified on current code): -->

From internal/validator/index.go:
```go
func BuildIndex(units map[string]*model.Unit, parentPath string) map[string]*UnitInfo
func populateIncomingLinks(index map[string]*UnitInfo)  // unexported — test lives in package validator
```
UnitInfo fields: Unit *model.Unit, FullPath string, Parent string. Mirror links carry `Mirror: true` (model.Link field).

From internal/c4d/parse.go:
```go
func Parse(data []byte) (*parser.Model, error)   // parses .c4d DSL text
```

From internal/view/scope.go / internal/graph/builder.go / internal/render/render.go:
```go
func GenerateC1View(m *parser.Model) *view.View
func BuildGraphWithPath(v *view.View, currentPath, basename, format string) *graph.Graph
func Render(g *graph.Graph, format string) ([]byte, error)   // "dot" | "svg" | "html" | ...
```

Pipeline order used by the CLI (cmd/c4drill/root.go processView + parseInput, and cmd/c4drill/convert_test.go runGraphPipeline):
parse (c4d.Parse) → include.Resolve → template.Expand → peer.Resolve → validator.Validate → view.GenerateC1View → graph.BuildGraphWithPath → render.Render
</interfaces>
</context>

<feature>
  <name>Deterministic incoming-mirror synthesis (issue #42 root fix)</name>
  <files>internal/validator/index.go, internal/validator/index_test.go, internal/render/deterministic_test.go</files>
  <behavior>
    Expected behavior, testable before implementation (TDD RED on current code):
    - Case: BuildIndex + populateIncomingLinks run 20 times over a model where 3+ units link to one shared target → every run records the SAME per-target sequence of mirror Peer values (currently RED: Go map iteration permutes the mirror order).
    - Case: the issue #42 reproducer model (10 containers in system "sys", container ci links `-> sys.cj` for j != i and (i+j)%3 == 0, plus unit `ext: personExternal "User"` linking `-> sys.c0`) rendered twice through the full pipeline → SVG bytes are exactly equal (currently RED), DOT bytes are exactly equal, HTML bytes are exactly equal.
    - Case: dedup semantics unchanged — after the fix, existing validator tests and builder edge tests still pass; no `FindLinkByPeer` behavior change.
  </behavior>
  <implementation>
    Per D-02 (locked): order the mirror synthesis at the SOURCE. In populateIncomingLinks, replace the bare `for sourcePath, sourceInfo := range index` with iteration over a sorted slice of index keys (collect keys, `sort.Strings`, then loop). The mirror-append body — FindLinkByPeer dedup check, the model.Link literal with Mirror: true, all preserved attributes — stays byte-identical. Do NOT touch buildEdges, DOT emission, or any SVG-layer code in this plan (D-04: no explicit edge ids in DOT; builder-side defense-in-depth sort is Plan 40-02's scope per D-03).
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author .c4d → validator index | Author-controlled unit paths and link peers flow into the validator index and edge cgraph names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | sanitizeEdgeName / assignEdgeName (builder.go) | accept | No change in this plan: edge cgraph names remain sanitized via the existing sanitizeEdgeName mirror (threat T-Q1-01 precedent); sorted keys are internal validator paths, never emitted raw |
| T-40-02 | Repudiation | render output bytes | mitigate | This plan IS the mitigation: deterministic ordering makes output diffable and CI-verifiable (issue #42 "Why it matters") |
| T-40-SC | Tampering | package installs | accept | None — stdlib `sort` only; no npm/pip/cargo installs in this phase |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RED — validator mirror-order determinism test</name>
  <files>internal/validator/index_test.go</files>
  <read_first>internal/validator/index.go, internal/validator/index_test.go, internal/model/link.go, .planning/phases/40-deterministic-svg-output/40-CONTEXT.md</read_first>
  <action>Add TestMirrorOrderDeterministicAcrossRuns to package validator (internal test — populateIncomingLinks is unexported). Build a model.Unit tree in-memory where three distinct source units (e.g. paths a, b, z, deliberately NOT inserted in sorted order) each hold one model.Link whose Peer is the same target unit t; call BuildIndex on the units map, then populateIncomingLinks, then record the sequence of Peer values in index["t"].Unit.LinksFrom entries where Link.Mirror is true. Repeat BuildIndex+populateIncomingLinks from scratch 20 times in a loop, asserting every run's recorded sequence equals the first run's (use testify require.Equal with a helpful message including the run index). Per the TDD gate: run `go test ./internal/validator/ -run TestMirrorOrderDeterministicAcrossRuns -count=1` and CONFIRM IT FAILS on current code (map iteration permutes) — if it unexpectedly passes, stop and investigate per tdd.md fail-fast rule 1 before committing. Commit RED: `test(40-01): add failing validator mirror-order determinism test`.</action>
  <verify>
    <automated>go test ./internal/validator/ -run TestMirrorOrderDeterministicAcrossRuns -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/validator/index_test.go contains func TestMirrorOrderDeterministicAcrossRuns
    - The test fails on pre-fix code with a mirror-order mismatch (RED gate confirmed in executor transcript), then passes after Task 3
    - The test asserts Mirror-flagged entries only (authored linkFrom entries excluded from the order assertion)
    - Commit `test(40-01): ...` exists in git log before the Task 3 commit
  </acceptance_criteria>
  <done>Failing (RED) determinism test committed in package validator</done>
</task>

<task type="auto">
  <name>Task 2: RED — issue #42 byte-equality regression test</name>
  <files>internal/render/deterministic_test.go</files>
  <read_first>internal/render/integration_test.go, cmd/c4drill/root.go (parseInput + processView), cmd/c4drill/convert_test.go (runGraphPipeline), internal/c4d/parse.go, internal/view/scope.go, .planning/phases/40-deterministic-svg-output/40-RESEARCH.md (reproducer translation)</read_first>
  <action>Create internal/render/deterministic_test.go in package render_test (or render — match the existing integration_test.go package declaration) with TestDeterministicByteIdenticalOutput. Build the issue #42 reproducer model exactly as specified in the research (n=10 containers c0..c9 inside `sys: system "S" { ... }`, container ci links `-> sys.cj` for every j != i with (i+j)%3 == 0, then `ext: personExternal "User" { -> sys.c0 }`); keep the model text as a const string in the test file (deterministic content — no randomness). Encapsulate one full pipeline run in a helper: c4d.Parse on the model bytes → peer.Resolve → validator.Validate (require.Empty on returned errors) → view.GenerateC1View → graph.BuildGraphWithPath with the top-level view (mirror the unitPath/basename arguments used by cmd/c4drill/root.go processView for a top-level drill-down render; read root.go first and copy its argument pattern). Call the helper twice and require.Equal the returned byte slices for format "svg", "dot", and "html" (render.Render). The svg assertion is the RED carrier; annotate with a t.Logf of the first differing byte index when unequal. Per the TDD gate: run `go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1` and CONFIRM the svg assertion FAILS on current code (dot may already pass — that asymmetry is the documented issue #42 evidence). Commit RED: `test(40-01): add failing issue-42 byte-equality regression test`.</action>
  <verify>
    <automated>go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/render/deterministic_test.go exists and contains TestDeterministicByteIdenticalOutput asserting byte equality for svg, dot, and html across two pipeline runs
    - The svg assertion fails on pre-fix code (RED gate confirmed); the test file compiles with `go vet ./internal/render/`
    - The model fixture matches the issue #42 generator: (i+j)%3==0 link predicate and ext→sys.c0 tail
    - No testdata file added — model text is inline in the test
  </acceptance_criteria>
  <done>Failing (RED) byte-equality regression test committed</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — sorted-key mirror synthesis in populateIncomingLinks</name>
  <files>internal/validator/index.go</files>
  <read_first>internal/validator/index.go, internal/validator/index_test.go, internal/view/scope.go (modelUnitOrder precedent), .planning/phases/40-deterministic-svg-output/40-CONTEXT.md (D-02)</read_first>
  <action>Implement the D-02 source fix in populateIncomingLinks: before the mirror loop, collect every key of the index map into a []string, sort it with sort.Strings (stdlib — add the "sort" import; do not introduce any dependency), and iterate that sorted slice, looking up sourceInfo := index[sourcePath] inside the loop. Keep the loop body byte-identical: the FindLinkByPeer already-exists check, the full model.Link literal with every preserved attribute and Mirror: true. Add a short comment citing issue #42 and D-02 (sorted source iteration makes LinksFrom mirror order a pure function of model content). Then run, in order: `go test ./internal/validator/ -run TestMirrorOrderDeterministicAcrossRuns -count=1` (must pass), `go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1` (must pass — GREEN gate), and `go test ./... -count=1` (full suite green; canonicalDOT goldens DI-1/COMPAT-02/REF-05 are the no-semantic-change gate per D-06). Commit GREEN: `feat(40-01): deterministic incoming-link mirror order (issue #42)`. If the full suite surfaces an SVG golden shift, STOP and apply the D-06 policy: verify the delta is id-numbers-only by canonicalizing `id="edge<N>"` before diffing; only then re-baseline once and record it in the SUMMARY.</action>
  <verify>
    <automated>go test ./internal/validator/ ./internal/render/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - populateIncomingLinks iterates a sorted key slice; grep of internal/validator/index.go shows sort.Strings (or equivalent stdlib sorted iteration) and no bare `range index` driving mirror appends
    - TestMirrorOrderDeterministicAcrossRuns passes; TestDeterministicByteIdenticalOutput passes for svg, dot, html (GREEN gate)
    - Full suite `go test ./... -count=1` exits 0 — canonicalDOT goldens untouched (D-06)
    - No DOT semantic change: `grep -rn 'id="edge' --include='*.go' internal/ cmd/ | grep -v _test` returns no hits (D-04)
    - Commit `feat(40-01): ...` follows the Task 1/2 test commits in git log
  </acceptance_criteria>
  <done>Byte-equality regression green end-to-end; full suite green; fix committed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/validator/ -run TestMirrorOrder -count=1` exits 0
- [ ] `go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1` exits 0
- [ ] `go test ./... -count=1` exits 0 (canonicalDOT goldens DI-1/COMPAT-02/REF-05 untouched)
- [ ] git log shows RED test commit(s) before the GREEN feat commit (TDD gate order)
- [ ] Rendering the reproducer twice in-process yields identical bytes for svg, dot, html
</verification>

<success_criteria>
- All tasks completed with TDD gate compliance (RED before GREEN)
- REPRO-01 pinned by TestDeterministicByteIdenticalOutput (svg); REPRO-03 asserted (dot/html); REPRO-02 source fix in place (mirror order deterministic)
- Full test suite green with zero golden churn (or a D-06-audited, id-numbers-only re-baseline recorded in SUMMARY)
- 2-3 commits: test(40-01) RED, test(40-01) RED, feat(40-01) GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/40-deterministic-svg-output/40-01-SUMMARY.md`
</output>
