---
phase: 40-deterministic-svg-output
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified: [internal/graph/builder.go, internal/graph/builder_test.go]
autonomous: true
requirements: [REPRO-02]
user_setup: []

must_haves:
  truths:
    - "g.Edges leaves buildEdges in ascending Edge.Name order — edge insertion order into the cgraph is a pure function of model content (D-03)"
    - "The byte-equality regression test from Plan 40-01 still passes after the sort (defense-in-depth is a no-op on a deterministic walk)"
    - "canonicalDOT goldens (DI-1/COMPAT-02/REF-05) and the full suite stay green — no rendered-semantics change (D-06)"
    - "The SVG id format remains GraphViz-generated edge<N> — no explicit edge ids emitted into DOT (D-04)"
  artifacts:
    - path: "internal/graph/builder.go"
      provides: "Stable sort of the final edge slice by the unique cgraph Edge.Name at the end of buildEdges (D-03)"
      contains: "SortStableFunc"
    - path: "internal/graph/builder_test.go"
      provides: "Unit test asserting edge names are non-decreasing in g.Edges and identical across repeated builds"
      contains: "TestEdgeOrderNameSortedAndStable"
  key_links:
    - from: "internal/graph/builder.go (buildEdges tail sort)"
      to: "internal/render/converter.go (createEdges insertion order)"
      via: "g.Edges slice order is iterated verbatim when building the cgraph, which drives edge<N> numbering"
      pattern: "createEdges"
    - from: "internal/graph/builder_test.go"
      to: "graph.BuildGraph"
      via: "builds views and asserts on g.Edges order"
      pattern: "BuildGraph"
---

<objective>
Add the D-03 defense-in-depth stable sort of `g.Edges` by the already-unique cgraph `Edge.Name` at the end of `buildEdges`, then run the D-06/D-04 no-semantic-change gate. With Plan 40-01's D-02 source fix in place the sort is a no-op on today's walk; if any future walk regresses into map iteration, the sort still guarantees insertion-order stability, keeping REPRO-02's guarantee (edge ids derived from model content, never map order) enforced at two layers.

Purpose: Belt-and-suspenders determinism — the renderer can never observe a permuted edge slice even if a future edit reintroduces map iteration upstream.

Output: Sorted edge tail in internal/graph/builder.go, an order unit test in internal/graph/builder_test.go, and a verified-green no-semantic-change gate recorded in the SUMMARY.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-deterministic-svg-output/40-CONTEXT.md
@.planning/phases/40-deterministic-svg-output/40-RESEARCH.md
@.planning/phases/40-deterministic-svg-output/40-PATTERNS.md
@.planning/phases/40-deterministic-svg-output/40-01-SUMMARY.md

@internal/graph/builder.go
@internal/graph/builder_test.go

<interfaces>
<!-- Key contracts the executor needs (verified on current code): -->

From internal/graph/builder.go:
```go
func BuildGraph(v *view.View) *Graph            // builder.go:33 — g.Edges = buildEdges(v) at :73
func BuildGraphWithPath(v *view.View, currentPath, basename, format string) *Graph  // :1611
func buildEdges(v *view.View) []*Edge           // :1030 — returns the slice to sort (unexported; test via BuildGraph)
func assignEdgeName(nameCounters map[string]int, source, target string) string  // :1097 — unique "{src}_to_{tgt}_{n}"
type Edge struct { ...; Name string; ... }      // Name is the unique, sanitized cgraph edge name
```

From internal/graph/builder_test.go:
```go
func TestBuildGraph_EdgeOverride(t *testing.T)  // :4706 — precedent for building a view and asserting on g.Edges
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author .c4d → edge cgraph names | Author-controlled unit paths reach cgraph edge identifiers via assignEdgeName |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Tampering | Edge.Name used as sort key | mitigate | Edge.Name is produced through sanitizeEdgeName (builder.go:1080, threat T-Q1-01) before use — the sort never touches raw author paths; task verifies the sort compares Edge.Name only |
| T-40-04 | DoS | sort cost on large models | accept | O(n log n) on an already-materialized slice; model sizes (reproducer scale ~tens of edges) make this negligible |
| T-40-SC | Tampering | package installs | accept | None — stdlib `slices`/`strings` only; no package installs in this phase |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stable-sort g.Edges by Edge.Name at the buildEdges tail (D-03)</name>
  <files>internal/graph/builder_test.go, internal/graph/builder.go</files>
  <read_first>internal/graph/builder.go (buildEdges :1030-1074, assignEdgeName :1094-1103, BuildGraph :33-80), internal/graph/builder_test.go (TestBuildGraph_EdgeOverride :4706), .planning/phases/40-deterministic-svg-output/40-CONTEXT.md (D-03, D-06)</read_first>
  <behavior>
    - Test 1: for a view with edges from multiple source units (at least one pair emitting 2+ pre-dedup links so per-pair counters advance), BuildGraph produces g.Edges whose Name values are non-decreasing under strings.Compare across the whole slice
    - Test 2: building the graph twice from the same model yields identical Edge.Name sequences (stability — SortStableFunc, not a re-derivation)
    - Test 3 (invariant): g.Edges names remain unique — no duplicates introduced by sorting
  </behavior>
  <action>RED first: add TestEdgeOrderNameSortedAndStable to internal/graph/builder_test.go following the TestBuildGraph_EdgeOverride pattern — construct a small view/model with edges from at least three source units including one multi-link pair, call graph.BuildGraph twice, and assert (a) names non-decreasing via strings.Compare, (b) both builds produce identical name sequences, (c) names unique. Run `go test ./internal/graph/ -run TestEdgeOrderNameSortedAndStable -count=1` and confirm the sortedness assertion FAILS on current code (walk order is UnitOrder-based, not name-ordered — commit RED as `test(40-02): add failing edge-order stability test`). GREEN: in internal/graph/builder.go buildEdges, immediately before `return edges`, add slices.SortStableFunc(edges, func(a, b *Edge) int { return strings.Compare(a.Name, b.Name) }) — add "slices" and "strings" imports if absent — with a comment citing D-03 and issue #42 (no-op after the D-02 mirror fix; guards future walk regressions; per-pair sequence counters stay meaningful because pair walks are deterministic). Do NOT change name assignment, dedup (markSeen first-wins), or pair styling — ordering only. Commit GREEN as `feat(40-02): name-ordered edge slice at buildEdges tail (D-03)`.</action>
  <verify>
    <automated>go test ./internal/graph/ -run TestEdgeOrderNameSortedAndStable -count=1</automated>
  </verify>
  <acceptance_criteria>
    - builder.go buildEdges contains slices.SortStableFunc comparing strings.Compare(a.Name, b.Name) as its final statement before return
    - builder_test.go contains TestEdgeOrderNameSortedAndStable asserting non-decreasing names, cross-build identity, and name uniqueness
    - RED commit precedes GREEN commit in git log (TDD gate)
    - assignEdgeName, markSeen, and applyCollapsedPairStyle bodies are unchanged (git diff scoped to the sort + imports + comment)
  </acceptance_criteria>
  <done>Edge slice provably name-ordered and stable across builds; test green</done>
</task>

<task type="auto">
  <name>Task 2: No-semantic-change gate — full suite, canonicalDOT goldens, D-04 audit</name>
  <files>.planning/phases/40-deterministic-svg-output/40-02-SUMMARY.md</files>
  <read_first>.planning/phases/40-deterministic-svg-output/40-CONTEXT.md (D-04, D-06), .planning/phases/40-deterministic-svg-output/40-01-SUMMARY.md</read_first>
  <action>Run the D-06/D-04 gate and record results: (1) `go test ./... -count=1` must exit 0 — the canonicalDOT goldens (DI-1/COMPAT-02/REF-05 covered by internal/graph, internal/render, internal/parser, internal/c4d suites) are the order-insensitive no-semantic-change gate; (2) `go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1` must still exit 0 — the Plan 40-01 byte pin proves the sort is a no-op on the deterministic walk; (3) D-04 audit: `grep -rn 'id="edge' --include='*.go' internal/ cmd/ | grep -v _test` must return zero hits — no explicit edge ids in DOT, id format stays GraphViz-emitted edge&lt;N&gt;; (4) if any golden shifted (not expected), apply the D-06 policy: canonicalize `id="edge<N>"` to a placeholder before diffing, confirm the delta is id-numbers-only, re-baseline once, and record the canonicalized diff evidence in the SUMMARY. Record all four check results with their command outputs in the SUMMARY's Self-Check section.</action>
  <verify>
    <automated>go test ./... -count=1 && go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1 && test -z "$(grep -rn 'id="edge' --include='*.go' internal/ cmd/ | grep -v _test)"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 (canonicalDOT goldens untouched per D-06)
    - TestDeterministicByteIdenticalOutput passes post-sort (byte pin intact)
    - grep audit output is empty — no `id="edge` literals in non-test Go source (D-04)
    - 40-02-SUMMARY.md Self-Check section lists all four gate results with PASS/FAIL
  </acceptance_criteria>
  <done>No-semantic-change gate fully green and documented in the plan SUMMARY</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/graph/ -run TestEdgeOrderNameSortedAndStable -count=1` exits 0
- [ ] `go test ./... -count=1` exits 0
- [ ] `go test ./internal/render/ -run TestDeterministicByteIdenticalOutput -count=1` exits 0 (Plan 40-01 pin intact)
- [ ] git log shows `test(40-02): ...` before `feat(40-02): ...`
- [ ] D-04 grep audit returns zero hits
</verification>

<success_criteria>
- All tasks completed; TDD gate compliance for Task 1 (RED before GREEN)
- g.Edges order is a pure function of model content at TWO layers: source mirror ordering (Plan 40-01, D-02) and name-sorted tail (this plan, D-03)
- REPRO-02 satisfied: edge ids derive from model content ordering, never Go map iteration order
- Zero rendered-semantics change: full suite + canonicalDOT goldens green, byte pin intact (D-06)
- 2 commits: test(40-02), feat(40-02)
</success_criteria>

<output>
After completion, create `.planning/phases/40-deterministic-svg-output/40-02-SUMMARY.md`
</output>
