---
phase: 42-desktop-gui-binding-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/gui/frontend/src/rpc.ts
  - internal/gui/frontend/src/rpc.test.ts
autonomous: true
requirements: [GUI-01, GUI-02]

must_haves:
  truths:
    - "In desktop-window mode, editor/preview/chat/export RPC flows through the Wails-generated binding window.go.main.desktop.Dispatch"
    - "In browser/--serve mode, RPC continues over POST /api/dispatch unchanged (existing e2e green)"
    - "A missing desktop binding resolves to null transport (isDesktop() false) — the miss is observable, never silent dead UI"
    - "No phantom namespace (main?.App / backend?.App) remains anywhere in frontend source"
  artifacts:
    - path: "internal/gui/frontend/src/rpc.ts"
      provides: "Desktop transport resolver bound to the real Wails namespace"
      contains: "main?.desktop?.Dispatch"
    - path: "internal/gui/frontend/src/rpc.test.ts"
      provides: "Binding-shape unit tests: resolve+dispatch through mocked window.go.main.desktop.Dispatch, fail-closed when absent"
      contains: "main.desktop"
  key_links:
    - from: "internal/gui/frontend/src/rpc.test.ts"
      to: "internal/gui/frontend/src/rpc.ts"
      via: "vitest import of isDesktop/backend/call + vi.stubGlobal window mock"
      pattern: "stubGlobal.*window|from.*\\./rpc"
    - from: "internal/gui/frontend/src/rpc.ts wailsBinding()"
      to: "Wails v2 generated binding (window.go.main.desktop.Dispatch)"
      via: "optional chain w.go?.main?.desktop?.Dispatch ?? null"
      pattern: "go\\?\\.main\\?\\.desktop\\?\\.Dispatch"
---

<objective>
Restore desktop-window RPC: point the frontend transport resolver at the Wails-generated namespace that matches the actually bound Go struct (`main.desktop`), developed test-first per D-03.

Purpose: Desktop-window mode has been broken since the #31/#37 restructure — rpc.ts probes phantom namespaces (`main?.App`, `backend?.App`) that never exist, so `wailsBinding()` always returns null and every desktop call falls through to a failing HTTP fetch. Fix direction locked by D-01 (update the frontend, not the Go binding).

Output: `internal/gui/frontend/src/rpc.ts` resolving `window.go.main.desktop.Dispatch` as the single canonical namespace; `internal/gui/frontend/src/rpc.test.ts` proving resolve+dispatch and fail-closed behavior; all D-04 gates green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-desktop-gui-binding-fix/42-CONTEXT.md
@.planning/phases/42-desktop-gui-binding-fix/42-RESEARCH.md
@.planning/phases/42-desktop-gui-binding-fix/42-PATTERNS.md
@.planning/phases/42-desktop-gui-binding-fix/42-UI-SPEC.md
@.planning/phases/42-desktop-gui-binding-fix/42-VALIDATION.md
@internal/gui/frontend/src/rpc.ts
@cmd/c4drill-gui/main.go

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration needed. -->

From internal/gui/frontend/src/rpc.ts (the fix site; lines 9-28):
```typescript
interface WailsWindow {
  go?: {
    main?: { App?: { Dispatch?: (method: string, params: string) => Promise<string> } };
    backend?: { App?: { Dispatch?: (method: string, params: string) => Promise<string> } };
  };
  runtime?: { EventsOn?; EventsOff?; OpenDirectoryDialog? };  // UNCHANGED — already correct
}
function wailsBinding(): ((method: string, params: string) => Promise<string>) | null;
export function wailsRuntime(): WailsWindow["runtime"];
export function isDesktop(): boolean;   // true iff wailsBinding() !== null
export const backend: { dispatch: DispatchFn; on(event, cb): () => void; startEvents(): void };
export function call<T>(method: string, params?: unknown): Promise<T>;
```

From cmd/c4drill-gui/main.go (read-only source of truth for the namespace):
```go
package main                                              // L15
type desktop struct { backend *app.App; ctx context.Context }  // L74
func (g *desktop) Dispatch(method, params string) (string, error)  // L86
Bind: []interface{}{g}                                    // L124
```
Wails v2 generates bindings at window.go.<package>.<struct>.<Method> ⇒ the only real namespace is window.go.main.desktop.Dispatch. Dispatch returns a JSON string; rpc.ts already normalizes raw ""/"null" to null (rpc.ts:41).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write binding-shape tests for the desktop transport (D-03)</name>
  <files>internal/gui/frontend/src/rpc.test.ts</files>
  <read_first>
    - internal/gui/frontend/src/rpc.ts (current resolver state, exported surface: isDesktop, backend, call)
    - internal/gui/frontend/src/language/c4d.test.ts (analog from PATTERNS.md — header comment style, describe/it naming, helper-function conventions)
    - .planning/phases/42-desktop-gui-binding-fix/42-PATTERNS.md (test harness facts: vitest 4 default node environment, vi.stubGlobal pattern)
    - .planning/phases/42-desktop-gui-binding-fix/42-CONTEXT.md (D-03 defines the exact test contract)
  </read_first>
  <behavior>
    - Case 1 (resolve + dispatch): window stubbed with go.main.desktop.Dispatch as a vi.fn() resolving JSON.stringify({ok:true}) ⇒ isDesktop() returns true; backend.dispatch("someMethod", {a:1}) resolves to {ok:true} and the mock received ("someMethod", JSON.stringify({a:1}))
    - Case 2 (fail-closed, per D-03): window stubbed as {} (namespace absent) ⇒ isDesktop() returns false and wailsBinding-equivalent resolution yields no transport
    - Case 3 (edge, existing normalization worth pinning): mock resolves "null" or "" ⇒ backend.dispatch resolves to null
  </behavior>
  <action>
    Create internal/gui/frontend/src/rpc.test.ts following the c4d.test.ts analog conventions per PATTERNS.md: leading file-header comment naming the issue (#38) and scope (binding-shape tests for the desktop transport resolver, GUI-01); named vitest imports (describe, expect, it, vi, afterEach); a small typed helper that stubs window via vi.stubGlobal with a given go-namespace shape and a mock Dispatch function; afterEach calls vi.unstubAllGlobals() so stubs never leak between tests. Tests import the real module under test — isDesktop and backend (or call) from ./rpc — and assert the three behavior cases above. This is the TDD RED test (D-03): with rpc.ts unmodified, Case 1 MUST fail because the resolver never finds main.desktop. No vitest config file is created; the default node environment plus vi.stubGlobal is the whole harness. Do not add jsdom or any dependency — package.json must remain untouched.
  </action>
  <verify>
    <automated>cd internal/gui/frontend && npx vitest run src/rpc.test.ts 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - internal/gui/frontend/src/rpc.test.ts exists and imports from ./rpc
    - Test run shows Case 1 FAILING against unmodified rpc.ts (RED confirmed — the failure names main.desktop resolution)
    - Cases 2 and 3 pass already (fail-closed and null-normalization hold in current code)
    - Test file contains no new dependencies; package.json and package-lock.json unmodified
  </acceptance_criteria>
  <done>RED state demonstrated: the resolve+dispatch test fails on current rpc.ts, fail-closed test passes, per D-03</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — align resolver to window.go.main.desktop.Dispatch (D-01, D-02)</name>
  <files>internal/gui/frontend/src/rpc.ts</files>
  <read_first>
    - internal/gui/frontend/src/rpc.ts (lines 9-28 — the WailsWindow.go declaration and wailsBinding() resolver being replaced)
    - cmd/c4drill-gui/main.go (L15 package main, L74 type desktop, L86 Dispatch, L124 Bind — the namespace source of truth)
    - .planning/phases/42-desktop-gui-binding-fix/42-PATTERNS.md (target shape excerpt for File 1)
  </read_first>
  <behavior>
    - Test 1 (from Task 1): resolver finds go.main.desktop.Dispatch ⇒ isDesktop() true, dispatch flows through the binding — now passes
    - Test 2: window {} ⇒ null transport — still passes
    - Test 3: raw "null"/"" ⇒ null result — still passes
    - Existing full suite (language/c4d.test.ts) stays green — no imports or exports changed
  </behavior>
  <action>
    Edit internal/gui/frontend/src/rpc.ts only, per D-01 and D-02. In the WailsWindow interface replace the two phantom go declarations (main?.App and backend?.App, lines 11-12) with a single declaration main?: { desktop?: { Dispatch?: (method: string, params: string) => Promise<string> } }. In wailsBinding() (line 23) replace the two-alternative chain with the single canonical resolution w.go?.main?.desktop?.Dispatch ?? null — plain optional chain, no typed constant (research question resolved during planning). Leave everything else untouched: the runtime?: block, isDesktop(), wailsRuntime(), the Backend class dispatch fetch branch, startEvents(), and all exports (GUI-02 zero-delta on the serve path; UI-SPEC forbids style.css/index.html/DOM changes). No Go files change (D-01 keeps the binding as-is).
  </action>
  <verify>
    <automated>cd internal/gui/frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run passes ALL tests including the Task 1 binding-shape suite (GREEN)
    - rpc.ts contains go.main.desktop.Dispatch resolution and zero occurrences of App? in the go declaration or resolver
    - npm run typecheck (tsc --noEmit, strict) exits 0
    - The fetch fallback branch in Backend.dispatch and startEvents() are byte-identical to before this task (serve path untouched per GUI-02)
  </acceptance_criteria>
  <done>Binding-shape tests green: desktop transport resolves through window.go.main.desktop.Dispatch; fail-closed behavior preserved; typecheck clean</done>
</task>

<task type="auto">
  <name>Task 3: Structural gate + full regression (D-04, GUI-02)</name>
  <files>internal/gui/frontend/src/rpc.ts, internal/gui/frontend/src/rpc.test.ts</files>
  <read_first>
    - internal/gui/frontend/src/rpc.ts (post-Task-2 state)
    - cmd/c4drill-gui/main_test.go (TestHTTPSmokeE2E — the GUI-02 serve-path e2e that must stay green)
    - .planning/phases/42-desktop-gui-binding-fix/42-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
    Run the D-04 structural gate and the full regression suite. Structural: grep the frontend source tree (internal/gui/frontend/src) for the phantom tokens App? and backend?.App and confirm zero matches — rpc.ts and rpc.test.ts are the only files ever involved, and rpc.test.ts must reference phantom namespaces only if a negative assertion requires it (prefer not referencing them at all; the absence grep is the gate). Regression: frontend typecheck + full build (npm run build regenerates dist so go:embed picks up the fixed resolver — a stale dist is the Pitfall 4 false-positive), go build ./... , and the serve e2e go test ./cmd/c4drill-gui/... . Fix any failure these uncover; do not widen scope beyond the two files this plan owns. Real desktop-window smoke (wails build + run) stays a manual post-merge verification per VALIDATION.md — the wails CLI is absent here by design and D-03/D-04 define the CI-verifiable contract.
  </action>
  <verify>
    <automated>! grep -rn "App?" internal/gui/frontend/src/ && cd internal/gui/frontend && npm run build >/dev/null 2>&1 && echo "frontend-build=ok" && cd ../../.. && go build ./... && go test ./cmd/c4drill-gui/... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "App?" internal/gui/frontend/src/ returns no matches (phantom namespaces gone per D-04)
    - npm run build exits 0 (grammar + tsc --noEmit + vite build; dist regenerated)
    - go build ./... exits 0
    - go test ./cmd/c4drill-gui/... passes — TestHTTPSmokeE2E and TestEventHubBroadcast green (GUI-02: HTTP fallback unchanged)
  </acceptance_criteria>
  <done>All D-04 gates green: zero phantom references, frontend build clean, go build clean, serve e2e green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview frontend → Go backend | Dispatch(method, params JSON) crosses from browser JS into Go — unchanged by this phase; existing app-layer validation covers it |
| resolver environment probe | wailsBinding() reads window.go — attacker-influenced only if the webview itself is compromised (out of threat model for a local desktop app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | transport selection (rpc.ts resolver) | mitigate | D-03 fail-closed contract + binding-shape tests: missing namespace resolves to null transport (observable), phantom fallbacks removed so divergence cannot hide (D-02) |
| T-42-02 | Information Disclosure | serve-mode HTTP fallback | accept | Untouched by this plan (GUI-02); existing localhost bind and e2e coverage retained; no new surface |
| T-42-03 | Elevation of Privilege | Wails bound surface (Bind) | accept | Plan does not expand the bound surface — single Dispatch method already funnels through app.Dispatch validation (e2e asserts unknown methods → 400) |
| T-42-SC | Tampering | package installs | accept | No package installs in this plan (frontend deps unchanged); supply-chain gate not triggered |

</threat_model>

<verification>
Phase-level checks (all automated, per 42-VALIDATION.md sampling):
- `cd internal/gui/frontend && npx vitest run` — full frontend suite green (binding-shape + grammar)
- `cd internal/gui/frontend && npm run typecheck` — strict types clean
- `! grep -rn "App?" internal/gui/frontend/src/` — phantom namespaces eradicated
- `go build ./... && go test ./cmd/c4drill-gui/...` — compile + serve e2e green
Manual (post-merge, VALIDATION.md): wails build + desktop smoke when the operator has the toolchain.
</verification>

<success_criteria>
- rpc.ts resolves exactly window.go.main.desktop.Dispatch; no main?.App / backend?.App anywhere in frontend source
- Binding-shape tests prove resolve+dispatch (GUI-01) and fail-closed behavior (D-03), written RED-first
- Serve HTTP path untouched and TestHTTPSmokeE2E green (GUI-02)
- Frontend typecheck/build and go build green (D-04)
- 2-3 commits present: test(42-01) RED, feat(42-01) GREEN, plus any refactor commit
</success_criteria>

<output>
Create `.planning/phases/42-desktop-gui-binding-fix/42-01-SUMMARY.md` when done
</output>
