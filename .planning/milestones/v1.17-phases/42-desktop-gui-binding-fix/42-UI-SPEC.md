---
phase: 42
slug: desktop-gui-binding-fix
status: approved
reviewed_at: 2026-09-03
shadcn_initialized: false
preset: none
created: 2026-09-03
---

# Phase 42 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope note:** Phase 42 is a transport-layer repair (Wails binding namespace in `src/rpc.ts`). It ships **zero visual changes**. This contract locks "inherit existing" for every visual dimension so the executor cannot drift, and pins the one UI-relevant behavior: a missing desktop binding must fail observably, not silently.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — deliberate (plain CSS custom properties in `src/style.css` `:root`; no tailwind/shadcn/components.json in this project; initializing a design system is out of scope for a binding fix) |
| Preset | not applicable |
| Component library | none — hand-rolled DOM in `src/main.ts` / `src/chat.ts` (existing) |
| Icon library | none (existing uses text glyphs, e.g. `⚠ ` prefix in chat error frames) |
| Font | unchanged — inherit `style.css` body defaults; this phase adds no font rules |

---

## Spacing Scale

**No new spacing declared.** This phase adds no elements and no CSS.

Declared values (must be multiples of 4) — recorded as INHERIT-ONLY, the existing scale used by `style.css`:

| Token | Value | Usage |
|-------|-------|-------|
| inherit | existing `style.css` values as-is | Any incidental class touched must keep its current spacing |

Exceptions: none

---

## Typography

**No changes.** This phase adds no text elements.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| (all) | unchanged — inherit `style.css` | unchanged | unchanged |

---

## Color

**No changes.** Existing tokens recorded so any touched markup keeps them:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--bg: #16181d` | Background (existing) |
| Secondary (30%) | `--bg-panel: #1d2026` / `--bg-raised: #242830` | Panels, raised surfaces (existing) |
| Accent (10%) | `--accent: #4f9cf9` | Existing interactive accents only — this phase must not add new accent usages |
| Destructive | `--error: #f47067` | Existing error surfaces only (`.preview-error`, `.proposal-error`) |

Accent reserved for: existing interactive elements only; **no new accent usage in this phase**.

---

## Copywriting Contract

**No new copy.** No CTA, empty state, or destructive flows are introduced by this phase.

| Element | Copy |
|---------|------|
| Primary CTA | n/a — no CTA in this phase |
| Empty state heading | n/a — unchanged |
| Empty state body | n/a — unchanged |
| Error state | unchanged — existing surfaces stay: chat frames render `⚠ {error}` (`src/chat.ts:314-317`), proposal failures render into `.proposal-error` (`src/chat.ts:404-407`), preview failures into `#preview-error` (`src/main.ts:73`). This phase's only copy-adjacent contract: when the desktop binding namespace is absent, the failure must surface through these existing paths (D-03 fail-closed) — never a silent dead UI and never new error UI. |
| Destructive confirmation | n/a — none in this phase |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required (tool: none) |
| third-party | none | not applicable |

---

## Behavioral UI Contract (phase-specific)

| Behavior | Requirement | Contract |
|----------|-------------|----------|
| Desktop binding present | GUI-01 | `window.go.main.desktop.Dispatch` resolves; all existing UI flows (editor, preview, chat, export) work unchanged |
| Desktop binding absent (serve/browser mode or regression) | GUI-01, D-03 | Resolver returns `null` transport — serve mode continues over `fetch /api/dispatch` (existing behavior); a desktop-mode miss is observable (no binding found ⇒ `isDesktop() === false`), never a hung/silent UI |
| Visual delta budget | this contract | **Zero** — diff must not touch `style.css`, `index.html`, or any DOM-building code in `src/*.ts` |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-09-03 (no-delta contract — zero visual surface in this phase)
