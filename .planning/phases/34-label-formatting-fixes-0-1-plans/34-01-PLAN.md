---
phase: 34-label-formatting-fixes-0-1-plans
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [internal/render/wrap.go, internal/render/wrap_internal_test.go]
autonomous: true
requirements: [LABEL-02, COMPAT-01]
user_setup: []

must_haves:
  truths:
    - "No label text is ever split mid-word — a word longer than the per-line character budget appears unsplit on its own line"
    - "In-budget unit labels render byte-identically to v1.10 (no wrapText behavior change outside the over-budget case)"
  artifacts:
    - path: "internal/render/wrap.go"
      provides: "word-boundary-only wrapping (splitLongWord fallback removed)"
      contains: "Word exceeds maxChars"
    - path: "internal/render/wrap_internal_test.go"
      provides: "re-asserted over-budget behavior (unsplit overflow)"
      contains: "over-budget word"
  key_links:
    - from: "internal/render/wrap.go wrapText"
      to: "wrapAndEscape"
      via: "same wrap machinery (no second wrapping path)"
      pattern: "wrapText\\(text, maxChars\\)"
---

<objective>
Remove the character-level fallback from `wrapText` so lines break at word boundaries only: a word longer than the per-line character budget (maxChars) starts its own line and stays unsplit, overflowing the width. The document author may reword instead (D-05).

Purpose: LABEL-02 — the alignment procedure must never split a word mid-word, in unit labels or edge labels. This is the shared wrap-machinery change both label kinds depend on; it also changes unit-label output for the pathological over-budget case, which is the intended semantic (STATE.md carry-forward), not a COMPAT-01 regression.

Output: `wrapText` without the `splitLongWord` branch/function; re-asserted `TestWrapText` cases proving unsplit overflow.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-label-formatting-fixes-0-1-plans/34-CONTEXT.md
@.planning/phases/34-label-formatting-fixes-0-1-plans/34-RESEARCH.md

Source files (read before editing):
@internal/render/wrap.go
@internal/render/wrap_internal_test.go

Decisions honored: D-05 (locked) — over-budget word starts its own line unsplit; no character-level fallback, no safety cap, no hyphen-point splitting. COMPAT-01 (locked) — in-budget wrapping must stay byte-identical (do NOT touch the `fitsOnLine`/flush logic or the `<BR/>` separator).
</context>

<interfaces>
From internal/render/wrap.go (current state — read the file for full context):
```go
func wrapText(text string, maxChars int) string           // word-boundary wrap, <BR/> separator; over-budget branch at lines ~82-89 calls splitLongWord
func fitsOnLine(currentLen, wordLen, maxChars int) bool   // currentLen > 0 && currentLen+1+wordLen <= maxChars
func flushIfPending(lines []string, currentLine *strings.Builder, currentLen *int) []string
func splitLongWord(word string, maxChars int) []string    // REMOVE per D-05 (lines ~121-138)
func wrapAndEscape(text string, maxChars int) string      // wrapText + html.EscapeString per <BR/>-split part
```
Constants: `htmlLineBreak = "<BR/>"`, `pointsPerChar = 8`, `pointsPerRow = 18`, `minCharsPerLine = 20`, `defaultLabelRatio = 1.6`.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>RED: Re-assert TestWrapText for unsplit overflow</name>
  <read_first>
    internal/render/wrap_internal_test.go (current assertions at lines 30-34 "forced character break" and 47-52 "multi-byte unicode")
    internal/render/wrap.go (current over-budget branch, lines 82-89)
  </read_first>
  <files>internal/render/wrap_internal_test.go</files>
  <behavior>
    - "forced character break" case (text "abcdefghij", maxChars 5): currently expects "abcde<BR/>fghij" (splitLongWord output) — re-assert to the D-05 semantic: the whole word on one line, unsplit: expected "abcdefghij". Rename the case to "over-budget word stays unsplit".
    - "multi-byte unicode" case (text "日本語テスト文字列", maxChars 4): currently expects "日本語テ<BR/>スト文字<BR/>列" — re-assert to "日本語テスト文字列" (whole word unsplit on one line; rune-count 9 > 4).
    - All other TestWrapText cases ("fits in one line", "word break", "short text unchanged", "empty string", "multiple words wrapping", "single word exactly maxChars") remain unchanged — verify they still pass after the re-assertion (their expectations do not depend on splitLongWord).
  </behavior>
  <action>
    In internal/render/wrap_internal_test.go TestWrapText: replace the "forced character break" table entry (name "forced character break", text "abcdefghij", maxChars 5, expected "abcde<BR/>fghij") with name "over-budget word stays unsplit", text "abcdefghij", maxChars 5, expected "abcdefghij". Replace the "multi-byte unicode" table entry (text "日本語テスト文字列", maxChars 4, expected "日本語テ<BR/>スト文字<BR/>列") with expected "日本語テスト文字列" (keep the name "multi-byte unicode"). Do NOT modify any other table entries. Keep the `//nolint:paralleltest // Consistent with project test patterns` comment on the test function.
  </action>
  <verify>
    <automated>go test ./internal/render/ -run TestWrapText -v</automated>
  </verify>
  <acceptance_criteria>
    - wrap_internal_test.go contains `name: "over-budget word stays unsplit"` and its expected value is exactly `"abcdefghij"` (no `<BR/>`)
    - wrap_internal_test.go "multi-byte unicode" entry expected value is exactly `"日本語テスト文字列"` (no `<BR/>`)
    - `go test ./internal/render/ -run TestWrapText -v` FAILS (RED gate): "over-budget word stays unsplit" and "multi-byte unicode" subtests fail against the current splitLongWord implementation
    - No other TestWrapText subtests changed
  </acceptance_criteria>
  <done>
    RED commit made: `test(34-01): re-assert wrapText over-budget behavior (unsplit overflow)`; the two re-asserted subtests fail for the right reason (behavior change, not compile errors)
  </done>
</task>

<task type="tdd" tdd="true">
  <name>GREEN: Remove character-level fallback from wrapText</name>
  <read_first>
    internal/render/wrap.go (over-budget branch lines 82-89, splitLongWord lines 121-138, wrapText doc comment lines 38-40)
    internal/render/wrap_internal_test.go (re-asserted cases from the RED task)
  </read_first>
  <files>internal/render/wrap.go</files>
  <behavior>
    - wrapText("abcdefghij", 5) == "abcdefghij" (word 10 runes > 5, emitted unsplit on its own line, no <BR/>)
    - wrapText("日本語テスト文字列", 4) == "日本語テスト文字列" (word 9 runes > 4, unsplit)
    - wrapText("one two three four", 9) == "one two<BR/>three<BR/>four" (in-budget behavior unchanged)
    - `grep -rn "splitLongWord" internal/` returns 0 hits (function deleted, no references)
  </behavior>
  <action>
    In internal/render/wrap.go: (1) replace the "Word exceeds maxChars - force character-level break" branch (currently a `for _, chunk := range splitLongWord(word, maxChars)` loop over lines 82-89) with a flush-and-emit-whole-word path: `lines = flushIfPending(lines, &currentLine, &currentLen)`, then write the entire word (`currentLine.WriteString(word)`), set `currentLen = wordLen`, with a comment citing D-05 ("word longer than the per-line budget starts its own line unsplit, overflowing the width — the author may reword; never split mid-word"). (2) Delete the `splitLongWord` function (lines 121-138) entirely. (3) Update the `wrapText` doc comment (lines 38-40): remove the "Falls back to character-level breaking for words exceeding maxChars." sentence; state that over-budget words are emitted unsplit on their own line. Do NOT touch `fitsOnLine`, `flushIfPending`, `wrapAndEscape`, the constants, or any other branch.
  </action>
  <verify>
    <automated>go test ./internal/render/ -run TestWrapText -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/render/ -run TestWrapText -v` PASSES (all subtests, including "over-budget word stays unsplit" and "multi-byte unicode")
    - `grep -rn "splitLongWord" internal/` returns 0 hits
    - wrap.go contains no `<BR/>`-chunking loop for over-budget words; the over-budget branch emits the full word via `currentLine.WriteString(word)`
    - wrap.go doc comment for wrapText no longer mentions character-level breaking
  </acceptance_criteria>
  <done>
    GREEN commit made: `feat(34-01): wrap at word boundaries only (no mid-word splits)`; re-asserted tests pass; no other test in the package regressed
  </done>
</task>

<task type="tdd" tdd="true">
  <name>REFACTOR + COMPAT-01: full package and golden regression run</name>
  <read_first>
    internal/render/wrap.go (post-GREEN state)
    .planning/phases/34-label-formatting-fixes-0-1-plans/34-RESEARCH.md (Golden Impact section — permitted deltas)
  </read_first>
  <files>internal/render/wrap.go</files>
  <behavior>
    - Full render package suite green: `go test ./internal/render/...`
    - canonicalDOT goldens byte-stable (DI-1): `go test ./cmd/c4drill/... ./internal/graph/...` passes — the multilevel fixture has no over-budget words, so LABEL-02 must not change any golden
  </behavior>
  <action>
    No code changes expected in this task unless the runs surface an issue. Run `go test ./internal/render/...` and `go test ./cmd/c4drill/... ./internal/graph/...`. If any canonicalDOT golden fails, investigate whether the change leaked outside the over-budget branch (e.g., fitsOnLine math, separator, escaping); the ONLY permitted output delta from this plan is the over-budget-word case. If a golden legitimately changes for another reason, STOP and report — do not silently re-baseline (RESEARCH.md Golden Impact). If everything is green, verify the wrapText change is minimal (git diff of wrap.go shows only the over-budget branch, the deleted splitLongWord, and the doc comment). Commit any refactor cleanup as `refactor(34-01): ...` (optional — only if cleanup was needed).
  </action>
  <verify>
    <automated>go test ./internal/render/... && go test ./cmd/c4drill/... ./internal/graph/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/render/...` passes
    - `go test ./cmd/c4drill/... ./internal/graph/...` passes (canonicalDOT goldens unchanged — COMPAT-01)
    - git diff for wrap.go contains no changes outside: the over-budget branch replacement, the splitLongWord deletion, the doc comment
  </acceptance_criteria>
  <done>
    Full-suite green; COMPAT-01 confirmed for the LABEL-02 change; plan SUMMARY.md written (`.planning/phases/34-label-formatting-fixes-0-1-plans/34-01-SUMMARY.md`) with RED/GREEN/REFACTOR commit list per TDD conventions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author TOML → wrapText | user-authored label text (descriptions, technologies) crosses into DOT label output here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | wrapText / wrapAndEscape | mitigate | Escaping is unchanged: wrapAndEscape still runs html.EscapeString per line (wrap.go:142-156). This plan only alters the word-splitting branch — do not bypass or reorder the escape path. |
| T-34-02 | DoS | wrapText | accept | Over-budget words now emit whole-word lines (bounded by input size); no unbounded loop introduced — the single-pass words loop is unchanged. |
| T-34-SC | Tampering | package installs | accept | No package installs in this phase (Go stdlib + existing pinned deps only) — slopcheck gate not applicable. |
</threat_model>

<verification>
- `go test ./internal/render/...` — package suite (unit)
- `go test ./cmd/c4drill/... ./internal/graph/...` — canonicalDOT goldens (COMPAT-01, DI-1)
- `grep -rn "splitLongWord" internal/` — 0 hits (LABEL-02 delivered)
</verification>

<success_criteria>
- [ ] No label text is ever split mid-word: wrapText emits over-budget words unsplit on their own line (D-05)
- [ ] `splitLongWord` deleted; zero references remain
- [ ] In-budget unit labels byte-identical: all canonicalDOT goldens pass unchanged (COMPAT-01)
- [ ] TestWrapText re-asserted with RED→GREEN commits; TDD gate sequence satisfied
</success_criteria>

<output>
Create `.planning/phases/34-label-formatting-fixes-0-1-plans/34-01-SUMMARY.md` when done (TDD plan: RED/GREEN/REFACTOR sections + commit list).
</output>
