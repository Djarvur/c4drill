---
phase: 03-compatibility-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/c4drill/testdata/multilevel.toml
  - cmd/c4drill/testdata/multilevel.expanded.dot
autonomous: true
requirements: [COMPAT-02]
must_haves:
  truths:
    - "A fresh checkout can run the full suite without the gitignored private fixture — the sanitized public fixture (multilevel.toml) stands in for the saira structure with generic names (D-01)"
    - "The committed golden baseline captures the v1.7-compatible expanded DOT: minlen=3 and penwidth=2 edge attributes present, all units at all levels included (D-02)"
    - "The fixture carries NO expansion hints (neither properties.expanded nor per-unit expanded) — the same baseline state the real saira file has, keeping COMPAT-01/COMPAT-02 semantics meaningful"
    - "The fixture preserves the structural skeleton that gives the tests their value: 5 top-level units, 4-level nesting, cross-level length links (Pitfall 2)"
  artifacts:
    - path: "cmd/c4drill/testdata/multilevel.toml"
      provides: "sanitized public fixture: 5 top-level units, 4-level nesting, cross-level length links"
      contains: "mainSystem.storages.externalStorage.client"
    - path: "cmd/c4drill/testdata/multilevel.expanded.dot"
      provides: "committed golden baseline for COMPAT-02 (DOT-level equivalence contract)"
      contains: "minlen=3"
  key_links:
    - from: "cmd/c4drill/testdata/multilevel.toml"
      to: "parser/validator pipeline"
      via: "CLI parse+validate run during fixture creation (must exit 0)"
      pattern: "go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml"
    - from: "cmd/c4drill/testdata/multilevel.toml"
      to: "cmd/c4drill/testdata/multilevel.expanded.dot"
      via: "exact pipeline ParseFile → Validate → GenerateExpandedView → BuildExpandedGraph → RenderDOT; filename per writer.go:66 {basename}.expanded.{format}"
      pattern: "multilevel.expanded.dot"
---

<objective>
Create the two committed test-data assets that make COMPAT-02 CI-enforceable (D-01): a sanitized public copy of the real-world multi-level TOML fixture and its `--expanded` DOT golden baseline.

Purpose: The two strongest compat tests (`TestBuildExpandedGraphRealToml`, `TestBuildExpandedGraphBaselineDOT`) currently read the gitignored private fixture `cyp-auth-infra/saira-20260320.c2.full.toml` and fail in fresh checkouts. This plan replaces that dependency with committed public assets so COMPAT-02 (the `--expanded` flag produces the single all-nested diagram unchanged, identical to v1.7) is testable everywhere with no skip-if-missing paths.

Output: `cmd/c4drill/testdata/multilevel.toml` (sanitized fixture, ~500 lines mirroring the saira structure) and `cmd/c4drill/testdata/multilevel.expanded.dot` (golden baseline generated by the exact CLI pipeline, filename per writer.go:66). No production code is touched — this phase is test infrastructure only.

Phase boundary (D-01..D-04): the private `cyp-auth-infra/` stays gitignored; the golden baseline is a NEW committed file (verified: no test consumes `testdata/expanded.dot` today — it is a stale C1 output, not an expanded-mode baseline); DOT-level equivalence is the contract (D-02), SVG byte-comparison is NOT.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compatibility-validation/03-CONTEXT.md
@.planning/phases/03-compatibility-validation/03-RESEARCH.md
@.planning/phases/03-compatibility-validation/03-PATTERNS.md
@cyp-auth-infra/saira-20260320.c2.full.toml
@cyp-auth-infra/saira-20260320.c2.full.expanded.dot
@cmd/c4drill/testdata/valid.toml
@cmd/c4drill/testdata/expanded.toml
@internal/output/writer.go

<interfaces>
Verified facts this session (do not re-derive):

- The saira fixture is 499 lines (`cyp-auth-infra/saira-20260320.c2.full.toml`): 5 top-level units (webUser, sshUser, adminUser — personExternal; keycloak — systemExternal; linuxSystem — system), 4-level nesting at linuxSystem.sshAuth.systemd.logind, cross-level link `{peer = "keycloak", length=3}` at linuxSystem.storages.keycloakStorage.client (:392-399), three length=2 links (:175, :259-260, :270). Inline `link = [ {peer = "..."}, ... ]` array syntax everywhere (NOT `[[unit.link]]`).
- The private expanded baseline (`cyp-auth-infra/saira-20260320.c2.full.expanded.dot`, 1041 lines) is XDOT: `digraph "" { ... }` with graph-level rankdir=TB and per-edge blocks carrying key=, minlen=N, penwidth=2, pos= geometry. Layout geometry (pos/bb) is deterministic for the go-graphviz version pinned in go.mod — the golden comparison is byte-exact.
- CLI pipeline == test pipeline for the baseline: runRoot (:133-136) → processExpandedView (:251) calls view.GenerateExpandedView → graph.BuildExpandedGraph → render.Render(g, format) → RenderDOT. LabelRatio: CLI default 1.6 == package default (wrap.go:29), so no divergence. Filename `{basename}.expanded.{format}` at internal/output/writer.go:64-67.
- Validator orphan rule (rules.go:122-139): a unit is an orphan if it has no Links, no LinksFrom, and no Subunits. `populateIncomingLinks` (index.go:50+) synthesizes LinksFrom mirrors for link targets, so a leaf is safe if it has outgoing links OR is someone's link target. Every unit in the saira structure already satisfies this — the sanitized copy inherits it.
- Baseline generation command (default output dir = input file's dir): `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded` writes cmd/c4drill/testdata/multilevel.expanded.dot.
- COMPAT-01/02 empirical anchor (valid.toml, run this session): C1 DOT has 2 nodes (user, app), NO clusters, app label "Application [+]", no "app.api" node; C2 sub-diagram valid/app.dot IS generated (Phase 2 auto-detect). NOT relevant to this plan's tasks — cited for completeness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the sanitized public fixture cmd/c4drill/testdata/multilevel.toml</name>
  <files>cmd/c4drill/testdata/multilevel.toml</files>
  <read_first>
    cyp-auth-infra/saira-20260320.c2.full.toml (499 lines — the structural source; read it fully, then apply the mechanical rename map below to every section header, link peer, display name, and description)
    cmd/c4drill/testdata/expanded.toml (committed-fixture syntax analog: inline array-of-tables and [[unit.link]] both parse; saira's inline `link = [ {...} ]` form is what multilevel.toml must use)
    cmd/c4drill/testdata/valid.toml (smallest committed fixture — placement and file-level conventions)
    .planning/phases/03-compatibility-validation/03-PATTERNS.md (fixture section: required skeleton table, TOML syntax notes, validator constraints, committed-placement analog)
  </read_first>
  <action>
    Create cmd/c4drill/testdata/multilevel.toml as a mechanical sanitized copy of cyp-auth-infra/saira-20260320.c2.full.toml. Apply this rename map to ALL occurrences (section headers, link peers, display names, references in other fields):

    | saira identifier | sanitized identifier |
    |---|---|
    | webUser (personExternal) | actorA |
    | sshUser (personExternal) | actorB |
    | adminUser (personExternal) | actorC |
    | keycloak (systemExternal) | externalSys |
    | linuxSystem (system, dotted-path prefix) | mainSystem |
    | keycloakStorage (dotted-path segment) | externalStorage |

    Keep every other unit name as-is (sshAuth, systemd, logind, userdbd, nss, etcStorage, pam, common, cypAuth, unix, authProxy, varlinkAPIs, sessionManager, sessionDb, localIDPClients, authAPI, sessionAPI, localIDP, rbac, dacService_lookupAPI, dacProxy, storages, localStorage, lookupAPI, managementAPI, provider, storage, dynamicUserManager, dynamicUserDb, authModules, otp, settingsAPI, validateAPI, db, userlock) — these are generic; do not rename.

    Display names: translate to generic English ("Web User"→"Actor A", "SSH User"→"Actor B", "Admin User"→"Actor C", "Keycloak"→"External IDP", "Linux System"→"Main System", "Keycloak Client"→"External Client", "Keycloak Storage"→"External Storage", etc.). All Russian descriptions → concise English equivalents. Comments: strip "CYP"/"SAIRA" references, keep structural comments.

    [properties]: name = "Multilevel Test System", description = "Sanitized multi-level test architecture".

    MUST preserve (these are the elements the repointed tests and the golden baseline depend on — do not flatten or drop):
    1. Exactly 5 top-level units: actorA, actorB, actorC (personExternal), externalSys (systemExternal), mainSystem (system).
    2. 4-level nesting: mainSystem.sshAuth.systemd.logind (component under container under containerBox under system).
    3. THE cross-level link: mainSystem.storages.externalStorage.client has exactly one link: {peer = "externalSys", length=3} (sanitized from :392-399).
    4. All three length=2 links preserved, renamed: the sessionAPI link at saira :175, the otp/userlock validateAPI links at :259-260, the localStorage authAPI link at :270.
    5. Inline `link = [ {peer = "...", length=N}, ... ]` array syntax exactly like saira.
    6. Same types, same nesting depth, same link topology as saira (all ~60 units) — full structural copy, identifiers renamed only.
    7. NO expansion hints anywhere: no [properties] expanded key, no per-unit expanded lists (verify with a grep).

    Constraints (validator must pass — the repointed tests run validator.Validate with require.Empty):
    - Every link peer must resolve to a defined unit (saira's topology already satisfies this; the rename must keep it consistent — rename inside link peers too).
    - No orphans: every unit has Links/LinksFrom or Subunits, or is a link target (populateIncomingLinks covers targets; saira's topology already satisfies this — e.g., mainSystem.sshAuth.systemd.logind is a link target at the renamed :176 site).
    - Do NOT add any link or unit beyond what saira has.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --output /tmp/multilevel-check --format dot</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && test -f /tmp/multilevel-check/multilevel.dot && grep -c $'^actorA\t\[' /tmp/multilevel-check/multilevel.dot && grep -c $'^externalSys\t\[' /tmp/multilevel-check/multilevel.dot && grep -c $'^mainSystem\t\[' /tmp/multilevel-check/multilevel.dot && test "$(grep -c 'subgraph cluster_' /tmp/multilevel-check/multilevel.dot)" = "0" && ! grep -q 'mainSystem\.sshAuth' /tmp/multilevel-check/multilevel.dot && grep -q 'Main System \[+\]' /tmp/multilevel-check/multilevel.dot && test -f /tmp/multilevel-check/multilevel/mainSystem.dot && test -f /tmp/multilevel-check/multilevel/mainSystem/sshAuth.dot</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && ! grep -rn "linuxSystem\|keycloak\|webUser\|sshUser\|adminUser" cmd/c4drill/testdata/multilevel.toml && ! grep -n "expanded" cmd/c4drill/testdata/multilevel.toml && grep -n 'length=3' cmd/c4drill/testdata/multilevel.toml | grep externalSys</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --output /tmp/multilevel-check --format dot` exits 0 (validator passes: no orphan/reference/nesting/box errors).
    - /tmp/multilevel-check/multilevel.dot contains the 5 top-level node lines (actorA, actorB, actorC, externalSys, mainSystem each matched by the ^name\t\[ pattern), zero "subgraph cluster_" lines, no "mainSystem.sshAuth" deep-path node, and label "Main System [+]" (collapsed indicator — no expansion hints).
    - C2/C3 sub-diagram files generated: /tmp/multilevel-check/multilevel/mainSystem.dot and /tmp/multilevel-check/multilevel/mainSystem/sshAuth.dot exist (auto-detect, Phase 2).
    - Sanitization gate: grep for linuxSystem|keycloak|webUser|sshUser|adminUser in multilevel.toml returns nothing; grep for "expanded" returns nothing; a line containing length=3 also contains externalSys.
    - Fixture committed: `git status --short` shows cmd/c4drill/testdata/multilevel.toml committed.
  </acceptance_criteria>
  <done>
    multilevel.toml committed: valid per validator, 5 top-level units, 4-level nesting, cross-level length=3 link to externalSys, length=2 links preserved, zero expansion hints, zero private-infra identifiers, C1 renders 5 collapsed nodes with C2/C3 sub-diagrams generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and commit the golden baseline cmd/c4drill/testdata/multilevel.expanded.dot</name>
  <files>cmd/c4drill/testdata/multilevel.expanded.dot</files>
  <read_first>
    cyp-auth-infra/saira-20260320.c2.full.expanded.dot (1041 lines — private generated baseline; shape reference for XDOT structure, per-edge blocks with key=, minlen=, penwidth=2, pos= geometry — do NOT copy from it, it is for format orientation only)
    internal/output/writer.go (:64-67 — WriteExpanded: relPath = "{basename}.expanded.{format}", this plan's file is multilevel.expanded.dot)
    cmd/c4drill/testdata/multilevel.toml (the fixture from Task 1 — must exist and pass validation first)
    .planning/phases/03-compatibility-validation/03-PATTERNS.md (golden baseline section: generation source, XDOT shape, golden comparison pattern, Pitfall 1 determinism)
  </read_first>
  <action>
    Generate the golden baseline with the exact CLI pipeline (identical to what TestBuildExpandedGraphBaselineDOT will reproduce via ParseFile → Validate → GenerateExpandedView → BuildExpandedGraph → RenderDOT):

    1. From the repo root run: `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded`. Default output dir = input file's dir, so this writes cmd/c4drill/testdata/multilevel.expanded.dot (writer.go:66 naming: {basename}.expanded.{format}).
    2. Run the same command a SECOND time and confirm the file is byte-identical (determinism check — Pitfall 1; XDOT layout geometry is deterministic for the pinned go-graphviz; if the second run differs, investigate and regenerate from the SAME pipeline before committing).
    3. Inspect the generated DOT: it must contain the edge block for `"mainSystem.storages.externalStorage.client" -> externalSys` with minlen=3 and penwidth=2 (D-02 contract attributes); at least one minlen=2 edge block; the deep node `"mainSystem.sshAuth.systemd.logind"`; and NO "[+]" anywhere in labels (expanded mode expands everything — D-04).
    4. Commit the file as a golden baseline. Do NOT modify its content by hand — it is generated output. Add no test yet (the comparison test lands in plan 03-02).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded && cmp -s cmd/c4drill/testdata/multilevel.expanded.dot cmd/c4drill/testdata/multilevel.expanded.dot.tmp || true</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && grep -n 'minlen=3' cmd/c4drill/testdata/multilevel.expanded.dot && grep -c 'penwidth=2' cmd/c4drill/testdata/multilevel.expanded.dot && grep -n '"mainSystem.sshAuth.systemd.logind"' cmd/c4drill/testdata/multilevel.expanded.dot && test "$(grep -c '\[+\]' cmd/c4drill/testdata/multilevel.expanded.dot)" = "0"</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ ./internal/graph/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - cmd/c4drill/testdata/multilevel.expanded.dot exists, is committed, and regenerating the CLI command reproduces it byte-identically (deterministic baseline).
    - The DOT contains: an edge block for "mainSystem.storages.externalStorage.client" -> externalSys with minlen=3 AND penwidth=2; at least one minlen=2 edge block; the deep node "mainSystem.sshAuth.systemd.logind"; zero "[+]" occurrences.
    - The file header matches the pipeline: `digraph "" {` with graph-level rankdir=TB (XDOT shape).
    - `go test ./cmd/c4drill/ ./internal/graph/ -count=1` is green (existing suite unaffected — the two builder tests still pass against the private fixture, which still exists locally; they are repointed in plan 03-02).
  </acceptance_criteria>
  <done>
    multilevel.expanded.dot committed as the COMPAT-02 golden baseline: generated by the exact pipeline, deterministic on regeneration, containing the length=3 cross-level edge with penwidth=2, deep 4-level nodes, and no collapsed indicators; existing test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML fixture → parser/validator | UNCHANGED pipeline — the new fixture is static test data that must pass the existing validator (require.Empty in the consuming tests); zero new input surface in production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | cmd/c4drill/testdata/multilevel.toml | mitigate | Fixture is validated by validator.Validate (require.Empty) in every consuming test (builder_test.go :808/:1215 pattern); a malformed edit breaks the suite loudly — no skip paths |
| T-03-02 | Tampering | fixture identifiers | mitigate | Sanitization gate in Task 1 acceptance: grep for linuxSystem\|keycloak\|webUser\|sshUser\|adminUser in multilevel.toml must return nothing — real infra identifiers never enter git |
| T-03-03 | Tampering | cmd/c4drill/testdata/multilevel.expanded.dot | mitigate | Golden file is generated output (never hand-edited); the plan-03-02 comparison test asserts byte-equality against the pipeline output, so any drift breaks the suite |
| T-03-SC | Tampering | package installs | accept | Zero new dependencies (stdlib + testify only — RESEARCH verified against go.mod); no package-manager commands in this plan; package legitimacy gate not applicable |

Note: ASVS L1 — no BLOCKING threats. Test-data-only phase; no production code changes (internal/view/scope.go and internal/graph/builder.go stay untouched).
</threat_model>

<verification>
- Per-task gates: the three automated commands in each task's verify (CLI exit 0 + DOT content greps, determinism re-run, targeted go test).
- Wave gate: `go test -v -race ./...` green after this plan (existing suite must be unaffected by the new committed assets).
- Scope guard: `git diff internal/view/scope.go internal/graph/builder.go` empty at plan end.
</verification>

<success_criteria>
- `cmd/c4drill/testdata/multilevel.toml` committed: passes the validator via the CLI, C1 renders exactly the 5 top-level nodes with no clusters (all collapsed), C2/C3 sub-diagram files generated, no expansion hints, no private identifiers.
- `cmd/c4drill/testdata/multilevel.expanded.dot` committed: regenerable byte-identically, contains minlen=3/penwidth=2 edge attributes and deep 4-level nodes.
- `go test -v -race ./...` green; `git diff internal/view/scope.go internal/graph/builder.go` empty.
</success_criteria>

<output>
Create `.planning/phases/03-compatibility-validation/03-01-SUMMARY.md` when done
</output>
