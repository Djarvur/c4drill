---
phase: 03-compatibility-validation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - internal/graph/builder_test.go
  - cmd/c4drill/root_test.go
  - internal/render/expanded_internal_test.go
autonomous: true
requirements: [COMPAT-01, COMPAT-02]
must_haves:
  truths:
    - "TestBuildExpandedGraphRealToml passes against the public fixture with sanitized names — the client → externalSys edge keeps MinLen=3 from the TOML length attribute and DOT contains minlen=3 (repointed, no gitignored paths)"
    - "TestBuildExpandedGraphBaselineDOT compares byte-exact against the committed golden baseline — COMPAT-02 is enforceable in CI on fresh checkouts with no skip-if-missing paths (D-01/D-02, DOT-level equivalence contract)"
    - "Expanded mode ignores properties.expanded: with Expanded=['mainSystem'] poisoned, the expanded view still contains ALL units and no '[+]' indicators anywhere (D-04)"
    - "valid.toml (no properties.expanded) generates a correct all-collapsed C1: no clusters, app node shows '[+]', no app.api node — COMPAT-01 (D-03 OR semantics are NOT duplicated here; already locked in Phase 2 by TestGenerateC1View_IsExpandedOrSemantics)"
    - "The multilevel fixture produces a 5-node C1 plus C2/C3 sub-diagram files end-to-end via the CLI (ROADMAP success criterion 4)"
    - "No test references ../../cyp-auth-infra/ after this plan — CI enforceability gate; render tests become deterministic (synthetic model only)"
  artifacts:
    - path: "internal/graph/builder_test.go"
      provides: "repointed RealToml + BaselineDOT (golden comparison) + D-04 regression"
      contains: "func TestBuildExpandedGraphIgnoresPropertiesExpanded"
    - path: "cmd/c4drill/root_test.go"
      provides: "COMPAT-01 regression + multilevel 5-node C1 / C2/C3 integration"
      contains: "func TestCompat01_ValidTomlAllCollapsed"
    - path: "internal/render/expanded_internal_test.go"
      provides: "private-fixture probing removed (deterministic synthetic model)"
      contains: "loadCYPAuthInfraModel"
  key_links:
    - from: "internal/graph/builder_test.go TestBuildExpandedGraphBaselineDOT"
      to: "cmd/c4drill/testdata/multilevel.expanded.dot"
      via: "os.ReadFile + require.Equal against RenderDOT output"
      pattern: "multilevel.expanded.dot"
    - from: "cmd/c4drill/root_test.go TestCompat01_ValidTomlAllCollapsed"
      to: "cmd/c4drill/testdata/valid.toml"
      via: "filepath.Join(\"testdata\", \"valid.toml\") CLI run"
      pattern: "valid.toml"
    - from: "internal/graph/builder_test.go TestBuildExpandedGraphIgnoresPropertiesExpanded"
      to: "internal/view/scope.go GenerateExpandedView (:13-49, READ-ONLY)"
      via: "poisoned m.Properties.Expanded must not shrink the view"
      pattern: "m.Properties.Expanded"
---

<objective>
Repoint the two private-fixture builder tests at the new public fixture, upgrade TestBuildExpandedGraphBaselineDOT to a committed-golden comparison (COMPAT-02 enforced at DOT level per D-01/D-02), add the D-04 regression (--expanded ignores properties.expanded), add the COMPAT-01 regression (valid.toml all-collapsed C1), add the multilevel 5-node C1 + C2/C3 integration test (ROADMAP success criterion 4), and remove the last cyp-auth-infra path probing from the render tests.

Purpose: Phase 3's compatibility guarantees become testable and enforceable in CI on fresh checkouts. All the machinery already exists and passes (Phases 1-2); every task in this plan is a test change only — there is NO production code change and NO RED state possible (the D-04 behavior is structurally guaranteed by GenerateExpandedView never consulting m.Properties.Expanded — scope.go:13-49). The D-04 test is therefore a LOCKING test (type auto): it must pass on first run; if it fails, the production machinery regressed.

Scope correction vs 03-PATTERNS.md (verified this session): PATTERNS listed 4 files; this plan also touches internal/render/expanded_internal_test.go because its loadCYPAuthInfraModel (:16-38) probes `../../cyp-auth-infra/cyp-auth-infra.toml` — the phase quality gate requires ZERO cyp-auth-infra references in .go files after execution. The synthetic fallback already matches all four render tests' assertions (server.* structure), so removing the probing only makes the tests deterministic (today they behave differently on machines with/without the private fixture).

Also corrected vs PATTERNS: the COMPAT-01 skeleton's `os.IsNotExist` assertion for valid/app.svg is WRONG — verified by CLI run this session: valid.toml's app unit HAS a subunit, so Phase 2 auto-detect generates valid/app.dot (the sub-diagram). COMPAT-01 is about C1 RENDERING (all units collapsed: no clusters, [+] indicators), not about absence of sub-diagram files. Do NOT assert sub-diagram absence.

Constraints: do NOT touch internal/view/scope.go, internal/graph/builder.go, or any penwidth/mirror machinery (Phase 1/2 constraints). Do NOT duplicate D-03 OR-semantics coverage (locked in Phase 2 by TestGenerateC1View_IsExpandedOrSemantics).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compatibility-validation/03-CONTEXT.md
@.planning/phases/03-compatibility-validation/03-RESEARCH.md
@.planning/phases/03-compatibility-validation/03-PATTERNS.md
@internal/graph/builder_test.go
@cmd/c4drill/root_test.go
@internal/render/expanded_internal_test.go
@cmd/c4drill/testdata/multilevel.toml
@cmd/c4drill/testdata/multilevel.expanded.dot
@cmd/c4drill/testdata/valid.toml
@internal/view/scope.go
@internal/model/properties.go

<interfaces>
Current state of the exact code to modify (verified this session — do not re-derive):

// builder_test.go imports (:1-16): stdlib {fmt, strings, testing}, module {graph, model, parser, render, validator, view}, testify {assert, require}. Group order: stdlib, module, third-party. "os" must be added to the stdlib group for the golden comparison.

// TestBuildExpandedGraphRealToml — builder_test.go:799-840 (package graph_test, t.Parallel):
//   :803  parser.ParseFile("../../cyp-auth-infra/saira-20260320.c2.full.toml")
//   :807-808  valErrors := validator.Validate(m); require.Empty(t, valErrors, "model should be valid")
//   :811  client := m.Units["linuxSystem"].Subunits["storages"].Subunits["keycloakStorage"].Subunits["client"]
//   :813-814  require.Len(t, client.Links, 1, "client should have 1 link"); assert.Equal(t, "keycloak", client.Links[0].Peer)
//   :815-816  expectedLength := client.Links[0].Length; require.Greater(t, expectedLength, 0, "link should have Length > 0")
//   :819-820  v := view.GenerateExpandedView(m); g := graph.BuildExpandedGraph(v)
//   :823-829  edge scan: edge.Source == "linuxSystem.storages.keycloakStorage.client" && edge.Target == "keycloak"
//   :832  assert.Equal(t, expectedLength, foundEdge.MinLen, "Edge MinLen should match TOML length attribute")
//   :835-839  render.RenderDOT(g); assert.Contains(t, string(dotData), fmt.Sprintf("minlen=%d", expectedLength), ...)

// TestBuildExpandedGraphBaselineDOT — builder_test.go:1205-1232:
//   :1211  parser.ParseFile("../../cyp-auth-infra/saira-20260320.c2.full.toml")  (same repoint as :803)
//   :1221-1224  penwidth loop: for _, edge := range g.Edges { assert.InDelta(t, 2.0, edge.PenWidth, 0.001, "expanded-mode edge %s -> %s should keep penwidth 2.0", ...) }  — KEEP
//   :1226-1231  dotData, err := render.RenderDOT(g); require.NoError; dot := string(dotData); assert.Contains(dot, "minlen="); assert.Contains(dot, "penwidth=2")  — REPLACE with golden comparison

// Golden comparison pattern (replaces :1226-1231; keeps the penwidth loop above):
//   dotData, err := render.RenderDOT(g)
//   require.NoError(t, err)
//   expected, err := os.ReadFile("../../cmd/c4drill/testdata/multilevel.expanded.dot")
//   require.NoError(t, err)
//   require.Equal(t, string(expected), string(dotData))

// Sanitized fixture facts (cmd/c4drill/testdata/multilevel.toml, from plan 03-01):
//   client path: mainSystem.storages.externalStorage.client; its single link: {peer = "externalSys", length=3}
//   deep 4-level leaf: mainSystem.sshAuth.systemd.logind
//   no expansion hints anywhere (no properties.expanded, no per-unit expanded)

// D-04 machinery (READ-ONLY references):
//   GenerateExpandedView — scope.go:13-49: sets View.AllExpanded=true; addUnitRecursive (:52-81) sets IsExpanded: len(unit.Subunits) > 0 — NEVER reads m.Properties.Expanded (model/properties.go:19-20).
//   buildNode "[+]" condition — builder.go:249-252: `if entry.HasSubunits && !entry.IsExpanded` → in expanded mode IsExpanded==HasSubunits, so "[+]" never appears.
//   BuildExpandedGraph — builder.go:125+: top-level units with subunits → buildNestedCluster into g.Clusters (recursive Cluster.Clusters); others → g.Nodes.
//   Cluster struct — graph.go:105-120: {ID string, Label *Label, Nodes []*Node, Clusters []*Cluster}.
//   Test analogs: TestBuildEdgesExpandedExemption (builder_test.go:1912-1949, D-02 view-constructed); TestBuildGraphCollapsedIndicator (builder_test.go:116-143, "[+]" assertion pattern).

// root_test.go conventions (verified):
//   package main; imports {bytes, os, path/filepath, testing} + testify. NO t.Parallel anywhere — every test carries "//nolint:paralleltest // go-graphviz WASM engine has concurrency issues" (e.g., :15, :583).
//   Harness analog TestFormatFlag_Dot (:583-599): tmpDir := t.TempDir(); cmd := NewRootCmd(); cmd.SetArgs([]string{filepath.Join("testdata", "valid.toml"), "--output", tmpDir, "--format", "dot"}); err := cmd.Execute(); require.NoError(t, err); os.ReadFile(filepath.Join(tmpDir, "valid.dot")).
//   File-existence pattern: os.Stat + require.NoError (TestExpandedUnits :568-577).

// COMPAT-01 empirical facts (valid.toml C1, CLI-verified this session):
//   C1 DOT: node lines "user\t[" and "app\t[", app label "Application [+]", NO "subgraph cluster_" lines, no "app.api" anywhere, edge "user -> app" with penwidth=2.
//   C2 sub-diagram valid/app.dot IS generated (Phase 2 auto-detect) — do NOT assert absence (PATTERNS skeleton was wrong on this point).

// internal/render/expanded_internal_test.go (:1-305): loadCYPAuthInfraModel (:16-38) probes 3 paths including "../../cyp-auth-infra/cyp-auth-infra.toml" (:21), falls back to createSyntheticCYPModel (:43-148). 4 call sites: TestExpandedViewNestedClusters (:157), TestHTMLTableAttributes (:200), TestClusterHTMLLabels (:227), TestNodeShapeBox (:276). All assertions target server.* paths present in the synthetic model — replacing the probing is safe. NOTE: createSyntheticCYPModel's "server.systemd" is TypeQueue (queueRatio), fine — keep as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint RealToml + BaselineDOT to the public fixture; upgrade BaselineDOT to golden comparison</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>
    internal/graph/builder_test.go (:795-840 TestBuildExpandedGraphRealToml and :1205-1232 TestBuildExpandedGraphBaselineDOT — the two bodies being modified; :1-16 imports — add "os" to the stdlib group)
    cmd/c4drill/testdata/multilevel.toml (fixture — sanitized names/paths the repoint targets)
    cmd/c4drill/testdata/multilevel.expanded.dot (golden baseline — the comparison target)
    .planning/phases/03-compatibility-validation/03-PATTERNS.md (builder_test.go section: repoint bodies, golden comparison pattern, D-13/assertion conventions)
  </read_first>
  <action>
    Two test-body migrations in internal/graph/builder_test.go (package graph_test; both tests keep t.Parallel() and their function names; comments reference D-01/D-02):

    1. TestBuildExpandedGraphRealToml (:799-840): change the fixture path at :803 to `parser.ParseFile("../../cmd/c4drill/testdata/multilevel.toml")`; at :811 change the unit-path walk to `m.Units["mainSystem"].Subunits["storages"].Subunits["externalStorage"].Subunits["client"]`; at :814 change the peer assertion to `assert.Equal(t, "externalSys", client.Links[0].Peer)`; at :825 change the edge scan condition to `edge.Source == "mainSystem.storages.externalStorage.client" && edge.Target == "externalSys"`. Keep everything else (validator require.Empty, expectedLength > 0, MinLen equality at :832, DOT minlen contains at :838-839). Update the function doc comment (:795-798) to say the fixture is the sanitized public copy (D-01) and keep the regression rationale (validator LinksFrom mirrors must preserve Length).

    2. TestBuildExpandedGraphBaselineDOT (:1205-1232): change the fixture path at :1211 to `../../cmd/c4drill/testdata/multilevel.toml`; KEEP t.Parallel and the penwidth 2.0 loop (:1221-1224); REPLACE the two assert.Contains checks (:1229-1231) with the golden comparison: `dotData, err := render.RenderDOT(g)` / `require.NoError(t, err)` / `expected, err := os.ReadFile("../../cmd/c4drill/testdata/multilevel.expanded.dot")` / `require.NoError(t, err)` / `require.Equal(t, string(expected), string(dotData))`. Add "os" to the stdlib import group (stdlib first, then module, then testify per CONVENTIONS). Update the doc comment (:1205-1207) to state the D-01/D-02 contract: byte-exact DOT-level equivalence against the committed golden baseline, regenerated via `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded`.

    Do NOT change any assertion semantics beyond the path/name repoints and the golden replacement; do NOT touch other tests in the file; do NOT modify production code.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestBuildExpandedGraphRealToml|TestBuildExpandedGraphBaselineDOT' -count=1</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -count=1</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && ! grep -n "cyp-auth-infra" internal/graph/builder_test.go</automated>
  </verify>
  <acceptance_criteria>
    - Both tests pass against ../../cmd/c4drill/testdata/multilevel.toml; `go test ./internal/graph/ -count=1` green.
    - builder_test.go contains zero "cyp-auth-infra" occurrences (grep returns nothing).
    - TestBuildExpandedGraphRealToml asserts: client path mainSystem.storages.externalStorage.client, peer "externalSys", MinLen == expectedLength (3), DOT contains "minlen=3".
    - TestBuildExpandedGraphBaselineDOT: keeps the penwidth InDelta 2.0 loop; ends with require.Equal(t, string(expected), string(dotData)) against multilevel.expanded.dot; "os" present in the stdlib import group.
    - Only the two test bodies and the import block changed in builder_test.go (git diff shows no other hunks); no production file modified.
    - Commit `test(03-02): repoint builder tests to public fixture with golden baseline`.
  </acceptance_criteria>
  <done>
    Both builder tests run against the committed public fixture; BaselineDOT enforces byte-exact DOT-level equality with the committed golden baseline (COMPAT-02 CI-enforceable, D-01/D-02); zero cyp-auth-infra references in builder_test.go.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-04 locking test — --expanded ignores properties.expanded</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>
    internal/graph/builder_test.go (TestBuildEdgesExpandedExemption :1912-1949 — D-04-adjacent analog and comment style "// D-02: ..."; TestBuildGraphCollapsedIndicator :116-143 — the "[+]" label assertion pattern; file imports now include "os" after Task 1)
    internal/view/scope.go (:13-49 GenerateExpandedView + addUnitRecursive — READ-ONLY reference proving the D-04 guarantee; do NOT edit)
    internal/model/properties.go (:19-20 — Expanded []string field the poison targets)
    cmd/c4drill/testdata/multilevel.toml (fixture the test drives)
  </read_first>
  <action>
    Add one locking test + two unexported helpers to internal/graph/builder_test.go (package graph_test, t.Parallel, place near TestBuildEdgesExpandedExemption):

    1. `TestBuildExpandedGraphIgnoresPropertiesExpanded` — comment "// D-04: --expanded mode ignores properties.expanded entirely — the flag expands EVERYTHING in one file (v1.7 contract)". Body:
       - m, err := parser.ParseFile("../../cmd/c4drill/testdata/multilevel.toml"); require.NoError(t, err).
       - require.Empty(t, validator.Validate(m), "model should be valid").
       - Poison: m.Properties.Expanded = []string{"mainSystem"} (D-04: if a future change makes GenerateExpandedView consult this, the view shrinks).
       - v := view.GenerateExpandedView(m); total := countModelUnits(m); require.Len(t, v.Units, total, "expanded view must contain ALL units even with properties.expanded set (D-04)").
       - g := graph.BuildExpandedGraph(v); require.NotNil(t, g).
       - nodeIDs, labels := collectExpandedGraphNodes(g); assert.Contains(t, nodeIDs, "mainSystem.sshAuth.systemd.logind"); assert.Contains(t, nodeIDs, "mainSystem.storages.externalStorage.client"); for _, name := range labels { assert.NotContains(t, name, "[+]", "expanded mode must not render collapsed indicators (D-04)") }.

    2. Unexported helpers (camelCase, no doc comments needed per CONVENTIONS; keep in builder_test.go):
       - `countModelUnits(m *parser.Model) int` — recursive walk: 1 + sum over m.Units[*].Subunits (use SubunitOrder if present, else map iteration, matching the parser's model shape).
       - `collectExpandedGraphNodes(g *graph.Graph) ([]string, []string)` — returns node IDs and label Names: walk g.Nodes plus a recursive walk of g.Clusters (Cluster.Nodes + Cluster.Clusters per graph.go:105-120; include Label.Name only when Label != nil).

    Expected outcome: the test passes IMMEDIATELY (locking test — production machinery already correct). If it FAILS on first run, the D-04 machinery regressed: investigate the failure against scope.go:13-49 BEFORE touching any production code, and if scope.go/builder.go must change, STOP and report to the coordinator — production changes are out of this phase's scope.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestBuildExpandedGraphIgnoresPropertiesExpanded' -count=1</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestBuildExpandedGraphIgnoresPropertiesExpanded exists and passes; it poisons m.Properties.Expanded = []string{"mainSystem"} BEFORE calling view.GenerateExpandedView (assertion order in the body proves the poison is set pre-view).
    - Assertions present: require.Len(v.Units, total) with total from a recursive model walk; deep leaves "mainSystem.sshAuth.systemd.logind" and "mainSystem.storages.externalStorage.client" present in collected node IDs; zero node Label.Name contains "[+]".
    - The test carries t.Parallel() and the "// D-04:" traceability comment; both helpers are unexported and camelCase.
    - `go test ./internal/graph/ -count=1` green; no production file modified (git diff internal/view/scope.go internal/graph/builder.go empty).
    - Commit `test(03-02): lock D-04 expanded-mode properties.expanded immunity`.
  </acceptance_criteria>
  <done>
    D-04 locked: with properties.expanded poisoned to ["mainSystem"], the expanded view still contains every unit of the multilevel fixture, deep 4-level leaves render as nodes, and no "[+]" indicator appears anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 3: COMPAT-01 regression + multilevel 5-node C1/C2/C3 integration; remove private-fixture probing from render tests</name>
  <files>cmd/c4drill/root_test.go, internal/render/expanded_internal_test.go</files>
  <read_first>
    cmd/c4drill/root_test.go (TestFormatFlag_Dot :583-599 — dot harness analog; TestExpandedUnits :554-578 — C2/C3 file-existence analog; file conventions: NO t.Parallel, every test starts with "//nolint:paralleltest // go-graphviz WASM engine has concurrency issues")
    cmd/c4drill/testdata/valid.toml (COMPAT-01 fixture: [user] person, [app] system with [app.api] container subunit, no expansion hints)
    cmd/c4drill/testdata/multilevel.toml (multilevel fixture for the integration test)
    internal/render/expanded_internal_test.go (:16-38 loadCYPAuthInfraModel — the probing to remove; :43-148 createSyntheticCYPModel — the deterministic replacement; 4 call sites :157/:200/:227/:276)
  </read_first>
  <action>
    Pure test changes — no production code.

    PART A — cmd/c4drill/root_test.go (package main; NO t.Parallel; "//nolint:paralleltest // go-graphviz WASM engine has concurrency issues" on each new test; harness per TestFormatFlag_Dot :583-599):

    1. `TestCompat01_ValidTomlAllCollapsed` — comment "// COMPAT-01: TOML without properties.expanded generates a correct C1 with all units collapsed". tmpDir := t.TempDir(); cmd := NewRootCmd(); cmd.SetArgs([]string{filepath.Join("testdata", "valid.toml"), "--output", tmpDir, "--format", "dot"}); require.NoError(t, cmd.Execute()); dotData, err := os.ReadFile(filepath.Join(tmpDir, "valid.dot")); require.NoError(t, err); dot := string(dotData). Assertions: assert.Contains(t, dot, "user\t[", "user node present in C1"); assert.Contains(t, dot, "app\t[", "app node present in C1"); assert.Contains(t, dot, "Application [+]", "app has subunits and no expansion hint -> collapsed with [+] in C1"); assert.NotContains(t, dot, "subgraph cluster_", "no clusters when everything is collapsed (COMPAT-01)"); assert.NotContains(t, dot, "app.api", "subunits must not appear in C1 when collapsed"). Do NOT assert anything about valid/app.dot existence or absence — the C2 sub-diagram IS generated by Phase 2 auto-detect (verified; the PATTERNS skeleton's os.IsNotExist assertion was wrong). Do NOT re-test D-03 OR semantics (Phase 2's TestGenerateC1View_IsExpandedOrSemantics owns that).

    2. `TestCompat02_MultilevelFixtureFiveNodeC1` — comment "// ROADMAP SC4: saira-equivalent public fixture -> 5-node C1 plus C2/C3 sub-diagrams (D-01)". Same harness with filepath.Join("testdata", "multilevel.toml"), output tmpDir, "--format", "dot". Read tmpDir/multilevel.dot. Assertions: assert.Contains(t, dot, "actorA\t[") / "actorB\t[" / "actorC\t[" / "externalSys\t[" / "mainSystem\t[", "the five top-level units render as C1 nodes"); assert.NotContains(t, dot, "subgraph cluster_", "all top-level units collapsed -> no clusters"); assert.NotContains(t, dot, "mainSystem.sshAuth", "deep subunit paths must not appear as C1 nodes"); assert.Contains(t, dot, "Main System [+]", "mainSystem collapsed indicator"). Sub-diagram files: require.NoError(t, err) on os.Stat(filepath.Join(tmpDir, "multilevel", "mainSystem.dot")) — C2; require.NoError on os.Stat(filepath.Join(tmpDir, "multilevel", "mainSystem", "sshAuth.dot")) — C3 (dotted-path layout per TestExpandedUnits :572-577 analog).

    PART B — internal/render/expanded_internal_test.go: replace the body of loadCYPAuthInfraModel (:16-38) with `t.Helper()` + `return createSyntheticCYPModel(t)` — delete the paths slice, the probing loop, and the t.Logf fallback branch. Update its doc comment: "// loadCYPAuthInfraModel returns the synthetic model matching the cyp-auth-infra structure. The private cyp-auth-infra/ fixture is gitignored; the synthetic model keeps render tests deterministic on every machine (D-01)." Keep all 4 call sites unchanged.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run 'TestCompat' -count=1</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/render/ -count=1</automated>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && ! grep -rn "cyp-auth-infra" --include="*.go" .</automated>
  </verify>
  <acceptance_criteria>
    - TestCompat01_ValidTomlAllCollapsed exists, passes, and asserts: user + app node lines present, "Application [+]" present, no "subgraph cluster_", no "app.api". It contains NO os.IsNotExist assertion on valid/app.dot.
    - TestCompat02_MultilevelFixtureFiveNodeC1 exists, passes, and asserts: 5 top-level node lines (actorA/actorB/actorC/externalSys/mainSystem), no clusters, no "mainSystem.sshAuth" deep path, "Main System [+]" label, and C2 (multilevel/mainSystem.dot) + C3 (multilevel/mainSystem/sshAuth.dot) files exist.
    - Both new root tests carry //nolint:paralleltest and NO t.Parallel.
    - internal/render/expanded_internal_test.go contains no cyp-auth-infra path strings; loadCYPAuthInfraModel is a thin wrapper over createSyntheticCYPModel; all 4 render tests pass.
    - Repo-wide gate: `grep -rn "cyp-auth-infra" --include="*.go" .` returns nothing (exit 1).
    - No production file modified; Commit `test(03-02): lock COMPAT-01 all-collapsed C1 and multilevel 5-node C1; drop private fixture probing`.
  </acceptance_criteria>
  <done>
    COMPAT-01 locked (valid.toml → all-collapsed C1: no clusters, [+] indicators, no nested nodes); the public multilevel fixture proven end-to-end (5-node C1 + C2/C3 sub-diagrams); zero cyp-auth-infra references remain in any .go file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → committed testdata | The repointed tests read only committed fixtures under cmd/c4drill/testdata/ (multilevel.toml, multilevel.expanded.dot, valid.toml) — the gitignored private fixture is fully removed from test inputs |
| expanded view (D-04) | The locking test poisons model data (m.Properties.Expanded) but only consumes read-only view/graph machinery — no new code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | builder_test.go / root_test.go paths | mitigate | Repo-wide gate in Task 3 verify: `! grep -rn "cyp-auth-infra" --include="*.go" .` — no test may reference the gitignored fixture after this plan (CI enforceability); D-01 forbids skip-if-missing paths |
| T-03-05 | DoS | golden baseline read (os.ReadFile) | mitigate | require.NoError on the read — a missing/corrupt baseline fails the suite loudly; no skip or tolerate paths |
| T-03-06 | Tampering | D-04 poison semantics | mitigate | Assertion order in the test body proves the poison is set BEFORE GenerateExpandedView; the require.Len(v.Units, total) assertion fails on any future properties.expanded consultation |
| T-03-SC | Tampering | package installs | accept | Zero new dependencies (stdlib + testify v1.11.1 only); no package-manager commands in this plan; package legitimacy gate not applicable |

Note: ASVS L1 — no BLOCKING threats. Test-only phase; internal/view/scope.go and internal/graph/builder.go remain read-only (scope guards in verification).
</threat_model>

<verification>
- Per-task: `go test ./internal/graph/ -run 'TestBuildExpandedGraph' -count=1`, `go test ./cmd/c4drill/ -run 'TestCompat' -count=1`, `go test ./internal/render/ -count=1`.
- CI gate: `! grep -rn "cyp-auth-infra" --include="*.go" .` returns nothing.
- Wave gate: `go test -v -race ./...` green; `mise lint` (golangci-lint) clean.
- Scope guard: `git diff internal/view/scope.go internal/graph/builder.go` empty at plan end.
- D-04 locking outcome: TestBuildExpandedGraphIgnoresPropertiesExpanded passes on first run (no RED expected — production machinery untouched; if it fails, production regressed and the executor must STOP and escalate, not edit production code).
</verification>

<success_criteria>
- TestBuildExpandedGraphRealToml and TestBuildExpandedGraphBaselineDOT run against the public fixture; BaselineDOT byte-equals the committed golden baseline (COMPAT-02, D-01/D-02).
- TestBuildExpandedGraphIgnoresPropertiesExpanded locks D-04 (expanded mode immune to properties.expanded).
- TestCompat01_ValidTomlAllCollapsed locks COMPAT-01 (all-collapsed C1 for hint-free TOML); D-03 OR coverage intentionally not duplicated.
- TestCompat02_MultilevelFixtureFiveNodeC1 proves ROADMAP success criterion 4 on the public fixture (5-node C1 + C2/C3 sub-diagrams).
- Zero cyp-auth-infra references in .go files; render tests deterministic via the synthetic model.
- `go test -v -race ./...` green, lint clean, no production file modified.
</success_criteria>

<output>
Create `.planning/phases/03-compatibility-validation/03-02-SUMMARY.md` when done
</output>
