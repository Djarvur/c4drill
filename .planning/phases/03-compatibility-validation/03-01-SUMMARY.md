---
phase: 03-compatibility-validation
plan: 01
subsystem: testing
tags: [testdata, golden-baseline, dot, xdot, compat, fixture, go-graphviz]
requires:
  - phase: 02-auto-generate-c2-c3-diagrams
    provides: C2/C3 auto-detect behavior the fixture exercises (sub-diagram generation)
provides:
  - sanitized public multi-level fixture (multilevel.toml, 499 lines) standing in for the private saira structure (D-01)
  - committed --expanded DOT golden baseline (multilevel.expanded.dot, 1041 lines, XDOT)
  - empirical determinism assessment of the render pipeline (semantic-stable, order-nondeterministic)
affects: [03-02-plan (repointed builder tests, golden comparison design), COMPAT-02 enforcement]
tech-stack:
  added: []
  patterns:
    - "sanitized structural copy: mechanical identifier rename (webUser→actorA, linuxSystem→mainSystem, keycloak→externalSys, keycloakStorage→externalStorage), structure/link topology preserved 1:1"
    - "CLI-generated golden baseline: {basename}.expanded.{format} per writer.go:66, never hand-edited"
key-files:
  created:
    - cmd/c4drill/testdata/multilevel.toml
    - cmd/c4drill/testdata/multilevel.expanded.dot
  modified: []
key-decisions:
  - "Committed both test-data assets despite render-pipeline order nondeterminism: the semantic content (D-02 contract: node/edge sets, attributes, cluster structure) is stable across runs; only sibling ordering and layout geometry flip (pinned go-graphviz fork map-order). 03-02 MUST compare order-insensitively (sort-normalize per RESEARCH Pitfall 1), NOT byte-exact require.Equal"
  - "No production code touched: scope guard git diff internal/view/scope.go internal/graph/builder.go is empty; the nondeterminism lives in the pinned external fork (onokonem/go-graphviz cgraph/gvc WASM layout), out of scope for a test-data-only plan"
patterns-established:
  - "Determinism verification protocol for XDOT baselines: run pipeline N times to /tmp, cmp all pairs; if ordering flips, verify semantic stability (attribute counts, node sets) before committing the reference"
requirements-completed: [COMPAT-02]
duration: 5min
completed: 2026-08-06
---

# Phase 03 Plan 01: Sanitized Public Fixture + Expanded DOT Golden Baseline Summary

**Committed sanitized 499-line multi-level TOML fixture (5 top-level units, 4-level nesting, cross-level length=3 link) and its CLI-generated 1041-line XDOT --expanded golden baseline, replacing the gitignored private saira fixture for CI-enforceable COMPAT-02 — with the pipeline's order-nondeterminism empirically characterized (semantic-stable, byte-unstable).**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-06T11:43:50Z
- **Completed:** 2026-08-06T11:47:56Z
- **Tasks:** 2
- **Files modified:** 2 (test data) + this SUMMARY + deferred-items.md

## Accomplishments

- `cmd/c4drill/testdata/multilevel.toml` (499 lines) — mechanical sanitized copy of the private saira structure: exactly 5 top-level units (actorA/actorB/actorC personExternal, externalSys systemExternal, mainSystem system), 4-level nesting `mainSystem.sshAuth.systemd.logind`, THE cross-level link `mainSystem.storages.externalStorage.client -> {peer="externalSys", length=3}` (fixture :398), all four length=2 links (:175, :259, :260, :270), inline `link = [{...}]` array syntax, zero expansion hints, zero private-infra identifiers. Validator-clean (CLI exit 0); C1 renders exactly the 5 top-level nodes with zero clusters, "Main System [+]" collapsed label, and C2/C3 sub-diagrams generated (`multilevel/mainSystem.dot`, `multilevel/mainSystem/sshAuth.dot`).
- `cmd/c4drill/testdata/multilevel.expanded.dot` (1041 lines, XDOT `digraph "" {` + rankdir=TB) — generated by the exact CLI pipeline (`--format dot --expanded`, writer.go:66 naming). Contains the COMPAT-02 contract: client→externalSys edge block with minlen=3 AND penwidth=2 (:1030-1041), four minlen=2 edge blocks, deep node `"mainSystem.sshAuth.systemd.logind"`, zero `[+]` indicators.
- Full suite green with race detector (`go test -v -race ./...` — all 7 packages ok); scope guard clean (`git diff internal/view/scope.go internal/graph/builder.go` empty).
- Determinism assessment (Pitfall 1): 5 pipeline runs (4x new fixture, 2x private fixture) — sibling cluster/node emission order and layout geometry (bb/pos/lp) flip per run; semantic content (minlen/penwidth counts, node/edge keys, labels, cluster structure) is byte-stable across runs. Root cause: map-order iteration in the pinned external fork `onokonem/go-graphviz` cgraph/gvc WASM layout — NOT in c4drill code (builder/converter are slice/order-deterministic).

## Task Commits

Each task was committed atomically:

1. **Task 1: Create the sanitized public fixture cmd/c4drill/testdata/multilevel.toml** - `bb536a6` (test)
2. **Task 2: Generate and commit the golden baseline cmd/c4drill/testdata/multilevel.expanded.dot** - `67f1098` (test)

**Plan metadata:** pending final commit

## Files Created/Modified

- `cmd/c4drill/testdata/multilevel.toml` - Sanitized 499-line public fixture: 5 top-level units, 4-level nesting, cross-level length=3 link, four length=2 links; validator-clean; no expansion hints; no private identifiers
- `cmd/c4drill/testdata/multilevel.expanded.dot` - 1041-line XDOT golden baseline generated by the exact CLI pipeline; minlen=3/penwidth=2 edge block, deep nodes, no collapsed indicators
- `.planning/phases/03-compatibility-validation/deferred-items.md` - Out-of-scope discovery log (render pipeline order nondeterminism + 03-02 test-design impact)

## Decisions Made

- Committed the golden baseline as the pipeline generated it (never hand-edited, T-03-03) even though byte-identical regeneration is impossible: the D-02 contract (node/edge sets, attributes, cluster structure) is stable and the file is a valid semantic reference. 03-02 must switch from byte-exact `require.Equal` to an order-insensitive comparison (sort-normalize per RESEARCH.md Pitfall 1, optionally strip layout geometry) — flagged as a blocker.
- No production code changes: fixing the nondeterminism would require modifying/repinning the external go-graphviz fork (Rule 4 territory) — explicitly out of scope for this test-data-only plan; left as a deferred item for a future phase.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Render pipeline output is order-nondeterministic (byte-identical regeneration impossible)**
- **Found during:** Task 2 (golden baseline generation, determinism check)
- **Issue:** The plan asserted "Layout geometry (pos/bb) is deterministic for the go-graphviz version pinned in go.mod — the golden comparison is byte-exact" (03-01-PLAN.md interfaces). Empirically false: regenerating the CLI command 4x yields 4 different files — sibling cluster order under a parent and node order within a cluster flip per run (verified: `systemd` vs `pam` first under sshAuth), and layout geometry is order-dependent. The private saira fixture shows the identical behavior (2/2 runs differ) — the existing 1041-line private baseline was generated once and its determinism was never actually verified. Root cause is map-order iteration inside the pinned external fork `github.com/onokonem/go-graphviz` (cgraph/gvc WASM layout), not c4drill code.
- **Fix:** Committed the pipeline-generated baseline per the plan's own instruction ("investigate and regenerate from the SAME pipeline before committing"). Verified semantic stability across runs (minlen=3 x1, penwidth=2 x56, minlen=2 x4, deep node present, zero `[+]` in every run) so the file satisfies the D-02 semantic contract. Documented that 03-02's byte-exact `require.Equal` golden comparison (03-02-PLAN.md:103-105) is invalid as designed and must become order-insensitive (sort-normalize before compare — RESEARCH.md Pitfall 1's own prescribed fallback).
- **Files modified:** none (test-data-only plan; no production code touched)
- **Verification:** 5 pipeline runs all differ pairwise (cmp), semantic attribute counts identical in all runs; `go test -v -race ./...` green
- **Committed in:** 67f1098 (Task 2 commit; deviation documented here and in deferred-items.md)

---

**Total deviations:** 1 auto-fixed (1 bug — pre-existing, not caused by this plan's changes)
**Impact on plan:** The two committed assets are correct and validator-clean; the plan's byte-determinism premise is disproven and MUST be corrected in 03-02's test design. No scope creep; no production code touched.

## Issues Encountered

- **Plan verify pattern shell-escaping:** Task 1's verify used `grep -c $'^actorA\t\['` which fails on this machine's ugrep (zsh `$'...'` quoting strips the backslash, producing an unescaped `[` → "mismatched [ ]" error), and the generated DOT indents node statements with a leading TAB, so `^actorA` never matches. Adjusted to `$'^\tactorA\t\\['` — semantics identical to the plan's intent (exactly-one top-level node line per unit). All five top-level units verified (count=1 each).
- **Task 2 byte-compare:** the plan's Task 2 verify `cmp -s ... && echo BYTE_IDENTICAL` was run without suppression and honestly failed — covered under Deviation 1 (not a one-off: 5/5 runs pairwise differ).

## Stub Tracking

None — both assets are complete generated/validated data; no placeholder values, no "coming soon" content, no unwired components.

## Threat Flags

None — the only new files are static test data under `cmd/c4drill/testdata/` consumed by existing validator/test pipelines; no new network endpoints, auth paths, file-access patterns, or schema changes at trust boundaries. T-03-01/T-03-02/T-03-03 mitigations hold (validator-gated fixture, sanitization gate clean, golden file never hand-edited).

## Next Phase Readiness

- 03-02 can repoint `TestBuildExpandedGraphRealToml` and `TestBuildExpandedGraphBaselineDOT` at `cmd/c4drill/testdata/multilevel.toml` (relative `../../cmd/c4drill/testdata/...`) with hardcoded names updated (builder_test.go :811/:814/:825 → `mainSystem.storages.externalStorage.client` / `externalSys`).
- **BLOCKER for 03-02:** the golden comparison must NOT be byte-exact `require.Equal(t, string(expected), string(dotData))` (03-02-PLAN.md:103-105) — it will fail on the first run (order flips). Required: sort-normalize both sides (block-level), or strip layout geometry (bb/pos/lp/height/width) and compare semantic content, or assert on sorted node/edge attribute sets. The committed `multilevel.expanded.dot` is the semantic reference for that comparison.
- Options for full byte-determinism (out of scope here, Rule 4): fix/repin the `onokonem/go-graphviz` fork (map-order emission) or switch renderer — needs user decision in a future phase if byte-equality is ever required.

---
*Phase: 03-compatibility-validation*
*Completed: 2026-08-06*
