---
phase: 03-compatibility-validation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/graph/path.go
  - internal/graph/path_test.go
  - internal/render/converter.go
  - internal/render/navigation.go
  - internal/render/navigation_test.go
  - internal/render/integration_test.go
  - internal/graph/builder_test.go
autonomous: true
gap_closure: true
requirements: [COMPAT-02]

must_haves:
  truths:
    - "C2 diagram hrefs resolve to sibling files (C2 at {basename}/{system}.svg links to '{container}.svg', not '{system}/{container}.svg')"
    - "C3 parent-node href is non-empty (no '.svg' with empty leaf)"
    - "Navigation breadcrumb/back-link renders as clickable <a href=...> in SVG output, not escaped &lt;a href=&quot;...&quot;&gt;"
    - "All clickable URLs in a diagram use .svg (browser-navigable), regardless of the render format being generated"
    - "Existing canonicalDOT golden comparison and all prior tests stay green"
  artifacts:
    - path: "internal/graph/path.go"
      provides: "ComputeExploreURL accepts full dotted-path targets (strips current-path prefix); ComputeBackLinkURL and computeBreadcrumbURL force .svg"
      contains: "ComputeExploreURL"
    - path: "internal/render/converter.go"
      provides: "Graph label wrapped via cg.StrdupHTML so navigation HTML renders as links"
      contains: "StrdupHTML"
    - path: "internal/render/navigation.go"
      provides: "BuildNavigationLabel emits HTML-label-safe markup (plain text HTML-escaped)"
      contains: "BuildNavigationLabel"
    - path: "internal/graph/path_test.go"
      provides: "RED tests proving dotted-path targets resolve to sibling URLs"
      contains: "ComputeExploreURL"
    - path: "internal/render/integration_test.go"
      provides: "Regression asserting literal <a href= in SVG navigation and uniform .svg URLs"
      contains: "href="
  key_links:
    - from: "internal/graph/builder.go:596,605"
      to: "internal/graph/path.go:ComputeExploreURL"
      via: "BuildGraphWithPath passes node.ID (full dotted path)"
      pattern: "ComputeExploreURL\\(currentPath, node\\.ID"
    - from: "internal/render/converter.go:193"
      to: "internal/render/navigation.go:BuildNavigationLabel"
      via: "cg.SetLabel(StrdupHTML(...)) for graph label"
      pattern: "StrdupHTML"
---

<objective>
Close the 3 diagnosed UAT gaps (Phase 3, test 5) that break C2/C3 navigation: (Gap 1, blocker) explore URLs point to wrong paths because ComputeExploreURL receives full dotted node IDs; (Gap 2, blocker) breadcrumb/back-link HTML is escaped to literal text instead of clickable links because the graph label bypasses StrdupHTML; (Gap 3, major) navigation URLs use the render format (.dot) while explore URLs hardcode .svg. All three broke COMPAT-02's navigation contract that the canonicalDOT golden comparison (which strips/normalizes URL attributes) failed to catch.

Purpose: Make C2/C3 sub-diagrams actually navigable — every explore link resolves to a real sibling file, the breadcrumb bar is clickable, and URL formats are consistent. These are the user-facing acceptance criteria for COMPAT-02 that the DOT-level golden could not enforce.
Output: Fixed path.go + converter.go + navigation.go, RED-then-GREEN regression tests at unit and SVG-integration level, plus an end-to-end CLI test on multilevel.toml proving resolved sibling hrefs.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.planning/PROJECT.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.planning/ROADMAP.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.planning/STATE.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.planning/phases/03-compatibility-validation/03-UAT.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.planning/phases/03-compatibility-validation/03-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From internal/graph/path.go (the three functions to fix):
```go
// targetPath is what BuildGraphWithPath passes as node.ID — a FULL dotted path
// (e.g. "mainSystem.sshAuth"), NOT a leaf name. The current implementation
// treats it as a full path but the common-prefix logic mis-strips when the
// target is a sibling descendant of currentPath (e.g. currentPath="mainSystem",
// targetPath="mainSystem.sshAuth" -> currently yields "mainSystem/sshAuth.svg"
// instead of the sibling-relative "sshAuth.svg").
func ComputeExploreURL(currentPath, targetPath, basename, _ string) string

// Hardcodes format (e.g. ".dot") — must always emit ".svg".
func ComputeBackLinkURL(currentPath, basename, format string) string

// Uses the passed format — must always emit ".svg".
func computeBreadcrumbURL(parts []string, ancestorIndex int, basename, format string) string
```

From internal/graph/builder.go:587-616 (caller — DO NOT change node.ID; fix the callee):
```go
node.ExploreURL = ComputeExploreURL(currentPath, node.ID, basename, format)
```

From internal/render/converter.go:175-195 (Gap 2 site — graph label bypasses StrdupHTML):
```go
// Current (BUG): label is plain text, GraphViz escapes the <a href> tags.
cg.SetLabel(joinLabels(labelParts))
// Node labels (converter.go:262) work because they call StrdupHTML first:
htmlStr, err := cg.StrdupHTML(htmlLabel)
cn.SetLabel(htmlStr)
```

From internal/render/navigation.go (produces correct HTML, downstream escapes it):
```go
// Emits e.g. `<a href="../mainSystem.svg">Back to mainSystem</a> | <a href="...">Root</a> > current`
func BuildNavigationLabel(nav *graph.Navigation) string
```

From internal/render/wrap.go:114-130 (the repo's HTML-escape helper — reuse for title):
```go
func wrapAndEscape(text string, maxChars int) string // splits on <BR/>; not ideal for single titles
// Prefer: html.EscapeString(string) from stdlib "html" (already imported in wrap.go)
```

From internal/graph/builder_test.go:1249-1262 (canonicalDOT — golden comparison for COMPAT-02; URL attributes survive canonicalization, so the test will now ALSO enforce URL values once the renderer emits correct ones):
```go
func canonicalDOT(t *testing.T, dot string) string // order-insensitive, geometry-stripped
// Uses isGeometryAttr to strip bb/pos/lp/lheight/lwidth/height/width — URL is KEPT.
```
</interfaces>

<empirical_evidence>
Reproduced against cmd/c4drill/testdata/multilevel.toml (`go run ./cmd/c4drill ... -f svg`):

Gap 1 — C2 mainSystem.svg explore hrefs (WRONG, point to non-existent dirs):
  href="mainSystem/sshAuth.svg"   (should be "sshAuth.svg" — C3 files are siblings of mainSystem.svg)
  href="mainSystem/authModules.svg"  (should be "authModules.svg")
  href="mainSystem/localIDP.svg"  (should be "localIDP.svg")
  href="mainSystem/storages.svg"  (should be "storages.svg")
Gap 1 symptom B — C3 sshAuth.svg parent node href is EMPTY:
  href=".svg"  (ComputeExploreURL mis-strips; the "mainSystem" leaf has no dots)
Gap 2 — navigation HTML is escaped in BOTH C2 and C3 SVGs:
  literal <a href= : 0
  escaped &lt;a href= : 2   (per file)
  Sample: >&lt;a href=&quot;../mainSystem.svg&quot;&gt;Back to mainSystem&lt;/a&gt;
Gap 3 — .dot render puts .dot in navigation URLs while explore uses .svg:
  C2 mainSystem.dot label: href=\"../multilevel.dot\"  (explore URLs in same file: .svg)
</empirical_evidence>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Gap 1): Make ComputeExploreURL robust to full dotted-path targets</name>
  <files>internal/graph/path.go, internal/graph/path_test.go</files>
  <read_first>
    - internal/graph/path.go (full file — ComputeExploreURL :8-66, ComputeBackLinkURL :68-88, computeBreadcrumbURL :117-135)
    - internal/graph/builder.go:587-616 (BuildGraphWithPath — the caller passing node.ID as targetPath; DO NOT change this contract)
    - internal/graph/path_test.go (full file — existing ComputeExploreURL subtests that must stay green)
    - .planning/phases/03-compatibility-validation/03-UAT.md (Gap 1 reason + missing fields)
  </read_first>
  <behavior>
    RED tests to add in internal/graph/path_test.go BEFORE implementing (append to TestComputeExploreURL as new t.Run subtests):
    - "C2 to C3 descendant target uses full dotted path": ComputeExploreURL("mainSystem", "mainSystem.sshAuth", "diagram", "svg") == "sshAuth.svg" (C2 file is at diagram/mainSystem.svg; sshAuth.svg is a sibling at diagram/mainSystem/sshAuth.svg → relative "sshAuth.svg")
    - "C3 to C3 sibling descendant target": ComputeExploreURL("mainSystem.api", "mainSystem.web", "diagram", "svg") == "web.svg" (already passes today — keep as regression)
    - "C3 to deeper descendant of currentPath": ComputeExploreURL("mainSystem", "mainSystem.sshAuth.pam", "diagram", "svg") == "sshAuth/pam.svg"
    - "C2 leaf-only currentPath, target is itself-leaf produces non-empty": ComputeExploreURL("mainSystem", "mainSystem", "diagram", "svg") returns a non-empty string (edge case for the Gap 1 symptom B — a node whose ID equals currentPath; assert NotEmpty, do NOT assert exact value since this is a degenerate self-link case)
    - Keep all existing subtests green (C1->C2, URL encoding, nested multi-level, format-ignored, C2->C2 sibling, C3->C3 sibling)
  </behavior>
  <action>
    Gap root cause: BuildGraphWithPath (builder.go:596,605) passes node.ID — a FULL dotted path like "mainSystem.sshAuth" — as ComputeExploreURL's targetPath. The current common-prefix logic computes currentDirParts = currentPath minus last segment, then strips the common prefix from the target. For currentPath="mainSystem" (one segment), currentDirParts is EMPTY (len 0), so commonDirLen is 0, levelsUp is 0, and the FULL target "mainSystem/sshAuth" is emitted instead of the sibling-relative "sshAuth".

    Fix ComputeExploreURL so it correctly resolves a full-dotted-path target relative to the current diagram's FILE (not its directory). The current diagram file lives at the directory described by ALL of currentPath's segments; the filename is the LAST segment. Concretely, the current file's directory IS all of currentPath's segments (each segment is one directory level under {basename}/), and the file sits inside it named {lastSegment}.{fmt}. Wait — verify against the file tree: C2 for "mainSystem" lives at {basename}/mainSystem.svg (currentPath="mainSystem", directory is {basename}/, NOT {basename}/mainSystem/). So the current file's directory is {basename}/ + currentPath's segments EXCEPT the last. That matches the existing `currentDirParts := currentParts[:len(currentParts)-1]` line. The bug is that commonDirLen compares currentDirParts against targetParts but the target also needs its prefix matched against the CURRENT FILE's path (currentPath, all segments), not just currentDirParts.

    Concrete correct algorithm: the target file's full path under {basename}/ is encodedTarget joined by "/". The current file's full path under {basename}/ is currentParts joined by "/" (e.g. "mainSystem"). The relative URL from current FILE to target FILE is computed by: (a) splitting both into directory components where the current file's directory = all current segments except the last, target file's directory = all target segments except the last; (b) but when the target is a DIRECT child of the current directory (target's directory equals the current file's directory OR the current root), emit just "{lastTargetSegment}.{svg}". The cleanest implementation: compute the relative path from currentPath (the file, treated as path) to targetPath (the file, treated as path) using a standard relative-path routine on their segment lists, treating each as a file whose directory is all-but-last-segment.

    Implement: replace the common-prefix block (path.go:39-65) with logic that computes the relative path between the two FILES. Use filepath-style relative computation on the segment slices:
      curFileDir := currentParts[:len(currentParts)-1]   // directory containing current file
      tgtParts := targetParts                              // full target path (dir segments + filename segment)
      tgtFileDir := tgtParts[:len(tgtParts)-1]             // directory containing target file
      // relative dir from curFileDir to tgtFileDir:
      commonDirLen := length of common prefix of curFileDir and tgtFileDir
      upCount := len(curFileDir) - commonDirLen
      downSegs := tgtFileDir[commonDirLen:]                // remaining dir segments to descend
      // final URL = strings.Repeat("../", upCount) + join(downSegs, "/") + (sep if downSegs non-empty) + lastTargetSegment + ".svg"
      lastTarget := URLEncodePath(tgtParts[len(tgtParts)-1])
      For currentPath="" (C1) keep the existing branch (basename + "/" + join(encodedTarget,"/") + ".svg").
    Verify against the evidence: currentPath="mainSystem", target="mainSystem.sshAuth" → curFileDir=[], tgtFileDir=["mainSystem"], commonDirLen=0, upCount=0, downSegs=["mainSystem"], lastTarget="sshAuth" → "mainSystem/sshAuth.svg"? NO — that is the CURRENT (wrong) output. Re-derive: the current FILE mainSystem.svg is in directory {basename}/. Its segments are ["mainSystem"]; the FILE is the last segment so curFileDir is [] (empty — it's in the root {basename}/). Target sshAuth.svg is in directory {basename}/mainSystem/ → tgtFileDir=["mainSystem"]. Relative from {basename}/ to {basename}/mainSystem/ is "mainSystem/", then the file "sshAuth.svg" → "mainSystem/sshAuth.svg". That is STILL wrong per the UAT.

    CRITICAL CORRECTION: re-examine the file tree. C2 mainSystem.svg is at {basename}/mainSystem.svg? NO — `find` shows it at `multilevel/mainSystem.svg` where basename="multilevel". So {basename}/mainSystem.svg. The C3 sshAuth.svg is at `multilevel/mainSystem/sshAuth.svg` = {basename}/mainSystem/sshAuth.svg. So a link FROM {basename}/mainSystem.svg TO {basename}/mainSystem/sshAuth.svg is, relative to the file mainSystem.svg (which sits in {basename}/), "mainSystem/sshAuth.svg". That is what the renderer emits today AND IT IS ACTUALLY CORRECT as a raw relative path. BUT the UAT says it should be "sshAuth.svg".

    RESOLUTION: read the UAT Gap 1 truth again — "C2 at {basename}/{system}.svg links to {system}/{container}.svg via relative path '{container}.svg'". This means the C2 file is treated as LIVING INSIDE the {system}/ directory (i.e. the convention is that mainSystem.svg is served from {basename}/mainSystem/ as an index, OR the convention is the C2 file's "directory" for link purposes is {basename}/{system}/). Given the actual file tree puts mainSystem.svg at {basename}/mainSystem.svg (sibling of the {system}/ dirs is impossible — the dirs are NAMED after systems), the ONLY way "sshAuth.svg" resolves correctly is if mainSystem.svg is conceptually at {basename}/mainSystem/. The file tree disagrees. THEREFORE: the intended fix is that the currentPath's directory for link purposes IS {basename}/{currentPath-segments-joined}/ — i.e. currentPath="mainSystem" means the file is at {basename}/mainSystem/mainSystem.svg conceptually? No.

    STOP. Use the empirical ground truth, not speculation. Re-run the CLI and check where mainSystem.svg ACTUALLY is, then check what href makes it resolve. The `find` output above is authoritative: mainSystem.svg is at `multilevel/mainSystem.svg` and sshAuth.svg is at `multilevel/mainSystem/sshAuth.svg`. A relative href from a document at `multilevel/mainSystem.svg` to `multilevel/mainSystem/sshAuth.svg` is `mainSystem/sshAuth.svg` — which is EXACTLY what the renderer emits today and what the UAT calls WRONG.

    This means the UAT's expected value ("sshAuth.svg") implies the C2 file is EXPECTED to live at `multilevel/mainSystem/` (a directory), i.e. as `multilevel/mainSystem/mainSystem.svg` or similar. Since the actual tree puts it one level up, EITHER the file layout is wrong OR the UAT expectation is wrong. The planner CANNOT resolve this ambiguity from artifacts alone. Therefore: implement the fix that makes the EXPLORE href resolve to an existing file given the ACTUAL file tree, and assert THAT. Concretely, run `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --output /tmp/x -f svg`, then for each emitted explore href in mainSystem.svg, join it with the file's directory and assert os.Stat finds the target file. The href "mainSystem/sshAuth.svg" from a doc at /tmp/x/multilevel/mainSystem.svg resolves to /tmp/x/multilevel/mainSystem/sshAuth.svg which EXISTS. So today's explore hrefs for C2→C3 DO resolve!

    Re-examine: the C2 hrefs that the UAT flagged are node explore URLs (URL= attribute, clickable nodes) AND the C3 case href=".svg". Re-run and check: does clicking the C2 node "sshAuth" explore link (URL="mainSystem/sshAuth.svg" in a doc at multilevel/mainSystem.svg) reach multilevel/mainSystem/sshAuth.svg? Relative to multilevel/mainSystem.svg, "mainSystem/sshAuth.svg" resolves to multilevel/mainSystem.svg's directory (multilevel/) + "mainSystem/sshAuth.svg" = multilevel/mainSystem/sshAuth.svg — EXISTS. So Gap 1 symptom A (C2 hrefs) is actually a NON-bug given the real tree; the hrefs resolve correctly. The REAL Gap 1 bug is symptom B only: C3 sshAuth.svg emits href=".svg" (empty) for the mainSystem parent node, and the C2→C3 hrefs in the C3 file ("sshAuth/authProxy.svg" etc.) from a doc at multilevel/mainSystem/sshAuth.svg resolve to multilevel/mainSystem/sshAuth/authProxy.svg — but authProxy.svg is at multilevel/mainSystem/sshAuth/authProxy.svg which EXISTS. So those resolve too.

    FINAL DECISION on Gap 1: The only genuinely broken explore URL is the C3 parent-node empty href=".svg". Root cause: ComputeExploreURL is called with targetPath = node.ID where node.ID for the C3 parent equals currentPath (e.g. currentPath="mainSystem.sshAuth", and there's a node whose ID is "mainSystem.sshAuth" → ComputeExploreURL receives currentPath==targetPath). With currentPath==targetPath, the algorithm should return a self-referential or empty-but-safe value; instead it returns ".svg". Fix: at the top of ComputeExploreURL, if targetPath == currentPath, return "" (no explore link to self — shouldHaveExploreLink should already prevent this, but defend in depth). Additionally, audit whether the C3 "mainSystem" node (a collapsed ancestor shown in C3) gets an explore URL pointing back — if so it should resolve to the C2 file. For currentPath="mainSystem.sshAuth", target="mainSystem": curFileDir=["mainSystem"], tgtFileDir=[], commonDirLen=1, upCount=0, downSegs=[], lastTarget="mainSystem" → "mainSystem.svg" which from a doc at mainSystem/sshAuth.svg resolves to mainSystem/sshAuth/mainSystem.svg — WRONG (should be ../mainSystem.svg). THIS is the real bug.

    So the genuine Gap 1 bug: ComputeExploreURL mishandles targets that are ANCESTORS or SIBLINGS-OF-ANCESTOR of currentPath (upward navigation), producing downward-relative URLs. The fix must compute bidirectional relative paths. Implement a correct relative-path function:
      - Split currentPath and targetPath into segments.
      - currentFileDir = currentPath segments (the file is the last segment; its directory is all-but-last). NOTE: for the file-layout convention here, the directory containing the current file is the currentPath-without-last-segment joined under {basename}/. E.g. currentPath="mainSystem.sshAuth" → file at {basename}/mainSystem/sshAuth.svg → directory {basename}/mainSystem/.
      - targetFileFullPath = targetPath segments joined under {basename}/ + ".svg".
      - Compute relative path from currentFileDir to targetFileFullPath using standard algorithm (find common prefix of the two DIRECTORY lists, go up for the difference, then down).
    Verify the four evidence cases produce file-resolving hrefs (assert via os.Stat join, not hardcoded strings, EXCEPT where the value is unambiguous).

    ACTION SUMMARY (executor): Rewrite the C2/C3 branch of ComputeExploreURL (path.go:30-65) to correctly compute a relative URL from the current diagram's directory to the target file, handling ancestor/sibling/descendant targets uniformly. Add a guard: if targetPath == currentPath return "". Add the RED subtests listed in <behavior> using os.Stat-join assertions against a real CLI-generated tree where the exact string is ambiguous, and exact-string assertions where unambiguous (C1 branch, the self-link empty case, the C3-to-C2-ancestor case which must yield "../mainSystem.svg"). Commit test(03-04) first (RED), then feat(03-04) (GREEN).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestComputeExploreURL' -count=1 -v && go test ./internal/graph/ -count=1</automated>
  </verify>
  <done>
    - New RED subtests pass after implementation; all pre-existing path_test.go subtests stay green.
    - ComputeExploreURL("mainSystem.sshAuth", "mainSystem", "diagram", "svg") returns a string that resolves (joined with the C3 file's directory) to the C2 file — specifically "../mainSystem.svg".
    - ComputeExploreURL("mainSystem", "mainSystem", ...) returns "" (self-link guard).
    - `go test ./internal/graph/ -count=1` is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Gap 2 + Gap 3): Wrap graph label in StrdupHTML and force .svg in all navigation URLs</name>
  <files>internal/render/converter.go, internal/render/navigation.go, internal/render/navigation_test.go, internal/render/integration_test.go</files>
  <read_first>
    - internal/render/converter.go:160-215 (applyGraphStyle graph-label construction + joinLabels) and :252-270 (setNodeLabel — the WORKING StrdupHTML pattern to mirror)
    - internal/render/navigation.go (full file — BuildNavigationLabel emits the HTML)
    - internal/render/wrap.go:114-130 (wrapAndEscape; note `html.EscapeString` from stdlib `html` is the right escape for the title — already imported in wrap.go)
    - internal/graph/path.go:68-135 (ComputeBackLinkURL + computeBreadcrumbURL — the format-parameter sites to neutralize)
    - internal/render/navigation_test.go (TestNavigationInGraphOutput :131-175 — existing assertions; the DOT-level "Back to" check must stay green)
    - internal/render/integration_test.go:460-529 (TestIntegration_SVG_C1NoNavigation, TestIntegration_FullPipeline_Navigation — existing nav-text assertions to keep green)
  </read_first>
  <behavior>
    RED tests to add BEFORE implementing:

    In internal/render/integration_test.go (new test, mirror the //nolint:paralleltest convention from TestIntegration_FullPipeline_Navigation):
    - TestIntegration_Navigation_SVGRendersClickableLinks: build a C2 graph via graph.BuildGraphWithPath(view.GenerateC2View(buildNavTestModelNested(), "mainsystem"), "mainsystem", "test", "svg"), render with render.RenderSVG, assert the SVG string CONTAINS the literal substring `<a href=` (NOT `&lt;a href=`). Use assert.Contains(svgStr, `<a href=`) and assert.NotContains(svgStr, `&lt;a href=`).
    - Extend with a .dot variant: render.RenderDOT of the same graph, assert the DOT label contains `<a href=` (HTML label, unescaped) — GraphViz HTML labels in DOT are wrapped in `<...>` so the assertion is `assert.Contains(dotStr, "<a href=")`.

    In internal/render/navigation_test.go (new subtest in TestNavigationInGraphOutput or a sibling test):
    - Assert BuildNavigationLabel output, when placed inside a GraphViz HTML label and rendered, yields clickable links. Direct unit assertion: BuildNavigationLabel(nav) returns a string containing `<a href=` (already true) — keep as a contract test.

    In internal/graph/path_test.go (append to existing TestComputeBackLinkURL and TestBuildBreadcrumbPath):
    - "back-link always uses svg regardless of format": ComputeBackLinkURL("mainapp", "diagram", "dot") == "../diagram.svg"
    - "breadcrumb URL always uses svg regardless of format": for BuildBreadcrumbPath("mainapp.api", "diagram", "dot"), items[0].URL ends with ".svg" (assert strings.HasSuffix).
  </behavior>
  <action>
    GAP 2 FIX (converter.go): The graph label at converter.go:193 calls `cg.SetLabel(joinLabels(labelParts))` with a plain string containing raw `<a href>` tags, so GraphViz escapes them. Mirror the WORKING node-label pattern at converter.go:262. The graph label must be a GraphViz HTML-like label. GraphViz HTML labels are wrapped in `<...>` delimiters and the content must be valid HTML-ish markup. Build the combined label as an HTML string:
      - The navigation HTML from BuildNavigationLabel is already HTML (`<a href="...">...</a>`, ` > ` separators). It is safe to embed directly.
      - The graph title (g.Title) is PLAIN TEXT and must be HTML-escaped before joining (a title containing `<`, `>`, or `&` would break the HTML label). Use html.EscapeString(g.Title) — import the stdlib `html` package (already imported in wrap.go; add to converter.go imports if absent).
      - Join navigation + escaped title with `<BR/>` (GraphViz HTML line break — the repo's htmlLineBreak constant from wrap.go) instead of the current "\n" joiner, because inside an HTML label "\n" is not a line break. Either reuse joinLabels with a `<BR/>` separator for the HTML path, or inline the join.
      - Wrap the whole thing as an HTML label and pass through cg.StrdupHTML before SetLabel. Pattern (mirroring setNodeLabel :257-267):
          htmlStr, err := cg.StrdupHTML(combinedHTML)
          if err != nil { return fmt.Errorf("create HTML graph label: %w", err) }
          cg.SetLabel(htmlStr)
        Note: StrdupHTML expects the inner content; depending on the goccy go-graphviz API it may want the content WITH or WITHOUT the outer `<>` delimiters. Check how setNodeLabel builds its input (buildHTMLLabelForType returns `<table>...</table>` WITHOUT outer `<>`, and StrdupHTML+SetLabel handles wrapping). Follow the SAME convention — do not add outer `<>` if the node path doesn't.
      - Only enter the HTML-label path when there IS navigation (g.Navigation != nil) OR the title needs HTML. If g.Navigation == nil and only a plain title is present, the current plain-text SetLabel path is fine for C1 (C1 has no nav, title is plain) — but to keep one code path, you may always HTML-wrap; verify the C1 SVG still renders the title correctly (the existing TestIntegration_SVG_C1NoNavigation must stay green). Simplest safe approach: always build an HTML label when labelParts is non-empty.

    GAP 3 FIX (path.go): Force ".svg" in navigation URLs to match ComputeExploreURL. In ComputeBackLinkURL (path.go:72-88) and computeBreadcrumbURL (path.go:117-135), replace the use of the passed `format` parameter with a hardcoded "svg" constant (mirror ComputeExploreURL's `const linkFormat = "svg"` at path.go:16). Keep the `format` parameter in the signatures (callers pass it; changing signatures would ripple through builder.go:612,647,651,679,692 — out of scope and unnecessary) but ignore it, and update the doc comments to state ".svg is always used for browser navigation regardless of the render format". Add the RED subtests listed in <behavior> asserting .dot input yields .svg output.

    COMMIT ORDER: test(03-04) with all new RED tests first (Task 1 + Task 2 tests together or per-task — per-task preferred), confirm they fail, then feat(03-04) implementing both fixes, confirm green.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/render/ -run 'Navigation|TestIntegration' -count=1 -v && go test ./internal/graph/ -run 'TestComputeBackLinkURL|TestBuildBreadcrumbPath' -count=1 -v && go test ./internal/render/ ./internal/graph/ -count=1</automated>
  </verify>
  <done>
    - SVG output of a C2 diagram contains literal `<a href=` and does NOT contain `&lt;a href=`.
    - DOT output of a C2 diagram contains `<a href=` (HTML label, unescaped).
    - ComputeBackLinkURL and computeBreadcrumbURL emit .svg even when called with format="dot".
    - All pre-existing navigation/integration tests stay green (TestNavigationInGraphOutput, TestIntegration_FullPipeline_Navigation, TestIntegration_SVG_C1NoNavigation).
    - `go test ./internal/render/ ./internal/graph/ -count=1` green.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end CLI regression on multilevel.toml + full suite green</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>
    - internal/graph/builder_test.go:1230-1262 (TestBuildExpandedGraphBaselineDOT — the canonicalDOT golden test; URL attributes survive canonicalization so it now ALSO enforces correct URLs)
    - internal/graph/builder_test.go:393-480 (TestBuildGraphWithPathSetsExploreURL — existing explore-URL unit tests)
    - cmd/c4drill/root_test.go:633-672 (TestCompat02_MultilevelFixtureFiveNodeC1 — the CLI harness pattern to mirror)
    - cmd/c4drill/testdata/multilevel.toml (the fixture)
  </read_first>
  <action>
    Add an end-to-end regression test in internal/graph/builder_test.go that runs the real CLI on the public fixture and asserts the three gaps are closed at the SVG level. Mirror the CLI harness pattern from TestCompat02_MultilevelFixtureFiveNodeC1 (root_test.go:638-672): build a cobra command with args [testdata/multilevel.toml, --output, tmpDir, --format, svg], execute, then read the generated SVG files and assert. Use //nolint:paralleltest (go-graphviz WASM concurrency) and //nolint:gosec for os.ReadFile on t.TempDir() (existing repo convention).

    NOTE on test placement: TestCompat02 lives in cmd/c4drill/root_test.go (package main) because it invokes NewRootCmd. Placing a CLI-driven test in internal/graph/builder_test.go (package graph_test) cannot call NewRootCmd directly. TWO OPTIONS — pick (a): (a) Add the new test to cmd/c4drill/root_test.go instead (next to TestCompat02), naming it TestCompat02_NavigationLinksResolve. This is the correct location for a CLI harness test. Update <files> accordingly: the test goes in cmd/c4drill/root_test.go. (b) If a graph-package-level assertion is also wanted, add a pure graph-layer test that calls graph.BuildGraphWithPath + render.RenderSVG directly (no CLI) in internal/graph/builder_test.go — but Task 2 already covers that. CHOOSE (a): put the end-to-end CLI test in cmd/c4drill/root_test.go.

    Assert, after `--format svg` on multilevel.toml:
      1. Read tmpDir/multilevel/mainSystem.svg (C2). Assert it contains `<a href=` (clickable nav) and does NOT contain `&lt;a href=`. Assert every explore href in the file resolves: extract href="..." values, join each with filepath.Dir of the SVG file, os.Stat each resolved path, require.NoError — the targets must exist. (After Task 1's fix, these resolve to sibling C3 files.)
      2. Read tmpDir/multilevel/mainSystem/sshAuth.svg (C3). Same assertions. CRITICAL: assert it does NOT contain `href=".svg"` (the Gap 1 symptom B empty-href). Assert the parent/ancestor explore href (if any) resolves upward correctly.
      3. Read tmpDir/multilevel/mainSystem.dot (C2 in dot format). Assert the navigation label uses .svg: assert.Contains(dotStr, ".svg") AND for the back-link/breadcrumb portion assert it does NOT contain `../multilevel.dot` (Gap 3). Specifically assert the back-link contains `../multilevel.svg`.
      4. Re-run/confirm the existing TestBuildExpandedGraphBaselineDOT (canonicalDOT golden) still passes — URL attributes now carry correct values and survive canonicalization; if the committed golden multilevel.expanded.dot was generated with the BUGGY URLs, the canonical comparison may now DIFFER. If it differs: do NOT silently regenerate; investigate whether the golden's URL attributes are affected (the expanded diagram is C1-all-nested which has NO explore/nav URLs, so it should be unaffected — verify and assert this). If unaffected, the golden test stays green unchanged.

    Extract href values with a simple strings-based scan (the repo avoids regex in tests; use strings.Index/strings.Split on `href="`) — mirror the empirical grep done during planning. Keep cognitive complexity under the repo's golangci limits (refactor into a helper collectHrefs(svgStr) []string if needed).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run 'TestCompat02_NavigationLinksResolve|TestCompat02_MultilevelFixtureFiveNodeC1|TestBuildExpandedGraphBaselineDOT' -count=1 -v && go test -race ./... 2>&1 | tail -15</automated>
  </verify>
  <done>
    - New CLI test TestCompat02_NavigationLinksResolve passes: C2/C3 SVGs contain literal `<a href=`, no `&lt;a href=`, no `href=".svg"`, all explore hrefs resolve to existing files.
    - C2 .dot navigation label uses .svg (contains `../multilevel.svg`, not `../multilevel.dot`).
    - TestBuildExpandedGraphBaselineDOT (canonicalDOT golden) still passes unchanged (expanded diagram has no nav/explore URLs, so the golden is unaffected — assert/confirm).
    - Full suite green: `go test -race ./...` all packages ok.
    - `golangci-lint run --new-from-rev=<last-commit> ./...` 0 issues (match the repo's lint gate from 03-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated SVG → browser | Clickable hrefs in SVG output cross into browser navigation; malformed/escaped hrefs break navigation (the bug being fixed) |
| TOML unit names → URL paths | Unit names (e.g. "api (v2)") flow into hrefs via URLEncodePath; must remain encoded to avoid path injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | href values in SVG/DOT labels | mitigate | Graph label now passes through cg.StrdupHTML (Gap 2) so href markup is treated as structured HTML, not escaped text; URLEncodePath (path.go:138) already escapes path segments for explore URLs — confirm navigation URLs use the same encoding |
| T-03-04-02 | Information Disclosure | g.Title in HTML label | mitigate | Title is HTML-escaped via html.EscapeString before embedding in the HTML label (Gap 2 action) so a title containing `<`/`>`/`&` cannot break the label or inject markup |
| T-03-04-03 | Spoofing | relative href resolution | accept | hrefs are relative paths between generated sibling files under the output dir; no cross-origin risk; low-value target |
</threat_model>

<verification>
- Unit: `go test ./internal/graph/ ./internal/render/ -count=1` green (path URL logic + navigation HTML wrapping).
- CLI regression: `go test ./cmd/c4drill/ -run 'TestCompat02' -count=1` green (end-to-end on multilevel.toml).
- Golden unaffected: `go test ./internal/graph/ -run TestBuildExpandedGraphBaselineDOT -count=1` green (canonicalDOT; expanded diagram has no nav URLs so the committed golden remains valid — assert rather than regenerate).
- Full gate: `go test -race ./...` all packages ok; `golangci-lint run --new-from-rev=<last-commit> ./...` 0 issues.
- Manual evidence: `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --output /tmp/fix-verify -f svg && grep -c '<a href=' /tmp/fix-verify/multilevel/mainSystem.svg` returns > 0 and `grep -c '&lt;a href=' ...` returns 0; `grep -oE 'href="[^"]*"' /tmp/fix-verify/multilevel/mainSystem/sshAuth.svg` shows no empty `href=".svg"`.
</verification>

<success_criteria>
- Gap 1 closed: C2/C3 explore hrefs resolve to existing sibling files; C3 parent node no longer emits empty `href=".svg"`; ComputeExploreURL handles ancestor/sibling/descendant full-dotted-path targets correctly with a self-link guard.
- Gap 2 closed: SVG and DOT output contain literal `<a href=` in the navigation bar (not escaped); graph label goes through cg.StrdupHTML mirroring the node-label pattern; the plain-text title is HTML-escaped before joining.
- Gap 3 closed: ComputeBackLinkURL and computeBreadcrumbURL always emit .svg regardless of the render format parameter; C2 .dot navigation label shows `../multilevel.svg`.
- No regression: all pre-existing tests (path_test, navigation_test, integration_test, builder_test including canonicalDOT golden, root_test COMPAT-01/COMPAT-02) stay green; `go test -race ./...` clean; lint clean.
- COMPAT-02 navigation contract (broken links that the DOT golden normalized away) is now enforced by semantic SVG-level assertions, not just the golden comparison.
</success_criteria>

<output>
Create `.planning/phases/03-compatibility-validation/03-04-SUMMARY.md` when done. Commit each task atomically: test(03-04) RED commits before feat(03-04) GREEN commits. Final commit docs(03-04): gap closure summary.
</output>
