---
phase: 43-bold-subject-boundary
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/graph/graph.go
  - internal/graph/builder.go
  - internal/render/converter.go
  - internal/graph/builder_test.go
  - internal/render/converter_test.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements: [BOLD-01, BOLD-02, BOLD-03]
must_haves:
  truths:
    - "Raw DOT of a drill-down view carries penwidth=3.0 on the subject boundary cluster ONLY — every other cluster on the same diagram keeps its previous attributes (BOLD-01/02, TDD RED→GREEN)"
    - "The emphasis appears on C2, C3 and deep-link drill-down views of the multi-level fixture; the collapsed C1 root output carries no penwidth (BOLD-01, D-01)"
    - "--plain and --no-styles renders keep the bold boundary; --expanded output is byte/canonical-identical to the committed goldens (BOLD-02/03, D-03/D-04)"
    - "NodeStyle.BorderWidth follows the 0-means-renderer-default contract of Edge.PenWidth — no attribute emitted when 0 (D-02)"
  artifacts:
    - path: "internal/graph/graph.go"
      provides: "NodeStyle.BorderWidth float64 field (0 = renderer default, no attribute; >0 emits penwidth)"
      contains: "BorderWidth float64"
    - path: "internal/graph/builder.go"
      provides: "buildBoundaryCluster assigns BorderWidth = 3.0 at the single subject-boundary construction site (outside applyUnitOverrides so it survives --plain)"
      contains: "style.BorderWidth = 3.0"
    - path: "internal/render/converter.go"
      provides: "applyClusterStyle emits penwidth via setClusterAttribute when BorderWidth > 0"
      contains: "setClusterAttribute(subgraph, \"penwidth\""
    - path: "internal/render/converter_test.go"
      provides: "raw-DOT cluster-penwidth multiset assertions (subject cluster only) incl. --plain/--no-styles/--no-colors variants"
      contains: "func TestSubjectBoundary"
    - path: "internal/graph/builder_test.go"
      provides: "graph-level emphasis + no-collateral guards (C1 root, --expanded canonical goldens stay green, deep-link C3)"
      contains: "func TestSubjectBoundary"
    - path: "cmd/c4drill/root_test.go"
      provides: "end-to-end flag matrix on multilevel.toml: C2/C3 drill-down DOTs carry penwidth=3.0, --expanded and C1 outputs do not"
      contains: "func TestSubjectBoundary"
  key_links:
    - from: "internal/graph/builder.go buildBoundaryCluster"
      to: "internal/graph/graph.go NodeStyle.BorderWidth"
      via: "style.BorderWidth = 3.0 assignment on the style returned in Cluster.Style"
      pattern: "style\\.BorderWidth = 3\\.0"
    - from: "internal/render/converter.go applyClusterStyle"
      to: "internal/graph/builder.go buildBoundaryCluster"
      via: "Cluster.Style.BorderWidth > 0 gate emits penwidth only for the subject boundary cluster"
      pattern: "style\\.BorderWidth > 0"
    - from: "internal/render/converter_test.go TestSubjectBoundary*"
      to: "internal/render/converter.go applyClusterStyle"
      via: "raw-DOT render of GenerateC2View/GenerateC3View graphs asserts the emitted attribute"
      pattern: "penwidth=3\\.0"
---

<objective>
Give the subject unit's boundary cluster a semantic triple-width border on every non-expanded drill-down view.

Purpose: BOLD-01 — the viewer must tell at a glance which element a C2/C3/deep-link diagram depicts. The emphasis is a navigation aid decided at graph construction (not author formatting), so it survives `--plain`/`--no-styles` (D-03) while every other render path stays byte-identical (D-04).
Output: NodeStyle.BorderWidth field + buildBoundaryCluster assignment + applyClusterStyle penwidth emission + the TDD RED test suite (unit + CLI flag matrix) that pins the contract.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-bold-subject-boundary/43-CONTEXT.md
@.planning/phases/43-bold-subject-boundary/43-RESEARCH.md
@.planning/phases/43-bold-subject-boundary/43-PATTERNS.md
@internal/graph/graph.go
@internal/graph/builder.go
@internal/render/converter.go
@internal/graph/builder_test.go
@internal/render/converter_test.go
@cmd/c4drill/root_test.go
@cmd/c4drill/testdata/multilevel.toml
@cmd/c4drill/testdata/multilevel.expanded.dot

<interfaces>
<!-- Key contracts the executor needs. Verified 2026-09-03. -->

From internal/graph/builder.go:
  BuildGraph(v *view.View) *Graph                            // :33 — routes C2/C3 drill-downs (Level != C1 && ExpandedUnit != "") to buildBoundaryViewGraph
  buildBoundaryCluster(v *view.View) *Cluster               // :373-441 — THE single subject-boundary construction site (D-01)
  applyUnitOverrides(style, unit, opts)                      // :624-651 — EARLY-RETURNS when opts.Plain; emphasis must NOT live here
  renderOptsFromView(v)                                      // :16-25 — view flags -> RenderOpts (Plain/NoColors/NoStyles)
  GenerateC2View/GenerateC3View                             // internal/view/scope.go:487/594 — test-side view construction
  BuildExpandedGraph(v)                                      // :447 — no boundary cluster; must stay untouched

From internal/graph/graph.go:
  type NodeStyle struct { FillColor, BorderColor, FontColor string; BorderStyle string }   // :195-206 — add BorderWidth float64

From internal/render/converter.go:
  applyClusterStyle(subgraph *cgraph.Graph, style *graph.NodeStyle) error   // :656-695 — runs for EVERY cluster in EVERY mode (createCluster :586)
  setClusterAttribute(subgraph, attr, value) error                          // :696-704 — empty-skip then SafeSet(attr, value, "")
  edge penwidth precedent                                                  // :899-905 — if edge.PenWidth > 0 { e.SetPenWidth(...) } else { e.SetPenWidth(1.0) }
  opts.Plain affects only the label path                                    // :619-620 — style emission is mode-agnostic (the D-03 survival mechanism)

From go-graphviz pinned fork (v0.2.10):
  Edge.SetPenWidth = SafeSet("penwidth", fmt.Sprint(v), "1.0")             // cgraph/attribute.go:1962 — Edge-only; clusters use SafeSet via setClusterAttribute
  SafeSet(name, value, def)                                                 // cgraph/cgraph.go:855 — omitted when value == def

Formatting lock (D-01):
  emit exactly "3.0": strconv.FormatFloat(style.BorderWidth, 'f', 1, 64) — NOT fmt.Sprint (would emit "3")

From internal/render/converter_test.go:
  dotEdgeMultiset(t, dot) + penRe := regexp.MustCompile(`penwidth=([\d.]+)`)  // :1257-1283 — raw-DOT multiset precedent

From cmd/c4drill/root_test.go:
  generateMultilevelOutput(t, args...)                                       // :680-800 — CLI harness; C2 = multilevel/mainSystem.dot, C3 = multilevel/mainSystem/sshAuth.dot
  //nolint:paralleltest on every render-touching test (WASM engine)
</interfaces>
</context>

<feature>
  <name>semantic bold subject boundary: penwidth=3.0 on the drill-down subject cluster only</name>
  <files>internal/graph/graph.go, internal/graph/builder.go, internal/render/converter.go, internal/graph/builder_test.go, internal/render/converter_test.go, cmd/c4drill/root_test.go</files>
  <behavior>
    Cases (raw-DOT assertions; unit views built in-test, CLI cases via generateMultilevelOutput):
    - C2 drill-down (GenerateC2View(m, "mainSystem") -> BuildGraph -> RenderDOT): exactly ONE subgraph carries penwidth, value "3.0", and it is cluster_mainSystem (the subject boundary cluster); ALL other clusters on the same diagram (wrapper/ancestor/sibling-subject clusters) carry NO penwidth attribute (BOLD-01/02)
    - C3 drill-down (GenerateC3View(m, "mainSystem.sshAuth")): subject boundary cluster_sshAuth carries penwidth=3.0 (BOLD-01 multi-level)
    - deep-link drill-down (multilevel fixture CLI run): multilevel/mainSystem/sshAuth.dot carries penwidth=3.0 (D-05 multi-level fixture, A1)
    - view.Plain = true and view.NoStyles = true variants: the subject cluster STILL carries penwidth=3.0 (BOLD-02/D-03 semantic survival)
    - --no-colors variant: byte-identical expectation on penwidth (no interaction, D-03)
    - collapsed C1 root (GenerateC1View -> BuildGraph -> RenderDOT): NO penwidth attribute anywhere (regexp no-match; D-04)
    - --expanded (GenerateExpandedView -> BuildExpandedGraph -> RenderDOT): canonical-equal to committed cmd/c4drill/testdata/multilevel.expanded.dot (existing COMPAT-02/REF-05 consumers must stay green unchanged; D-04)
    - CLI end-to-end: c4drill render testdata/multilevel.toml -o <tmp> --format dot -> multilevel.dot (C1) has no penwidth; multilevel/mainSystem.dot (C2) has penwidth=3.0 on cluster_mainSystem only; --expanded -> no penwidth anywhere; --plain -> C2 still has penwidth=3.0 (BOLD-02 end-to-end)
  </behavior>
  <implementation>
    1. RED: add TestSubjectBoundary* tests (see tasks) — run, MUST fail (no cluster penwidth emitted today; zero matches / attr mismatch), commit test(43-01).
    2. GREEN: add BorderWidth float64 to NodeStyle (graph.go:195); in buildBoundaryCluster set style.BorderWidth = 3.0 once after applyUnitOverrides and before the return (single construction site, covers the nil-unit fallback style too, outside the Plain-gated override path); in applyClusterStyle emit penwidth via setClusterAttribute gated on style.BorderWidth > 0 with FormatFloat('f',1,64). Commit feat(43-01).
    3. REFACTOR: gofmt + vet + full suite; per-golden canonical-diff audit (any failing golden must differ ONLY by the subject-cluster penwidth line — re-baseline exactly then, else fix the code); regression sweep. Commit refactor(43-01) only if changed.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — subject-boundary penwidth test suite</name>
  <files>internal/render/converter_test.go, internal/graph/builder_test.go, cmd/c4drill/root_test.go</files>
  <read_first>internal/render/converter_test.go lines 1257-1283 (dotEdgeMultiset raw-DOT extraction + penwidth regexp precedent), internal/graph/builder_test.go lines 1220-1300 (in-test parser.Model fixture + canonical golden consumer COMPAT-02) and 1960-2020 (PenWidth unit-assertion style), internal/graph/builder.go lines 16-55 (RenderOpts + BuildGraph routing) and 370-441 (buildBoundaryCluster), internal/view/scope.go lines 485-600 (GenerateC2View/GenerateC3View call signatures), cmd/c4drill/root_test.go lines 680-800 (generateMultilevelOutput harness + flag-arg shape), 43-PATTERNS.md (analog excerpts)</read_first>
  <action>Add raw-DOT cluster-penwidth extraction helper to converter_test.go mirroring dotEdgeMultiset: parse the DOT into per-subgraph attribute sets (reuse the existing statement-walk style or regexp over `subgraph cluster_\w+ {[^}]*}` blocks — whichever the file already uses; do NOT copy canonical.go parser logic) plus a sorted penwidth-value multiset per cluster. Write TestSubjectBoundaryClusterPenwidth in converter_test.go covering: (a) C2 drill-down — build parser.Model in-test with mainSystem (TypeSystem, SubunitOrder auth+db, Subunits auth/auditLog/db) + external peer, GenerateC2View -> BuildGraph -> RenderDOT; assert exactly one cluster carries penwidth and it is the subgraph whose id ends cluster_mainSystem with value "3.0", and every other cluster attribute set contains no penwidth key; (b) C3 drill-down — same model, GenerateC3View(m, "mainSystem.auth") -> BuildGraph -> RenderDOT; subject cluster_auth carries "3.0", others none; (c) variants — v.Plain = true, v.NoStyles = true, v.NoColors = true (set flags AFTER Generate*View on the returned view, mirroring renderOptsFromView wiring) each still produce penwidth=3.0 on the subject cluster only. Mark every test //nolint:paralleltest (WASM engine). In builder_test.go add TestSubjectBoundaryNoCollateral: C1 root (GenerateC1View -> BuildGraph -> RenderDOT) contains NO penwidth anywhere (regexp `penwidth=` must not match); --expanded graph (GenerateExpandedView -> BuildExpandedGraph -> RenderDOT) canonical-compares equal to cmd/c4drill/testdata/multilevel.expanded.dot (same assertion style as COMPAT-02 at :1233-1241); and a deep-link C3 unit: model where mainSystem.iam (container, has subunits) is linked from an external/webUser unit so ensureDeepLinkChain registers the chain (mirror the fixture at builder_test.go:1237-1298), GenerateC3View(m, "mainSystem.iam") -> BuildGraph -> RenderDOT carries penwidth=3.0 on cluster_iam. In cmd/c4drill/root_test.go add TestSubjectBoundaryCLIFlagMatrix using generateMultilevelOutput: run [testdata/multilevel.toml, -o, tmp, --format, dot] -> read multilevel.dot (C1: no penwidth), multilevel/mainSystem.dot (C2: exactly one penwidth=3.0 on cluster_mainSystem), multilevel/mainSystem/sshAuth.dot (C3 deep-link: penwidth=3.0 on cluster_sshAuth); run --expanded variant -> no penwidth anywhere; run --plain variant -> C2 still exactly one penwidth=3.0. Do NOT touch any golden files. Run the three package test suites; they MUST fail on the penwidth assertions (RED — clusters currently emit no penwidth). Commit test(43-01).</action>
  <verify>
    <automated>go test ./internal/render/ -run 'TestSubjectBoundary' -count=1; go test ./internal/graph/ -run 'TestSubjectBoundary' -count=1; go test ./cmd/c4drill/ -run 'TestSubjectBoundary' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/render/ -run 'TestSubjectBoundary' -count=1 FAILS on penwidth assertions (RED state, not a compile error)
    - go test ./internal/graph/ -run 'TestSubjectBoundary' -count=1 FAILS identically; go test ./cmd/c4drill/ -run 'TestSubjectBoundary' -count=1 FAILS identically
    - converter_test.go contains a per-cluster penwidth extraction helper and func TestSubjectBoundaryClusterPenwidth covering C2, C3, Plain, NoStyles, NoColors variants
    - builder_test.go contains func TestSubjectBoundaryNoCollateral with the C1 no-penwidth regexp assertion and the canonical COMPAT-02 equality for --expanded
    - cmd/c4drill/root_test.go contains func TestSubjectBoundaryCLIFlagMatrix hitting multilevel.dot, mainSystem.dot, sshAuth.dot and the --expanded/--plain variants
    - git log shows the test(43-01) commit preceding any feat(43-01) commit (RED before GREEN)
  </acceptance_criteria>
  <done>Failing tests committed; git log test(43-01) present before feat(43-01)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — BorderWidth field, construction assignment, penwidth emission</name>
  <files>internal/graph/graph.go, internal/graph/builder.go, internal/render/converter.go</files>
  <read_first>internal/graph/graph.go lines 195-206 (NodeStyle — add the field with the D-02 0-means-default doc comment), internal/graph/builder.go lines 370-441 (buildBoundaryCluster — insert after the applyUnitOverrides call at the unit-branch tail, before return &Cluster{...}, single assignment covering both branches) and 624-651 (applyUnitOverrides — touch NOTHING here, D-03), internal/render/converter.go lines 656-704 (applyClusterStyle + setClusterAttribute) and 899-905 (edge penwidth precedent for the gate shape), 43-RESEARCH.md (Code Examples + Pitfalls 1/4), 43-PATTERNS.md (pattern assignments)</read_first>
  <action>In graph.go add to NodeStyle: `// BorderWidth is the penwidth attribute emitted on this style; 0 means the renderer default (no attribute emitted). Mirrors Edge.PenWidth (D-02).` followed by `BorderWidth float64`. In builder.go buildBoundaryCluster, directly after the applyUnitOverrides(style, unit, renderOptsFromView(v)) call inside the unit != nil branch, set `style.BorderWidth = 3.0` with a comment anchored to BOLD-01/D-03 ("semantic emphasis — set at construction, outside the author-override path, so it survives --plain/--no-styles"; do NOT touch applyUnitOverrides). In converter.go applyClusterStyle, after the style-string block (after the final SafeSet("style",...) call) and before return nil, add: `if style.BorderWidth > 0 {` -> `if err := setClusterAttribute(subgraph, "penwidth", strconv.FormatFloat(style.BorderWidth, 'f', 1, 64)); err != nil { return err }` — comment referencing BOLD-01/D-02 ("only the subject boundary cluster carries a non-zero BorderWidth; 0 = renderer default"). Add strconv to the converter.go import block if missing (stdlib only). Do NOT modify any other cluster/render path (createCluster, nil-style branch, edges, labels, URLs). Commit feat(43-01).</action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/render/ -run 'TestSubjectBoundary' -count=1 && go test ./internal/graph/ ./internal/render/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/graph/ ./internal/render/ -run 'TestSubjectBoundary' -count=1 passes (GREEN); full graph+render packages pass
    - internal/graph/graph.go contains "BorderWidth float64" inside type NodeStyle
    - internal/graph/builder.go contains "style.BorderWidth = 3.0" inside buildBoundaryCluster (single occurrence; grep -c = 1) and applyUnitOverrides is unchanged (no BorderWidth references inside it)
    - internal/render/converter.go contains `setClusterAttribute(subgraph, "penwidth",` guarded by `style.BorderWidth > 0`; strconv imported; no penwidth changes in createCluster/applyEdgeAttributes
    - git log shows feat(43-01) after test(43-01)
  </acceptance_criteria>
  <done>feat(43-01) commit present; subject-boundary tests GREEN; graph+render suites green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — regression sweep + golden diff audit</name>
  <files>internal/graph/builder.go, internal/render/converter.go, internal/graph/graph.go, internal/render/converter_test.go, internal/graph/builder_test.go, cmd/c4drill/root_test.go</files>
  <read_first>post-Task-2 state of the three code files; 43-VALIDATION.md (full-suite gate); 43-RESEARCH.md Pitfall 5 (golden audit bar)</read_first>
  <action>Run gofmt -l on internal/graph, internal/render, cmd/c4drill and go vet on the same packages — fix any findings. Run the FULL module suite (go test ./... -count=1). For every test failure that involves a committed golden (*.dot in cmd/c4drill/testdata), produce the canonical diff (canonical.Canonical on both sides) and audit it: the ENTIRE diff must be the added `penwidth` attribute line on the SUBJECT boundary cluster of that diagram — nothing else (no attribute order, geometry, label, or other-cluster changes). If the diff matches the bar, re-baseline by regenerating that golden from the current output and commit test(43-01)/chore as an explicit re-baseline commit; if any golden fails the bar, STOP and fix the code instead (a golden must never be re-baselined to hide collateral change, D-04). Confirm the existing COMPAT-02/REF-05 canonical consumers (multilevel.expanded.dot) pass UNCHANGED. Only commit refactor(43-01) when REFACTOR actually changed code.</action>
  <verify>
    <automated>test -z "$(gofmt -l internal/graph internal/render cmd/c4drill)" && go vet ./internal/graph/ ./internal/render/ ./cmd/c4drill/ && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - gofmt -l prints nothing for the three packages; go vet exits 0; go test ./... -count=1 exits 0
    - multilevel.expanded.dot and every other committed golden is byte-unchanged UNLESS its canonical diff vs the new output is exactly the subject-cluster penwidth line (audited per golden; zero re-baselines expected per RESEARCH.md A2)
    - any re-baseline commit message makes the golden name visible (e.g. goldens(re-baseline): ...) and is accompanied by the audited diff in the commit body
  </acceptance_criteria>
  <done>Full module suite green; no golden silently altered; refactor(43-01) commit only when changes were made</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model content -> renderer attributes | c4drill reads author-controlled .toml/.c4d; the new penwidth attribute is a compile-time constant 3.0 — NOTHING from the model flows into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | attribute emission path | accept | penwidth value is a hard-coded constant (builder.go assignment); no user/model input reaches the attribute — no injection surface added |
| T-43-02 | Information Disclosure | rendered output | accept | the emphasis only restates which unit the diagram depicts — already visible via cluster label/URL; no new information exposed |
| T-43-03 | Repudiation | golden re-baseline | mitigate | D-04 byte-equality guards + per-golden canonical-diff audit (Task 3) keep any re-baseline deliberate and bounded to the penwidth line |
| T-43-SC | Tampering | dependency installs | mitigate | none needed — plan installs zero packages (RESEARCH.md Package Legitimacy: no new dependencies) |
</threat_model>

<verification>
- go test ./... -count=1 exits 0 (full module suite, incl. existing canonical golden consumers COMPAT-02/REF-05 unchanged)
- go test ./internal/graph/ ./internal/render/ ./cmd/c4drill/ -run 'TestSubjectBoundary' -count=1 -v shows all subject-boundary assertions passing (C2, C3, deep-link, Plain, NoStyles, NoColors, C1-no-penwidth, --expanded unchanged, CLI matrix)
- git log --grep "^test(43-01)" then --grep "^feat(43-01)" order verified (TDD gate: RED before GREEN)
</verification>

<success_criteria>
- Raw DOT of C2/C3/deep-link drill-down views asserts penwidth=3.0 on the subject boundary cluster ONLY — every other cluster keeps its previous attributes (BOLD-01/02, ROADMAP SC 1)
- Emphasis appears on C2, C3 and deep-link drill-down views of the multi-level fixture; collapsed C1 root carries no penwidth (BOLD-01, ROADMAP SC 2)
- --plain and --no-styles renders keep the bold boundary; --expanded output canonical-identical to committed goldens (BOLD-02/03, ROADMAP SC 3)
- NodeStyle.BorderWidth follows the Edge.PenWidth 0-means-default contract; no new CLI flag, no model key (D-02/D-03)
</success_criteria>

<output>
Create `.planning/phases/43-bold-subject-boundary/43-01-SUMMARY.md` when done
</output>