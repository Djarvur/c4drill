---
phase: 43-bold-subject-boundary
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [BOLD-03]
must_haves:
  truths:
    - "Every committed golden either is byte-unchanged from v1.17 or carries a canonical diff consisting ONLY of the subject-boundary penwidth line — golden updates are limited to the subject-boundary delta (BOLD-03)"
    - "PROJECT.md documents the v1.18 semantic bold boundary (survives --plain/--no-styles) so the milestone capability is discoverable"
    - "Extended/audited render paths (--expanded, C1 root, legend, node borders, edges) show no diff beyond the audited subject-boundary goldens (BOLD-03)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "As of v1.18 paragraph describing the semantic bold subject boundary"
      contains: "As of v1.18"
  key_links:
    - from: ".planning/PROJECT.md As of v1.18"
      to: "internal/graph/builder.go buildBoundaryCluster"
      via: "documented capability matches the shipped implementation (BorderWidth = 3.0 on the subject boundary cluster only)"
      pattern: "boundary of the depicted unit"
---

<objective>
Close BOLD-03: prove golden updates are limited to the subject-boundary delta and record the v1.18 capability in PROJECT.md.

Purpose: 43-01 shipped the emphasis; this plan makes the no-collateral contract auditable (per-golden canonical diff = sole penwidth line on the subject cluster, expected zero re-baselines) and keeps the project's per-milestone documentation convention (As of v1.15/v1.16/v1.17 paragraphs).
Output: golden audit + (only if the audit bar holds) re-baseline commit; PROJECT.md "As of v1.18" paragraph.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-bold-subject-boundary/43-CONTEXT.md
@.planning/phases/43-bold-subject-boundary/43-RESEARCH.md
@.planning/milestones/v1.17-phases/40-deterministic-svg-output/40-SUMMARY.md
@cmd/c4drill/testdata/multilevel.expanded.dot

<interfaces>
<!-- Facts verified 2026-09-03 -->

From .planning/PROJECT.md:
  "As of v1.15" paragraph at line 9, "As of v1.16" at line 11, "As of v1.17" at line 13 — one sentence per milestone capability, same evolution section; v1.18 paragraph appends after line 13 in matching style.

From RESEARCH.md A2:
  expected re-baseline scope is ZERO — all committed goldens are expanded/plain/nolabels variants; the audit bar is: a golden may be re-baselined ONLY when its canonical diff (canonical.Canonical both sides) is exactly the added `penwidth` attribute line on the SUBJECT boundary cluster of that diagram.

Golden consumers (existing suites, must stay green unchanged):
  internal/graph/builder_test.go:1233-1241 (COMPAT-02) and :2846-2851 (REF-05) compare against cmd/c4drill/testdata/multilevel.expanded.dot via canonical.Canonical.
</interfaces>
</context>

<feature>
  <name>golden-delta audit + v1.18 documentation</name>
  <files>.planning/PROJECT.md</files>
  <behavior>
    Cases:
    - go test ./... -count=1 exits 0 with NO golden file modified (expected) — the audit proves it
    - IF any golden fails: its canonical diff vs the new output contains exactly one change — penwidth="3.0" added to the subject boundary cluster — then re-baseline that golden only
    - IF any golden fails the bar (any diff beyond the subject-cluster penwidth line): the phase is BLOCKED — handler must NOT re-baseline; report and stop
    - PROJECT.md renders a v1.18 "As of" paragraph stating the semantic bold boundary survives --plain/--no-styles and no other render path changed
  </behavior>
  <implementation>
    1. Golden audit run (see task 1): full suite, canonical-diff audit per failing golden, re-baseline only on bar-pass.
    2. PROJECT.md: append the v1.18 paragraph after line 13 (see task 2).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: golden-delta audit — prove BOLD-03</name>
  <files>cmd/c4drill/testdata/</files>
  <read_first>43-RESEARCH.md (A2 + Pitfall 5), 43-VALIDATION.md (full-suite gate), internal/testutil/canonical/canonical.go (Canonical signature), git log --oneline -10 (baseline refs)</read_first>
  <action>Run go test ./... -count=1. Compare git status against HEAD for cmd/c4drill/testdata/*.dot: no golden may change as a side effect. For every golden that fails or that the suites touch on drill-down paths, capture the canonical diff (write a throwaway test or use the existing canonical consumers' assertion output) and audit: the sole change must be the added penwidth attribute on the SUBJECT boundary cluster of that diagram. Expected outcome: zero re-baselines (A2). If the bar passes and a re-baseline is needed, regenerate only those goldens from the current output and commit with message `goldens(re-baseline): <golden names> — subject-boundary penwidth only` including the audited diff in the body; if any diff exceeds the bar, do NOT re-baseline — leave the failure visible and mark the task blocked for the phase handler. Confirm git status shows no other file drift (PROJECT.md untouched by this task).</action>
  <verify>
    <automated>go test ./... -count=1 && git diff --stat HEAD -- cmd/c4drill/testdata/ | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./... -count=1 exits 0
    - git diff --stat HEAD -- cmd/c4drill/testdata/ is empty (byte-identical goldens) OR lists ONLY goldens whose canonical diff equals the sole subject-cluster penwidth line, each with a goldens(re-baseline) commit
    - every audited diff (audit notes in the SUMMARY) shows no attribute/order/geometry change beyond the subject-cluster penwidth line
  </acceptance_criteria>
  <done>Golden-delta audit recorded; suite green; zero unauthorized re-baselines</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md — As of v1.18 paragraph</name>
  <files>.planning/PROJECT.md</files>
  <read_first>.planning/PROJECT.md lines 1-20 (evolution section style: single-sentence capability paragraphs), .planning/phases/43-bold-subject-boundary/43-CONTEXT.md (D-01..D-05 wording), git show 486fe3c (docs-evolve commit precedent)</read_first>
  <action>Append after the "As of v1.17" paragraph (PROJECT.md line 13) a new paragraph starting "As of v1.18, ..." in the same one-sentence style, stating: on every non-expanded drill-down view the boundary of the depicted unit draws with a bold triple-width penwidth=3.0 border (semantic navigation aid — survives --plain/--no-styles; no new flag), and expanded views, the collapsed C1 root, other clusters, nodes, legend, and edges are unchanged. Keep the Key Decisions table untouched unless a row for the v1.18 milestone already exists in the phase-convention table (append one only if the table lists per-version rows). Commit docs(43-02).</action>
  <verify>
    <automated>grep -c 'As of v1.18' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains exactly one "As of v1.18" paragraph (grep -c 'As of v1.18' = 1)
    - grep -c 'penwidth=3.0' .planning/PROJECT.md == 1 AND grep -c -- '--plain' .planning/PROJECT.md >= 1 AND grep -c 'no new flag' .planning/PROJECT.md == 1 (the paragraph's factual tokens are present)
    - git log shows docs(43-02) commit
  </acceptance_criteria>
  <done>docs(43-02) committed; PROJECT.md documents the v1.18 bold boundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> readers | PROJECT.md is the milestone capability record; wording must match the shipped renderer exactly (no invented flags) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | golden files | mitigate | re-baseline bar enforced (canonical diff = sole subject-cluster penwidth line); any other diff blocks the task instead of being hidden |
| T-43-05 | Information Disclosure | PROJECT.md | accept | documents only public CLI behavior; matches CONTEXT D-01..D-05 wording |
| T-43-SC | Tampering | dependency installs | mitigate | none needed — zero packages installed (docs + test-only plan) |
</threat_model>

<verification>
- go test ./... -count=1 exits 0
- git diff --stat HEAD -- cmd/c4drill/testdata/ empty OR audited re-baselines only
- grep -c 'As of v1.18' .planning/PROJECT.md == 1
</verification>

<success_criteria>
- Golden updates (if any) are provably limited to the subject-boundary penwidth delta (BOLD-03, ROADMAP SC 4)
- PROJECT.md's "As of v1.18" paragraph matches the shipped behavior byte-for-byte in meaning (BOLD-03 docs)
</success_criteria>

<output>
Create `.planning/phases/43-bold-subject-boundary/43-02-SUMMARY.md` when done
</output>