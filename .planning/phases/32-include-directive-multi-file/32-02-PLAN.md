---
phase: 32-include-directive-multi-file
plan: 02
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - internal/include/resolve.go
  - internal/include/merge.go
  - internal/include/include_test.go
  - internal/include/testdata/main.toml
  - internal/include/testdata/auth.toml
  - internal/include/testdata/nested_subunits_main.toml
  - internal/include/testdata/nested_subunits_auth.toml
  - cmd/c4drill/root.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements: [INC-01, INC-02, INC-03, INC-04, INC-05, INC-06, INC-07, INC-08, INC-09, INC-10, XC-02]
must_haves:
  truths:
    - "An entry file with [[include]] directives plus all transitively-included files merge into one *parser.Model that, when validated and rendered, is indistinguishable from a hand-authored single-file equivalent (INC-01)"
    - "Include paths resolve relative to the INCLUDING file's directory (filepath.Dir of the including file), so the model renders identically regardless of the CLI working directory (INC-02) — verified by a cd-proof test"
    - "Transitive includes resolve recursively: A includes B includes C yields a model with A's, B's, and C's units merged (INC-03)"
    - "A direct (self) or transitive (A→B→A) include cycle is a fatal *parser.ParseError naming the cycle path; a diamond (A→B→D, A→C→D) is NOT flagged as a cycle (INC-04, INC-05, D-11)"
    - "once=true skips re-inclusion of an already-included file; a same-file diamond (D reached via two non-ancestral paths) is auto-deduped silently (INC-06, D-11)"
    - "Two DIFFERENT files defining the same unit path (or subunit path, or template name) hard-error naming both files (INC-05, INC-07, D-10, D-11 cross-file)"
    - "[properties] follows root-file-wins: the entry file's name/description are authoritative; any non-zero field conflict from an included file is a hard error naming both files (INC-08)"
    - "UnitOrder concatenation preserves authoring order: entry file's units first, then each included file's units appended in include-directive order (INC-09, D-09)"
    - "A missing include file is a fatal *parser.ParseError naming the referenced path AND the including file (not a generic os.ReadFile error) (INC-10, D-12)"
    - "An included file may add subunits to a top-level unit declared in the entry file (or another include), appending to that parent's Subunits/SubunitOrder in include-file order (D-10)"
    - "Templates defined in an included file flow into the merged Model.Templates so [[use]] in the entry file can instantiate them (XC-02, conditional on Phase 31)"
    - "include.Resolve runs FIRST in the pipeline (before template.Expand when Phase 31 lands), producing a model validator.Validate consumes UNCHANGED (STATE.md D-12)"
  artifacts:
    - path: "internal/include/resolve.go"
      provides: "Resolve(entry, entryDir) — recursive include resolution with cycle stack, once/diamond visited-set, missing-file hard-error, max-depth cap"
      contains: "func Resolve"
    - path: "internal/include/merge.go"
      provides: "merge(dst, src, dstFile, srcFile) — per-field struct-union (Units union/conflict-error, UnitOrder append, Subunits deep-merge, Properties root-wins/conflict-error, Templates union/conflict-error)"
      contains: "func merge"
    - path: "internal/include/include_test.go"
      provides: "INC-01..10 + D-10/D-11/XC-02 unit tests (cycle, diamond, once, transitive, cd-proof paths, cross-file dup, properties conflict, missing file, subunit merge, template carry)"
      contains: "TestResolveCycleFatal"
    - path: "cmd/c4drill/root.go"
      provides: "Stage 1a pipeline insertion: include.Resolve(m, filepath.Dir(inputPath)) between ParseFile and Validate"
      contains: "include.Resolve"
    - path: "cmd/c4drill/root_test.go"
      provides: "end-to-end pipeline test: multi-file model resolves+validates+renders; include.Resolve runs before Validate"
      contains: "TestPipelineIncludeBeforeValidate"
  key_links:
    - from: "cmd/c4drill/root.go ParseFile"
      to: "internal/include.Resolve"
      via: "include.Resolve(m, filepath.Dir(inputPath)) inserted at Stage 1a"
      pattern: "include\\.Resolve"
    - from: "internal/include/resolve.go Resolve"
      to: "internal/parser.ParseFile + internal/include.merge"
      via: "recursive ParseFile per include directive, then merge into entry model"
      pattern: "parser\\.ParseFile"
    - from: "internal/include/merge.go merge"
      to: "Model.Units / Model.UnitOrder / Model.Properties / Model.Templates / Unit.Subunits"
      via: "per-field union/append/root-wins rules"
      pattern: "dst\\.Units\\["
---

<objective>
Build the `internal/include` package (recursive resolver + struct merge) and wire it into the pipeline as the FIRST pre-processing pass. This is the bulk of Phase 32: it delivers INC-01 through INC-10 plus XC-02 (templates in included files visible to `[[use]]`).

Purpose: enable users to assemble a single diagram from multiple TOML files — isolating template libraries into their own files and splitting large models across files (the two motivating use cases). The merged model is structurally identical to a hand-authored single-file model, so validator/view/render stay UNCHANGED (STATE.md D-12).

Output: New `internal/include/` package (`resolve.go` ~60 LOC, `merge.go` ~80 LOC), comprehensive test suite + multi-file fixtures, and the single pipeline insertion in `cmd/c4drill/root.go`. Consumes `Model.Includes` from Plan 01. Conditional on Phase 31 for the XC-02 template-carry test only (the rest is fully testable against `*parser.Model` without templates).

CONCURRENCY: Phase 31 edits `cmd/c4drill/root.go` at the same insertion point (Stage 1a/1b). When both land, the order MUST be `include.Resolve` THEN `template.Expand` (pipeline ordering load-bearing per RESEARCH System Architecture Diagram). This plan inserts `include.Resolve`; the executor handles integration ordering if Phase 31 has already added its `template.Expand` call.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-include-directive-multi-file/32-CONTEXT.md
@.planning/phases/32-include-directive-multi-file/32-RESEARCH.md
@.planning/phases/32-include-directive-multi-file/32-PATTERNS.md
@.planning/phases/32-include-directive-multi-file/32-VALIDATION.md

@internal/parser/parser.go
@internal/parser/errors.go
@internal/model/unit.go
@internal/model/properties.go
@internal/validator/index.go
@cmd/c4drill/root.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + Plan 01. -->

From internal/parser/parser.go (Model after Plan 01 + Phase 31 Plan 1):
```go
type Model struct {
    Properties     model.Properties      `toml:"properties"`
    UnitOrder      []string              // toml:"-"
    Units          map[string]*model.Unit
    Includes       []IncludeDirective    `toml:"-"` // Plan 01
    Templates      map[string]*TemplateDef  `toml:"-"` // Phase 31 Plan 1 (XC-02 carry)
    Instantiations []Instantiation       `toml:"-"` // Phase 31 Plan 1 (carried through merge)
}

type IncludeDirective struct {
    Path string `toml:"path"`
    Once bool   `toml:"once"`
}

// IncludeDirective and the extraction land in Plan 01; Resolve (below) consumes Model.Includes.
```

From internal/parser/parser.go (ParseFile, lines 323-336 — the recursion entry point Resolve calls):
```go
func ParseFile(path string) (*Model, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return nil, &ParseError{Message: "failed to read file", Context: path, Cause: err}
    }
    return Parse(data)
}
```

From internal/parser/errors.go (ParseError, lines 13-22 — the error shape for cycle/missing/dup):
```go
type ParseError struct {
    Message string
    Line    int
    Context string  // <-- carries filename(s) for multi-file attribution
    Cause   error
}
```

From internal/model/unit.go (Unit, lines 41-72 — Subunits/SubunitOrder are the D-10 merge targets):
```go
type Unit struct {
    // ... Type, Name, Description, Technology, Color, Style, Border, Edges, Width, Height ...
    Expanded      []string         `toml:"expanded"`
    Links         []Link           `toml:"link"`
    LinksFrom     []Link           `toml:"linkFrom"`
    SubunitOrder  []string         `toml:"-"`
    Subunits      map[string]*Unit `toml:",inline"`  // line 71 — non-strict toml is load-bearing (BC-3)
}
```

From internal/model/properties.go (Properties, lines 4-21 — the INC-08 root-wins/conflict merge target):
```go
type Properties struct {
    Name        string   `toml:"name"`
    Description string   `toml:"description"`
    Color       string   `toml:"color"`
    Style       string   `toml:"style"`
    Border      string   `toml:"border"`
    Edges       string   `toml:"edges"`
    LineLength  int      `toml:"lineLength"`
    Expanded    []string `toml:"expanded"`
}
```

From internal/validator/index.go (BuildIndex recursive subunit walk, lines 23-46 — the analog for mergeSubunits recursion):
```go
// BuildIndex walks Units and recurses into unit.Subunits — mergeSubunits mirrors this
// to attach an included file's [parent.child] subunits to an entry-defined [parent].
func BuildIndex(units map[string]*model.Unit, parentPath string) map[string]*UnitInfo { ... }
```

From cmd/c4drill/root.go (Parse→Validate staging, lines 112-118 — the insertion point):
```go
m, err := parser.ParseFile(inputPath)
if err != nil {
    return fmt.Errorf("parse: %w", err)
}
// Stage 1a inserts HERE (Phase 32)
// (Stage 1b template.Expand inserts here too when Phase 31 lands — include MUST be first)
valErrors := validator.Validate(m)
```
</interfaces>
</context>

<feature>
  <name>internal/include package: recursive resolver + struct merge + pipeline wiring</name>
  <files>internal/include/resolve.go, internal/include/merge.go, internal/include/include_test.go, internal/include/testdata/*.toml, cmd/c4drill/root.go, cmd/c4drill/root_test.go</files>
  <behavior>
    `include.Resolve(entry, entryDir)` walks `entry.Includes`, and for each directive: canonicalizes the path relative to the including file's dir, checks the cycle stack and once/diamond visited-set, recursively ParseFile+Resolve the included file, then merges the resolved included model into the entry. The merged model's `.Includes` is drained to nil; `.Units`/`.UnitOrder`/`.Properties`/`.Templates`/`.Instantiations` are assembled per D-09/D-10/D-11/INC-08.

    Cases (one test per INC + D-10/D-11/XC-02):
    - INC-01: entry (`main.toml`) + 1 include (`auth.toml`) → merged `Units` has both files' top-level units; `UnitOrder` = main's units then auth's units in include-directive order.
    - INC-02: resolving the same fixture from two different cwds (via os.Chdir in the test) yields identical merged `Units`/`UnitOrder`.
    - INC-03: `main_transitive.toml` → `mid.toml` → `leaf.toml` chain → merged `Units` has all three files' units.
    - INC-04: `cycle_a.toml` (includes cycle_b) + `cycle_b.toml` (includes cycle_a) → fatal `*ParseError` whose message names the cycle; `self_cycle.toml` (includes itself) → fatal.
    - INC-05: `diamond_top.toml` (includes left+right) → left+right both include `diamond_shared.toml` → NOT a cycle; shared's units present once (auto-dedup, D-11).
    - INC-06: `once_main.toml` includes `once_lib.toml` twice (second with `once=true`); `once_other.toml` also includes `once_lib.toml` with `once=true` → lib's units present once.
    - INC-07: `dup_main.toml` + `dup_other.toml` both define `[mailAdapter]` → fatal `*ParseError` naming both files.
    - INC-08: `props_main.toml` (name="Main") + `props_conflict.toml` (name="Conflict") → fatal `*ParseError` naming both files; if conflict's name is empty → root-wins, no error.
    - INC-09: covered by INC-01's `UnitOrder` assertion (append in include order).
    - INC-10: `missing_main.toml` includes nonexistent `ghost.toml` → fatal `*ParseError` whose Context names both `ghost.toml` and the including file.
    - D-10: `subunits_main.toml` (`[linuxSystem]`) + `subunits_auth.toml` (`[linuxSystem.auth]`, `[linuxSystem.db]`) → merged `Units["linuxSystem"].Subunits` has "auth" and "db", appended to `SubunitOrder` in include-file order; neither appears as a phantom top-level unit.
    - D-11: same-file diamond (diamond fixture) auto-dedup, no error; cross-file dup (INC-07) hard-errors.
    - XC-02 (conditional on Phase 31): `templates_lib.toml` (defines `[template.svc]`) + `templates_main.toml` (`[[include]]` + `[[use]] template="svc"`) → merged `Model.Templates` has key "svc". (Full instantiation test is Phase 31's Expand; here we assert template carry through the merge.)
    - XC-01/XC-05 integration: multi-file model runs through the full pipeline (ParseFile → include.Resolve → Validate → render) without error; `include.Resolve` runs BEFORE `Validate`.
  </behavior>
  <implementation>
    RED: write the test suite + fixtures first (Task 1). GREEN: implement resolve.go + merge.go (Task 2), then wire root.go + add the integration test (Task 3). REFACTOR: extract mergeSubunits helper from merge if the subunit-walk grows complex.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing include tests + core multi-file fixtures (RED)</name>
  <files>internal/include/include_test.go, internal/include/testdata/main.toml, internal/include/testdata/auth.toml, internal/include/testdata/nested_subunits_main.toml, internal/include/testdata/nested_subunits_auth.toml</files>
  <behavior>
    - TestResolveTwoFilesMerge (INC-01, INC-09): ParseFile testdata/main.toml, Resolve(entry, "testdata"); assert merged.Units has both main's and auth's top-level units; merged.UnitOrder == main's units followed by auth's units in include-directive order; merged.Includes is nil (drained).
    - TestResolveRelativePathIndependentOfCwd (INC-02): in a t.TempDir, write a main+include pair via os.WriteFile; resolve from two different cwds (os.Chdir with t.Cleanup to restore); assert the two merged.Units/UnitOrder are deep-equal.
    - TestResolveTransitive (INC-03): in a t.TempDir, write a 3-file chain (top→mid→leaf via relative [[include]] paths); assert merged.Units has all three files' units.
    - TestResolveCycleFatal (INC-04): in a t.TempDir, write cycle_a.toml (includes cycle_b) + cycle_b.toml (includes cycle_a); Resolve on cycle_a → fatal *parser.ParseError whose .Error() contains "cycle"; also a self-include file → fatal.
    - TestResolveDiamondNotCycle (INC-05, D-11): in a t.TempDir, write a 4-file diamond (top→left+right, left→shared, right→shared); Resolve on top → no error (NOT a cycle); merged.Units has shared's units exactly once (auto-dedup).
    - TestResolveOnceDedup (INC-06): in a t.TempDir, write main (includes lib twice, second with once=true) + other (includes lib with once=true); Resolve on main → lib's units present once; no duplicate-definition error.
    - TestMergeDuplicateUnitPathError (INC-07, D-11 cross-file): in a t.TempDir, write dup_main + dup_other both defining [mailAdapter]; Resolve → fatal *parser.ParseError naming both files.
    - TestMergePropertiesConflictError (INC-08): in a t.TempDir, write props_main (name="Main") + props_conflict (name="Conflict"); Resolve → fatal; variant where conflict's name is empty → root-wins, no error, merged.Properties.Name == "Main".
    - TestResolveMissingIncludeError (INC-10, D-12): in a t.TempDir, write missing_main (includes nonexistent ghost.toml); Resolve → fatal *parser.ParseError whose .Error()/Context names both "ghost.toml" and the including file.
    - TestMergeCrossFileSubunits (D-10): ParseFile testdata/nested_subunits_main.toml ([linuxSystem]) which includes nested_subunits_auth.toml ([linuxSystem.auth], [linuxSystem.db]); Resolve → merged.Units["linuxSystem"].Subunits has "auth" and "db"; SubunitOrder ends with ["auth", "db"] in include-file order; neither "auth" nor "db" is a phantom top-level unit (not in merged.Units directly, not in merged.UnitOrder).
    - TestMergeCarriesTemplates (XC-02, conditional on Phase 31): in a t.TempDir, write templates_lib (defines [template.svc]) + templates_main (includes lib); Resolve → merged.Templates has key "svc". SKIP via t.Skip if Model.Templates does not exist yet (Phase 31 not landed).
    - TestMergeUnitOrderAppend (INC-09): explicit assertion (alongside INC-01) that entry units come before included units in UnitOrder.
  </behavior>
  <action>
    Fixture strategy — TWO complementary approaches: (a) committed on-disk fixtures under internal/include/testdata/ for the stable, human-readable cases (main.toml + auth.toml for the canonical INC-01/09 merge; nested_subunits_main.toml + nested_subunits_auth.toml for D-10 — these double as documentation of the include feature's TOML shape, modeled on testdata/valid.toml + testdata/nested.toml); (b) a `writeFiles(t, map[string]string) string` test helper that takes a t.TempDir() and a name→content map, writes each file via os.WriteFile, and returns the temp dir path — used for the cycle/diamond/once/transitive/dup/props/missing/templates cases so their multi-file graphs are self-contained in the test source (idiomatic Go for multi-file tests; keeps the committed fixture count low and the test intent co-located with the test). Each multi-file fixture's [[include]] paths are RELATIVE (e.g. main.toml has [[include]] path="auth.toml") so they resolve relative to the including file's directory. nested_subunits_auth.toml uses [linuxSystem.auth]/[linuxSystem.db] dotted-path subtable syntax (the same shape testdata/nested.toml uses).
    Write internal/include/include_test.go as package include_test, using t.Parallel() (these are pure-Go tests of the resolver, NOT go-graphviz tests, so parallelism is safe — unlike cmd/c4drill/root_test.go), testify require/assert. For the committed-fixture tests, parser.ParseFile("testdata/main.toml") (relative to the internal/include/ package dir) then include.Resolve(entry, "testdata"). For the writeFiles-based tests, parser.ParseFile(filepath.Join(tmpDir, "entry.toml")) then include.Resolve(entry, tmpDir). For the cd-proof test, save cwd via os.Getwd, os.Chdir to two different temp dirs between resolves, restore via t.Cleanup. For cycle/missing/dup tests, type-assert *parser.ParseError (or errors.As) and check .Error()/.Context contains the expected file references. Run the suite and confirm it FAILS to compile (include.Resolve / the internal/include package does not exist yet).
  </action>
  <read_first>
    - internal/parser/parser_test.go (the fixture-driven test idiom — t.Parallel, require/assert, os.ReadFile)
    - internal/parser/parser.go (Model struct + ParseFile signature — what Resolve consumes/calls)
    - internal/parser/errors.go (ParseError shape — what cycle/missing/dup errors must look like)
    - testdata/valid.toml and testdata/nested.toml (the TOML shape to model the committed fixtures on — properties/unit/[[link]] and the dotted-path subtable [parent.child] syntax for D-10)
    - .planning/phases/32-include-directive-multi-file/32-VALIDATION.md (Per-Task Verification Map — the test names match)
    - .planning/phases/32-include-directive-multi-file/32-PATTERNS.md (File: internal/include/include_test.go section — the test idiom + cd-proof + writeFiles-helper approach)
  </read_first>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/include/ 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <done>
    internal/include/include_test.go exists with the 12 named tests plus the writeFiles helper, and the 4 committed testdata/ fixtures (main.toml, auth.toml, nested_subunits_main.toml, nested_subunits_auth.toml) are present. The package FAILS to compile because include.Resolve / the internal/include package does not exist yet (RED). The 4 committed fixtures are valid TOML (verifiable: each parses standalone via parser.ParseFile without error — the ParseError surfaced is "undefined: include.Resolve", not a TOML decode error).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resolve.go + merge.go (GREEN)</name>
  <files>internal/include/resolve.go, internal/include/merge.go</files>
  <behavior>
    - All tests from Task 1 now PASS.
    - `Resolve(entry *parser.Model, entryDir string) (*parser.Model, error)` walks entry.Includes in order; for each directive: canonicalize path (`filepath.Clean(filepath.Abs(filepath.Join(includingDir, dir.Path)))`); cycle-check against the stack; once/diamond-check against the visited-set; ParseFile the included file (missing → *ParseError naming path + including file, INC-10/D-12); recurse Resolve with the included file's dir as the new includingDir and the updated stack (transitive, INC-03); mark visited; merge; drain entry.Includes to nil.
    - `merge(dst, src *parser.Model, dstFile, srcFile string) (*parser.Model, error)` applies: Units union (cross-file dup key → *ParseError naming both files, INC-07/D-11); UnitOrder append in include order (D-09); per-unit Subunits deep-merge via mergeSubunits (D-10); Properties root-wins with non-zero conflict → *ParseError (INC-08); Templates union with dup name → *ParseError (XC-02 carry); Instantiations append.
    - Three distinct data structures for three concerns: stack `[]string` (cycle, INC-04), visited-set `map[string]bool` (once + same-file diamond, INC-06/D-11), canonical path as the key for both. Max-depth cap (100) as defense-in-depth.
    - Non-strict TOML stays (no DisallowUnknownFields — STATE.md DI-1/BC-3).
  </behavior>
  <action>
    resolve.go: package include. Define maxIncludeDepth = 100. Exported `func Resolve(entry *parser.Model, entryDir string) (*parser.Model, error)` initializes visited := map[string]bool{} and delegates to an unexported `func resolve(m, includingDir string, stack []string, visited map[string]bool) (*parser.Model, error)` that does the per-directive loop. For each IncludeDirective in m.Includes: (1) absPath := filepath.Clean(filepath.Abs(filepath.Join(includingDir, dir.Path))); (2) cycle check: iterate stack, if any element == absPath return *parser.ParseError{Message: "include cycle detected: " + strings.Join(append(stackSliceFromCycle, absPath), " -> ")}; (3) if visited[absPath] { continue } (covers once + same-file diamond — D-11 automatic); (4) included, err := parser.ParseFile(absPath); if err != nil return *parser.ParseError{Message: "include not found", Context: fmt.Sprintf("%s (included from %s)", dir.Path, includingDirOrFile), Cause: err} — name both the referenced path and the including file (INC-10/D-12); (5) included, err = resolve(included, filepath.Dir(absPath), append(stack, absPath), visited); (6) visited[absPath] = true; (7) m, err = merge(m, included, includingDirFile, absPath); (8) after the loop, set m.Includes = nil and return m, nil. Add a maxIncludeDepth check (len(stack) > maxIncludeDepth → *ParseError) at the top of the unexported resolve. Use a small helper to track the including file's display name for error messages (the entry's "file" is inputPath-derived; for nested, it's absPath).
    merge.go: package include. `func merge(dst, src *parser.Model, dstFile, srcFile string) (*parser.Model, error)` — for k, u := range src.Units: if existing, ok := dst.Units[k]; ok { if err := mergeSubunits(existing, u, k, dstFile, srcFile); err != nil { return nil, err } } else { dst.Units[k] = u; dst.UnitOrder = append(dst.UnitOrder, k) }. (Note: src.UnitOrder is already handled per-key — only new top-level keys append.) `func mergeSubunits(dstU, srcU *model.Unit, path, dstFile, srcFile string) error` — for child, su := range srcU.Subunits: if _, ok := dstU.Subunits[child]; ok { return &parser.ParseError{Message: fmt.Sprintf("subunit %s.%s defined in both %s and %s", path, child, dstFile, srcFile)} } else { dstU.Subunits[child] = su; dstU.SubunitOrder = append(dstU.SubunitOrder, child) }. Properties (INC-08): use reflection OR explicit field checks on model.Properties — for each non-zero field in src.Properties, if dst.Properties' same field is non-zero → *ParseError naming both files; else copy. (Lean conflict-error for ALL fields per CONTEXT discretion / RESEARCH Open Question 2 — reflection over model.Properties is cleanest; OR enumerate the 8 fields explicitly. The executor chooses; tests verify.) Templates (XC-02): if src.Templates != nil { for k, t := range src.Templates { if _, ok := dst.Templates[k]; ok { return error naming both files } else { dst.Templates[k] = t } } } — guard with `if dst.Templates == nil { dst.Templates = map[string]*parser.TemplateDef{} }` first; wrap the whole Templates block in a check that Model.Templates exists (Phase 31 conditional). Instantiations: dst.Instantiations = append(dst.Instantiations, src.Instantiations...). Do NOT enable DisallowUnknownFields. Do NOT round-trip-copy Links (move units by pointer — no aliasing risk since each included file's units are distinct map entries). Add a code comment that non-strict toml is load-bearing (BC-3) and that filepath.Abs/Clean canonicalization is load-bearing for cycle/dedup correctness (IN-4/IN-5).
  </action>
  <read_first>
    - internal/parser/parser.go (ParseFile:323, Parse:47, Model struct:35 — the recursion entry + types)
    - internal/parser/errors.go (ParseError:13 — the error shape for cycle/missing/dup; Context field carries filenames)
    - internal/model/unit.go (Unit.Subunits:71 inline tag, SubunitOrder:70 — the D-10 merge targets)
    - internal/model/properties.go (Properties:4-21 — the 8 fields for INC-08 root-wins/conflict)
    - internal/validator/index.go (BuildIndex:23-46 — the recursive subunit-walk analog for mergeSubunits)
    - .planning/phases/32-include-directive-multi-file/32-RESEARCH.md (Pattern 2: three data structures; Pattern 3: merge rules; Code Examples: Resolve skeleton + signature decision)
    - .planning/phases/32-include-directive-multi-file/32-PATTERNS.md (File: internal/include/resolve.go + merge.go — analog excerpts and per-field rules)
  </read_first>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/include/ -x 2>&1 | grep -v '^#' | tail -30 && golangci-lint run ./internal/include/ 2>&1 | tail -5</automated>
  </verify>
  <done>
    `go test ./internal/include/` is fully green (all 12 tests pass, including cycle/diamond/once/transitive/cd-proof/dup/properties/missing/subunits — and XC-02 template carry if Phase 31 landed, else that one test is skipped via t.Skip). Resolve uses a stack for cycles and a visited-set for once/diamond (distinct data structures). All error cases return *parser.ParseError naming the relevant files. `golangci-lint run ./internal/include/` and `go vet ./internal/include/` are clean. No DisallowUnknownFields anywhere in the package.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire include.Resolve into the pipeline + end-to-end integration test</name>
  <files>cmd/c4drill/root.go, cmd/c4drill/root_test.go</files>
  <behavior>
    - runRoot calls include.Resolve(m, filepath.Dir(inputPath)) as Stage 1a, immediately after ParseFile and before Validate (and before template.Expand when Phase 31 lands — include is FIRST).
    - A multi-file CLI invocation (entry file with [[include]]) parses, resolves, validates, and renders without error — the merged model flows through the unchanged validator/view/render.
    - include.Resolve runs BEFORE Validate (XC-01 pipeline ordering) — verified by an integration test.
    - A single-file model (no [[include]]) still renders identically (no regression — include.Resolve is a no-op when Model.Includes is empty).
  </behavior>
  <action>
    (1) In cmd/c4drill/root.go, add "github.com/Djarvur/c4drill/internal/include" to the import block (alphabetical, after "github.com/Djarvur/c4drill/internal/graph"). (2) Between the ParseFile block (root.go:112-115) and the Validate block (root.go:118), insert: `// Stage 1a: Resolve includes (recursive, merges *parser.Model structs) — runs FIRST` newline `if m, err = include.Resolve(m, filepath.Dir(inputPath)); err != nil {` newline `    return fmt.Errorf("include: %w", err)` newline `}`. Match the existing fmt.Errorf("stage: %w", err) idiom. (3) If Phase 31 has already added a template.Expand call at the same insertion point, ensure include.Resolve sits ABOVE it (include before template — pipeline ordering load-bearing). (4) Add an integration test TestPipelineIncludeBeforeValidate to cmd/c4drill/root_test.go: build a temp dir with a multi-file model (entry + one include), invoke the CLI via NewRootCmd().SetArgs([]string{entryPath, "--format", "dot", "--output", tempOutDir}) (mirror the existing root_test.go invocation pattern — note these tests use //nolint:paralleltest because go-graphviz WASM has concurrency issues, so do NOT add t.Parallel()), assert no error and that the expected output files are written. Assert that include.Resolve ran before Validate by checking the model is valid post-resolve (a pre-resolve model with unresolved includes would have phantom-unit validation errors). Add a second test TestPipelineSingleFileNoRegression asserting an existing single-file fixture still renders identically.
  </action>
  <read_first>
    - cmd/c4drill/root.go (the Parse→Validate staging at 112-118 + the import block at 12-18 + the fmt.Errorf idiom)
    - cmd/c4drill/root_test.go (the existing integration-test idiom — NewRootCmd, SetArgs, //nolint:paralleltest, NO t.Parallel due to go-graphviz WASM concurrency)
    - internal/include/resolve.go (the Resolve signature: Resolve(entry, entryDir) — entryDir is filepath.Dir(inputPath))
    - .planning/phases/32-include-directive-multi-file/32-RESEARCH.md (Code Examples: Pipeline insertion — the exact target code)
    - .planning/phases/32-include-directive-multi-file/32-PATTERNS.md (File: cmd/c4drill/root.go — the insertion excerpt + Phase 31 concurrency note)
  </read_first>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./... 2>&1 | grep -v '^#' | tail -25 && golangci-lint run ./cmd/c4drill/ 2>&1 | tail -5</automated>
  </verify>
  <done>
    `go test ./...` is fully green (the new cmd/c4drill integration tests pass; the full suite shows no regression in validator/view/render/graph — they consume the merged model unchanged). include.Resolve is wired as Stage 1a in root.go between ParseFile and Validate. `golangci-lint run ./cmd/c4drill/` and `go vet ./cmd/c4drill/` are clean. A single-file model renders identically to before (no-regression test passes). The multi-file integration test produces the expected rendered output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local TOML files (entry + included) → include.Resolve | Author-controlled local file content crosses here; include.Resolve reads additional files from disk based on paths in the entry TOML |
| Resolved model → validator/view/render | The merged *parser.Model crosses into the unchanged downstream pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Denial of Service | Pathological acyclic-but-huge or deep include graph (zip-bomb / diamond explosion) | mitigate | maxIncludeDepth = 100 cap (defense-in-depth, go-metadot precedent) PLUS the visited-set ensures any single file is ParseFile'd at most once regardless of how many paths reach it (D-11 same-file diamond dedup is automatic; once is opt-in). Total work is bounded to O(number of distinct files). |
| T-32-04 | Tampering | Path traversal via crafted include path (e.g. `../../../../etc/passwd`) | accept | C4Drill is a local rendering tool operating on author-controlled TOML; the author already has full filesystem access as the invoking user, so traversal grants no new capability. filepath.Clean/Abs canonicalize for cycle/dedup correctness, NOT as a security boundary. Document this in a code comment. |
| T-32-05 | Tampering/DoS | Symlink-based cycle evasion (symlink loop dodges filepath.Abs-based stack/visited check) | accept (v1.10) | filepath.Abs does NOT resolve symlinks (only filepath.EvalSymlinks does). A symlink loop could evade the check. Acceptable for v1.10: C4Drill is author-controlled local tooling. Document as a known limitation; filepath.EvalSymlinks is the future hardening path. NOT a blocker (no high-severity threat — local tool, no privilege boundary). |
| T-32-06 | Information Disclosure | Error messages leak file paths | accept | Cycle/missing/dup errors name file paths by design (INC-04/INC-10/INC-07 require naming files). These are the user's own local paths; disclosure is the intended behavior, not a leak. |
| T-32-07 | Tampering | package installs | accept | This plan installs ZERO packages (stdlib path/filepath + os + existing parser/testify only — verified against go.mod in RESEARCH.md). No slopcheck vector. |
</threat_model>

<verification>
- `go test ./internal/include/ -x` — all include-package tests green (INC-01..10, D-10, D-11, XC-02)
- `go test ./internal/parser/ -x` — Plan 01's parser tests still green (no regression from Plan 02)
- `go test ./cmd/c4drill/ -x` — pipeline integration tests green (include.Resolve before Validate; single-file no-regression)
- `go test ./...` — full suite green (validator/view/render/graph consume the merged model unchanged)
- `golangci-lint run ./...` — clean
- `go vet ./...` — clean
- Pipeline ordering confirmed: include.Resolve is Stage 1a (FIRST pre-processing pass), before template.Expand (Phase 31) and Validate
- No phantom units: a multi-file model produces a merged Model.Units with only real renderable units (no include/template/use stubs)
- Multi-file model renders equivalently to the hand-authored single-file equivalent (merge-equivalence asserted at the *parser.Model level; SVG golden comparison deferred to Phase 33 per PATTERNS.md canonicalDOT note)
</verification>

<success_criteria>
- INC-01: multi-file model merges into one logical *parser.Model
- INC-02: include paths resolve relative to including file's dir (cd-proof test passes)
- INC-03: transitive includes resolve recursively
- INC-04: cycle (self + mutual) fatal, names the cycle
- INC-05: diamond not a cycle; no-`once` cross-file dup-path hard-errors
- INC-06: once=true skips re-inclusion
- INC-07: flat merge; cross-file dup unit path hard-errors naming both files
- INC-08: properties root-wins; conflict hard-errors
- INC-09: UnitOrder concatenation preserves authoring order
- INC-10: missing include names referenced path + including file
- D-10: cross-file subunit merge works (included file adds subunits to entry-defined parent)
- D-11: same-file diamond auto-dedup; cross-file collision hard-errors
- D-12: missing include hard-errors unconditionally (no optional flag)
- XC-02: templates in included file flow into merged Model.Templates (conditional on Phase 31; test skips gracefully if not landed)
- include.Resolve wired as Stage 1a in root.go (FIRST pass, before template.Expand/Validate)
- No regression: single-file models render identically; validator/view/render/graph unchanged
</success_criteria>

<output>
Create `.planning/phases/32-include-directive-multi-file/32-02-SUMMARY.md` when done
</output>
