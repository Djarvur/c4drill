---
phase: 39-edge-style-override-edges-cli-flag
plan: 03
type: execute
wave: 3
depends_on:
  - "39-02"
files_modified:
  - README.adoc
  - skill/SKILL.md
  - plugins/c4drill/skills/c4drill-toml/SKILL.md
  - plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md
autonomous: true
requirements:
  - GEDGE-03
  - GEDGE-06

must_haves:
  truths:
    - "A user reading README.adoc's CLI Reference can discover --edges, its four allowed values, the square=ortho alias, and that it overrides both global and per-unit model edges without editing the model"
    - "A user reading the README --plain section sees the documented delta: an explicit --edges survives --plain because user intent beats author-format suppression (D-05) — the exact-union wording is amended, not contradicted"
    - "All three SKILL.md copies are byte-identical and document --edges (D-07, 37-06 sync convention)"
    - "Milestone v1.16 is shipped: tag v1.23.0 pushed, the Release workflow is green, and the GitHub release carries the 6 platform binaries (v1.15 REL-01 convention)"
  artifacts:
    - path: README.adoc
      provides: "--edges entry in the CLI Reference flags block + narrative bullet + --plain exact-union delta + C4D Edges section cross-reference"
      contains: "--edges"
    - path: skill/SKILL.md
      provides: "canonical skill doc copy with --edges flag examples and plain-composition note"
      contains: "--edges"
    - path: plugins/c4drill/skills/c4drill-toml/SKILL.md
      provides: "byte-identical copy 2"
      contains: "--edges"
    - path: plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md
      provides: "byte-identical copy 3"
      contains: "--edges"
  key_links:
    - from: "README.adoc --plain section"
      to: "D-05/GEDGE-06 behavior"
      via: "explicit delta sentence: explicit CLI --edges wins over --plain suppression"
      pattern: "--edges"
    - from: "tag v1.23.0"
      to: "GitHub Release workflow (.github/workflows/release.yml)"
      via: "tag push triggers 6-binary build + gh release create"
      pattern: "v1\\.23\\.0"
---

<objective>
Document the `--edges` flag across the repo's doc surfaces (D-07) — including the deliberate `--plain` contract delta (D-05) — and ship milestone v1.16 as release v1.23.0.

Purpose: Docs follow the established repo convention (every shipped flag is documented in the CLI Reference and mirrored byte-identically across the 3 SKILL.md copies); the `--plain` delta MUST be stated explicitly because it amends the documented KEY-02 exact-union contract. The release task closes milestone v1.16 per the v1.15 REL-01 precedent (38-06).

Output: Updated README.adoc + 3 byte-identical SKILL.md copies + published v1.23.0.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Verified doc touchpoints (2026-08-31): -->

README.adoc:
- `== CLI Reference` heading at line 1231; the `Flags:` transcript block at lines 1237-1249 lists every persistent flag alphabetically (--expanded, -f/--format, -h, --label-ratio, --no-colors, --no-labels, --no-length, --no-rank, --no-styles, -o/--output, --plain, -v) — --edges inserts alphabetically after --expanded
- Flag narrative bullets begin line 1251; --plain bullet at 1256-1283 (ends with an "What is ignored" list); granular-switch bullets ~1284-1323 include the composition rules and line ~1317 states: "`properties.edges` (spline routing) is tied to `--plain` only"
- `=== Edges` (C4D Format section) at line 958 documents the author-side edges property — add a one-line cross-reference to the CLI override there

skill/SKILL.md (canonical, 1033 lines): flag examples at lines ~1006-1007 (--no-labels + composition style), --plain section "Plain Rendering (--plain)" at ~971-998
plugins/c4drill/skills/c4drill-toml/SKILL.md and plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md: byte-identical copies of skill/SKILL.md (verified with cmp on 2026-08-31)

Release: .github/workflows/release.yml triggers on tags matching v*, builds 6 binaries (linux/darwin/windows × amd64/arm64) with -X main.version=<tag>, and creates the GitHub release with generate-notes. Precedent: v1.15 REL-01 (38-06) — tag v1.22.0, release workflow green, 6 binaries.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs content → users | Documentation only — no executable surface |
| release tag → CI pipeline | A pushed tag triggers a public release build; a tag on a red or wrong commit would publish broken binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | release tagging (Task 3) | mitigate | Tag ONLY after go test ./... and golangci-lint run ./... are green on the exact release commit, and after Tasks 1-2 are committed; verify the workflow run and the 6 assets post-push (v1.15 REL-01 precedent) |
| T-39-06 | Repudiation | docs drift across SKILL copies | mitigate | byte-identical sync enforced with cmp across all three copies in the task verification |
| T-39-SC | Tampering | package installs | accept | No packages installed in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: README.adoc — CLI Reference entry, narrative bullet, --plain delta, C4D cross-reference</name>
  <files>README.adoc</files>
  <read_first>
    - README.adoc lines 1231-1330 (the whole CLI Reference section: transcript block format, bullet style, granular-switch bullets, and the --plain "What is ignored" list + composition rules around lines 1311-1323)
    - README.adoc lines 958-1005 (=== Edges under C4D Format — where the author-side edges property is documented)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md (D-05 delta wording; D-07 docs scope)
  </read_first>
  <action>
    Four edits, matching the section's established voice:
    (1) CLI Reference transcript block (after the --expanded line, alphabetical): add the --edges line exactly as cobra renders it — `      --edges string        Override edge routing style for every generated diagram (straight|spline|square|ortho)` — then regenerate the block mentally against `go run ./cmd/c4drill --help` and fix any spacing mismatch so the block stays a faithful transcript.
    (2) New narrative bullet after the --expanded bullet (before --plain), in the same `* ...` style: --edges overrides the edge routing style for the WHOLE invocation — every generated diagram (C1, all drill-downs, the --expanded copy) renders with the requested style, winning over BOTH the global properties.edges and per-unit edges values with no model edit (v1.16). Cover: the four values, that square is the documented ortho alias (GEDGE-02), that an invalid value is a hard error naming the value and the enum, and one or two copy-paste examples in the section's example style (e.g. `c4drill architecture.toml --edges straight --expanded` rendering the expanded variant without editing the model).
    (3) The --plain delta (D-05, REQUIRED by GEDGE-06's docs clause): amend the composition-rules area where line ~1317 says properties.edges is tied to --plain only — keep that statement true for the MODEL property and add the explicit exception: an explicit CLI `--edges` value is user intent, not author formatting, and SURVIVES --plain (pinned by test); --plain with no --edges still suppresses author edges exactly as before.
    (4) In === Edges (line 958 area), append one sentence cross-referencing the CLI override: the edges property styles one model; `--edges` (see CLI Reference) overrides it per invocation.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && grep -c '\-\-edges' README.adoc && go run ./cmd/c4drill --help 2>&1 | grep -A1 'edges' | head -3</automated>
  </verify>
  <acceptance_criteria>
    - README.adoc CLI Reference transcript block contains a --edges line whose wording matches go run ./cmd/c4drill --help output for the flag
    - README.adoc contains a narrative --edges bullet naming all four values, the global+per-unit override scope, the square/ortho alias, and the loud-invalid-value behavior, with at least one usage example
    - The --plain documentation contains an explicit sentence stating an explicit CLI --edges survives --plain (user intent wins over author-format suppression) while --plain without the flag still suppresses properties.edges
    - The === Edges C4D section cross-references the --edges CLI override
    - asciidoc structure intact (no broken lists/bullets around the edits)
  </acceptance_criteria>
  <done>README documents the flag and the plain delta; commit docs(39-03): document --edges flag and --plain delta in README</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md — byte-identical 3-copy sync (D-07)</name>
  <files>skill/SKILL.md, plugins/c4drill/skills/c4drill-toml/SKILL.md, plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md</files>
  <read_first>
    - skill/SKILL.md lines 965-1015 (Plain Rendering section + the --no-labels flag-example block at ~1006-1007 — the insertion neighborhood and example style)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md (D-07: byte-identical sync per 37-06 convention)
  </read_first>
  <action>
    Edit the CANONICAL copy skill/SKILL.md first: add a compact --edges entry in the flag examples area beside the --no-labels block (same style: one-line command comment pairs, e.g. the flag rendering all views with one routing style and its --plain composition), covering the four values, the square=ortho alias, the beats-global-and-per-unit scope, and the survives---plain delta in one or two sentences. Keep it brief — the SKILL doc is a quick-reference, README carries the full story (mirror the LBL-02 precedent's brevity).
    Then copy the edited canonical over BOTH mirror copies byte-for-byte: plugins/c4drill/skills/c4drill-toml/SKILL.md and plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md (cp, not independent edits — byte-identity is the contract).
    Verify with cmp across all three pairs; all three must exit 0.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && cmp skill/SKILL.md plugins/c4drill/skills/c4drill-toml/SKILL.md && cmp skill/SKILL.md plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md && cmp plugins/c4drill/skills/c4drill-toml/SKILL.md plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md && grep -c '\-\-edges' skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skill/SKILL.md documents --edges (four values, alias, override scope, --plain delta) in the flag-examples area, matching the --no-labels block's style
    - cmp succeeds for all three pairs — the three SKILL.md copies are byte-identical (D-07)
    - No other content changed in the copies beyond this edit (diff against git HEAD shows only the one addition replicated 3×)
  </acceptance_criteria>
  <done>Three copies byte-identical with the --edges docs; commit docs(39-03): sync --edges docs across 3 SKILL.md copies</done>
</task>

<task type="auto">
  <name>Task 3: Release v1.23.0 — ship milestone v1.16</name>
  <files>.</files>
  <read_first>
    - .github/workflows/release.yml (whole file — tag trigger, 6-binary matrix, gh release create with generate-notes)
    - .planning/milestones/v1.15-ROADMAP.md lines ~140-150 (38-06 REL-01 precedent: tag on final commit, workflow green, 6 binaries published)
  </read_first>
  <action>
    Precondition gate: confirm the working tree is clean and the release commit has Plans 39-01..39-03 artifacts — run go test ./... and golangci-lint run ./... one final time; both must be green BEFORE tagging (T-39-05 mitigation).
    Tag the final docs commit as v1.23.0 (annotated tag with a one-paragraph message summarizing v1.16: the --edges invocation-global override) and push the tag to origin.
    Monitor the Release workflow to completion (gh run watch or gh run list/download polling). On green: gh release view v1.23.0 and verify it is published with exactly 6 assets (c4drill-linux-amd64, linux-arm64, darwin-amd64, darwin-arm64, windows-amd64.exe, windows-arm64.exe).
    If the workflow fails: diagnose via gh run view --log-failed, fix, and re-tag (delete the broken tag/release first with gh release delete + git push --delete) — do not leave a partial release published.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && git describe --tags --exact-match && gh release view v1.23.0 --json assets,isDraft --jq '{assets: [.assets[].name], draft: .isDraft}'</automated>
  </verify>
  <acceptance_criteria>
    - go test ./... and golangci-lint run ./... exit 0 on the tagged commit before the tag was pushed
    - git tag v1.23.0 exists on the final plan-03 commit and has been pushed to origin
    - The Release workflow run for v1.23.0 completed successfully
    - gh release view v1.23.0 shows a published (non-draft) release with 6 binary assets
    - STATE.md/ROADMAP milestone updates are NOT part of this task (verify-phase/complete-milestone owns them)
  </acceptance_criteria>
  <done>Milestone v1.16 shipped as v1.23.0 with green workflow and 6 published binaries; commit chore(39-03): release v1.23.0 (tag commit itself carries the docs; release recorded in SUMMARY)</done>
</task>

</tasks>

<verification>
- README.adoc + all three SKILL.md copies mention --edges with the four-value enum and the --plain delta (D-05 documented, per GEDGE-06's docs clause)
- cmp across the three SKILL.md copies exits 0 (byte-identical, D-07)
- gh release view v1.23.0 — published, 6 assets (milestone v1.16 shipped)
- go test ./... green on the release commit
</verification>

<success_criteria>
- D-07 satisfied: README usage/flags + 3 byte-identical SKILL.md copies document --edges and the --plain delta
- GEDGE-06 docs clause satisfied: the README --plain section states the explicit-flag-wins delta explicitly
- Milestone v1.16 shipped: tag v1.23.0, green Release workflow, 6 published binaries (REL precedent)
- 2-3 commits; SUMMARY.md written per <output>
</success_criteria>

<output>
Create `.planning/phases/39-edge-style-override-edges-cli-flag/39-03-SUMMARY.md` when done
</output>
