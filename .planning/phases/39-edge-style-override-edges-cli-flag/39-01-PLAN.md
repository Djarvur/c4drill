---
phase: 39-edge-style-override-edges-cli-flag
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/c4drill/root.go
  - internal/view/view.go
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements:
  - GEDGE-03
  - GEDGE-04
  - GEDGE-05
  - GEDGE-06
  - GEDGE-08

must_haves:
  truths:
    - "c4drill model.toml --edges <style> (straight|spline|square|ortho) renders every generated diagram — C1 root, all C2/C3 drill-downs, and the --expanded copy — with that routing style, beating BOTH the global properties.edges AND any per-unit edges value (D-03, GEDGE-03, GEDGE-05)"
    - "An invalid --edges value fails loudly before any file I/O with an error naming the offending value and the allowed enum, and writes no output (D-02, GEDGE-04)"
    - "--plain --edges <style> renders the requested style — the explicit CLI flag survives --plain's author-format suppression (D-05, GEDGE-06)"
    - "Without --edges, behavior is byte-identical to today: every existing canonicalDOT golden passes untouched (D-04, GEDGE-08)"
  artifacts:
    - path: cmd/c4drill/root.go
      provides: "--edges persistent flag, errInvalidEdges sentinel, early enum validation in runRoot, threading in processView + processExpandedView"
      contains: "errInvalidEdges"
    - path: internal/view/view.go
      provides: "EdgesOverride carrier field on View (default empty = flag absent = zero behavior change)"
      contains: "EdgesOverride"
    - path: internal/graph/builder.go
      provides: "post-PLAIN-02 override application in BOTH BuildGraph and BuildExpandedGraph"
      contains: "EdgesOverride"
    - path: internal/graph/builder_test.go
      provides: "builder-tier precedence pins (override beats plain, empty override = unchanged, override beats model value)"
      contains: "EdgesOverride"
    - path: cmd/c4drill/root_test.go
      provides: "TestEdgesFlagValidation (accept/reject table) + GEDGE-06 --plain-survival pin on the plain.toml fixture"
      contains: "invalid edges"
  key_links:
    - from: "cmd/c4drill/root.go (processView + processExpandedView PLAIN-01 blocks)"
      to: "view.View.EdgesOverride"
      via: "flag threading at both choke points"
      pattern: "EdgesOverride = edges"
    - from: "internal/graph/builder.go (BuildGraph + BuildExpandedGraph)"
      to: "Graph.EdgeStyle"
      via: "override applied AFTER the v.Plain zeroing"
      pattern: "if v\\.EdgesOverride != \"\""
    - from: "Graph.EdgeStyle"
      to: "internal/render/converter.go configureGraphSettings (UNCHANGED)"
      via: "existing splines mapping (spline→true, straight→false, ortho/square→ortho)"
      pattern: "SetSplines"
---

<objective>
Implement the `--edges <style>` CLI flag with invocation-global override semantics (TDD).

Purpose: Users can render the SAME model as per-invocation variants (e.g. expanded-with-straight vs non-expanded-with-spline) without editing or duplicating the model file — the motivating case from the folded design todo. This plan delivers the full override chain: flag surface (D-01), loud validation (D-02), invocation-global semantics beating global AND per-unit model values (D-03), flag-off invariance (D-04), and `--plain` survival (D-05).

Output: Working, TDD-tested `--edges` override spanning cmd → view → graph builder; render/converter untouched (its splines mapping already handles all four enum values).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md
@.planning/phases/39-edge-style-override-edges-cli-flag/39-RESEARCH.md
@.planning/phases/39-edge-style-override-edges-cli-flag/39-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-08-31. -->

From cmd/c4drill/root.go (sentinel + validation precedent, lines 26-33 and 138-141):
- errInvalidFormat = errors.New("invalid format: must be dot, svg, or html") — wrapped as fmt.Errorf("%w: %q", errInvalidFormat, format) at the TOP of runRoot, before Stage 1 parse
- Package-level flag vars live in one //nolint:gochecknoglobals block (lines 49-62); registrations at lines 96-115 via cmd.PersistentFlags().StringVar/BoolVar/Float64Var

From cmd/c4drill/root.go (PLAIN-01 threading blocks — the exact insertion points):
- processView lines 330-338: v.Plain = plain; v.NoColors = noColors; v.NoStyles = noStyles; v.NoLength = noLength; v.NoRank = noRank; v.NoLabels = noLabels
- processExpandedView lines 383-391: identical block for the --expanded copy

From internal/view/view.go (View struct, lines 20-91):
- Edges string (line 33) — the model-resolved routing style (DO NOT repurpose)
- NoLabels bool (lines 56-63) — the doc-comment style to copy for the new field
- View generation sets Edges at internal/view/scope.go:25 (expanded: m.Properties.Edges), :133 (C1: m.Properties.Edges), :504 (C2: cmp.Or(systemUnit.Edges, m.Properties.Edges)), :599 (C3: cmp.Or(containerUnit.Edges, m.Properties.Edges)) — scope.go is UNCHANGED by this plan

From internal/graph/builder.go (the PLAIN-02 blocks this plan extends — keep BOTH in sync):
- BuildGraph lines 38-49 and BuildExpandedGraph lines 423-436, identical logic:
  edgeStyle := v.Edges; if v.Plain { edgeStyle = "" }; then Graph{EdgeStyle: edgeStyle, ...}

From internal/render/converter.go (UNCHANGED — zero render-side changes in this phase):
- configureGraphSettings lines 264-271: switch g.EdgeStyle { case "spline": SetSplines("true"); case "straight": SetSplines("false"); case "ortho", "square": SetSplines("ortho") } — empty string emits no splines attribute

From cmd/c4drill/testdata/plain.toml line 8: edges = "straight" # author edge routing — ignored under --plain
</interfaces>
</context>

<feature>
  <name>Invocation-global --edges override with loud validation and --plain survival</name>
  <files>cmd/c4drill/root.go, internal/view/view.go, internal/graph/builder.go, internal/graph/builder_test.go, cmd/c4drill/root_test.go</files>
  <behavior>
    Flag validation (GEDGE-04, D-02):
    - --edges straight|spline|square|ortho → accepted (renders normally)
    - --edges diagonal → error containing `invalid edges` AND the quoted value `"diagonal"` AND the full enum wording `straight, spline, square, or ortho`; no output files written
    - --edges "" (empty) → treated as flag absent (no error, no override)
    - --edges SPLINE (wrong case) → error (enum is exact-match, like --format)

    Override semantics (GEDGE-05, D-03) — builder tier:
    - BuildGraph with v.Edges="straight", v.Plain=true, v.EdgesOverride="spline" → Graph.EdgeStyle == "spline" (explicit beats plain zeroing)
    - BuildGraph with v.Edges="spline", v.Plain=true, v.EdgesOverride="" → Graph.EdgeStyle == "" (plain suppression unchanged when flag absent)
    - BuildGraph with v.Edges="straight", v.Plain=false, v.EdgesOverride="ortho" → Graph.EdgeStyle == "ortho" (flag beats model value without plain)
    - Same three cases for BuildExpandedGraph (its PLAIN-02 block is a duplicate — both must be pinned)
    - Flag-off: EdgesOverride empty → Graph.EdgeStyle identical to today for every combination (D-04)

    CLI tier:
    - c4drill testdata/plain.toml --edges spline --format dot → RAW dot contains splines="true" (beats global edges="straight" at plain.toml:8)
    - c4drill testdata/plain.toml --plain --edges spline --format dot → RAW dot contains splines="true" (GEDGE-06 pin: user intent survives author-format suppression)
    - c4drill testdata/plain.toml --plain --format dot (no --edges) → RAW dot contains NO splines attribute (plain suppression preserved)
    - Flag-off run unchanged → all existing canonicalDOT goldens pass untouched (GEDGE-08)
  </behavior>
  <implementation>
    1. internal/view/view.go: add field `EdgesOverride string` to View (after Edges, line ~33) with a doc comment in the NoLabels style naming the flag, D-03/D-05 and GEDGE-05/06, and stating default-empty = flag absent = zero behavior change (D-04). Do NOT touch scope.go resolution.
    2. internal/graph/builder.go: in BOTH BuildGraph (lines 38-49) and BuildExpandedGraph (lines 423-436), immediately AFTER the `if v.Plain { edgeStyle = "" }` zeroing, add: if v.EdgesOverride is non-empty, edgeStyle = v.EdgesOverride — with a comment citing D-05/GEDGE-06 (explicit CLI is USER intent, not author formatting). Keep the two sites textually identical.
    3. cmd/c4drill/root.go: add `edges string` to the //nolint:gochecknoglobals var block; register PersistentFlags().StringVar(&edges, "edges", "", "Override edge routing style for every generated diagram (straight|spline|square|ortho)") — no shorthand, matching the switch family; add sentinel errInvalidEdges = errors.New("invalid edges: must be straight, spline, square, or ortho") to the static-errors block; validate in runRoot directly BELOW the format check (lines 138-141) and BEFORE Stage 1: if edges is non-empty and not one of the four enum values, return fmt.Errorf("%w: %q", errInvalidEdges, edges) (D-02); thread v.EdgesOverride = edges into BOTH PLAIN-01 blocks (processView ~line 338, processExpandedView ~line 391), updating the block comments to name it (PLAIN-01 pattern per GEDGE-05).
    4. Do NOT modify internal/view/scope.go, internal/render/converter.go, or any testdata golden. Invalid values must NOT be validated in the converter — its silent switch is internal-only; CLI validation is the only loud gate.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → flag parser → render pipeline | Untrusted `--edges` string value crosses into the pipeline; the render converter silently ignores unknown values, so an unvalidated value would be silently swallowed (wrong output, no error) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | runRoot flag validation (cmd/c4drill/root.go) | mitigate | Allow-list validate --edges against exactly straight|spline|square|ortho at the top of runRoot BEFORE Stage 1 file I/O; unknown value = hard sentinel error, never silent pass-through (D-02, GEDGE-04; ASVS V5) |
| T-39-02 | Information Disclosure | error message for invalid --edges | mitigate | Quote the offending value with %q in the sentinel wrap (repo precedent fmt.Errorf("%w: %q", ...)) — no unquoted echo of raw input (ASVS V5) |
| T-39-03 | Denial of Service | pipeline resource use via flag values | accept | --edges influences styling only — no loops, no allocation amplification, no paths; rendering cost identical for all four values |
| T-39-SC | Tampering | package installs | accept | No packages installed in this phase (zero new dependencies — see 39-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for override semantics, validation, and plain survival</name>
  <files>internal/graph/builder_test.go, cmd/c4drill/root_test.go</files>
  <read_first>
    - internal/graph/builder_test.go lines 40-60 (EdgeStyle pin "Test 3" pattern with assert message args) and lines 3312-3400 (TestBuildGraph_PlainSkipsUnitOverrides plain-guard family — view construction + v.Plain = true shape)
    - cmd/c4drill/root_test.go lines 114-171 (TestFlagValidation table shape: NewRootCmd + SetOut/SetErr + bytes.Buffer + PersistentFlags().Set + Execute + assert.Error / assert.NotContains) and lines 1951-1981 (generateFixtureOutput helper + golden test idiom)
    - internal/view/view.go lines 20-91 (View struct — field order and doc-comment style)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-PATTERNS.md (analog excerpts)
  </read_first>
  <action>
    Write the following FAILING tests (per the <behavior> block above):
    In internal/graph/builder_test.go — a new TestBuildGraph_EdgeOverride family (table-driven, t.Run subtests, t.Parallel() IS allowed here — no rendering):
    (a) override beats plain: view with Edges "straight", Plain true, EdgesOverride "spline" → BuildGraph(v).EdgeStyle == "spline";
    (b) absent override keeps plain suppression: Edges "spline", Plain true, EdgesOverride "" → EdgeStyle == "";
    (c) override beats model without plain: Edges "straight", Plain false, EdgesOverride "ortho" → EdgeStyle == "ortho";
    (d) the same three cases through BuildExpandedGraph.
    If the tests do not compile because View.EdgesOverride does not exist yet, add the bare field declaration `EdgesOverride string` to view.go FIRST (declaration only, zero logic) so RED fails on assertions, not compilation — tdd.md gate rule: tests must fail for the right reason.
    In cmd/c4drill/root_test.go — TestEdgesFlagValidation copying the TestFlagValidation shape (//nolint:paralleltest // go-graphviz WASM engine has concurrency issues on the subtests): valid values straight, spline, square, ortho execute past flag validation (error, if any, must NOT contain `invalid edges` — file-not-found is fine, use a dummy arg like TestFlagValidation does); invalid values diagonal, empty-after-trim note (skip empty — it means flag absent, assert NO error instead), SPLINE, and `ortho ` (trailing space) return an error containing `invalid edges`, the quoted offending value, and `straight, spline, square, or ortho`; for at least one invalid value also assert no output file was created (run against a real fixture with --output into t.TempDir() and assert the dir is empty).
    Add one failing CLI e2e cell: generateFixtureOutput(t, "plain", "dot", "--edges", "spline") → read plain.dot from the returned dir and assert it contains `splines="true"` (fails today: flag does not exist).
    Verify RED: run the new tests and confirm they FAIL (assertion or missing-flag failure — not compile errors).
  </action>
  <verify>
    <automated>go test ./internal/graph/ -run 'TestBuildGraph_EdgeOverride' -v 2>&1 | tail -5; go test ./cmd/c4drill/ -run 'TestEdgesFlagValidation' -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - internal/graph/builder_test.go contains TestBuildGraph_EdgeOverride with subtests covering BuildGraph AND BuildExpandedGraph × (beats-plain, absent-keeps-suppression, beats-model)
    - cmd/c4drill/root_test.go contains TestEdgesFlagValidation with the four valid enum values and at least three invalid values, asserting error text contains `invalid edges`
    - All new tests FAIL before implementation (RED gate) — run output shows FAIL, not build errors
    - New render-touching tests carry //nolint:paralleltest // go-graphviz WASM engine has concurrency issues; builder-only tests use t.Parallel()
  </acceptance_criteria>
  <done>RED gate satisfied: new tests fail for behavioral reasons; commit test(39-01): add failing tests for --edges override semantics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement flag, validation, threading, and builder precedence</name>
  <files>internal/view/view.go, internal/graph/builder.go, cmd/c4drill/root.go</files>
  <read_first>
    - cmd/c4drill/root.go lines 26-33 (static errors block), 49-62 (flag var block), 96-115 (registrations), 128-155 (runRoot validation + stage order), 313-341 (processView incl. PLAIN-01 block), 376-395 (processExpandedView incl. PLAIN-01 block)
    - internal/graph/builder.go lines 36-52 (BuildGraph PLAIN-02 + Graph construction) and 419-438 (BuildExpandedGraph PLAIN-02 + Graph construction)
    - internal/view/view.go lines 30-40 (Edges field + Plain field doc comments — style to match)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md (D-01..D-05 — locked, cite the IDs in comments)
  </read_first>
  <action>
    Implement exactly per the <feature> <implementation> steps 1-4 (view field if not already declared in Task 1, both builder sites, root.go flag + sentinel + validation + threading). Concrete targets: the new builder code goes immediately after the existing `if v.Plain { edgeStyle = "" }` lines in BOTH functions and reads EdgesOverride from the view; the new root.go validation sits between the format check and the render.LabelRatio assignment; the threading assignments sit at the end of both PLAIN-01 blocks. Update the PLAIN-01 comment lines to also name --edges (they already enumerate KEY-01/LBL-02 heritage). Scope discipline: internal/view/scope.go, internal/render/converter.go, and all testdata goldens must have ZERO diffs. No new imports are needed in any file (errors/fmt already imported in root.go).
    Verify GREEN: Task 1 tests now pass; full suite passes with all goldens untouched.
    Commit feat(39-01): implement --edges override with plain survival (D-01..D-05).
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./cmd/c4drill/ -run 'EdgeOverride|EdgesFlag' -v 2>&1 | tail -5 && go test ./... 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - internal/view/view.go contains `EdgesOverride string` with a doc comment citing D-03/D-05
    - internal/graph/builder.go contains the EdgesOverride application in BOTH BuildGraph and BuildExpandedGraph, positioned AFTER the v.Plain zeroing (grep finds >= 2 occurrences of EdgesOverride in builder.go)
    - cmd/c4drill/root.go contains: `edges string` var, PersistentFlags().StringVar for "edges" with enum-bearing help text, errInvalidEdges sentinel whose message lists straight, spline, square, or ortho, a validation branch before Stage 1 wrapping with %q, and EdgesOverride assignments in processView AND processExpandedView
    - git diff shows NO changes to internal/view/scope.go, internal/render/converter.go, or any file under cmd/c4drill/testdata/
    - Task 1 tests pass; go test ./... passes (GREEN gate)
  </acceptance_criteria>
  <done>GREEN gate satisfied: all new tests pass and the full suite is green; commit feat(39-01): implement --edges override</done>
</task>

<task type="auto">
  <name>Task 3: Pins — dedicated GEDGE-06 --plain pin, flag-off invariance, lint gate</name>
  <files>cmd/c4drill/root_test.go</files>
  <read_first>
    - cmd/c4drill/root_test.go lines 1951-1981 (generateFixtureOutput + readOutputFile helpers and golden-idiom comments)
    - cmd/c4drill/testdata/plain.toml (whole file — global edges = "straight" at line 8 is the author value the flag must beat)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md (D-04, D-05, D-06 — pin wording)
  </read_first>
  <action>
    Add TestEdgesSurvivesPlain (GEDGE-06 dedicated pin, //nolint:paralleltest): three cells over the plain fixture in dot format — (1) --edges spline → RAW dot contains splines="true" (explicit user intent survives --plain AND beats the author value); (2) --plain --edges spline → RAW dot STILL contains splines="true" (the D-05 pin); (3) --plain alone → RAW dot contains NO `splines=` attribute (author suppression preserved when the flag is absent — guards the KEY-02 exact-union delta from over-correcting). Add TestEdgesFlagOffInvariant (D-04/GEDGE-08): generateFixtureOutput on plain WITHOUT --edges produces dot containing splines="false" (model value still honored) — the committed plain.dot golden test already pins byte-equality; do NOT regenerate any golden.
    Run the full suite and golangci-lint; fix any lint findings in the new code only (nolint explanations, doc comments).
  </action>
  <verify>
    <automated>go test ./... 2>&1 | tail -8 && golangci-lint run ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cmd/c4drill/root_test.go contains TestEdgesSurvivesPlain with the three cells (flag alone, flag+plain, plain alone) asserting the splines= attribute per cell
    - cmd/c4drill/root_test.go contains TestEdgesFlagOffInvariant asserting splines="false" without the flag
    - go test ./... exits 0 — every existing golden (plain.dot, nolabels.dot, nolabels.expanded.dot, multilevel.expanded.dot) passes with zero re-baselining (GEDGE-08)
    - golangci-lint run ./... exits 0
    - git status shows no modified files under cmd/c4drill/testdata/
  </acceptance_criteria>
  <done>Pins green, goldens untouched, lint clean; commit test(39-01): pin --plain survival and flag-off invariance</done>
</task>

</tasks>

<verification>
- go test ./... — full suite green including ALL pre-existing canonicalDOT goldens (GEDGE-08 proves D-04)
- go test ./cmd/c4drill/ -run 'TestEdges' -v — the new family covers GEDGE-03 (accept all four values), GEDGE-04 (loud rejection), GEDGE-05 (beats model at builder + CLI tier), GEDGE-06 (plain survival pin)
- golangci-lint run ./... — clean (repo CI gate)
- git log shows the TDD gate sequence: test(39-01) RED commit before feat(39-01) GREEN commit
</verification>

<success_criteria>
- All four must_haves truths hold (override everywhere, loud invalid rejection, plain survival, flag-off byte-invariance)
- requirements GEDGE-03/04/05/06/08 each pinned by at least one automated test in this plan
- 2-3 atomic commits in TDD gate order (test → feat → test/refactor)
- Zero diffs in internal/view/scope.go, internal/render/, and cmd/c4drill/testdata/
- SUMMARY.md written per <output>
</success_criteria>

<output>
Create `.planning/phases/39-edge-style-override-edges-cli-flag/39-01-SUMMARY.md` when done
</output>
