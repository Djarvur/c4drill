---
phase: 39-edge-style-override-edges-cli-flag
plan: 02
type: execute
wave: 2
depends_on:
  - "39-01"
files_modified:
  - cmd/c4drill/root_test.go
  - cmd/c4drill/testdata/edges_override.toml
autonomous: true
requirements: [GEDGE-05, GEDGE-07]

must_haves:
  truths:
    - "The switch-matrix E2E covers --edges × generation (root / drill-down / --expanded) × --plain, asserting the graphviz splines attribute in RAW dot output for each combination (GEDGE-07)"
    - "A dedicated matrix cell proves the flag beats a per-unit edges override (D-03 pin) using a golden-safe fixture"
    - "A flag-off cell proves per-unit resolution is unchanged: the per-unit edges value still wins at its drill-down without the flag (D-04 guard)"
  artifacts:
    - path: cmd/c4drill/testdata/edges_override.toml
      provides: "golden-safe fixture with global properties edges AND a per-unit edges override on one subunit (no committed golden depends on it)"
      contains: "edges"
    - path: cmd/c4drill/root_test.go
      provides: "TestEdgesComposition matrix: style × generation × plain with splines= assertions on RAW dot, plus the D-03 per-unit pin and the D-04 flag-off cell"
      contains: "TestEdgesComposition"
  key_links:
    - from: "cmd/c4drill/root_test.go TestEdgesComposition"
      to: "generateFixtureOutput helper"
      via: "matrix cells run the real CLI with --edges/--plain extras into t.TempDir()"
      pattern: "generateFixtureOutput"
    - from: "RAW dot output"
      to: "splines attribute"
      via: "assert.Contains on the generated dot file content per matrix cell"
      pattern: "splines="
---

<objective>
Extend the KEY-03-style switch matrix E2E with the `--edges` case (GEDGE-07).

Purpose: The matrix is the phase's behavioral contract surface — it proves the flag works across every generation and composes with `--plain` exactly as D-05/D-06 specify, and pins the per-unit override precedence (D-03) that no unit test can show end-to-end.

Output: New golden-safe fixture `edges_override.toml` + `TestEdgesComposition` matrix in cmd/c4drill/root_test.go.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md
@.planning/phases/39-edge-style-override-edges-cli-flag/39-RESEARCH.md
@.planning/phases/39-edge-style-override-edges-cli-flag/39-PATTERNS.md

<interfaces>
<!-- Contracts from plan 39-01 (wave 1) that this plan consumes: -->
- cmd/c4drill/root.go exposes persistent flag `--edges <style>` (straight|spline|square|ortho), validated loudly, threaded invocation-globally, surviving --plain
- internal/view/view.go View has EdgesOverride string (default empty)

Existing harness (cmd/c4drill/root_test.go):
- generateFixtureOutput(t, fixture, format string, extraArgs ...string) string — runs the CLI on testdata/<fixture> into a fresh t.TempDir() with extra args, requires clean exit, returns the dir (lines 1951-1969)
- readOutputFile(t, path) string — reads a generated file for assertions
- keyDrilldowns() map — fixture → drill-down relative path (plain → plain/orders.dot; multilevel → multilevel/mainSystem.dot; styles → styles/app.dot)
- TestKeyComposition (lines 1998-2035) — the matrix organization to mirror: per-switch → per-fixture → per-generation subtests
- ALL tests in this file that render carry //nolint:paralleltest // go-graphviz WASM engine has concurrency issues

Fixture conventions (cmd/c4drill/testdata/plain.toml:8):
  edges = "straight" # author edge routing — ignored under --plain
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → render pipeline | Fixtures are committed repo content (trusted); no untrusted input crosses in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | new testdata fixture | accept | Fixture is committed repo content rendered in-process by tests; no user-controlled input, no new attack surface (security-enforcement formality — no V5 surface in test-only changes) |
| T-39-SC | Tampering | package installs | accept | No packages installed in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the golden-safe per-unit-edges fixture</name>
  <files>cmd/c4drill/testdata/edges_override.toml</files>
  <read_first>
    - cmd/c4drill/testdata/plain.toml (whole file — the fixture house style: properties block with name + edges + a small system/unit set, annotated values)
    - cmd/c4drill/testdata/multilevel.toml (whole file — the multi-level shape: system with subunits so a drill-down and a per-unit override are expressible)
    - internal/validator/ rules for minimum valid model shape (a link between units where required) — read testdata/valid.toml as the minimal passing example
  </read_first>
  <action>
    Create cmd/c4drill/testdata/edges_override.toml: a minimal but drillable model in the plain.toml/multilevel.toml house style — properties with a name and global edges = "spline" (annotated: CLI flag must beat this global value), at least one top-level system containing at least two subunits where exactly ONE subunit carries a per-unit edges = "ortho" (annotated: --edges must beat this per-unit value per D-03), and a link so the diagram renders edges. Constraints: the file must render cleanly through the full CLI (it becomes a matrix fixture), and NO committed golden may depend on it — it is new, so grep the test suite to confirm nothing references edges_override before this plan.
    Verify the fixture renders: run the CLI on it with --format dot into a temp dir and confirm non-empty dot output for C1, the system drill-down, and --expanded.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && tmp=$(mktemp -d) && go run ./cmd/c4drill cmd/c4drill/testdata/edges_override.toml --format dot --output "$tmp" && ls "$tmp" && go test ./cmd/c4drill/ 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - cmd/c4drill/testdata/edges_override.toml exists and parses as valid TOML through the full pipeline (CLI exits 0)
    - properties carries edges = "spline" (global) and exactly one subunit carries its own edges = "ortho" (per-unit)
    - The run produces C1 dot, at least one drill-down dot, and an .expanded dot in the output dir
    - grep -r edges_override --include="*_test.go" finds no reference BEFORE this plan's Task 2 (fixture introduces no golden churn — GEDGE-08 stays intact: go test ./... still passes with existing goldens)
  </acceptance_criteria>
  <done>Fixture committed, renders cleanly through all three generations, zero golden impact</done>
</task>

<task type="auto">
  <name>Task 2: TestEdgesComposition — the GEDGE-07 matrix</name>
  <files>cmd/c4drill/root_test.go</files>
  <read_first>
    - cmd/c4drill/root_test.go lines 1998-2110 (TestKeyComposition + keySwitches + runKeySwitchMatrix — mirror this organization), lines 1951-1969 (generateFixtureOutput), and the Task-1/2 output of plan 39-01 (TestEdgesSurvivesPlain — do not duplicate its cells, reference its intent)
    - internal/render/converter.go lines 264-271 (the splines mapping your assertions encode: spline→splines="true", straight→splines="false", ortho AND square→splines="ortho", empty→attribute absent)
    - .planning/phases/39-edge-style-override-edges-cli-flag/39-CONTEXT.md (D-03, D-05, D-06 pin wording)
  </read_first>
  <action>
    Add TestEdgesComposition (//nolint:paralleltest // go-graphviz WASM engine has concurrency issues), organized as subtests style × generation × plain, over fixtures plain and edges_override:
    For each style in spline, straight, ortho, square: for each generation in C1 (root dot), drilldown (the fixture's system drill-down dot — plain fixture drills to plain/orders.dot; edges_override drills to its system dot), expanded (the {base}.expanded.dot): run generateFixtureOutput with --format dot and --edges <style>, read the RAW dot, and assert.Contains the exact attribute the converter emits: splines="true" for spline, splines="false" for straight, splines="ortho" for BOTH ortho and square (the GEDGE-02 alias must survive the flag path).
    Plain composition (GEDGE-07 × D-05): repeat the 4 styles × 3 generations with --plain --edges <style> — the SAME splines= value must appear in every cell (explicit flag survives plain suppression everywhere).
    Plain-alone guard cell: --plain with NO --edges on the plain fixture → dot contains NO `splines=` attribute (author suppression intact; complements 39-01's TestEdgesSurvivesPlain cell 3).
    D-03 pin (per-unit override): on the edges_override fixture, run --edges spline and assert the PER-UNIT system's drill-down dot contains splines="true" (flag beats that unit's edges="ortho"); then the D-04 flag-off cell: same drill-down WITHOUT the flag contains splines="ortho" (per-unit resolution unchanged without the flag), and the C1 dot contains splines="true" (global properties value wins at C1 where no unit override applies).
    Keep assertions on RAW dot content (the dot surface is the precise suppression surface per the KEY-03 header comment); no golden files are created or compared in this test.
  </action>
  <verify>
    <automated>go test ./cmd/c4drill/ -run 'TestEdgesComposition' -v 2>&1 | tail -6 && go test ./... 2>&1 | tail -5 && golangci-lint run ./... 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - cmd/c4drill/root_test.go contains TestEdgesComposition covering 4 styles × 3 generations × {flag, flag+plain} = 24 asserting cells plus the plain-alone guard, D-03 per-unit pin, and D-04 flag-off cells
    - Every splines assertion uses the exact converter output strings ("true", "false", "ortho") — square asserts splines="ortho" (GEDGE-02 alias)
    - The D-03 cell proves flag-beats-per-unit on the drill-down generation specifically
    - go test ./... exits 0 (all existing goldens still untouched — GEDGE-08); golangci-lint clean
    - The test uses generateFixtureOutput and asserts on RAW dot text; no new golden files added
  </acceptance_criteria>
  <done>Matrix green across all cells; GEDGE-07 satisfied end-to-end; commit test(39-02): add --edges switch-matrix E2E (GEDGE-07)</done>
</task>

</tasks>

<verification>
- go test ./cmd/c4drill/ -run 'TestEdgesComposition' -v — every matrix cell green
- go test ./... — full suite green, zero golden re-baselining (GEDGE-08 holds)
- golangci-lint run ./... — clean
- grep -c "splines=" assertions in TestEdgesComposition covers all four enum values including the square→ortho alias
</verification>

<success_criteria>
- GEDGE-07 fully satisfied: --edges × generation (root / drill-down / --expanded) × --plain asserted via the graphviz splines attribute in RAW dot output
- D-03 (flag beats per-unit edges) and D-04 (flag-off resolution unchanged) each pinned by a dedicated matrix cell
- 2 commits (fixture, matrix), zero golden churn, lint clean
- SUMMARY.md written per <output>
</success_criteria>

<output>
Create `.planning/phases/39-edge-style-override-edges-cli-flag/39-02-SUMMARY.md` when done
</output>
