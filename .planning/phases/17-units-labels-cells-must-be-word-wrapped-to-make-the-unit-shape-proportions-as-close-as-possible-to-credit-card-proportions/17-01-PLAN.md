---
phase: 17-units-labels-cells-must-be-word-wrapped-to-make-the-unit-shape-proportions-as-close-as-possible-to-credit-card-proportions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/render/wrap.go
  - internal/render/wrap_test.go
  - internal/render/labels.go
  - internal/render/converter.go
  - internal/render/render.go
  - cmd/c4drill/root.go
autonomous: false
requirements: [WRAP-01, WRAP-02, WRAP-03, WRAP-04, WRAP-05]
user_setup: []

must_haves:
  truths:
    - "User can configure ratio via --label-ratio flag"
    - "User can configure ratio via C4DRILL_LABEL_RATIO env var"
    - "Long text wraps at word boundaries"
    - "Very long words break at character boundaries"
    - "All label fields (name, technology, description) are wrapped"
  artifacts:
    - path: "internal/render/wrap.go"
      provides: "Word-wrap implementation with character fallback"
      exports: ["wrapText", "estimateCharsFromWidth", "calculateTextWidth"]
      min_lines: 40
    - path: "internal/render/wrap_test.go"
      provides: "Unit tests for wrapping functions"
      contains: "TestWrapText"
    - path: "cmd/c4drill/root.go"
      provides: "CLI flag and env var handling"
      contains: "label-ratio"
  key_links:
    - from: "cmd/c4drill/root.go"
      to: "internal/render"
      via: "labelRatio variable passed to render functions"
      pattern: "labelRatio"
    - from: "internal/render/wrap.go"
      to: "internal/render/labels.go"
      via: "wrapText() called before html.EscapeString()"
      pattern: "wrapText\\("
---

<objective>
Implement word-wrapping for HTML label cells to achieve configurable width:height ratios (default 8/5 = 1.6:1 credit card proportions).

Purpose: Long names and descriptions currently stretch units horizontally. Wrapping constrains width to create more balanced, credit-card-like proportions.
Output: Word-wrapped labels with configurable ratio via CLI flag and environment variable.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-units-labels-cells-must-be-word-wrapped-to-make-the-unit-shape-proportions-as-close-as-possible-to-credit-card-proportions/17-CONTEXT.md
@.planning/phases/17-units-labels-cells-must-be-word-wrapped-to-make-the-unit-shape-proportions-as-close-as-possible-to-credit-card-proportions/17-RESEARCH.md

<interfaces>

From internal/graph/graph.go (Label struct):
```go
type Label struct {
    Name        string  // Primary name (with [+] indicator if expandable)
    Technology  string  // Technology string (optional)
    Description string  // Description text (optional)
    Icon        string  // Emoji/icon prefix (optional)
}
```

From cmd/c4drill/root.go (existing flag pattern):
```go
var (
    format    string
    outputDir string
    expanded  bool
    version   = "dev"
)

func NewRootCmd() *cobra.Command {
    cmd := &cobra.Command{...}
    cmd.PersistentFlags().StringVarP(&format, "format", "f", formatSVG, ...)
    cmd.PersistentFlags().StringVarP(&outputDir, "output", "o", "", ...)
    cmd.PersistentFlags().BoolVar(&expanded, "expanded", false, ...)
    return cmd
}
```

From internal/render/labels.go (label builder pattern):
```go
func buildPersonHTMLLabel(label *graph.Label, iconRelPath string) string {
    // ...
    sb.WriteString(`<td valign="bottom"><b>`)
    sb.WriteString(html.EscapeString(label.Name))  // <-- wrapText() goes here
    sb.WriteString(`</b></td>`)
    // ...
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write interface contracts and failing tests</name>
  <files>internal/render/wrap.go, internal/render/wrap_test.go</files>
  <behavior>
    - Test 1: wrapText("hello world", 10) returns "hello world" (fits in one line)
    - Test 2: wrapText("hello world", 5) returns "hello&lt;BR/&gt;world" (word break)
    - Test 3: wrapText("abcdefghij", 5) returns "abcde&lt;BR/&gt;fghij" (forced char break)
    - Test 4: wrapText("a b c", 10) returns "a b c" (short text unchanged)
    - Test 5: wrapText("", 10) returns "" (empty string)
    - Test 6: wrapText handles multi-byte unicode correctly
    - Test 7: estimateCharsFromWidth(100) returns reasonable character estimate
    - Test 8: calculateTextWidth(3, 1.6) returns positive width in points
  </behavior>
  <action>
    Create internal/render/wrap.go with stub functions that panic:
    - wrapText(text string, maxChars int) string
    - estimateCharsFromWidth(widthPoints int) int
    - calculateTextWidth(rowCount int, ratio float64) int

    Create internal/render/wrap_test.go with test cases above.
    Run tests to confirm they FAIL (RED phase).

    Commit: test(17-01): add failing tests for word-wrap functions
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestWrap -v 2>&1 | grep -E "(FAIL|panic)"</automated>
  </verify>
  <done>wrap.go and wrap_test.go exist, tests fail as expected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement word-wrap functions</name>
  <files>internal/render/wrap.go, internal/render/wrap_test.go</files>
  <behavior>
    - All 8 tests from Task 1 must pass
    - wrapText uses utf8.RuneCountInString for character counting
    - wrapText breaks at word boundaries first
    - wrapText forces character break for words exceeding maxChars
    - wrapText returns "&lt;BR/&gt;" for line breaks (GraphViz HTML)
  </behavior>
  <action>
    Implement wrapText in wrap.go:
    1. Use strings.Fields() to split into words
    2. Use utf8.RuneCountInString() for character count (multi-byte support)
    3. Build lines: if word fits, add to current line; else start new line
    4. For words exceeding maxChars, force break into chunks using []rune
    5. Join lines with "&lt;BR/&gt;" (GraphViz HTML line break, uppercase required)

    Implement estimateCharsFromWidth(widthPoints int) int:
    - Use ~8 points per character as approximation
    - Return widthPoints / 8

    Implement calculateTextWidth(rowCount int, ratio float64) int:
    - pointsPerRow = 18 (12pt font + 6pt padding)
    - iconColumnWidth = 36 (fixed)
    - totalHeight = rowCount * pointsPerRow
    - totalWidth = totalHeight * ratio
    - textWidth = totalWidth - iconColumnWidth
    - Minimum 50 points for readability

    Run tests until all pass (GREEN phase).

    Commit: feat(17-01): implement word-wrap with character fallback
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestWrap -v</automated>
  </verify>
  <done>All 8 wrap tests pass, functions implemented correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add --label-ratio CLI flag with env var fallback</name>
  <files>cmd/c4drill/root.go</files>
  <action>
    Add labelRatio package variable following existing pattern:
    1. Add `labelRatio float64` to var block (line 34)
    2. In NewRootCmd(), add:
       `cmd.PersistentFlags().Float64Var(&amp;labelRatio, "label-ratio", 0, "Width:height ratio for unit labels (default: 1.6)")`
    3. Create getLabelRatio() helper function:
       - If labelRatio &gt; 0, return it (CLI flag takes precedence)
       - Else if C4DRILL_LABEL_RATIO env var is set, parse and return it
       - Else return 1.6 (default credit card ratio)
    4. Update runRoot() to use getLabelRatio() and pass to render functions

    Follow the existing flag pattern exactly (see format/outputDir/expanded flags).

    Commit: feat(17-01): add --label-ratio CLI flag and C4DRILL_LABEL_RATIO env var
  </action>
  <verify>
    <automated>go build ./cmd/c4drill &amp;&amp; ./c4drill --help | grep label-ratio</automated>
  </verify>
  <done>CLI flag documented in --help, getLabelRatio() returns correct value</done>
</task>

<task type="auto">
  <name>Task 4: Integrate word-wrap into HTML label builders</name>
  <files>internal/render/labels.go</files>
  <action>
    Modify all 6 HTML label builder functions to wrap text:

    For buildPersonHTMLLabel, buildDbHTMLLabel, buildQueueHTMLLabel, buildSystemHTMLLabel, buildContainerHTMLLabel, buildComponentHTMLLabel:

    1. Add ratio parameter to each function signature:
       `func buildXHTMLLabel(label *graph.Label, iconRelPath string, ratio float64) string`

    2. Calculate rowCount and textWidth:
       - rowCount = count of present fields (name + optional tech + optional desc)
       - textWidth = calculateTextWidth(rowCount, ratio)
       - maxChars = estimateCharsFromWidth(textWidth)

    3. Create wrapAndEscape helper that:
       - Wraps raw text first using wrapText()
       - Then escapes each line with html.EscapeString()
       - Rejoins with "&lt;BR/&gt;"

    4. Replace all `html.EscapeString(label.Name)` with `wrapAndEscape(label.Name, maxChars)`
       (Same for Technology and Description)

    5. Add WIDTH attribute to text td cells:
       `&lt;td width="[textWidth]" valign="bottom"&gt;`

    Commit: feat(17-01): integrate word-wrap into HTML label builders
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestHTML -v</automated>
  </verify>
  <done>All HTML label tests pass, labels contain &lt;BR/&gt; for wrapped content</done>
</task>

<task type="auto">
  <name>Task 5: Wire ratio from CLI through to label builders</name>
  <files>internal/render/converter.go, internal/render/render.go</files>
  <action>
    Update the call chain to pass labelRatio:

    1. In render.go, update RenderSVGWithOutput signature to accept ratio:
       `func RenderSVGWithOutput(g *graph.Graph, outputDir string, ratio float64) ([]byte, error)`
       Also update Render() to accept ratio for consistency.

    2. In converter.go, update NewConverter to accept and store ratio:
       - Add ratio field to Converter struct
       - Update NewConverter signature
       - Pass ratio to all buildXHTMLLabel calls

    3. In cmd/c4drill/root.go, pass getLabelRatio() result:
       - Update RenderSVGWithOutput call to include ratio
       - Update Render call to include ratio

    Ensure ratio flows: CLI flag/env -&gt; getLabelRatio() -&gt; RenderSVGWithOutput -&gt; Converter -&gt; buildXHTMLLabel

    Commit: feat(17-01): wire label-ratio from CLI to label builders
  </action>
  <verify>
    <automated>go test ./... -v 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass, ratio flows from CLI to labels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify word-wrapped labels in generated diagrams</name>
  <files>N/A</files>
  <action>
    Verify the complete word-wrapping system works correctly:
    1. Build the binary: go build ./cmd/c4drill
    2. Generate a diagram with long text
    3. Verify unit shapes appear more square-like
    4. Verify long names/descriptions wrap
    5. Test custom ratio flag
    6. Test env var configuration
  </action>
  <verify>
    <what-built>Complete word-wrapping system for HTML labels with configurable ratio</what-built>
    <how-to-verify>
      1. Build the binary: `go build ./cmd/c4drill`
      2. Generate a diagram with long text: `./c4drill testdata/example.toml -o /tmp/test-wrap`
      3. Open the SVG in a browser: `open /tmp/test-wrap/example.svg`
      4. Verify unit shapes appear more square-like (not stretched horizontally)
      5. Verify long names/descriptions wrap to multiple lines
      6. Test custom ratio: `./c4drill testdata/example.toml --label-ratio 2.0 -o /tmp/test-ratio`
      7. Test env var: `C4DRILL_LABEL_RATIO=1.2 ./c4drill testdata/example.toml -o /tmp/test-env`
    </how-to-verify>
    <resume-signal>Type "approved" or describe issues found</resume-signal>
  </verify>
  <done>User confirms word-wrapping works correctly in generated diagrams</done>
</task>

</tasks>

<verification>
1. All unit tests pass: `go test ./... -v`
2. CLI help shows --label-ratio flag: `./c4drill --help | grep label-ratio`
3. Generated SVGs contain &lt;BR/&gt; in label text: `grep -c "BR/" output.svg`
4. Word-wrap handles multi-byte characters correctly (run TestWrap/unicode)
</verification>

<success_criteria>
- [ ] wrapText function wraps at word boundaries
- [ ] wrapText forces character break for long words
- [ ] --label-ratio CLI flag works
- [ ] C4DRILL_LABEL_RATIO env var works
- [ ] All label fields (name, technology, description) are wrapped
- [ ] All existing tests still pass
- [ ] Unit shapes approximate target ratio
</success_criteria>

<output>
After completion, create `.planning/phases/17-units-labels-cells-must-be-word-wrapped-to-make-the-unit-shape-proportions-as-close-as-possible-to-credit-card-proportions/17-01-SUMMARY.md`
</output>
