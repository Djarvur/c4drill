---
phase: 33-docs-sweep-end-to-end-goldens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/examples/06-templates.toml
  - skill/examples/07-relative-peer.toml
  - skill/examples/08-include/entry.toml
  - skill/examples/08-include/auth.toml
  - skill/examples/08-include/billing.toml
  - skill/examples/09-composed/entry.toml
  - skill/examples/09-composed/templates.toml
  - skill/examples/09-composed/domains/auth.toml
  - skill/examples/09-composed/single-file-equivalent.toml
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "skill/examples/06-templates.toml exists and demonstrates [template.*] define + [[use]] instantiate, with ${param} substitution into Name/Description/Technology/Reference, a fixed link set, and a subunit subtree (TMPL-04)"
    - "skill/examples/07-relative-peer.toml exists and demonstrates bare peers resolving via walk-up: a sibling match, an aunt (grandparent) match, a root (top-level) match, and an absolute-fallback peer containing a dot (ERGO-01/02 per D-13/D-14/D-15/D-16)"
    - "skill/examples/08-include/ is a multi-file set (entry.toml + 2 included files) demonstrating transitive include, once=true, and cross-file subunits (D-10: an included file defines subunits under a top-level unit declared in the entry file)"
    - "skill/examples/09-composed/ is a multi-file set (entry.toml + templates.toml + domains/auth.toml) that uses templates + include + relative-peer + reference together, AND ships a single-file-equivalent.toml that is the hand-expanded equivalent (templates inlined, includes spliced, relative peers absolute, references inlined)"
    - "Every fixture conforms to the v1.10 grammar (the final state after Phases 30-32 ship): [template.*], [[use]], [[include]] tables; bare relative peers; reference URLs"
    - "Each fixture has a header comment naming the feature(s) it demonstrates and the requirement IDs it covers"
    - "The 09-composed/single-file-equivalent.toml, when rendered through the full pipeline after 30-32 ship, produces DOT that canonicalizes EQUAL to 09-composed/entry.toml rendered the same way (this is asserted by Plan 04's XC-05 test; the fixtures are authored here, the assertion lands in Wave 2)"
  artifacts:
    - skill/examples/06-templates.toml
    - skill/examples/07-relative-peer.toml
    - skill/examples/08-include/entry.toml
    - skill/examples/08-include/auth.toml
    - skill/examples/08-include/billing.toml
    - skill/examples/09-composed/entry.toml
    - skill/examples/09-composed/templates.toml
    - skill/examples/09-composed/domains/auth.toml
    - skill/examples/09-composed/single-file-equivalent.toml
  key_links:
    - "skill/examples/06-templates.toml referenced by README.md templates section pointer (Plan 03) and SKILL.md templates section pointer (Plan 03)"
    - "skill/examples/08-include/ referenced by README.md include section pointer (Plan 03)"
    - "skill/examples/09-composed/ read by cmd/c4drill/root_test.go XC-05 + XC-01 tests (Plan 04)"
---

<objective>
Author the four example-fixture sets (D-17): three per-feature standalones (`06-templates.toml`, `07-relative-peer.toml`, `08-include/`) and one composed multi-file set (`09-composed/`) that doubles as the XC-05 golden fixture. These are the runnable tutorials that DOC-03 requires, and the composed set is the input pair (multi-file + hand-expanded single-file) that Plan 04's XC-05/XC-01 tests render and compare.

Purpose: CONTEXT D-17 specifies a two-tier fixture set — per-feature standalones for learning, plus one composed example that proves all four features work together. The composed set ships BOTH the multi-file form AND its hand-expanded single-file equivalent so the XC-05 "≡ single-file" test compares two real committed artifacts, not synthetics. Reference, omittable-name, and omittable-type are already demonstrated in `05-ecommerce.toml` (Phase 28 added Stripe's `reference`; Phase 29 documented humanization), so no new fixtures for those three.

Output: 9 committed TOML files across `skill/examples/06-*.toml`, `07-*.toml`, `08-include/` (3 files), and `09-composed/` (4 files). Each file is valid v1.10 grammar, loadable by `c4drill` once Phases 30-32 ship, and styled to match the existing `01-05` graduated tutorial set.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md
@.planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md
@.planning/phases/31-template-expansion/31-CONTEXT.md
@.planning/phases/32-include-directive-multi-file/32-CONTEXT.md
@.planning/phases/30-relative-peer-resolution/30-CONTEXT.md

<interfaces>
<!-- v1.10 grammar reference (the target grammar after Phases 30-32 ship). -->

// Template define + instantiate (TMPL-01..04, from 31-CONTEXT D-03/D-04):
[template.microservice]
params = ["name", "domain", "tech", "upstreamBus"]   # declared named params (all required, TMPL-02)
name = "${name} Service"                              # ${param} substitution (TMPL-03)
type = "container"
technology = "${tech}"
description = "${name} handles ${domain}"
reference = "https://wiki.example.com/${name}"        # reference param substitution (TMPL-10)
[[template.microservice.link]]                        # FIXED link count (TMPL-03); fields parametrized
peer = "${upstreamBus}"
description = "Publishes ${domain} events"

[template.microservice.cache]                         # subunit subtree (TMPL-04); key verbatim (D-04)
type = "componentDb"

[[use]]                                               # instantiation (D-03)
template = "microservice"
parent = "platform"                                   # placement; omitted/empty = top-level (D-03)
name = "auth"                                         # fills top-level produced unit's last segment (D-04)
domain = "authentication"
tech = "Go, gRPC"
upstreamBus = "messageBus"

// Include directive (INC-01..10, from 32-CONTEXT D-09/D-10/D-11):
[[include]]                                           # bare table name (D-06)
path = "templates/common.toml"                        # relative to INCLUDING file's dir (INC-02)
once = true                                           # include_once semantics (INC-06)

[[include]]
path = "domains/auth.toml"

// Cross-file subunits (D-10): entry has [platform]; auth.toml has [platform.auth], [platform.db]

// Relative-peer walk-up (ERGO-01/02, from 30-CONTEXT D-13/D-14/D-15/D-16):
[platform.auth]                                       # enclosing parent is platform
peer = "cache"                                        # bare → walk-up: sibling platform.cache? else root cache
peer = "platform.cache"                               # absolute (has dot) → resolved as-is (D-16 step 1)

// Fixture style template (verified read of skill/examples/05-ecommerce.toml, 02-nested.toml):
//   - header comment: "# <Title> Example\n# Demonstrates <features>"
//   - [properties] block first (name, description, optional edges/expanded)
//   - units grouped by domain with "# ===== SECTION =====" separators
//   - explicit type= on every unit (pre-DOC-01 style; fixtures stay explicit for clarity)
//   - [[unit.link]] array-of-tables for relationships
</interfaces>
</context>

<feature>
  <name>Example fixtures (D-17 two-tier set)</name>
  <files>skill/examples/06-templates.toml, skill/examples/07-relative-peer.toml, skill/examples/08-include/*, skill/examples/09-composed/*</files>
  <behavior>
    Four fixture sets, all valid v1.10 grammar, all loadable by `c4drill` once Phases 30-32 ship:

    **06-templates.toml** — standalone templates tutorial. Demonstrates: defining a `[template.microservice]` with declared params + a subunit subtree (TMPL-04); instantiating it 2x via `[[use]]` with distinct param values; `${param}` substitution into Name/Description/Technology/Reference (TMPL-03, TMPL-10); a fixed link set whose peer field is parametrized. Must NOT use include or relative-peer (those have their own fixtures). All units get explicit `type =` for clarity (the fixture is a tutorial, not a DOC-01 demo).

    **07-relative-peer.toml** — standalone relative-peer tutorial. Demonstrates the 4 resolution cases from 30-CONTEXT's Specific Ideas: (a) sibling match (`[sys.api]` with `peer = "cache"` where `[sys.cache]` exists); (b) aunt/grandparent match (`[sys.api.handlers.auth]` with `peer = "queue"` resolving to `[sys.queue]` via walk-up); (c) root/top-level match (`[sys.api.handlers.auth]` with `peer = "messageBus"` resolving to top-level `[messageBus]`); (d) absolute fallback (`[sys.api]` with `peer = "external.gateway"` — has a dot, resolved as-is per D-16 step 1). Must NOT use templates or include.

    **08-include/** — standalone include tutorial (3 files). `entry.toml` has `[platform]` + 2 `[[include]]` directives (`auth.toml` and `billing.toml`, one with `once=true`). `auth.toml` defines `[platform.auth]` + `[platform.auth.db]` (cross-file subunits, D-10). `billing.toml` defines `[platform.billing]`. One of the two includes is `once=true` to demonstrate dedup. Demonstrates transitive include (one included file itself includes a shared file), relative-to-including-file paths (INC-02), and flat merge. Must NOT use templates.

    **09-composed/** — the composed multi-file set that doubles as the XC-05 golden fixture. Uses ALL FOUR features together: `entry.toml` does `[[include]] path="templates.toml" once=true` then `[[include]] path="domains/auth.toml"` then `[[use]] template="microservice" parent="platform" name="auth" ...` with a relative peer; `templates.toml` defines `[template.microservice]` with a subunit + a parametrized reference URL + a fixed link with a parametrized peer; `domains/auth.toml` defines cross-file subunits appending to `[platform]`. PLUS `single-file-equivalent.toml` — the hand-expanded equivalent (template inlined as concrete units, includes spliced, relative peers rewritten absolute, references inlined) that renders to identical DOT (asserted in Plan 04).

    Every fixture carries a header comment naming the feature(s) and requirement IDs. Fixture style matches `05-ecommerce.toml` (header comment, [properties] first, section separators, explicit type=).
  </behavior>
  <implementation>
    **Authoring order (matters for the composed set):**
    1. `06-templates.toml`, `07-relative-peer.toml`, `08-include/*` — independent, author in parallel. Keep each small (10-20 units) and self-documenting via comments. Use a coherent mini-domain per fixture (e.g. 06 = "payment platform with 2 microservice instances"; 07 = "a system with handlers needing queue access"; 08 = "a platform split into auth + billing domains").
    2. `09-composed/` — author the multi-file set FIRST (entry + templates + domains/auth). Render it through the pipeline (after 30-32 ship — this is why execution waits) and capture the DOT. Then hand-author `single-file-equivalent.toml` by inlining every `[[use]]` as concrete units (substitute the params into each field), splicing every `[[include]]`'s units directly into the single file (preserving order per D-09: entry units first, then included units in include order), and rewriting every bare relative peer to its resolved absolute path. Verify `canonical.Canonical(multi-file-DOT) == canonical.Canonical(single-file-DOT)` BEFORE committing the golden (Pitfall 4 — the single-file side must be correct).

    **06-templates.toml shape (sketch — executor refines to a coherent domain):**
    - `[properties]` with name/description.
    - `[template.microservice]` with `params = ["name", "tech", "upstreamBus"]`, `${param}` in name/description/technology/reference, a `[[template.microservice.link]]` with `peer = "${upstreamBus}"`, and a `[template.microservice.cache]` subunit (TMPL-04).
    - 2x `[[use]]` instantiating it under the same parent with different names (e.g. `auth`, `billing`), different tech, different upstreamBus.
    - A top-level `[messageBus]` the parametrized peer resolves to.
    - A `[customer]` person linking to one of the instantiated units (proves instantiated units participate in views, TMPL-05).

    **07-relative-peer.toml shape (the 4 cases must all be present and labeled with comments):**
    - `[messageBus]` (top-level — for the root-match case).
    - `[platform]` system with `[platform.cache]`, `[platform.queue]`, `[platform.api]` (sibling case: api.peer=cache), `[platform.api.handlers]`, `[platform.api.handlers.auth]` (aunt case: auth.peer=queue → platform.queue), and the same auth with `peer=messageBus` (root case). Plus `[platform.api]` with `peer="externalGateway.gateway"` or an external unit with a dotted peer (absolute case).

    **08-include/ shape:**
    - `entry.toml`: `[properties]`, `[platform]` (just the parent unit, no subunits), `[[include]] path="auth.toml"`, `[[include]] path="billing.toml" once=true`, `[customer]` linking to `platform.auth`.
    - `auth.toml`: `[platform.auth]`, `[platform.auth.db]` (cross-file subunits under entry's platform, D-10). Optionally a transitive `[[include]] path="shared/colors.toml"` (or skip transitive to keep it 3 files — planner's call; transitive is demonstrated sufficiently by the 2-level chain).
    - `billing.toml`: `[platform.billing]`.

    **09-composed/ shape (the XC-05 fixture — must be the cleanest):**
    - `entry.toml`: `[properties]`, `[platform]`, `[messageBus]`, `[[include]] path="templates.toml" once=true`, `[[include]] path="domains/auth.toml"`, `[[use]] template="microservice" parent="platform" name="auth" domain="authentication" tech="Go, gRPC" upstreamBus="messageBus"`, a `[customer]` linking to `platform.auth`. Keep it SMALL (the golden must be auditable by a human).
    - `templates.toml`: `[template.microservice]` (params, ${param} in fields including reference, one parametrized link, one subunit). This file is included `once=true` so it's safe to re-include.
    - `domains/auth.toml`: `[platform.auditLog]` (a cross-file subunit under entry's platform, D-10) — proves include + cross-file-subunit composition.
    - `single-file-equivalent.toml`: the hand-expanded result. The `[[use]]` becomes `[platform.auth]` with all `${param}` substituted to concrete values, the subunit inlined as `[platform.auth.cache]`, the parametrized link rewritten with the concrete peer. The `[[include]] path="domains/auth.toml"` becomes the inline `[platform.auditLog]`. The `[[include]] path="templates.toml"` is DROPPED (the template definition is not a unit; after expansion only the instantiated units remain). Relative peers (if any in the template) are rewritten to their absolute resolved paths.

    All fixtures use explicit `type =` (fixtures are tutorials, not DOC-01 demos — explicit is clearer). All headers cite the feature + REQ-IDs. All multi-file paths are relative (INC-02).
  </implementation>
</feature>

<tasks>

<task type="execute">
  <name>Author 06-templates.toml (standalone templates fixture)</name>
  <files>skill/examples/06-templates.toml</files>
  <read_first>
    - skill/examples/05-ecommerce.toml (fixture style template — header comment, [properties] first, section separators, [[unit.link]] form)
    - skill/examples/02-nested.toml (smaller fixture style for nesting)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-17 fixture spec)
    - .planning/phases/31-template-expansion/31-CONTEXT.md (D-03 [[use]] syntax, D-04 name param semantics, TMPL-01..10 contract, the instantiation syntax sketch in Specific Ideas)
    - .planning/REQUIREMENTS.md (TMPL-01 through TMPL-10 full text)
  </read_first>
  <action>
    Create `skill/examples/06-templates.toml`. Header comment: `# Templates Example\n# Demonstrates [template.*] define + [[use]] instantiate with ${param} substitution (TMPL-01..10)`. Include a `[properties]` block. Define `[template.microservice]` with `params = ["name", "tech", "upstreamBus"]`, `${param}` substitution into name/description/technology/reference (TMPL-03, TMPL-10), one `[[template.microservice.link]]` with `peer = "${upstreamBus}"` (fixed link count, parametrized field — TMPL-03), and one subunit `[template.microservice.cache]` (TMPL-04). Instantiate it 2x via `[[use]]` under the same parent with distinct params (D-03/D-04). Include a top-level `[messageBus]` unit that the parametrized peer resolves to, and a `[customer]` person linking to one instantiated unit (proves TMPL-05 participation). Every unit gets explicit `type =`. Keep it under ~25 units. Do NOT use `[[include]]` or bare relative peers (those have their own fixtures).
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; head -5 skill/examples/06-templates.toml &amp;&amp; grep -c '\[\[use\]\]\|\[template\.' skill/examples/06-templates.toml</verify>
  <acceptance_criteria>
    - `skill/examples/06-templates.toml` exists
    - `grep -c '\[template\.' skill/examples/06-templates.toml` returns at least 2 (the template + its subunit, or 2 templates)
    - `grep -c '\[\[use\]\]' skill/examples/06-templates.toml` returns at least 2 (two instantiations)
    - `grep '\${' skill/examples/06-templates.toml` returns at least 3 matches (param substitution in multiple fields)
    - `grep 'reference = "https' skill/examples/06-templates.toml` returns at least 1 (TMPL-10 reference param)
    - Header comment names the feature + cites TMPL requirement IDs
    - Every unit has explicit `type =`
    - Commit message starts with `docs(33-02):` (fixture authoring is docs-shaped)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Author 07-relative-peer.toml (standalone relative-peer fixture, 4 cases)</name>
  <files>skill/examples/07-relative-peer.toml</files>
  <read_first>
    - skill/examples/05-ecommerce.toml (fixture style)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-17 fixture spec)
    - .planning/phases/30-relative-peer-resolution/30-CONTEXT.md (D-13/D-14/D-15/D-16 walk-up semantics; the 5 example resolution traces in Specific Ideas)
    - .planning/REQUIREMENTS.md (ERGO-01, ERGO-02)
  </read_first>
  <action>
    Create `skill/examples/07-relative-peer.toml`. Header comment: `# Relative Peer Resolution Example\n# Demonstrates bare peer walk-up: sibling, aunt, root, and absolute-fallback (ERGO-01/02)`. Include `[properties]`. Build a small architecture exercising all 4 resolution cases with an inline comment labeling each:
    - **Sibling match (D-13):** `[platform.api]` with `peer = "cache"` where `[platform.cache]` exists → resolves to `platform.cache`.
    - **Aunt/grandparent match (D-14):** `[platform.api.handlers.auth]` with `peer = "queue"` where `[platform.queue]` exists (no sibling/aunt queue at api or handlers level) → walk-up resolves to `platform.queue`.
    - **Root/top-level match (D-15):** the same `[platform.api.handlers.auth]` (or another deep unit) with `peer = "messageBus"` where `[messageBus]` is top-level → walk-up reaches root, resolves.
    - **Absolute fallback (D-16 step 1):** a unit with `peer = "externalGateway.gateway"` (or any peer containing a dot) → resolved as-is, no walk-up.
    Every unit gets explicit `type =`. Do NOT use templates or include. Keep it under ~20 units. Label each resolution case with a `# Case N: <description>` comment so the tutorial is self-documenting.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; head -5 skill/examples/07-relative-peer.toml &amp;&amp; grep -c 'Case [1-4]' skill/examples/07-relative-peer.toml</verify>
  <acceptance_criteria>
    - `skill/examples/07-relative-peer.toml` exists
    - `grep -c 'Case [1-4]' skill/examples/07-relative-peer.toml` returns 4 (all four resolution cases labeled)
    - `grep 'peer = "cache"' skill/examples/07-relative-peer.toml` returns at least 1 (sibling case)
    - `grep 'peer = "queue"' skill/examples/07-relative-peer.toml` returns at least 1 (aunt case)
    - `grep 'peer = "messageBus"' skill/examples/07-relative-peer.toml` returns at least 1 (root case)
    - `grep 'peer = ".*\..*"' skill/examples/07-relative-peer.toml` returns at least 1 (absolute case — peer with a dot)
    - Header comment names the feature + cites ERGO-01/02
    - Every unit has explicit `type =`
    - Commit message starts with `docs(33-02):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Author 08-include/ multi-file fixture (entry + 2 includes)</name>
  <files>skill/examples/08-include/entry.toml, skill/examples/08-include/auth.toml, skill/examples/08-include/billing.toml</files>
  <read_first>
    - skill/examples/05-ecommerce.toml (fixture style)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-17 fixture spec)
    - .planning/phases/32-include-directive-multi-file/32-CONTEXT.md (D-09 append ordering, D-10 cross-file subunits, D-11 diamond dedup, the directive syntax sketch in Specific Ideas)
    - .planning/REQUIREMENTS.md (INC-01 through INC-10)
  </read_first>
  <action>
    Create the `skill/examples/08-include/` directory with 3 files:
    - `entry.toml`: header `# Include Directive Example (entry file)\n# Demonstrates [[include]] multi-file composition (INC-01..10)`. `[properties]`, a `[platform]` parent unit (no subunits in the entry — they come from includes), `[[include]] path = "auth.toml"`, `[[include]] path = "billing.toml" once = true`, and a `[customer]` person linking to `platform.auth`. Keep paths relative to the entry file's directory (INC-02).
    - `auth.toml`: header `# Include Directive Example (auth domain)\n# Demonstrates cross-file subunits under entry's [platform] (D-10)`. Defines `[platform.auth]` and `[platform.auth.db]` (subunits appending to the entry's platform, D-10). Every unit explicit `type =`.
    - `billing.toml`: header `# Include Directive Example (billing domain)\n# Included with once=true from entry.toml (INC-06)`. Defines `[platform.billing]`. Every unit explicit `type =`.

    Do NOT use templates or bare relative peers. The fixture's job is to show include + cross-file-subunit + once. Keep each file small.
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; ls skill/examples/08-include/ &amp;&amp; grep -c '\[\[include\]\]' skill/examples/08-include/entry.toml &amp;&amp; grep 'once = true' skill/examples/08-include/entry.toml</verify>
  <acceptance_criteria>
    - `skill/examples/08-include/entry.toml`, `auth.toml`, `billing.toml` all exist
    - `grep -c '\[\[include\]\]' skill/examples/08-include/entry.toml` returns at least 2 (two include directives)
    - `grep 'once = true' skill/examples/08-include/entry.toml` returns at least 1 (INC-06 once demonstrated)
    - `grep '\[platform\.auth\]' skill/examples/08-include/auth.toml` returns 1 (cross-file subunit, D-10)
    - `grep '\[platform\.billing\]' skill/examples/08-include/billing.toml` returns 1
    - Every unit in all 3 files has explicit `type =`
    - All include paths are relative (no leading `/`, no absolute paths) — INC-02
    - Each file has a header comment citing INC requirement IDs
    - Commit message starts with `docs(33-02):`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Author 09-composed/ multi-file set + single-file-equivalent golden</name>
  <files>skill/examples/09-composed/entry.toml, skill/examples/09-composed/templates.toml, skill/examples/09-composed/domains/auth.toml, skill/examples/09-composed/single-file-equivalent.toml</files>
  <read_first>
    - skill/examples/06-templates.toml (the template grammar you just authored — reuse the same template shape for consistency)
    - skill/examples/08-include/entry.toml (the include grammar you just authored)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-CONTEXT.md (D-17 composed spec, the fixture shape sketch in Specific Ideas)
    - .planning/phases/33-docs-sweep-end-to-end-goldens/33-RESEARCH.md (§Common Pitfalls Pitfall 4 — single-file-equivalent correctness)
    - .planning/phases/31-template-expansion/31-CONTEXT.md (D-03/D-04 instantiation + name semantics)
    - .planning/phases/32-include-directive-multi-file/32-CONTEXT.md (D-09/D-10 ordering + cross-file subunits)
    - .planning/phases/30-relative-peer-resolution/30-CONTEXT.md (D-16 absolute-vs-bare gate; the XC-03 instantiation-site resolution)
  </read_first>
  <action>
    Create the `skill/examples/09-composed/` directory (with a `domains/` subdirectory) with 4 files. This set uses ALL FOUR features together and is the XC-05 golden fixture pair.

    **`templates.toml`** (included `once=true`): header `# Composed Example — template library\n# Included once=true from entry.toml; defines [template.microservice] (XC-02)`. Define `[template.microservice]` with `params = ["name", "domain", "tech", "upstreamBus"]`, `${param}` in name/description/technology/reference (TMPL-10), one `[[template.microservice.link]]` with `peer = "${upstreamBus}"` (parametrized peer — pipeline-order-dependent), and one subunit `[template.microservice.cache]` (TMPL-04).

    **`domains/auth.toml`**: header `# Composed Example — auth domain\n# Cross-file subunits under entry's [platform] (D-10)`. Define `[platform.auditLog]` (a cross-file subunit appending to entry's platform). Keep it to 1-2 units.

    **`entry.toml`**: header `# Composed Example (entry — all four features)\n# include + template + relative-peer + reference (XC-05 golden fixture)`. `[properties]`, `[platform]` (parent), `[messageBus]` (top-level, the parametrized peer target), `[[include]] path = "templates.toml" once = true`, `[[include]] path = "domains/auth.toml"`, `[[use]] template = "microservice" parent = "platform" name = "auth" domain = "authentication" tech = "Go, gRPC" upstreamBus = "messageBus"`, and a `[customer]` person linking to `platform.auth` (bare relative peer resolving to the just-instantiated sibling — ERGO-01). Keep it the SMALLEST coherent composition (auditable by a human).

    **`single-file-equivalent.toml`**: header `# Composed Example — hand-expanded single-file equivalent\n# Renders to identical DOT as entry.toml through the full pipeline (XC-05)`. This is the hand-expansion: the `[[use]]` becomes `[platform.auth]` with ALL `${param}` substituted to the concrete values from the `[[use]]` (name→"auth Service" or per the template's name field, domain→"authentication", tech→"Go, gRPC", upstreamBus→"messageBus"), the subunit inlined as `[platform.auth.cache]`, the parametrized link rewritten `peer = "messageBus"` (the concrete value). The `[[include]] path="domains/auth.toml"` becomes the inline `[platform.auditLog]`. The `[[include]] path="templates.toml"` is DROPPED entirely (template definitions produce no units after expansion — only the instantiated units remain). The `[customer]` link `peer = "platform.auth"` stays (already absolute, or if bare, write the resolved absolute form).

    After authoring, the executor MUST verify equivalence once 30-32 ship (this is the XC-05 test in Plan 04; the fixture is authored now, the assertion lands in Wave 2). If executing this plan AFTER 30-32 have shipped, render both files through `go run ./cmd/c4drill <file> -f dot` and confirm canonical equality before committing — fix the single-file side until they match (Pitfall 4).
  </action>
  <verify>cd /Users/nil/DiskD/W/Djarvur/c4drill &amp;&amp; ls skill/examples/09-composed/ skill/examples/09-composed/domains/ &amp;&amp; grep -c '\[\[include\]\]\|\[\[use\]\]' skill/examples/09-composed/entry.toml &amp;&amp; grep -c '\[template\.' skill/examples/09-composed/templates.toml</verify>
  <acceptance_criteria>
    - `skill/examples/09-composed/entry.toml`, `templates.toml`, `domains/auth.toml`, `single-file-equivalent.toml` all exist
    - `grep -c '\[\[include\]\]' skill/examples/09-composed/entry.toml` returns 2 (templates + domains)
    - `grep -c '\[\[use\]\]' skill/examples/09-composed/entry.toml` returns at least 1 (the composed instantiation)
    - `grep -c 'once = true' skill/examples/09-composed/entry.toml` returns at least 1 (templates include is once)
    - `grep -c '\[template\.' skill/examples/09-composed/templates.toml` returns at least 2 (template + its subunit)
    - `grep '\[platform\.auditLog\]' skill/examples/09-composed/domains/auth.toml` returns 1 (cross-file subunit)
    - `single-file-equivalent.toml` contains ZERO `[[use]]` and ZERO `[[include]]` directives (fully hand-expanded) — `grep -c '\[\[use\]\]\|\[\[include\]\]' skill/examples/09-composed/single-file-equivalent.toml` returns 0
    - `single-file-equivalent.toml` contains the concrete `[platform.auth]` unit with substituted values (no `${` remaining) — `grep '\${' skill/examples/09-composed/single-file-equivalent.toml` returns 0
    - Every unit has explicit `type =`
    - All include paths are relative
    - Commit message starts with `docs(33-02):`
    - NOTE for executor: if 30-32 have shipped by execution time, additionally verify `canonical.Canonical(render(entry.toml)) == canonical.Canonical(render(single-file-equivalent.toml))` before committing; if they have NOT shipped, defer the equivalence check to Plan 04 (the fixtures are valid v1.10 grammar regardless)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | These are committed example TOML files, parsed by the already-shipped v1.10 parser (once Phases 30-32 land). No network, no untrusted input, no secrets. The fixtures are documentation artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-I | Information Disclosure | N/A — fixtures contain no secrets/PII | accept | Example architectures use synthetic domain names (platform, auth, billing); no real system names, URLs are example.com placeholders. |
| T-33-02-SC | Tampering | N/A — committed files, no supply chain | accept | Zero dependencies added; TOML is data, not code. |

**Net security impact: none.** Documentation fixtures only.
</threat_model>

<verification>
Phase-level checks (executor runs after all tasks):
1. `ls skill/examples/06-templates.toml 07-relative-peer.toml 08-include/ 09-composed/` — all 4 fixture sets exist
2. `find skill/examples/06-*.toml skill/examples/07-*.toml skill/examples/08-include skill/examples/09-composed -name '*.toml' | wc -l` — 9 files total (1 + 1 + 3 + 4)
3. `grep -L 'type =' skill/examples/06-templates.toml skill/examples/07-relative-peer.toml` — empty (every top-level unit has explicit type)
4. (If 30-32 shipped) `go run ./cmd/c4drill skill/examples/09-composed/entry.toml -f dot` succeeds (the composed multi-file renders)
5. (If 30-32 shipped) `go run ./cmd/c4drill skill/examples/09-composed/single-file-equivalent.toml -f dot` succeeds
6. The XC-05 canonical-equivalence assertion itself lives in Plan 04 (Wave 2) — this plan ships the fixtures; Plan 04 ships the test
</verification>

<success_criteria>
- 4 fixture sets ship (06-templates, 07-relative-peer, 08-include, 09-composed) — DOC-03 satisfied
- 06 demonstrates TMPL-01..10 (define, instantiate, ${param}, fixed link, subunit, reference param)
- 07 demonstrates all 4 relative-peer resolution cases (sibling, aunt, root, absolute-fallback)
- 08 demonstrates INC-01..10 (include, transitive, once, cross-file subunits, relative paths)
- 09 composed set uses all four features together AND ships the hand-expanded single-file equivalent
- Every fixture is valid v1.10 grammar (loadable once 30-32 ship)
- Every fixture has a header comment citing feature + REQ-IDs
- Fixture style matches the existing 01-05 set
</success_criteria>

<output>
Create `.planning/phases/33-docs-sweep-end-to-end-goldens/33-02-SUMMARY.md` when done. List the 9 files created, the mini-domain chosen for each fixture, and (if 30-32 shipped during execution) note whether the XC-05 equivalence was pre-verified or deferred to Plan 04.
</output>
