---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 06
type: tdd
wave: 4
depends_on: ["35-04", "35-05"]
files_modified:
  - internal/testutil/canonsrc/canonsrc.go
  - internal/testutil/canonsrc/canonsrc_test.go
  - internal/c4d/parity_test.go
  - testdata/c4d/external-types.toml
  - testdata/c4d/linkfrom.toml
  - testdata/c4d/multiline-strings.toml
  - testdata/c4d/rank-equal.toml
  - testdata/c4d/template-nested-use.toml
  - testdata/c4d/unicode-strings.toml
autonomous: true
requirements: [D-22, D-26]
must_haves:
  truths:
    - "TOML -> C4D -> TOML and C4D -> TOML -> C4D round-trips over the full fixture corpus produce canonically-equal text under whitespace/quoting/key-order normalization (D-22)"
    - "For each corpus fixture, canonicalDOT of the .toml render equals canonicalDOT of its .c4d round-trip render — Model-level equivalence proven end to end (D-22, DI-1 machinery)"
    - "Explicit defaults may normalize away; the normalizer treats them as equal (D-22)"
    - "Values containing newlines normalize identically regardless of source representation — TOML escaped \\n, TOML multi-line basic string, and C4D triple-quoted forms are one canonical value (D-06/D-22)"
    - "Mixed .c4d/.toml include graphs round-trip as whole graphs with include paths rewritten to the twin extension (D-26 groundwork for --follow-includes)"
artifacts:
  - path: "internal/testutil/canonsrc/canonsrc.go"
    provides: "NormalizeTOML + NormalizeC4D canonical source normalizers"
    min_lines: 60
  - path: "internal/c4d/parity_test.go"
    provides: "corpus round-trip + render-equivalence tests"
    min_lines: 80
  - path: "testdata/c4d/"
    provides: "edge-case fixture corpus"
key_links:
  - from: "internal/testutil/canonsrc/canonsrc.go"
    to: "go-toml unstable API + internal/c4d AST"
    via: "parse to structure, drop trivia, sorted canonical serialization"
    pattern: "NormalizeTOML|NormalizeC4D"
  - from: "internal/c4d/parity_test.go"
    to: "internal/testutil/canonical.Canonical"
    via: "render-equivalence comparison per DI-1"
    pattern: "canonical\\.Canonical"
---

<objective>
Prove parity: build the canonical-equivalent source normalizer (D-22, the DI-1 precedent applied to source formats), run round-trip tests over the whole fixture corpus in both directions, add render-equivalence checks via canonicalDOT, and land the edge-case fixture set.

Purpose: this is the phase's parity contract — converters are only correct if round-trips are canonically-equal and renders are identical.
Output: internal/testutil/canonsrc package, parity test suites, edge-case fixtures under testdata/c4d/.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Exact contracts. -->

From internal/testutil/canonical/canonical.go (DI-1 precedent — copy its package
conventions verbatim: test-only-by-convention package doc, Canonical(t, dot) shape,
pure function, stdlib only):
```go
func Canonical(t *testing.T, dot string) string
```

Prior plans supply the pipeline under test:
- parser.Parse / parser.ParseFile ([]byte -> *parser.Model)
- c4d.Parse / c4d.ParseFile (-> *parser.Model), c4d.ParseAST / c4d.ParseASTFile
  (-> *ast.Document; EXPORTED by 35-05 Task 2 — canonsrc consumes them cross-package)
- c4d.EmitTOML(m) (string, error), c4d.FromModel(m) + c4d.EmitC4D(doc)

Render path for equivalence checks: cmd/c4drill composes views -> graph -> render;
tests replicate the composition WITHOUT t.Parallel (WASM engine rule — 91 existing
annotations; follow cmd/c4drill/root_test.go lines 23-25 pattern).

Fixture corpus (35-RESEARCH §3): testdata/*.toml, cmd/c4drill/testdata/*.toml,
skill/examples/**/*.toml (09-composed is the 4-file include graph).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: canonsrc normalizer package</name>
  <files>internal/testutil/canonsrc/canonsrc.go, internal/testutil/canonsrc/canonsrc_test.go</files>
  <read_first>internal/testutil/canonical/canonical.go (FULL — package doc conventions, Canonical shape, purity rules), internal/parser/parser.go captureDefinitionOrder (unstable API precedent, lines 430-492), internal/c4d/ast.go</read_first>
  <behavior>
    - Test: NormalizeTOML(t, "a = \"x\"\nb = 2") equals NormalizeTOML(t, "b=2\na='x'") — key order and quoting normalize away
    - Test: NormalizeTOML drops comments and blank lines; normalize-normalize is a fixpoint
    - Test: NormalizeC4D drops comments/whitespace; `db:db{description:cache}` equals `db: db { description: cache }`
    - Test: explicit defaults normalize away — an omitted field equals the field set to its zero/default representation (D-22; e.g. arrow absent == arrow "forward" — encode defaults for arrow=forward, rank=forward, labelPosition=middle where the Model treats them as defaults)
    - Test: newline normalization — the SAME value authored as a TOML single-line string with escaped \n, a TOML multi-line basic string ("""), and a C4D triple-quoted string (D-06) all normalize to one identical canonical form (embedded newlines carried uniformly in the canonical serialization; part of the D-22 defaults/quoting normalization list)
    - Test: malformed input fails the test via t.Fatalf (same contract as canonical.Canonical)
  </behavior>
  <action>
    Write failing tests first. Create internal/testutil/canonsrc/canonsrc.go copying the canonical package's conventions verbatim (package doc explaining D-22 contract, test-only by convention, pure, stdlib + repo packages only). Expose NormalizeTOML(t *testing.T, src string) string and NormalizeC4D(t *testing.T, src string) string. TOML path: parse via the go-toml unstable API into an ordered event stream (precedent: captureDefinitionOrder) or via parser.Parse into *parser.Model then serialize a canonical form (PREFER the Model route — it is the semantic hub; unstable API only if Model loses information needed for equality, e.g. distinguish omitted vs default — encode the D-22 default list explicitly). C4D path: c4d.ParseAST, strip Comments/Pos, serialize fields in the D-23 canonical order, sorted where order is non-semantic. Newline rule (D-06/D-22): values containing newlines serialize through ONE canonical representation (literal newline inside the canonical quoted form), so a TOML escaped-\n emission, a TOML multi-line emission and a C4D triple-quoted emission of the same value compare equal — round-trips through differently-quoted multiline forms must not fail on representation. Both outputs are deterministic strings usable with require.Equal.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/testutil/canonsrc/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Package exposes NormalizeTOML and NormalizeC4D with (t, string) string signatures
    - Fixpoint property asserted in tests (normalize(normalize(x)) == normalize(x))
    - Quoting/key-order/comment-insensitivity each pinned by a dedicated test case
    - Newline-representation insensitivity pinned by a dedicated three-form test case
  </acceptance_criteria>
  <done>The D-22 normalizer exists with DI-1-grade rigor; round-trip tests can compare canonical forms.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Edge-case fixture corpus</name>
  <files>testdata/c4d/external-types.toml, testdata/c4d/linkfrom.toml, testdata/c4d/multiline-strings.toml, testdata/c4d/rank-equal.toml, testdata/c4d/template-nested-use.toml, testdata/c4d/unicode-strings.toml</files>
  <read_first>testdata/valid.toml, testdata/template_subtree.toml, testdata/template_3x_instantiate.toml (fixture conventions), internal/model/unit.go (type vocabulary for external variants)</read_first>
  <behavior>
    - Test: every new fixture parses via parser.Parse with no error and passes validator.Validate (fixtures are VALID documents — invalid ones live elsewhere)
    - Test: external-types covers at least personExternal, systemExternal, dbExternal, queueExternal
    - Test: linkfrom exercises linkFrom + a bidirectional + a none-arrow link
    - Test: multiline-strings carries at least two values containing embedded newlines (one TOML multi-line basic string, one escaped-\n single-line string) plus a long single word — forces the newline emission paths in both emitters (35-04 Tasks 1-2) and the canonsrc newline rule through the corpus round-trip (D-06)
    - Test: rank-equal sets rank = "equal" on at least one link
    - Test: template-nested-use combines [template.X] + [[unit.Y.use]] + [[template.X.use]] (both D-16/D-17 TOML forms)
    - Test: unicode-strings carries multi-byte names/descriptions and a long single word (wrap-relevant)
  </behavior>
  <action>
    Author the six fixtures under testdata/c4d/ following testdata/ conventions (explanatory # header naming what is demonstrated). Each is a .toml file (the corpus feeds TOML-first round-trips; .c4d twins are generated by the converters under test, not committed here). Add parse+validate guards in the parity test suite so broken fixtures fail loudly.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -run TestFixtures -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Six fixture files exist under testdata/c4d/ and all parse + validate green
    - Each fixture's header comment names the decisions/constructs it exercises
    - multiline-strings.toml values with embedded newlines survive the Task 3 round-trip byte-for-byte at Model level (require.Equal) and canonically at text level (canonsrc)
  </acceptance_criteria>
  <done>Edge-case corpus covering external types, linkFrom, multiline strings, rank equal, nested use, unicode — the round-trip hard cases.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Corpus round-trip + render-equivalence tests</name>
  <files>internal/c4d/parity_test.go</files>
  <read_first>internal/testutil/canonsrc/canonsrc.go, internal/testutil/canonical/canonical.go, internal/c4d/tomodel_test.go (pipeline composition from 35-05 Task 3), cmd/c4drill/root_test.go lines 23-25 (no-t.Parallel render rule), skill/examples/ directory listing</read_first>
  <behavior>
    - Test: for every .toml in testdata/, testdata/c4d/, cmd/c4drill/testdata/ (top-level only — exclude *.dot/*.svg artifacts), skill/examples/ (recursive, skip 09-composed for the single-file loop): parser.Parse -> EmitC4D(FromModel(m)) -> c4d.Parse -> EmitTOML -> parser.Parse; assert canonsrc.NormalizeTOML(first-emit) == NormalizeTOML(second-emit) AND the two Models are require.Equal (D-22 TOML->C4D->TOML)
    - Test: reverse direction — for every corpus .toml: emit C4D, parse it, emit TOML, parse, emit C4D again; assert NormalizeC4D stable across the loop (D-22 C4D->TOML->C4D)
    - Test: 09-composed graph: convert the whole include graph (entry + 3 includes) with include paths rewritten to the twin extension (in-test graph conversion mirroring what convert --follow-includes will do; D-26) — rendered/expanded Model equals the original's after include.Resolve
    - Test: render equivalence — for valid.toml, nested.toml, links.toml, template_basic.toml and the six testdata/c4d fixtures: render the .toml Model and the round-tripped .c4d Model to DOT via the real view/graph pipeline; assert canonical.Canonical(dotA) == canonical.Canonical(dotB) (DI-1). NO t.Parallel in these tests
  </behavior>
  <action>
    Write internal/c4d/parity_test.go: corpus walker via filepath.WalkDir filtering *.toml (skip .dot/.svg/explicit invalid_* fixtures — invalid fixtures assert conversion REFUSAL instead: invalid_links.toml converted must error, mirroring D-24's validate gate at model level). Round-trip assertions per the behavior bullets — Model equality via require.Equal (order-sensitive: UnitOrder must survive, proving the order-preservation contract), text equality via canonsrc. Render-equivalence tests replicate the root command's stage composition (views -> graph -> render with format=dot) — copy the composition from cmd/c4drill/root.go processView path, keep them serial (no t.Parallel, nolint annotation per repo rule). If a round-trip exposes an emitter/parser gap, fix the gap in the offending plan's file (emit_toml/emit_c4d/tomodel) — the parity suite is the contract enforcer.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -run 'TestRoundTrip|TestRenderEquivalence|TestComposedGraph' -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Round-trip suite walks ALL corpus .toml files (assert the walker count > 15 in a log/tally so silent corpus shrinkage fails review)
    - Model require.Equal passes for every valid fixture in both directions (including multiline-strings values with embedded newlines)
    - canonicalDOT equality passes for the pinned render set
    - invalid fixtures assert conversion refusal (error), not output
    - go test ./... green
  </acceptance_criteria>
  <done>The parity contract is enforced: canonical-equivalent round-trips and render-equivalent output across the corpus, including the composed include graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture corpus -> test walker | local fixture files; walker must not follow symlinks out of the corpus roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06-01 | Tampering | corpus walker | mitigate | filepath.WalkDir does not follow symlinked directories; filter to *.toml/*.c4d extensions only; skip binary artifacts (.dot/.svg). |
| T-35-06-02 | DoS | render-equivalence tests | accept | WASM engine serial execution (repo rule) bounds concurrency; corpus size is fixed and small. |
| T-35-06-03 | Repudiation | parity failures | mitigate | Round-trip failures are hard test failures naming the fixture path — no silent skips (skip only with explicit allowlist comment). |
</threat_model>

<verification>
- go test ./internal/testutil/canonsrc/ ./internal/c4d/ -count=1 green
- go test ./... -count=1 green
- Round-trip tally covers testdata/, testdata/c4d/, cmd/c4drill/testdata/, skill/examples/
</verification>

<success_criteria>
- D-22 contract enforced by tests in both directions over the full corpus
- Render equivalence via canonicalDOT proven for the pinned set
- Composed include graph round-trips with rewritten paths (D-26)
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-06-SUMMARY.md` when done
</output>
