---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 03
type: tdd
wave: 2
depends_on: ["35-01"]
files_modified:
  - internal/c4d/c4d.peg
  - internal/c4d/parser_gen.go
  - internal/c4d/ast.go
  - internal/c4d/reserved.go
  - internal/c4d/composition_test.go
autonomous: true
requirements: [D-13, D-14, D-15, D-19]
must_haves:
  truths:
    - "template name(p1, p2) { ... } declarations parse with full unit grammar in the body, and use name(v1, v2) instantiations parse at top level, inside unit blocks and inside template bodies (D-13, D-17 grammar surface)"
    - "include path statements parse with the optional once modifier, path relative semantics deferred to include.Resolve (D-14)"
    - "List values parse in both inline [a, b] and one-per-line forms (D-15)"
    - "A unit id colliding with a reserved field/statement keyword is a hard parse error carrying a Levenshtein (did you mean ...?) suggestion (D-19)"
  artifacts:
    - path: "internal/c4d/c4d.peg"
      provides: "composition statements: template, use, include, list forms"
      contains: "Template"
    - path: "internal/c4d/reserved.go"
      provides: "reserved keyword set + FormatSuggestion wiring"
      min_lines: 20
  key_links:
    - from: "internal/c4d/reserved.go"
      to: "internal/validator FormatSuggestion"
      via: "parse-level suggestion on reserved collision"
      pattern: "FormatSuggestion"
    - from: "internal/c4d/ast.go UseStmt"
      to: "parser.Instantiation shape"
      via: "Template + Parent-relative + Params fields mirroring Instantiation"
      pattern: "UseStmt"
---

<objective>
Complete the C4D grammar with the composition statements — template declarations, use instantiations (top-level, in unit blocks, in template bodies), include directives, both list-value forms — and enforce the reserved-keyword rule with Levenshtein suggestions.

Purpose: after this plan the grammar accepts the full C4D surface (D-13, D-14, D-15, D-19); Plans 04/05 can map and emit it.
Output: extended c4d.peg + regenerated parser + reserved-keyword enforcement with suggestions.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Built by prior plans; consumed here. -->

From 35-01 (internal/c4d/ast.go): typed nodes Document, PropertiesBlock, UnitNode
(ID, Type, External, Name, Fields, Edges, Subunits, Comments, Pos), EdgeStmt, FieldStmt.

From 35-02 (internal/parser/parser.go): TemplateDef gained
`Instantiations []Instantiation` (parents relative to template unit root) — the C4D
UseStmt node needs fields that map 1:1 onto Instantiation{Template, Parent, Params}.

From internal/parser/parser.go isBuiltinField (lines 744-752) — the reserved-word seed list:
type, name, description, technology, reference, color, style, border, edges,
width, height, expanded, link, linkFrom — plus statement keywords once, use, include,
template, properties.

From internal/validator/suggest.go (exported, directly callable):
```go
func SuggestSimilar(typo string, candidates []string) string
func FormatSuggestion(typo string, candidates []string) string // ` (did you mean "x"?)`
```

From skill/examples/06-templates.toml and 08-include/entry.toml: the authoring constructs
the new statements must express (params lists, [[use]] args, [[include]] path/once).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: template/use/include grammar + AST nodes + list forms</name>
  <files>internal/c4d/c4d.peg, internal/c4d/parser_gen.go, internal/c4d/ast.go, internal/c4d/composition_test.go</files>
  <read_first>internal/c4d/c4d.peg, internal/c4d/ast.go, skill/examples/06-templates.toml, skill/examples/08-include/entry.toml, 35-CONTEXT.md D-13/D-14/D-15</read_first>
  <behavior>
    - Test: `template service(name, tech) { svc: system { technology: ${tech} } }` parses to a TemplateDecl node with Params=[name tech] and a body UnitNode whose field carries the literal token `${tech}` (tokens NOT substituted at parse — TemplateDef contract)
    - Test: `use service("api", "Go")` parses at top level with positional args recorded in order
    - Test: `use service(name: "api", tech: "Go")` — named args variant — parses with key/value pairs (choose ONE canonical AST arg representation: ordered []Arg{Name, Value}; positional args get empty Name — document the choice in ast.go)
    - Test: `use billing(name: "b1")` inside a unit block parses as a UseStmt child of that UnitNode (D-16 C4D authoring surface)
    - Test: `use helper(x: 1)` inside a template body parses as a UseStmt inside the TemplateDecl body (D-17 surface)
    - Test: `include ./shared.c4d` and `include ./shared.c4d once` parse with Once flag false/true (D-14)
    - Test: `expanded: [c1, c2]` inline list and the one-per-line form (`expanded: [` newline `c1` newline `c2` newline `]`) both parse to the same field value (D-15)
    - Test: template bodies accept the FULL unit grammar — nesting, fields, edges with relative peers inside a template body all parse (D-13)
  </behavior>
  <action>
    Write failing tests in internal/c4d/composition_test.go first, then extend c4d.peg and ast.go. New AST nodes: TemplateDecl{Name, Params []string, Body *UnitNode or body statements, Comments, Pos}, UseStmt{Template, Args []Arg, Comments, Pos} (Arg{Name, Value}), IncludeStmt{Path, Once, Comments, Pos}. Grammar rules: TemplateDecl = "template" identifier "(" param-list ")" unit-body (body is the same UnitBody rule as units — D-13 full grammar); UseStmt = "use" identifier "(" arg-list ")"; IncludeStmt = "include" literal-path ["once"]. Allow UseStmt wherever a statement is legal (document top-level, unit body, template body). Path literal for include: quoted string or bareword allowing ./ ../ / prefixes and .c4d/.toml extensions (D-14 — resolution is include.Resolve's job at pipeline time, the grammar only captures the text). Extend the Literal/field-value rule with the two list forms from D-15: inline `[ item, item ]` and one-per-line bracket form; both produce the same []string-valued field. Param values accept any Literal form. Regenerate parser_gen.go.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - ast.go exports TemplateDecl, UseStmt, IncludeStmt, Arg types with Comments and Pos fields
    - The literal-${token} test asserts the body field value still contains "${tech}" verbatim after parse
    - Both list forms produce identical parsed field values in one test
    - go test ./internal/c4d/ -count=1 green including 35-01 core tests
  </acceptance_criteria>
  <done>Full C4D authoring surface parses: templates with params, use in all three positions, include with once, both list forms.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Reserved-keyword enforcement with Levenshtein suggestions</name>
  <files>internal/c4d/reserved.go, internal/c4d/c4d.peg, internal/c4d/parser_gen.go, internal/c4d/composition_test.go</files>
  <read_first>internal/parser/parser.go isBuiltinField (lines 744-752), internal/validator/suggest.go (FormatSuggestion), 35-CONTEXT.md D-19, 35-PATTERNS.md "Hard-error suggestion UX" section</read_first>
  <behavior>
    - Test: `description: system { }` — a unit whose id is the reserved word "description" — is a hard parse error whose message contains `(did you mean` with a plausible suggestion (D-19)
    - Test: reserved set covers every isBuiltinField key (type, name, description, technology, reference, color, style, border, edges, width, height, expanded, link, linkFrom) plus statement keywords (use, include, template, properties, once) — table-driven: each keyword used as a unit id errors
    - Test: a NEAR-miss id like "descripton" is legal (not reserved) — only exact collisions error
    - Test: field KEYS outside the known set (e.g. `unknown: x` inside a unit body) produce a parse error with the same suggestion treatment (candidates = reserved set + field keywords)
    - Test: error is *parser.ParseError with the offending Line
  </behavior>
  <action>
    Write internal/c4d/reserved.go: exported ReservedKeywords() []string built from the isBuiltinField list plus the statement keywords once/use/include/template/properties (copy the exact strings from internal/parser/parser.go lines 744-752 — do not retype from memory), and a CheckUnitID(id string, pos) error helper that returns *parser.ParseError with Message = fmt.Sprintf("unit id %q is a reserved word%s", id, validator.FormatSuggestion(id, ReservedKeywords())) and the DSL-native line. Wire the check into the grammar action for unit headers (and field-key validation where the grammar accepts a field key) so collisions fail at parse level per D-19 — the grammar-level keyword exclusions from 35-01 Task 2 already stop these from parsing as normal units; the action-level check turns them into a quality error instead of a generic syntax failure. Table-driven lexer tests pin the disambiguation order (35-RESEARCH Risk 2).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/c4d/reserved.go exists and its keyword list contains all 14 isBuiltinField strings + 5 statement keywords (19 total)
    - The "description as unit id" test asserts the message matches `did you mean`
    - The near-miss "descripton" test asserts NO error
    - All failures are *parser.ParseError with Line > 0
  </acceptance_criteria>
  <done>Reserved-keyword collisions are hard errors with suggestions at parse level; the bareword/reserved disambiguation is pinned by table-driven tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .c4d file -> grammar + reserved check | untrusted input; parse must fail closed on ambiguity, bounded work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | Tampering | reserved-keyword disambiguation | mitigate | Exact-match table-driven tests pin the reserved set; near-miss ids remain legal so the check cannot be abused to reject valid documents. |
| T-35-03-02 | DoS | composition grammar combinatorics | mitigate | Keep Memoize(true) + MaxExpressions cap from 35-01; one-per-line list forms are bounded by input size. |
| T-35-03-03 | Information disclosure | suggestion output | accept | FormatSuggestion echoes only the author's own token and candidate keywords from a fixed public list — no disclosure surface. |
</threat_model>

<verification>
- go test ./internal/c4d/ -count=1 green (core + composition + reserved suites)
- go build ./... green
- golangci-lint run clean on new/changed files
</verification>

<success_criteria>
- The grammar accepts templates, use (3 positions), include once, both list forms — full C4D surface per CONTEXT <domain>
- Reserved collisions hard-error with suggestions (D-19)
- parser_gen.go regenerated and committed
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-03-SUMMARY.md` when done
</output>
