---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 07
type: tdd
wave: 4
depends_on: ["35-04", "35-05"]
files_modified:
  - cmd/c4drill/root.go
  - cmd/c4drill/convert.go
  - cmd/c4drill/convert_test.go
  - cmd/c4drill/root_test.go
autonomous: true
requirements: [D-24, D-25, D-27, D-28, D-29, D-30]
must_haves:
  truths:
    - "c4drill diagram.c4d renders directly through the full pipeline — C4D is a first-class authoring format (D-29)"
    - "Input dispatch is by extension: .toml -> TOML parser, .c4d -> C4D parser, anything else a hard error naming the accepted extensions (D-27)"
    - "c4drill convert to-toml <file.c4d> and convert to-c4d <file.toml> validate the model first — invalid input is a hard error and NO output file is written (D-24, D-28)"
    - "The D-24 pipeline runs ONLY as a validation gate on a discarded model copy — the emission model is a FRESH source parse, so single-file convert output preserves include directives verbatim (D-25), template declarations + use instantiations intact (no Expand), and authored relative peers non-absolutized (no peer.Resolve; D-22 round-trip parity with 35-06)"
    - "Converted output lands next to the input with swapped extension; -o overrides the directory (D-30)"
    - "convert --follow-includes converts the whole include graph — each file gets its format twin, include paths rewritten to the new extension (D-25)"
artifacts:
  - path: "cmd/c4drill/convert.go"
    provides: "convert cobra subcommand (to-toml/to-c4d, --follow-includes, -o)"
    min_lines: 80
  - path: "cmd/c4drill/root.go"
    provides: "extension dispatch in runRoot Stage 1 + AddCommand(newConvertCmd())"
    contains: ".c4d"
key_links:
  - from: "cmd/c4drill/root.go runRoot"
    to: "internal/c4d.ParseFile"
    via: "extension branch on .c4d"
    pattern: "c4d\\.ParseFile"
  - from: "cmd/c4drill/convert.go"
    to: "validator.Validate + c4d.EmitTOML/EmitC4D"
    via: "validate on pipeline-mutated copy (include -> expand -> peer -> Validate, D-24 gate) THEN emit from a fresh re-parse (D-25 preservation)"
    pattern: "Validate"
---

<objective>
Wire the CLI: extension-based input dispatch on the render path (.c4d renders directly, D-27/D-29) and the convert subcommand with validate-first semantics, swapped-extension output placement, and --follow-includes graph conversion (D-24, D-25, D-28, D-30).

Purpose: users can author in C4D and render, and migrate files/directories of either format into the other.
Output: cmd/c4drill/convert.go, modified root.go dispatch, CLI test coverage.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Exact contracts. -->

From cmd/c4drill/root.go — Stage 1 today (lines ~110-115):
m, err := parser.ParseFile(inputPath); err -> fmt.Errorf("parse: %w", err)
NewRootCmd (line 50): cobra builder with SilenceUsage, PersistentFlags
(-f/-o/--expanded/--label-ratio), Args: cobra.MaximumNArgs(1), RunE: runRoot.
Stage prefixes: "parse:", "include:", "expand:", "resolve peers:", sentinel errors
in a var block commented "// Static errors for better error handling."
deriveBasename (line 192) / resolveOutDir (line 182) — output naming helpers.
These are the FIRST subcommands in the repo — wire via cmd.AddCommand(newConvertCmd())
inside NewRootCmd (no AddCommand precedent exists; follow the same builder shape).

Pipeline stages available: parser.ParseFile, c4d.ParseFile, include.Resolve(m, dir, file),
template.Expand(m), peer.Resolve(m), validator.Validate(m) + validator.ReportErrors,
c4d.EmitTOML(m), c4d.FromModel(m) + c4d.EmitC4D(doc).

STAGE MUTATION WARNING (load-bearing for convert): include.Resolve drains
m.Includes to nil and merges included units in place (internal/include/resolve.go:89-91);
template.Expand consumes Templates/Instantiations and attaches expanded units;
peer.Resolve rewrites every bare Link.Peer to an absolute dotted path in place
(internal/peer/resolve.go:90,99). A model that has been through the pipeline CANNOT
be emitted losslessly — the emission model must never see these stages.

Mixed include graphs already resolve (35-05); graph traversal + cycle detection
live in internal/include (canonicalize, maxIncludeDepth, visited set).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Root dispatch — .c4d renders directly</name>
  <files>cmd/c4drill/root.go, cmd/c4drill/root_test.go</files>
  <read_first>cmd/c4drill/root.go (runRoot Stage 1 lines ~110-155, NewRootCmd, deriveBasename, resolveOutDir), cmd/c4drill/root_test.go (render test conventions — NO t.Parallel, nolint annotation), testdata/valid.toml, skill/examples/01-minimal.toml</read_first>
  <behavior>
    - Test: rendering a .c4d document (hand-written minimal twin of skill/examples/01-minimal.toml placed in a t.TempDir) produces the same view outputs (C1 dot) as rendering the .toml original — canonical.Canonical comparison (D-29)
    - Test: passing a .json input exits with error whose message names both accepted extensions ".toml" and ".c4d" (D-27)
    - Test: .toml inputs behave exactly as before — existing root tests untouched and green
    - Test: help text Use/Long mention .c4d acceptance
  </behavior>
  <action>
    Write failing tests first (render tests WITHOUT t.Parallel per repo rule — copy the existing nolint annotation). Modify runRoot Stage 1: branch on strings.HasSuffix(filepath.Ext(inputPath), ...) — .toml -> parser.ParseFile, .c4d -> c4d.ParseFile, default -> fmt.Errorf naming accepted extensions (hard error, D-27). Everything downstream (include -> expand -> peer -> validate -> views -> render) untouched — TestXC01_PipelineOrdering stays green. Update NewRootCmd Use to "c4drill <input.toml|input.c4d>" and Long to mention both formats. Keep the "parse:" error prefix on both branches.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run 'TestRoot|TestC4DRender|TestXC01' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A .c4d input renders SVG/DOT outputs next to the input (t.TempDir assert on created files)
    - .json input error message contains ".toml" and ".c4d"
    - Use string contains ".c4d"
    - Existing root_test.go cases pass unmodified
  </acceptance_criteria>
  <done>c4drill diagram.c4d works end to end; unknown extensions fail closed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: convert subcommand — to-toml / to-c4d with validate-first + output placement</name>
  <files>cmd/c4drill/convert.go, cmd/c4drill/convert_test.go, cmd/c4drill/root.go</files>
  <read_first>cmd/c4drill/root.go (NewRootCmd builder shape, stage composition, sentinel error block lines 26-31, deriveBasename), internal/validator/validator.go (Validate/ReportErrors), internal/include/resolve.go lines 78-91 (Includes drained to nil + in-place merge), internal/peer/resolve.go lines 79-99 (in-place Peer absolutization), internal/c4d/emit_toml.go + emit_c4d.go + frommodel.go, 35-CONTEXT.md D-24/D-28/D-30</read_first>
  <behavior>
    - Test: `convert to-toml diagram.c4d` writes diagram.toml next to the input; content re-parses via parser.Parse to a Model require.Equal to a FRESH c4d.ParseFile of the source (NOT the pipeline-mutated model) (D-28, D-30)
    - Test: `convert to-c4d diagram.toml` writes diagram.c4d next to the input; content re-parses via c4d.Parse to a Model require.Equal to a fresh parser.ParseFile of the source (D-28, D-30)
    - Test: single-file preservation — converting a source WITH include directives, template declarations/use stmts, and bare relative peers produces output that STILL contains them: include directives verbatim (D-25 default: single-file mode must not include.Resolve the emission model), template decls + use instantiations intact (no Expand — 35-09 twins depend on template form surviving), authored bare peers non-absolutized (no peer.Resolve rewriting — D-22 round-trip parity with 35-06); assert via re-parse Model equality against the fresh source parse PLUS explicit presence checks on Includes/Templates/bare Peer strings
    - Test: -o flag redirects output to the given directory (file created there, not next to input) (D-30)
    - Test: converting testdata/invalid_links.toml exits non-zero with validation errors reported AND no output file exists (D-24 — validate first, hard error, no output)
    - Test: converting a file with unresolvable includes/peers errors with the stage prefix ("include:" / "expand:" / "resolve peers:") matching root command conventions
    - Test: wrong direction extension (convert to-toml on a .toml) is a hard error explaining the expected input extension
    - Test: convert help text documents both directions
  </behavior>
  <action>
    Write failing tests first (convert_test.go, black-box via cobra command invocation with t.TempDir; no render involved so t.Parallel allowed). Create cmd/c4drill/convert.go: newConvertCmd() returning a cobra command Use "convert <to-toml|to-c4d> <file>" with a subcommand-per-direction structure (toTOMLCmd/toC4DCmd or a direction arg — follow cobra idiom; planner prefers nested subcommands matching D-28's literal names). RunE uses TWO parses of the source file (planner-pinned shape, see the interfaces STAGE MUTATION WARNING): (1) VALIDATION MODEL — parse (extension-appropriate for the SOURCE format), then the SAME stage composition as root up to Validate: include.Resolve -> template.Expand -> peer.Resolve -> validator.Validate (D-24: ReportErrors + sentinel errValidationFailed, NO emit on failure); these stages mutate the model in place, so this model is DISCARDED after the gate — it is never emitted. (2) EMISSION MODEL — re-parse the source file fresh (Includes, Templates/Instantiations and authored relative peers intact) and emit THAT via c4d.EmitTOML / c4d.FromModel + c4d.EmitC4D -> write file. Output path: same dir as input, extension swapped (deriveBasename + Ext swap); -o flag overrides the directory (create it if missing, mirror resolveOutDir behavior). Register in NewRootCmd via cmd.AddCommand(newConvertCmd()). Copy the lowercase stage-prefix error wrapping and the sentinel var block comment convention verbatim. File writes with 0644 via os.WriteFile with //nolint:gosec only if the lint demands (prefer no nolint — plain WriteFile is G306-safe at 0644).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run TestConvert -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both directions produce twins that re-parse to Models equal to a FRESH source parse (asserted in tests)
    - Preservation test asserts the emitted twin still carries its include directives (verbatim path + once), template decls/use stmts, and bare peer strings when the source had them (D-25 default)
    - Invalid input test asserts output file DOES NOT exist after failure
    - -o redirect test asserts file exists only in the target dir
    - Stage error prefixes match root conventions ("parse:", "include:", ...)
  </acceptance_criteria>
  <done>convert to-toml/to-c4d work single-file with validate-first hard errors, correct output placement, and lossless structure preservation.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: convert --follow-includes graph conversion</name>
  <files>cmd/c4drill/convert.go, cmd/c4drill/convert_test.go</files>
  <read_first>cmd/c4drill/convert.go (Task 2 result), internal/include/resolve.go (canonicalize, visited set, cycle detection — the traversal pattern to reuse), skill/examples/09-composed/ (the 3-file include graph: entry.toml -> templates.toml + domains/auth.toml; single-file-equivalent.toml is NOT in the graph), skill/examples/08-include/ (3-file graph)</read_first>
  <behavior>
    - Test: `convert to-c4d skill/examples/09-composed/entry.toml --follow-includes` produces .c4d twins for every file in the include graph — entry.c4d, templates.c4d, domains/auth.c4d — with include paths inside entry.c4d rewritten to .c4d (D-25)
    - Test: `once` flags survive path rewriting (include ./x once stays once)
    - Test: an include cycle in the graph errors with the include cycle message, no partial garbage outputs (reuse include's detection — traversal must not hang)
    - Test: without --follow-includes, includes are untouched — single file converts, include directives preserved verbatim (D-25 default)
    - Test: mixed graph (a .toml including a .c4d, from 35-05 fixtures) converts whole-graph in either direction
  </behavior>
  <action>
    Extend convert.go with a --follow-includes bool flag on both directions. When set: walk the include graph from the entry file using the same canonical-path visited-set + maxIncludeDepth pattern as internal/include/resolve.go (extract or reuse — do NOT reimplement cycle logic; call into include's traversal if exported, else replicate the exact stack/visited pattern with a test pinning cycle behavior). Per file in the graph (in deterministic traversal order): parse in its native format and emit the twin from THAT fresh parse — per-file emission never runs include.Resolve/template.Expand/peer.Resolve on the emission model (same preservation rule as Task 2); only the include-directive PATH STRINGS are rewritten (extension swap; preserve relative form and once flag). D-24 gate for graph mode: run the full pipeline (include.Resolve -> template.Expand -> peer.Resolve -> Validate) ONCE on a separately parsed copy of the ORIGINAL entry graph as authored (not the rewritten twins); included fragments may not validate standalone, so only the merged entry model is validated, mirroring how include.Resolve merges before validate in the normal pipeline — document this choice in a comment referencing D-24. No twin is written if the gate fails. Write each twin next to its source (or under -o preserving relative directory structure from the entry's dir).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run 'TestConvertFollowIncludes|TestConvertGraph' -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 09-composed conversion creates exactly the 3 graph twins (entry.c4d, templates.c4d, domains/auth.c4d) with rewritten include paths (test asserts each exists and the entry's include directives reference .c4d paths)
    - Cycle test asserts error contains "cycle" and hangs nothing (test timeout guards)
    - Default (no flag) preserves include directives byte-for-byte
  </acceptance_criteria>
  <done>Whole-graph migration works in both directions with cycle safety and path rewriting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (-o dir, input path) -> file writes | user-controlled output location; output must not clobber unrelated files or escape expectations |
| include graph -> file reads/writes | --follow-includes walks author-controlled paths; cycles/symlinks must not hang or escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 | Tampering | convert output writes | mitigate | Output filename = input basename + swapped extension ONLY (deriveBasename + Ext swap — no user-controlled filename portion); -o accepts a directory the user explicitly named (standard CLI trust); refuse conversion when target file exists AND differs? NO — overwrite is the idempotent contract (documented); mitigation is the fixed name shape. |
| T-35-07-02 | DoS | --follow-includes traversal | mitigate | Reuse include's visited-set + maxIncludeDepth=100 cycle detection; no unbounded recursion. |
| T-35-07-03 | Tampering | symlinked include targets | mitigate | Traversal operates on canonicalized paths (include.canonicalize precedent); a symlink loop collapses into the visited-set cycle error. |
| T-35-07-04 | Information disclosure | error output | accept | Stage-prefixed errors echo author-supplied paths/messages only — same surface as the existing root command. |
</threat_model>

<verification>
- go test ./cmd/c4drill/ -count=1 green (root + convert + existing suites)
- go test ./... -count=1 green
- Manual spot: go run ./cmd/c4drill convert to-c4d testdata/valid.toml && cat testdata/valid.c4d renders back (covered by tests; command provided for humans)
</verification>

<success_criteria>
- .c4d renders directly (D-29); dispatch errors name extensions (D-27)
- convert validates first, refuses invalid, places output by Ext swap + -o (D-24, D-28, D-30)
- convert emits from a fresh source parse — includes/templates/bare peers preserved in single-file mode (D-25, D-22); --follow-includes converts whole graphs with rewritten paths and cycle safety (D-25)
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-07-SUMMARY.md` when done
</output>
