---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 05
type: tdd
wave: 3
depends_on: ["35-02", "35-03"]
files_modified:
  - internal/parser/parser.go
  - internal/c4d/tomodel.go
  - internal/c4d/tomodel_test.go
  - internal/include/resolve.go
  - internal/include/resolve_test.go
autonomous: true
requirements: [D-02, D-10, D-11, D-21, D-26]
must_haves:
  truths:
    - "c4d.Parse produces a *parser.Model — the DSL parses DIRECTLY to the Model hub so include/expand/peer/validate run unchanged downstream (D-21)"
    - "A C4D unit with omitted type gets the same defaultTypeForParent/inferGenericType inference and Humanize name derivation as the TOML front-end — both front-ends produce identical Models for equivalent documents (D-02)"
    - "Bare peer names and dotted paths in C4D edges resolve through internal/peer.Resolve unchanged (D-10)"
    - "Two edge statements for the same (unit, peer) pair in one block are a hard error (D-11)"
    - "include.Resolve accepts .c4d and .toml files mixed in one include graph (D-26)"
  artifacts:
    - path: "internal/c4d/tomodel.go"
      provides: "ToModel(doc *ast.Document) (*parser.Model, error) with inference + order + semantic checks"
      min_lines: 100
    - path: "internal/parser/parser.go"
      provides: "exported DefaultTypeForParent and InferGenericType"
      contains: "func DefaultTypeForParent"
  key_links:
    - from: "internal/c4d/tomodel.go"
      to: "internal/parser.DefaultTypeForParent/InferGenericType/model.Humanize"
      via: "the three post-parse hooks from parseUnitWithOrder lines 599-615"
      pattern: "DefaultTypeForParent|Humanize"
    - from: "internal/c4d/tomodel.go"
      to: "parser.Model"
      via: "fill Properties/UnitOrder/Units/Templates/Instantiations/Includes"
      pattern: "parser\\.Model"
    - from: "internal/include/resolve.go"
      to: "extension-branched parse"
      via: ".c4d -> c4d front-end, .toml -> parser.ParseFile"
      pattern: "\\.c4d"
---

<objective>
Close the C4D front-end: toModel.go converts the typed AST into *parser.Model with identical inference/humanize/order semantics to the TOML parser (D-02, D-21), enforces duplicate-edge hard errors (D-11), proves peer-resolution passthrough (D-10), and extends include.Resolve with extension-based parse dispatch so include graphs mix .c4d and .toml freely (D-26).

Purpose: after this plan `c4d.ParseFile` yields a Model that flows through the entire existing pipeline — the parity hub is complete and Plan 06 can test round-trips end to end.
Output: ToModel, exported inference helpers, mixed-format include resolution.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Exact contracts. -->

From internal/parser/parser.go — the three post-parse hooks toModel MUST apply
(parseUnitWithOrder lines 599-615, currently unexported — Task 1 exports them):
```go
if unit.Type == "" {
	unit.Type = defaultTypeForParent(parentType)
}
unit.Type = inferGenericType(unit.Type, parentType)
if unit.Name == "" {
	unit.Name = model.Humanize(name)
}
```

From internal/parser/parser.go (35-02 additions):
TemplateDef{Params, Unit, Instantiations}; Instantiation{Template, Parent, Params};
IncludeDirective{Path, Once}.

From internal/include/resolve.go:
Resolve(entry *parser.Model, entryDir, entryFile string) (*parser.Model, error)
-> resolveOne parses included files (currently TOML-only via parser.ParseFile —
Task 3 branches on extension).

From internal/peer/resolve.go: Resolve(m *parser.Model) error — relative-peer pass,
reused verbatim per D-10 (no changes; toModel only stores Peer strings).

From internal/template/expand.go: Expand(m *parser.Model) (*parser.Model, error) —
consumes Templates + Instantiations produced by ToModel.

Prior plans: 35-01/35-03 internal/c4d/ast.go (full node set), internal/c4d/parse.go
(Parse/ParseFile returning *ast.Document).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Export inference helpers from internal/parser</name>
  <files>internal/parser/parser.go</files>
  <read_first>internal/parser/parser.go lines 572-745 (parseUnitWithOrder, defaultTypeForParent line 673, inferGenericType line 706, genericDbTypes map line 29, isBuiltinField line 744)</read_first>
  <behavior>
    - Test: parser.DefaultTypeForParent exists with signature func(parentType model.UnitType) model.UnitType and returns the same values the unexported function returned (existing call sites now use the exported name — behavior unchanged)
    - Test: parser.InferGenericType exists with signature func(unitType, parentType model.UnitType) model.UnitType, genericDbTypes behavior preserved
    - Test: full parser package test suite green — no behavioral change, pure rename-export
  </behavior>
  <action>
    Rename defaultTypeForParent to DefaultTypeForParent and inferGenericType to InferGenericType in internal/parser/parser.go, add doc comments (who calls them: parseUnitWithOrder and the C4D front-end toModel — D-02 inference parity), update the internal call sites in parseUnitWithOrder. Keep genericDbTypes unexported (only InferGenericType touches it). This is the export-in-place option from 35-PATTERNS PLANNING LANDMINE — moving to internal/model would churn more files for zero benefit.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/parser/ -count=1 && grep -q "func DefaultTypeForParent" internal/parser/parser.go && grep -q "func InferGenericType" internal/parser/parser.go</automated>
  </verify>
  <acceptance_criteria>
    - grep finds exported declarations `func DefaultTypeForParent(parentType model.UnitType) model.UnitType` and `func InferGenericType(unitType model.UnitType, parentType model.UnitType) model.UnitType` (verify exact param shape against source while editing)
    - No remaining references to the unexported names (go build fails otherwise)
    - go test ./internal/parser/ green
  </acceptance_criteria>
  <done>Inference helpers callable from internal/c4d; parser behavior byte-identical.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: tomodel.go — AST to *parser.Model with full parity hooks</name>
  <files>internal/c4d/tomodel.go, internal/c4d/tomodel_test.go</files>
  <read_first>internal/parser/parser.go parseUnitWithOrder (lines 572-664 — the exact hook sequence and error shapes), internal/c4d/ast.go, internal/c4d/parse.go, internal/model/unit.go, internal/model/link.go, internal/template/expand_test.go (what Expand expects from Templates/Instantiations)</read_first>
  <behavior>
    - Test: ToModel maps a full document — properties block, units in statement order, nested subunits, edges, template decls, use stmts, includes — into the corresponding parser.Model fields; UnitOrder and SubunitOrder reflect AST statement order
    - Test: inference parity — the C4D document `api { }` nested under a system parent and the TOML equivalent produce IDENTICAL model.Unit structs (Type inferred, Name humanized) — assert with require.Equal on the parsed Units
    - Test: external modifier — `person external "X" { }` produces Type personExternal (D-04 mapping at Model level)
    - Test: edge statements — `->` appends to Links with ArrowForward, `<-` appends to LinksFrom (mirroring TOML linkFrom semantics — read the TOML parser's link extraction to match Arrow/Rank defaults exactly), `<->` ArrowBidirectional, `--` ArrowNone; option block keys map to Link fields color/style/rank/labelPosition/length; peer strings stored verbatim (bare or dotted — peer.Resolve does resolution, D-10)
    - Test: duplicate edge hard error — two `-> db` statements in one block return an error, message contains "duplicate" and the peer name (D-11); a `-> db` plus `<- other` pair does NOT error
    - Test: UseStmt parents — top-level use -> Instantiation{Parent: ""}; use inside unit block at path a.b -> Instantiation{Parent: "a.b"}; use inside template body -> TemplateDef.Instantiations with root-relative Parent (D-16/D-17 surfaces)
    - Test: template bodies carry literal ${param} tokens into TemplateDef.Unit (not substituted)
    - Test: the C4D twin of testdata/template_basic.toml produces require.Equal Models (modulo fields the fixtures do not use)
  </behavior>
  <action>
    Write failing tests first (internal/c4d/tomodel_test.go). Implement internal/c4d/tomodel.go exposing ToModel(doc *ast.Document) (*parser.Model, error) plus update internal/c4d/parse.go so Parse/ParseFile return (*parser.Model, error) directly (D-21 — the DSL parses directly to Model; the AST remains available to fmt via a separate entry, keep an internal parseAST helper for fmt's use). Recursive unit walk mirroring parseUnitWithOrder's hook order exactly: empty Type -> parser.DefaultTypeForParent(parentType); then parser.InferGenericType; then empty Name -> model.Humanize(id). Map external modifiers to the *External UnitType variants (type keyword + external prefix, validated against the 17-type vocabulary — unknown type keyword is a hard error with FormatSuggestion against the type list). Edges: build model.Link from EdgeStmt (glyph -> Arrow mapping; options block -> Link fields); `<-` statements go to LinksFrom matching the TOML linkFrom semantics (read the TOML link/linkFrom extraction in parser.go while implementing — do not guess Arrow defaults). Duplicate detection per D-11: same (unit, peer) pair twice in one block's Links OR twice in LinksFrom -> structured error (follow ExpandError's Kind/Site/Detail shape, Kind "duplicate edge"). Templates -> TemplateDef{Params, Unit, Instantiations}; use -> Instantiation; include -> IncludeDirective. All errors are *parser.ParseError with AST positions.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -count=1 && go test ./internal/parser/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - c4d.Parse signature is now func(data []byte) (*parser.Model, error) (D-21)
    - The parity test asserts require.Equal on parsed Unit structs between equivalent TOML and C4D documents (inference + humanize identical)
    - Duplicate-edge test asserts the error message contains the peer name
    - All internal/c4d suites green after the signature change (fix 35-01/35-03 test call sites)
  </acceptance_criteria>
  <done>The C4D front-end emits the same Model the TOML front-end would emit for equivalent documents; semantic checks hard-error.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: include.Resolve extension dispatch + pipeline integration proof</name>
  <files>internal/include/resolve.go, internal/include/resolve_test.go, internal/c4d/tomodel_test.go</files>
  <read_first>internal/include/resolve.go (Resolve, resolveOne, canonicalize — where parser.ParseFile is called for included files), internal/c4d/parse.go, cmd/c4drill/root.go lines 110-155 (stage composition), cmd/c4drill/root_test.go TestXC01_PipelineOrdering</read_first>
  <behavior>
    - Test: an entry .toml including a .c4d file (and an entry .c4d including a .toml file) resolves — mixed graphs work (D-26)
    - Test: unknown include extension (.json) is a hard error naming accepted extensions
    - Test: include cycle across mixed formats still errors with the chain message
    - Test: integration — a .c4d document with templates, nested use and relative peers runs the FULL pipeline in a test: c4d.ParseFile -> include.Resolve -> template.Expand -> peer.Resolve -> validator.Validate returns zero errors on a valid composed document (D-10, D-21 proof; no render — validator is the last stage needed)
    - Test: an unresolvable bare peer in .c4d produces the same peer.Resolve error as the TOML twin (D-10)
  </behavior>
  <action>
    Write failing tests first (add mixed-format fixtures under internal/include/testdata/ — entry + included files in both formats). Modify internal/include/resolve.go resolveOne: branch on filepath.Ext of the included path — .toml -> parser.ParseFile, .c4d -> c4d.ParseFile, anything else -> hard error `*parser.ParseError` naming accepted extensions (mirrors D-27's dispatch at include level; extract a tiny shared dispatch helper if it avoids duplication, e.g. an unexported parseByExtension in include or a small function in internal/c4d — planner leaves placement to executor, keep it one call site). Then add the pipeline integration test in internal/c4d/tomodel_test.go composing the real stages in the load-bearing order (include -> expand -> peer -> validate) on a .c4d composed fixture — asserting no error and spot-checking expanded units. Render is deliberately NOT invoked (WASM parallel rule; render equivalence is Plan 06).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/include/ ./internal/c4d/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Mixed-format include fixture pair (toml-includes-c4d AND c4d-includes-toml) both resolve in tests
    - Unknown-extension include test asserts the error names ".toml" and ".c4d"
    - The full-pipeline integration test asserts validator.Validate returns empty for the composed .c4d fixture
    - go test ./... green
  </acceptance_criteria>
  <done>Mixed .c4d/.toml include graphs resolve; the C4D front-end is proven pipeline-compatible end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .c4d/.toml include graph -> parser dispatch | file paths from include directives drive file reads; extension dispatch must fail closed on unknown types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05-01 | Tampering | include extension dispatch | mitigate | Unknown extension = hard *parser.ParseError naming accepted extensions; no fallback parsing, no content-sniffing. |
| T-35-05-02 | DoS | mixed-graph cycles | mitigate | Existing maxIncludeDepth=100 + visited-set cycle detection apply unchanged — dispatch change does not touch traversal. |
| T-35-05-03 | Elevation | Model construction | accept | ToModel builds in-memory structs only; no I/O, no exec; unknown types/edges fail closed. |
</threat_model>

<verification>
- go test ./internal/c4d/ ./internal/parser/ ./internal/include/ ./internal/template/ -count=1 green
- go test ./... -count=1 green (12/12+ packages)
- TestXC01_PipelineOrdering untouched and passing
</verification>

<success_criteria>
- c4d.ParseFile returns *parser.Model (D-21) with inference parity (D-02)
- Duplicate edges hard-error (D-11); peers resolve via existing pass (D-10)
- Mixed include graphs resolve (D-26)
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-05-SUMMARY.md` when done
</output>
