---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 04
type: tdd
wave: 3
depends_on: ["35-03"]
files_modified:
  - internal/c4d/emit_toml.go
  - internal/c4d/emit_c4d.go
  - internal/c4d/frommodel.go
  - internal/c4d/emit_test.go
autonomous: true
requirements: [D-23, D-33]
must_haves:
  truths:
    - "Model to TOML text emission uses the fixed canonical field order: type, name, description, technology, reference, color, style, border, edges, expanded — deterministic diffs, source order normalized away (D-23)"
    - "Model to C4D text emission uses the compact-leaf style: leaf units without subunits or edges emit on one line, nested units multi-line, 2-space indent (D-33)"
    - "A *parser.Model round-trips to C4D text via frommodel (Model to AST) plus emit_c4d (AST to text) with comments empty on the convert path"
    - "Emitted TOML matches the fixture style of skill/examples/06-templates.toml and testdata/valid.toml ([properties], [unit.X] tables, link tables, [[use]], [[include]], [template.X])"
    - "Both emitters are deterministic: emitting twice yields byte-identical output"
  artifacts:
    - path: "internal/c4d/emit_toml.go"
      provides: "EmitTOML(m *parser.Model) (string, error) in fixed canonical order"
      min_lines: 80
    - path: "internal/c4d/emit_c4d.go"
      provides: "EmitC4D(doc *ast.Document) string — comment-aware compact-leaf printer"
      min_lines: 80
    - path: "internal/c4d/frommodel.go"
      provides: "FromModel(m *parser.Model) *ast.Document — canonical Model-to-AST inverse of toModel"
      min_lines: 60
  key_links:
    - from: "internal/c4d/emit_toml.go"
      to: "parser.Model"
      via: "walk UnitOrder/SubunitOrder emitting tables in order"
      pattern: "UnitOrder"
    - from: "internal/c4d/frommodel.go"
      to: "internal/c4d/ast.go node types"
      via: "construct UnitNode/EdgeStmt/TemplateDecl/UseStmt/IncludeStmt"
      pattern: "UnitNode"
    - from: "internal/c4d/emit_c4d.go"
      to: "internal/c4d/ast.go Comments fields"
      via: "emit attached comments before their statements (fmt groundwork, D-32)"
      pattern: "Comments"
---

<objective>
Build the two emitters: Model→TOML in a fixed canonical field order (D-23) and AST→C4D in the compact-leaf style (D-33), plus frommodel.go — the Model→AST inverse that lets convert to-c4d reuse the comment-aware C4D printer.

Purpose: converters (Plan 07) and fmt (Plan 08) both consume these emitters; one emitter with comment support serves both the convert path (comments empty) and the fmt path (comments re-attached).
Output: EmitTOML, EmitC4D, FromModel — deterministic, tested.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Exact contracts. -->

From internal/parser/parser.go — what the emitters walk:
```go
type Model struct {
	Properties     model.Properties
	UnitOrder      []string
	Units          map[string]*model.Unit
	Templates      map[string]*TemplateDef   // Params, Unit, Instantiations (35-02)
	Instantiations []Instantiation           // Template, Parent, Params
	Includes       []IncludeDirective        // Path, Once
}
```

From internal/model/unit.go Unit fields (emit source set):
Type, Name, Description, Technology, Reference, Color, Style, Border, Edges,
Expanded, Links []Link, LinksFrom []Link, Subunits map + SubunitOrder []string.
From internal/model/link.go Link fields: Peer, Arrow, Rank, Color, Style, Technology,
Description, LabelPosition, Length (Mirror is never serialized).

From internal/model/properties.go Properties fields:
Name, Description, Color, Style, Border, Edges, LineLength, Expanded.

From 35-01/35-03 ast.go: Document, PropertiesBlock, UnitNode, EdgeStmt, FieldStmt,
TemplateDecl, UseStmt, IncludeStmt — all carrying Comments + Pos (D-32 fmt groundwork).

Target TOML style fixtures (the emitted output must be shaped like):
skill/examples/06-templates.toml ([template.X] with params + body, [[use]] with
template/parent/params), skill/examples/08-include/entry.toml ([[include]] path/once),
testdata/valid.toml ([properties], [unit.X] tables, link tables).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: emit_toml.go — Model to TOML in canonical field order</name>
  <files>internal/c4d/emit_toml.go, internal/c4d/emit_test.go</files>
  <read_first>skill/examples/06-templates.toml, skill/examples/08-include/entry.toml, testdata/valid.toml, internal/parser/parser.go (Model/TemplateDef/Instantiation/IncludeDirective structs), internal/model/unit.go, internal/model/link.go, internal/model/properties.go</read_first>
  <behavior>
    - Test: a Model with two top-level units emits [unit.first] before [unit.second] when UnitOrder says so — order always follows UnitOrder/SubunitOrder, never map iteration
    - Test: unit fields appear in EXACTLY the canonical order type, name, description, technology, reference, color, style, border, edges, expanded — a unit authored with scrambled field order in the AST still emits canonically (D-23)
    - Test: link fields appear in the order peer, arrow, rank, color, style, technology, description, labelPosition, length; LinksFrom emit as linkFrom tables; Mirror links are never emitted
    - Test: links emit as inline tables on the owning unit section (matching fixture style: link = { ... } or [[unit.X.link]] per fixture precedent — follow testdata/valid.toml)
    - Test: Templates emit as [template.<name>] with params = [...] and the unit body; Instantiations (top-level) emit as [[use]] with template/parent/params — Parent omitted when empty; TemplateDef.Instantiations emit as [[template.<name>.use]]
    - Test: Includes emit as [[include]] with path and once = true only when set
    - Test: strings containing double quotes/backslashes/non-ASCII emit correctly quoted; empty fields are OMITTED (never name = "")
    - Test: EmitTOML called twice on the same Model returns byte-identical strings
  </behavior>
  <action>
    Write failing tests first (internal/c4d/emit_test.go, black-box, t.Parallel fine). Implement internal/c4d/emit_toml.go exposing EmitTOML(m *parser.Model) (string, error) using strings.Builder following the recursive serializer shape of internal/testutil/canonical/canonical.go serializeDOTStatements (per 35-PATTERNS — deterministic tree serialization). Walk m.UnitOrder then SubunitOrder recursively. Fixed orders (D-23, pinned by this plan): unit fields type, name, description, technology, reference, color, style, border, edges, expanded; link fields peer, arrow, rank, color, style, technology, description, labelPosition, length; properties fields name, description, color, style, border, edges, lineLength, expanded. Omit empty/zero values. Table headers: [properties], [unit.<path>] for units and nested subunits (dotted paths), [template.<name>], [[use]], [[template.<name>.use]], [[include]] — mirror the fixture files' shapes exactly (they are the de-facto spec per 35-PATTERNS). String quoting via a quoteTOML helper handling escapes. Error return is reserved for future validation failures (nil today unless model is nil).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -run TestEmitTOML -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The canonical-order test constructs a Model with fields set in scrambled order and asserts the emitted lines follow the exact D-23 sequence
    - testdata/valid.toml parsed by parser.Parse then emitted produces tables in the same section order as the fixture (spot-assert [properties] first, units in UnitOrder)
    - Determinism test: two EmitTOML calls byte-equal
  </acceptance_criteria>
  <done>Any *parser.Model emits to deterministic canonical TOML matching fixture style.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: frommodel.go + emit_c4d.go — Model to AST to C4D text</name>
  <files>internal/c4d/frommodel.go, internal/c4d/emit_c4d.go, internal/c4d/emit_test.go</files>
  <read_first>internal/c4d/ast.go (full node set from 35-01/35-03), internal/c4d/emit_test.go (conventions from Task 1), internal/testutil/canonical/canonical.go lines 277-308 (serializer shape), 35-CONTEXT.md D-33</read_first>
  <behavior>
    - Test: FromModel maps Units to UnitNodes in UnitOrder, links to EdgeStmts, templates to TemplateDecls, instantiations to UseStmts, includes to IncludeStmts — spot-assert id, type, name, edge arrow glyph
    - Test: arrow glyph inverse mapping — ArrowForward -> "->", ArrowReverse rendered via the linkFrom statement form "<-", ArrowBidirectional -> "<->", ArrowNone -> "--" (Links emit "->"/"<->"/"--" statements; LinksFrom emit "<-" statements)
    - Test: compact-leaf style — a unit with no subunits, no edges and at most one short field emits on ONE line like `db: db { description: cache }`; a unit with subunits or edges emits multi-line with 2-space indent per depth (D-33)
    - Test: edge emission — `-> peer: "tech | desc"` with both parts; description-only omits the pipe; tech-only emits trailing pipe `tech |`; option blocks emit as trailing `{ color: red }` when options are set (D-09 inverse)
    - Test: template/use/include emission — `template n(p1, p2) { ... }`, `use n(name: "x")` (named args), `include ./x.c4d once`
    - Test: one blank line between top-level units (planner-pinned style rule under D-33 discretion)
    - Test: comments attached to AST statements emit on their own line immediately before the statement (fmt contract, D-32)
    - Test: EmitC4D(frommodel(parse(text))) is a fixpoint — running it twice changes nothing (idempotency prerequisite)
  </behavior>
  <action>
    Write failing tests first. Implement internal/c4d/frommodel.go exposing FromModel(m *parser.Model) *ast.Document: build Document with PropertiesBlock, UnitNodes (ID from map key, Name from unit.Name — note when Name equals the id the emitter still writes both since the header form is id: type "Name"), EdgeStmts from Links (glyph mapping per behavior bullet) and LinksFrom (as "<-" statements), TemplateDecls with Params and body, UseStmts with named Args from Params map, IncludeStmts. Comments stay nil on this path (convert loses comments by design — Model has none; only fmt preserves them, D-32). Then internal/c4d/emit_c4d.go exposing EmitC4D(doc *ast.Document) string: strings.Builder, 2-space indent per nesting depth, one-line leaf blocks per D-33 (planner-pinned rule: leaf = no subunits AND no edges AND at most 3 fields AND single-line-able values; otherwise multi-line), blank line between top-level statements of different kinds and between top-level units, header `id: type "Name" {` (external modifier prefix `system external` when set), fields in the D-23 order, edges after fields, subunits last, use statements inside blocks where they were authored. Deterministic: map iteration never used — walk orders only.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -run 'TestEmitC4D|TestFromModel' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The compact-leaf test asserts a leaf unit's output contains no newline between `{` and `}`
    - The nested test asserts indented multi-line output with exactly 2 spaces per depth
    - The fixpoint test asserts emit(parse(emit(x))) == emit(x) for a corpus document
    - Arrow inverse mapping has one test per glyph (4 cases)
  </acceptance_criteria>
  <done>Model→C4D emission works through the typed AST with the compact style; the single emitter serves both convert (no comments) and fmt (comments).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model -> emitted text | output written by convert/fmt later; emitters themselves are pure functions (no I/O) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Tampering | emitter determinism | mitigate | Emitters walk UnitOrder/SubunitOrder only — never map iteration; idempotency fixpoint test pins emit(emit(x)) == emit(x) so fmt cannot oscillate. |
| T-35-04-02 | Information disclosure | quoted string emission | mitigate | quoteTOML/quoteC4D escape control characters and quotes — no raw injection into the output format (a unit named with `}` cannot break out of a C4D block because names always emit quoted). |
| T-35-04-03 | Repudiation | canonical order | accept | D-23 fixed order makes diffs reviewable — a security-relevant field change always shows in the same position. |
</threat_model>

<verification>
- go test ./internal/c4d/ -count=1 green (emit suites + prior plans' suites)
- Emitted TOML from testdata/valid.toml re-parses via parser.Parse without error (round-trip prerequisite — assert in tests)
</verification>

<success_criteria>
- EmitTOML deterministic, canonical order, fixture-shaped
- FromModel + EmitC4D produce compact-leaf C4D, fixpoint-stable
- Comment emission path present for fmt (D-32 groundwork)
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-04-SUMMARY.md` when done
</output>
