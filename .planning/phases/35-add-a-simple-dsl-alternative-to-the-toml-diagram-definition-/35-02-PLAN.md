---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/parser/parser.go
  - internal/parser/parser_test.go
  - internal/template/expand.go
  - internal/template/expand_test.go
autonomous: true
requirements: [D-16, D-17]
must_haves:
  truths:
    - "A TOML file can express nested template instantiation via [[unit.<name>.use]] array-of-tables under a unit section, desugaring to Instantiation{Parent: enclosing unit path} (D-16)"
    - "A template body can contain [[template.<name>.use]] entries that expand recursively when the outer template is instantiated, with params flowing outer-to-inner (D-17)"
    - "Template A using B using A is a hard error naming the cycle chain (D-17)"
    - "The v1.10 STATE.md deferral of template nesting is lifted — nesting works in the shipped Expand (D-17)"
    - "Deep-copy (HS-1) holds at every recursion level — 3-level nesting with links produces disjoint LinksFrom, no shared Mirror state (D-17)"
  artifacts:
    - path: "internal/parser/parser.go"
      provides: "TemplateDef.Instantiations field + [[unit.X.use]] / [[template.X.use]] extraction"
      contains: "Instantiations"
    - path: "internal/template/expand.go"
      provides: "recursive template-body expansion with cycle detection and depth cap"
      contains: "cycle"
  key_links:
    - from: "internal/parser/parser.go extractUnitUses"
      to: "parser.Instantiation"
      via: "desugar to Parent = enclosing unit dotted path"
      pattern: "Instantiation\\{"
    - from: "internal/template/expand.go"
      to: "internal/parser TemplateDef.Instantiations"
      via: "post-substitution recursive expandInstantiation with ancestor stack"
      pattern: "Instantiations"
---

<objective>
Land nested use and template-instantiating-template in the TOML front-end and Expand pass: `[[unit.<name>.use]]` TOML sugar (D-16), `use` inside template bodies with recursive expansion, outer-to-inner param flow, and cycle detection (D-17).

Purpose: this is the genuinely new Expand work of Phase 35 (the existing `Instantiation.Parent` mechanism already attaches nested instantiations — 35-RESEARCH §4 — so C4D use-in-block later desugars onto what this plan builds).
Output: parser.Model capable of expressing nested + recursive instantiation; Expand that recurses safely.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md

<interfaces>
<!-- Exact contracts from the live codebase. Executor: use these directly. -->

From internal/parser/parser.go:
```go
type TemplateDef struct {
	Params []string
	Unit   *model.Unit
}
type Instantiation struct {
	Template string            // template name
	Parent   string            // dotted placement path ("" = top level)
	Params   map[string]string // includes "name"
}
```

From internal/template/expand.go (call graph):
Expand(m) loops m.Instantiations -> expandInstantiation(m, inst, i, produced) ->
tmpl.Unit.Clone() (HS-1 deep copy — reflection/gob would drop unexported Link.Mirror) ->
buildReplacer(tmpl.Params, inst.Params) -> applySubstitution(clone, replacer) ->
attachProduced / attachTopLevel / attachNested (attachNested already walks dotted Parent
paths and appends to Subunits/SubunitOrder — REUSE, do not rewrite).

From internal/include/resolve.go (cycle-detection pattern to copy):
const maxIncludeDepth = 100; stack []string; slices.Contains(stack, absPath) check;
error message "include cycle detected: " + strings.Join(cycle, " -> ") wrapped in
*parser.ParseError with Context = including file.

From internal/template/expand.go (error type): ExpandError{Kind, Site, Detail} with
Error() "template expand: %s at %s: %s".

Existing test to keep green: TestXC01_PipelineOrdering (pipeline order), TestExpandThreeInstantiationsHS1 (HS-1 disjointness).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: [[unit.X.use]] TOML sugar + [[template.X.use]] body extraction</name>
  <files>internal/parser/parser.go, internal/parser/parser_test.go</files>
  <read_first>internal/parser/parser.go (extractInstantiations lines 280-341, extractTemplates lines 182-218, parseIncludeDirective for array-of-tables walking), internal/parser/parser_test.go (test conventions), testdata/template_use_array.toml (existing [[use]] fixture)</read_first>
  <behavior>
    - Test: [[unit.mainapp.use]] with template/name/params keys desugars to Instantiation{Template, Parent: "mainapp", Params} appended in document order — identical to the equivalent top-level [[use]] with parent = "mainapp"
    - Test: nested sections work — [[unit.a.b.use]] desugars to Parent: "a.b"
    - Test: [[unit.X.use]] and top-level [[use]] entries interleave in document order
    - Test: [[template.svc.use]] entries populate TemplateDef.Instantiations with Parent relative to the template's unit root (empty Parent = template root)
    - Test: [[unit.X.use]] with an unknown key other than template/parent/params is a hard *parser.ParseError (mirror extractInstantiations' strictness)
    - Test: testdata/valid.toml and all existing template_* fixtures still parse identically (regression)
  </behavior>
  <action>
    Write failing tests first, then extend internal/parser/parser.go. Add field `Instantiations []Instantiation` to TemplateDef with a doc comment stating parents are relative to the template unit root and expansion is internal/template.Expand's job (mirror the existing TemplateDef comment style). For D-16: add an extraction pass (e.g. extractUnitUses(rawMap, m)) walking the rawMap unit subtrees for "use" arrays under each unit section, desugaring every entry to Instantiation{Template, Parent: <enclosing unit dotted path>, Params} — the SAME Instantiation form the existing top-level [[use]] produces (35-RESEARCH §4: this is the Phase 31 XC-03 Parent mechanism; no parallel semantics, attachNested already handles attachment). For D-17 TOML form: extend extractTemplates/parseTemplateDef to collect [[template.<name>.use]] arrays into TemplateDef.Instantiations the same way. Reuse the existing key-switch shape from extractInstantiations lines 296-330 (template/parent special keys, everything else into Params). Document both forms as equivalent in the doc comments.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/parser/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TemplateDef struct has exported Instantiations []Instantiation field
    - A test asserts [[unit.mainapp.use]] produces byte-identical Model.Instantiations to the parent="mainapp" top-level form
    - A test asserts [[template.svc.use]] lands in Templates["svc"].Instantiations
    - go test ./internal/parser/ -count=1 passes including all pre-existing tests
  </acceptance_criteria>
  <done>TOML authors can place use under unit sections and inside template bodies; both normalize to the single Instantiation mechanism.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Recursive template-body expansion + cycle detection in Expand</name>
  <files>internal/template/expand.go, internal/template/expand_test.go</files>
  <read_first>internal/template/expand.go (Expand lines 62-97, expandInstantiation lines 99-165, attachNested lines 283-314, pathTracker lines 455-503, assertNoResidualTokens lines 350-362), internal/include/resolve.go (maxIncludeDepth pattern, lines 32, 71-76, 118-125)</read_first>
  <behavior>
    - Test: template outer(p) whose body contains use inner(q = ${p}) — instantiating outer expands inner with q substituted from the outer param (outer-to-inner param flow, D-17)
    - Test: 3-level chain outer -> mid -> inner expands fully; produced units land at Parent paths inside the outer clone's subtree
    - Test: template A whose body uses B, and B's body uses A — Expand returns an error whose message contains "cycle" and the chain "A -> B -> A" (D-17)
    - Test: template using itself directly — cycle error names the self-loop
    - Test: pathological depth (chain of ~150 distinct templates, or a generated deep chain) hits a depth cap error rather than stack growth — cap mirrors maxIncludeDepth = 100
    - Test: TMPL-06 residual scan still fires post-recursion (a ${unfilled} token in a nested body is caught)
    - Test: TMPL-07 full-path collision still fires when a nested use produces a path that collides with a hand-authored unit
    - Test: HS-1 — 3-level nesting with links and linkFrom in bodies yields disjoint LinksFrom across instantiations (extend TestExpandThreeInstantiationsHS1 pattern)
    - Test: Expand fast path unchanged — model with no templates and no instantiations returns m untouched (existing contract)
  </behavior>
  <action>
    Write failing tests first (extend internal/template/expand_test.go; t.Parallel fine — no render). Then modify expandInstantiation: after Clone + buildReplacer + applySubstitution on the produced subtree, process the template's body Instantiations (TemplateDef.Instantiations from Plan-02 Task 1) — for each, substitute param VALUES through the outer replacer (outer-to-inner flow), then recursively expand it against the template registry with an ancestor stack of template names. Cycle detection copies the include pattern verbatim per 35-PATTERNS: `slices.Contains(stack, name)` -> error naming the chain "A -> B -> A"; depth cap constant (suggest maxTemplateDepth = 100 mirroring maxIncludeDepth); use ExpandError{Kind: "cycle" / "depth exceeded", Site: siteLabel(inst, index), Detail: chain} — follow the existing Error() format. Attach produced units INSIDE the clone's subtree via the existing resolveUnitByPath/attachNested machinery (Parent paths are relative to the template root — resolve against the clone, not m). Every level goes through tmpl.Unit.Clone() before substitution (HS-1 — never share subtrees between instantiations). Keep the post-loop TMPL-06/TMPL-07 checks as the recursion's exit gates (35-RESEARCH §4 confirms they naturally cover recursion). Recursion must preserve m.Instantiations document order semantics for top-level entries.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/template/ -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Nested-body instantiation test asserts the inner unit's fields carry outer param values substituted
    - Cycle test asserts the error message matches "A -> B -> A" (or the exact chain built)
    - Depth-cap test asserts an error (not panic, not hang) beyond 100 levels
    - Full `go test ./...` green — TestXC01_PipelineOrdering and all v1.10 template tests untouched and passing
  </acceptance_criteria>
  <done>Template-instantiating-template works recursively with param flow, cycle hard-errors, depth cap, HS-1 preserved; the v1.10 template-nesting deferral is functionally lifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored TOML -> Expand recursion | untrusted author input drives recursion depth; unbounded recursion = DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | DoS | internal/template/expand.go recursion | mitigate | Ancestor-stack cycle detection + maxTemplateDepth=100 depth cap copied from the include pattern; every level allocates via Clone so memory is bounded by depth * body size, and the cap bounds depth. |
| T-35-02-02 | Tampering | pathTracker claim ordering | mitigate | TMPL-07 full-path collision check runs AFTER all recursion completes (exit gate) — nested uses cannot silently overwrite hand-authored units; keep the post-loop placement. |
| T-35-02-03 | Repudiation | error reporting | accept | ExpandError carries Kind/Site/Detail naming the instantiation index and chain — sufficient for diagnosis; no security logging surface here. |
</threat_model>

<verification>
- go test ./internal/template/ ./internal/parser/ -count=1 green
- go test ./... -count=1 green (12/12 packages — 35-RESEARCH Risk 7 regression bar)
- testdata/template_* fixtures unchanged in behavior (no golden edits needed)
</verification>

<success_criteria>
- TOML: [[unit.X.use]] and [[template.X.use]] both authorable, both normalizing to Instantiation
- Expand recurses through template bodies with correct outer-to-inner params
- Cycles and depth overruns are hard errors naming the chain
- No existing template/include/peer behavior change (additive only)
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-02-SUMMARY.md` when done
</output>
