---
phase: 35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools.go
  - go.mod
  - go.sum
  - internal/c4d/c4d.peg
  - internal/c4d/parser_gen.go
  - internal/c4d/ast.go
  - internal/c4d/errors.go
  - internal/c4d/parse.go
  - internal/c4d/parse_test.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-12, D-18, D-20]
must_haves:
  truths:
    - "A .c4d document with brace-block units, nested subunits, fields, in-block edge statements and a properties block parses into a typed C4D AST (D-01, D-08, D-12)"
    - "Unit headers accept id, optional type keyword and optional quoted display name in the form id: type \"Name\" { ... } (D-02)"
    - "Type keywords are the exact TOML type strings (person, personExternal, system, db, queue, box, container, ...) and the external modifier maps to *External variants (D-03, D-04)"
    - "Arrows ->, <-, <-> and -- parse as edge statements with the desc/tech pipe shorthand (D-05, D-09)"
    - "Barewords, double-quoted, triple-quoted multi-line strings and # comments parse; URLs with scheme are valid barewords (D-06)"
    - "Identifiers match the TOML bare-key class [A-Za-z0-9_-]+ and dotted paths use . for peer references (D-07)"
    - ";' acts as a statement separator enabling one-line nested blocks, and empty blocks are legal (D-18)"
    - "go generate regenerates parser_gen.go from c4d.peg via pigeon pinned in go.mod (D-20)"
    - "Parse failures surface as *parser.ParseError with DSL-native line numbers"
  artifacts:
    - path: "internal/c4d/c4d.peg"
      provides: "PEG grammar for core C4D statements"
      min_lines: 150
    - path: "internal/c4d/parser_gen.go"
      provides: "generated pigeon parser (committed)"
      min_lines: 200
    - path: "internal/c4d/ast.go"
      provides: "typed C4D AST nodes with positions and attached comments"
      min_lines: 80
    - path: "internal/c4d/errors.go"
      provides: "pigeon errList to *parser.ParseError wrapping"
      min_lines: 30
    - path: "tools.go"
      provides: "build-time pigeon tool dependency"
      min_lines: 10
  key_links:
    - from: "internal/c4d/parse.go"
      to: "github.com/mna/pigeon generated Parse"
      via: "call generated parser, assert any result to typed AST"
      pattern: "ParseFile|pigeon"
    - from: "internal/c4d/errors.go"
      to: "internal/parser.ParseError"
      via: "errors.As wrapping with line from pigeon position"
      pattern: "parser\\.ParseError"
---

<objective>
Stand up the C4D front-end foundation: the pigeon toolchain (D-20), the core PEG grammar for brace-block units (D-01..D-09, D-12, D-18), the typed comment/position-aware AST that fmt will later consume (D-32 groundwork), and the *parser.ParseError error contract.

Purpose: everything else in Phase 35 (composition grammar, toModel, emitters, fmt, CLI) builds on this grammar and AST.
Output: importable internal/c4d package whose Parse/ParseFile return a typed AST for core documents; committed generated parser; tools.go pattern for regeneration.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-CONTEXT.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-RESEARCH.md
@.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Exact contracts from the live codebase. Executor: use these directly. -->

From internal/parser/errors.go — the error contract cmd/c4drill already handles:
```go
type ParseError struct {
	Message string
	Line    int    // 1-indexed, 0 if unknown
	Context string
	Cause   error
}
```

From internal/model/unit.go — type keyword vocabulary (D-03 exact strings):
person, personExternal, system, systemExternal, db, dbExternal, queue, queueExternal, box,
container, containerDb, containerQueue, containerBox,
component, componentDb, componentQueue, componentBox

From internal/model/link.go — edge option vocabulary:
ArrowDirection: forward | reverse | bidirectional | none
RankDirection: forward | reverse | equal
Link fields: Peer, Arrow, Rank, Color, Style, Technology, Description, LabelPosition, Length

From internal/parser/parser.go — ParseFile shape to mirror (with its //nolint:gosec comment):
```go
func ParseFile(path string) (*Model, error) {
	data, err := os.ReadFile(path)
	...
	return Parse(data)
}
```

pigeon API (pkg.go.dev, verified in 35-RESEARCH.md §2): generated parser exports
Parse(in string, data []byte, opts ...Option) (any, error); options include Recover(false),
Memoize(true), MaxExpressions(n); action code runs on a *current receiver with c.pos
(line/col 1-based), c.text, c.globalStore.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pigeon toolchain + minimal generating grammar</name>
  <files>tools.go, go.mod, go.sum, internal/c4d/c4d.peg, internal/c4d/parser_gen.go</files>
  <read_first>go.mod, .planning/codebase/CONVENTIONS.md, 35-RESEARCH.md section 2 (mna/pigeon integration)</read_first>
  <behavior>
    - Test: `go generate ./internal/c4d` (or direct `pigeon` invocation) regenerates parser_gen.go with no diff when the grammar is unchanged
    - Test: `go build ./...` passes with the generated parser compiled in
    - Test: golangci-lint reports no findings on internal/c4d (generated file carries nolint directives from pigeon's -nolint flag)
  </behavior>
  <action>
    Add `github.com/mna/pigeon` pinned at v1.0.0 to go.mod as a build-time-only dependency via the standard tools.go pattern: file tools.go at repo root with `//go:build tools` build tag importing `_ "github.com/mna/pigeon"` (per D-20; pigeon is a code generator, zero runtime imports). Create internal/c4d/c4d.peg with a minimal but valid grammar: one `Document` rule matching zero or more statements plus a `Comment` rule (`#` to end of line). Add the go:generate directive in a non-generated file in internal/c4d: `//go:generate pigeon -o parser_gen.go -nolint c4d.peg` — the -nolint flag emits the blanket nolint header the repo's all-linters gate requires (35-PATTERNS Risk 1). Install pigeon locally (`go install github.com/mna/pigeon@v1.0.0`), run the generation, and COMMIT internal/c4d/parser_gen.go so consumers never need pigeon (35-RESEARCH.md §2). Run `go mod tidy`. Keep the grammar entrypoint name `Document`.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go build ./... && go vet ./internal/c4d/ && grep -q "mna/pigeon" go.mod && test -f internal/c4d/parser_gen.go</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `github.com/mna/pigeon v1.0.0` (exact pinned version, not @latest)
    - tools.go exists with `//go:build tools` tag and the pigeon blank import
    - internal/c4d/parser_gen.go exists, is committed, and its header contains a nolint directive (from -nolint)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Pigeon pinned and importable, c4d.peg stub generates parser_gen.go, whole repo builds, lint gate not violated by generated code.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Core grammar + typed AST (units, nesting, fields, edges, properties, literals, comments)</name>
  <files>internal/c4d/c4d.peg, internal/c4d/parser_gen.go, internal/c4d/ast.go, internal/c4d/parse_test.go</files>
  <read_first>internal/c4d/c4d.peg, internal/model/unit.go (type keyword constants), internal/model/link.go (Link field vocabulary), internal/model/properties.go (Properties fields), 35-CONTEXT.md decisions D-01..D-09, D-12, D-18, skill/examples/02-nested.toml and 03-links.toml (the constructs the DSL must express)</read_first>
  <behavior>
    - Test: `system "Payment Gateway" { description: processes cards }` parses to AST node with Kind=system, Name="Payment Gateway", one field description
    - Test: omitted type parses (`api { }`) with empty Type slot in AST (inference happens later in toModel, NOT here — D-02 says type is omittable, inference is a Model-level hook)
    - Test: `system external "Partner" { }` parses with External modifier set (D-04)
    - Test: nesting `box "Platform" { api: system { } db: db { } }` produces Subunits in statement order (D-01)
    - Test: arrows — `-> peers`, `<- peers`, `<-> peers`, `-- peers` all parse as edge statements with the correct arrow glyph recorded (D-05, D-08)
    - Test: edge label shorthand — `-> db: queries orders` records Description="queries orders" Technology=""; `-> db: sql |` records Technology="sql" Description=""; `-> db: sql | queries` records both (D-09 — single un-piped value is DESCRIPTION, user's explicit override; get this right before emitters exist, 35-RESEARCH Risk 3)
    - Test: edge option block — `-> db: "sql | q" { color: red rank: equal }` parses trailing brace options (D-09)
    - Test: literals — bareword `description: simple value here`, double-quoted with `{` `}` `:` `|` and edge whitespace inside, triple-quoted multi-line string, `#` comment lines (D-06)
    - Test: `reference: https://example.com/docs` parses as bareword (scheme-prefixed URL, D-06)
    - Test: identifiers match `[A-Za-z0-9_-]+`; dotted paths `-> billing.svc` parse as peer refs (D-07, D-10)
    - Test: `properties { name: Demo }` top-level block parses into a Properties node with the same key set as TOML [properties]: name, description, color, style, border, edges, lineLength, expanded (D-12)
    - Test: `api: container { technology: Go; db: db { } }` — semicolon separator (D-18); `x: system { }` — empty block (D-18)
    - Test: comments attach to the following statement's AST node with their line position (D-32 groundwork — fmt needs trivia in the tree)
  </behavior>
  <action>
    Write the failing table-driven tests first in internal/c4d/parse_test.go (black-box package c4d_test, testify require/assert with per-field msg strings, t.Parallel allowed — no render involvement; conventions from internal/parser/parser_test.go per 35-PATTERNS). Then extend c4d.peg with the full core grammar and write internal/c4d/ast.go with typed nodes: Document, PropertiesBlock, UnitNode (ID, Type, External modifier, Name, Fields in order, Edges, Subunits, UseStmts placeholder, Comments, Pos), EdgeStmt (ArrowGlyph, Peer, Technology, Description, Options, Comments, Pos), FieldStmt, Comment node. PEG rules to encode: UnitHeader (identifier, optional `: type [external]`, optional quoted Name — D-02 form `id: type "Name"`), UnitBody (fields, edge statements, nested units, `;` separators, empty), Literal (bareword excluding `{ } : |` and edge-whitespace, double-quoted with escapes, triple-quoted), Comment token rule attached at statement level per 35-RESEARCH §2 (pigeon has no built-in trivia — the GRAMMAR captures it; actions run on the *current receiver with c.pos and append in match order). Bareword rule must accept scheme-prefixed URLs (D-06). Reserve statement keywords (use, include, template, properties) at the grammar level so they cannot parse as unit ids — full D-19 reserved-word error handling lands in Plan 03. Use Memoize(true) and MaxExpressions cap (e.g. 1000000) in the parser options for pathological-input safety. Regenerate parser_gen.go after every grammar change.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/c4d/ passes with table-driven cases covering every behavior bullet above
    - The D-09 triple case (desc-only, tech-only-pipe, both) has three distinct named test cases
    - ast.go exports typed nodes carrying Pos (line) and Comments fields
    - c4d.peg contains rules named at least: Document, Unit, UnitHeader, EdgeStmt, Literal, Comment, Properties
  </acceptance_criteria>
  <done>Core C4D constructs (units, nesting, fields, all four arrows, edge shorthand + options, properties, all literal forms, semicolons, empty blocks, attached comments) parse into the typed AST with green tests.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Front-end Parse/ParseFile + ParseError wrapping</name>
  <files>internal/c4d/parse.go, internal/c4d/errors.go, internal/c4d/parse_test.go</files>
  <read_first>internal/parser/parser.go lines 99-110 and 757-769 (Parse/ParseFile shape), internal/parser/errors.go (ParseError contract, wrapDecodeError pattern), internal/c4d/ast.go, 35-PATTERNS.md "internal/c4d/errors.go" section</read_first>
  <behavior>
    - Test: c4d.Parse([]byte) returns (*ast.Document, error) — success path on a valid minimal document
    - Test: syntax error input returns an error that errors.As-decodes to *parser.ParseError with Line matching the actual offending line (1-indexed)
    - Test: c4drill-style Context field carries the failing snippet or file path
    - Test: c4d.ParseFile(path) mirrors parser.ParseFile: read error returns *parser.ParseError with Message "failed to read file" and Context=path (copy the shape verbatim, including the //nolint:gosec G304 comment and explanation)
    - Test: Unwrap() on the returned error reaches the pigeon error cause
  </behavior>
  <action>
    Write internal/c4d/parse.go exposing Parse(data []byte) (*ast.Document, error) and ParseFile(path string) (*ast.Document, error). ParseFile copies the internal/parser ParseFile shape verbatim per 35-PATTERNS (os.ReadFile with //nolint:gosec G304 explanation, failure wrapped as &parser.ParseError{Message: "failed to read file", Context: path, Cause: err}). Parse calls the generated pigeon parser with the options fixed in Task 2, asserts the `any` result to *ast.Document (failure to assert = internal error wrapped as *parser.ParseError). Write internal/c4d/errors.go: a wrapPigeonError(err) helper that converts pigeon's errList (each *parserError carries Inner and position) into *parser.ParseError{Message, Line, Context, Cause} — mirror wrapDecodeError's errors.As structure from internal/parser/errors.go lines 45-75 and reuse the Error() format priority (line > context > plain). DSL-native line numbers come from the pigeon position (c.pos.line) per D-21.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/c4d/ -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/c4d exports Parse and ParseFile with the signatures above
    - A test feeds a 3-line document with the error on line 3 and asserts ParseError.Line == 3
    - errors.As(err, &parser.ParseError) succeeds on every parse failure path
  </acceptance_criteria>
  <done>C4D front-end is importable with the repo's standard error contract; cmd/c4drill error handling will need zero changes for parse failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .c4d file → parser | untrusted author input crosses into the parser; malformed/hostile input must fail closed with bounded work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-SC | Tampering | go.mod / pigeon install | mitigate | Pin github.com/mna/pigeon v1.0.0 exact version; go.sum checksums verified by Go toolchain; generated parser_gen.go committed so downstream builds never invoke pigeon. Tool was user-specified in D-20 with URL — provenance is the user, and Go module checksum DB covers the pin. |
| T-35-01-01 | DoS | internal/c4d/parse.go | mitigate | Enable pigeon Memoize(true) and MaxExpressions(1_000_000) so pathological inputs (catastrophic backtracking) terminate with an error instead of hanging. |
| T-35-01-02 | Information disclosure | internal/c4d/errors.go | mitigate | ParseError carries Message/Line/Context only — never dump full file contents or absolute home paths beyond the caller-supplied path. |
| T-35-01-03 | Tampering | parser_gen.go | accept | Committed generated code matches the pinned pigeon + committed grammar; regeneration diff is reviewable in CI. |
</threat_model>

<verification>
- go build ./... passes
- go test ./internal/c4d/ -count=1 passes (table-driven coverage of D-01..D-09, D-12, D-18 constructs)
- go test ./... stays green (no existing package touched except go.mod/tools.go additions)
- golangci-lint run reports no new findings in internal/c4d (generated file nolint header present)
</verification>

<success_criteria>
- c4d.ParseFile("x.c4d") returns a typed AST for every core construct
- Every parse failure is a *parser.ParseError with a DSL-native line number
- pigeon is pinned, generated code committed, regeneration is one go:generate command
- All 12+ existing packages still green
</success_criteria>

<output>
Create `.planning/phases/35-add-a-simple-dsl-alternative-to-the-toml-diagram-definition-/35-01-SUMMARY.md` when done
</output>
