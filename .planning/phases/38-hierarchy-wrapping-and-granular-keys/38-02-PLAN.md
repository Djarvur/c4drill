---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 02
type: tdd
wave: 2
depends_on: [38-01]
files_modified:
  - cmd/c4drill/root.go
  - cmd/c4drill/root_test.go
  - internal/view/view.go
  - internal/graph/graph.go
  - internal/graph/builder.go
  - internal/graph/builder_test.go
autonomous: true
requirements: [KEY-01, KEY-02]

must_haves:
  truths:
    - "--no-colors suppresses ALL colouring: unit Color/Border fills, link Color, AND kind-derived colours; legend kind rows adjust emergently"
    - "--no-styles suppresses unit Style/BorderStyle and link Style including the applyCollapsedPairStyle aggregate override"
    - "--no-length suppresses link Length/minlen; --no-rank suppresses RankReverse swap + NoConstraint at all render emission sites"
    - "With no switch set, behavior is exactly post-38-01 v1.14+WRAP; --plain is the UNION of the granular switches — its output is canonically identical with and without the granular flags (TestPlainUnionParity). The committed plain goldens (plain.dot/plain.expanded.dot) may fail ONLY as members of the 38-01 WRAP census (structure delta re-baselined in 38-04); no NEW failure may appear"
    - "properties.edges stays tied to --plain only (documented, not a granular aspect)"
  artifacts:
    - path: "internal/graph/graph.go"
      provides: "RenderOpts struct (Plain, NoColors, NoStyles, NoLength, NoRank) on Graph"
      contains: "RenderOpts"
    - path: "cmd/c4drill/root.go"
      provides: "--no-colors/--no-styles/--no-length/--no-rank flags threaded onto every view"
      contains: "no-colors"
    - path: "internal/graph/builder_test.go"
      provides: "per-switch guard tests"
      contains: "NoColors"
  key_links:
    - from: "cmd/c4drill/root.go"
      to: "internal/view/view.go View opts fields"
      via: "per-view assignment in processView + processExpandedView"
      pattern: "NoColors|NoStyles|NoLength|NoRank"
    - from: "internal/graph/builder.go createEdge"
      to: "RenderOpts guards"
      via: "opts.Plain || opts.NoX guard conditions"
      pattern: "opts\\.(Plain|NoColors|NoStyles|NoLength|NoRank)"
---

<objective>
Add granular CLI switches — `--no-colors`, `--no-styles`, `--no-length`, `--no-rank` — each suppressing exactly one formatting aspect, composing with the master `--plain` (KEY-01), with `--plain` locked as the exact UNION of the switches (KEY-02, relative parity lock — the committed plain goldens themselves are re-baselined for the WRAP delta in plan 38-04, NOT here).
Purpose: fine-grained opt-out control following the v1.14 --plain plumbing precedent.
Output: RenderOpts threading + 4 cobra flags + per-switch guard tests. No golden re-baselining in this plan (opt-in flags only; additive goldens land in 38-04).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-RESEARCH.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-01-SUMMARY.md

<interfaces>
<!-- Verified against current source 2026-08-30 -->

From cmd/c4drill/root.go:
- var block (L~49-56, //nolint:gochecknoglobals): format, outputDir, expanded, labelRatio, plain, version
- `cmd.PersistentFlags().BoolVar(&plain, "plain", false, ...)` (L97) — registration analog
- v.Plain = plain in processView (L~317) and processExpandedView (L~364) — per-view assignment analog

From internal/view/view.go: View.Plain bool (L39) — sibling fields go here
From internal/graph/graph.go: Graph.Plain bool (L44); Edge.NoConstraint/RankReverse (L95-101)

From internal/graph/builder.go (signatures carry `plain bool`):
- buildNode(entry, plain bool), buildCluster(entry, v, plain), createEdge(source, target, link model.Link, sourceEntry, penWidth, plain bool)
- applyUnitOverrides — colour/style/border application site
- applyCollapsedPairStyle — aggregate pair-style override (must be suppressed by NoStyles)
- kindColour (kind-derived colours) — SUPPRESSED by NoColors (planner pin: --no-colors means ALL colouring; legendKindEntries reads final edge colours so kind legend rows drop emergently — do not special-case the legend)
- BuildGraph / BuildExpandedGraph / BuildGraphWithPath — all three copy view fields into Graph

From internal/render/converter.go (rank emission sites tests must assert):
- endpoint swap `if edge.RankReverse` (~L555), setEdgeDir dir inversion (~L605), `if edge.NoConstraint { e.SetConstraint(false) }` (~L652)
- converter's `plain bool` call-tree threading (buildCgraph → createTopLevelNodes/createCluster/createEdges)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): per-switch guard tests + plain-parity lock</name>
  <files>internal/graph/builder_test.go, cmd/c4drill/root_test.go</files>
  <read_first>
    - internal/graph/builder_test.go (existing plain guard test patterns from phase 37)
    - cmd/c4drill/root_test.go (TestPlainFlag* E2E patterns, plain.dot/plain.expanded.dot golden tests, plain.toml fixture — reuse it: it carries color/border/style/length=3/rank=reverse/rank=equal/kind)
    - internal/graph/builder.go (applyUnitOverrides, createEdge, kindColour, applyCollapsedPairStyle)
    - 38-01-SUMMARY.md (current expected-failure census)
  </read_first>
  <behavior>
    - TestNoColorsSuppressesAllColouring: with NoColors, unit Color/Border empty, edge Color empty, AND kind-derived colour absent (kindColour suppressed); legend has no kind rows for colours no longer emitted
    - TestNoStylesSuppressesStyleAndBorders: unit Style/BorderStyle empty; edge Style empty; applyCollapsedPairStyle override not applied
    - TestNoLengthSuppressesMinlen: edge.MinLen == 0 despite link.Length = 3
    - TestNoRankSuppressesRanking: edge.RankReverse == false and NoConstraint == false despite link.Rank reverse/equal
    - TestDefaultPathUnchangedByOptFlags: with all four flags false, colors/styles/minlen/rank all present exactly as without them
    - TestPlainImpliesAllAspects: Plain=true yields the same suppression as Plain+all four granular flags set (union semantics)
    - TestPlainUnionParity (E2E, the KEY-02 lock): rendering plain.toml with `--plain` alone and with `--plain --no-colors --no-styles --no-length --no-rank` produces canonically IDENTICAL DOT (canonical.Canonical). NOTE: do NOT assert the committed plain.dot/plain.expanded.dot goldens green in this plan — they carry the 38-01 WRAP structure delta and are re-baselined in 38-04; the golden tests may be red in this plan's window as census members.
    - E2E per-switch: generated DOT from plain.toml with each single flag asserts the suppressed attribute absent and the OTHER aspects still present (e.g. --no-length output still carries author colour)
  </behavior>
  <action>
    Write guard tests at the builder level (construct Graph with opts set, assert Edge/Node fields) plus E2E tests in root_test.go following the TestPlainFlag* pattern. Run — MUST fail (flags/fields do not exist yet; write tests against the target API: View.NoColors/NoStyles/NoLength/NoRank + Graph opts). Commit test(38-02).
  </action>
  <verify>
    <automated>go test ./internal/graph/ -run 'NoColors\|NoStyles\|NoLength\|NoRank\|PlainImplies\|PlainUnion\|DefaultPathUnchanged' -v # RED</automated>
  </verify>
  <done>Guard + E2E tests compile against the target API and fail on missing implementation; TestPlainUnionParity may pass already (lock it green)</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): RenderOpts struct + flag threading + guards</name>
  <files>internal/view/view.go, internal/graph/graph.go, internal/graph/builder.go, internal/render/converter.go, cmd/c4drill/root.go</files>
  <read_first>
    - internal/view/view.go, internal/graph/graph.go (Plain field + doc-comment style)
    - internal/graph/builder.go in full (BuildGraph, BuildExpandedGraph, BuildGraphWithPath field copies; applyUnitOverrides + its call sites; createEdge; kindColour; applyCollapsedPairStyle)
    - internal/render/converter.go (plain call-tree threading — the internal signature shape to mirror; three rank emission sites)
    - cmd/c4drill/root.go (flag var block, PersistentFlags registration, processView/processExpandedView)
    - Task 1 tests
  </read_first>
  <behavior>
    All Task 1 tests pass. Default path byte-identical with flags unset.
  </behavior>
  <action>
    Introduce `RenderOpts` struct in internal/graph: fields Plain, NoColors, NoStyles, NoLength, NoRank bool. View gains NoColors/NoStyles/NoLength/NoRank bools beside Plain. Graph replaces its lone `Plain bool` with `Opts RenderOpts` (update the three field-copy sites: BuildGraph, BuildExpandedGraph, BuildGraphWithPath). Migrate the internal `plain bool` parameters in builder.go (buildNode, buildCluster, createEdge, applyUnitOverrides guards) and converter.go call tree to pass RenderOpts — mechanical; guard conditions become `opts.Plain || opts.NoColors` etc. so --plain remains the union. Guard mapping (pinned): NoColors → applyUnitOverrides skips Color/Border fills AND createEdge skips both link.Color and kindColour; NoStyles → skips Style/BorderStyle AND applyCollapsedPairStyle override; NoLength → MinLen stays 0; NoRank → NoConstraint/RankReverse stay false (this automatically neutralizes all three converter emission sites — assert them in tests, do not add converter-side guards). properties.edges remains tied to Plain only. cmd/c4drill: add four bool flag vars + PersistentFlags().BoolVar registrations after --plain (`--no-colors`, `--no-styles`, `--no-length`, `--no-rank`), assign onto every view in BOTH processView and processExpandedView. No package globals beyond the existing cobra var block convention.
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/render/ ./cmd/c4drill/ -count=1 -run 'NoColors\|NoStyles\|NoLength\|NoRank\|PlainImplies\|PlainUnion\|DefaultPathUnchanged' -v && gofmt -l internal/ cmd/ && go vet ./...</automated>
  </verify>
  <done>All guard tests green (TestPlainUnionParity locks union semantics); the committed plain goldens (plain.dot, plain.expanded.dot) are NOT asserted here — if their tests fail, the failures must be exactly the 38-01 WRAP census (structure-only), recorded in the SUMMARY for 38-04; no testdata file modified (git status shows no testdata changes); gofmt/vet clean</done>
</task>

<task type="auto">
  <name>Task 3: composition smoke — switches with each other and with WRAP output</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>
    - Task 1/2 tests; 38-01-SUMMARY.md (post-WRAP boundary view shape)
  </read_first>
  <behavior>
    - TestSwitchCombination: pairwise combinations (--no-colors+--no-length, --no-styles+--no-rank, all four) each suppress exactly the union of their aspects and nothing else, over the multilevel boundary view (post-WRAP nested clusters intact)
  </behavior>
  <action>
    Add the combination test over the multilevel fixture (exercises flags against wrapped boundary views from 38-01). This is a lock test — if it fails, fix Task 2 guards. Full quick suite after.
  </action>
  <verify>
    <automated>go test ./internal/... -count=1 2>&1 | tail -15 # failures == exactly 38-01's expected-failure census</automated>
  </verify>
  <done>Combination test green; failure set identical to 38-01's census (KEY is opt-in, zero new default-path failures)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → render pipeline | boolean flags cross into rendering; no untrusted input beyond existing author-model boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Repudiation | flag semantics drift (--plain no longer the union) | mitigate | TestPlainImpliesAllAspects + TestPlainUnionParity lock union semantics; absolute golden-green is asserted in plan 38-04 after the WRAP re-baseline |
| T-38-SC | Tampering | no new package installs | not-applicable | zero dependencies added this plan |
</threat_model>

<verification>
- go test ./internal/graph/ -run 'NoColors|NoStyles|NoLength|NoRank' -v green
- TestPlainUnionParity green (--plain == union of switches, canonical parity)
- go test -count=1 ./... — failure set == 38-01 census exactly (plain golden tests may appear there as WRAP-delta members)
</verification>

<success_criteria>
- KEY-01: four granular switches each suppress exactly one aspect; default path unchanged; kind-colour and legend boundary honored per pin
- KEY-02: --plain is the exact union of the granular switches (canonical parity locked); committed plain goldens carry only the 38-01 WRAP census failures, re-baselined in 38-04
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-02-SUMMARY.md` when done
</output>
