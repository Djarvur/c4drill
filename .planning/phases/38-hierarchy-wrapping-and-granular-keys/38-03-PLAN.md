---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 03
type: tdd
wave: 3
depends_on: [38-02]
files_modified:
  - cmd/c4drill/root.go
  - cmd/c4drill/root_test.go
  - internal/view/view.go
  - internal/graph/graph.go
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - internal/render/converter_test.go
autonomous: true
requirements: [LBL-01, LBL-02, LBL-03]

must_haves:
  truths:
    - "--no-labels renders all nodes, edges, and clusters as shapes only — no label text — so dot lays out without label geometry"
    - "Units render as their plain default shape (ShapeForType) with an empty label — no HTML tables, no record labels"
    - "Works on drill-downs AND --expanded generation, across dot/svg/html formats"
    - "Composes with --plain and the granular switches"
    - "The legend STAYS under --no-labels (planner pin: legend is metadata governed by properties.legend, not an element label)"
  artifacts:
    - path: "internal/graph/builder.go"
      provides: "NoLabels suppression at the GRAPH layer (labels dropped before emission)"
      contains: "NoLabels"
    - path: "cmd/c4drill/root.go"
      provides: "--no-labels flag threaded onto every view"
      contains: "no-labels"
    - path: "internal/render/converter_test.go"
      provides: "label-absence assertions in emitted DOT"
      contains: "NoLabels"
  key_links:
    - from: "cmd/c4drill/root.go"
      to: "View.NoLabels"
      via: "processView + processExpandedView assignment"
      pattern: "NoLabels"
    - from: "internal/graph/builder.go buildNode"
      to: "empty-label default-shape node"
      via: "opts.NoLabels guard skipping label construction"
      pattern: "NoLabels"
---

<objective>
Add `--no-labels`: suppress all label text (nodes, edges, clusters) at the graph layer so layout re-flows without label-induced routing distortion (LBL-01), everywhere and composing (LBL-02), with the legend pinned to stay (LBL-03).
Purpose: shape-only diagrams for layout-sensitive use.
Output: NoLabels threading + flag + label-absence tests. Opt-in — zero default-path change; additive E2E goldens deferred to 38-04.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-RESEARCH.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-02-SUMMARY.md

<interfaces>
<!-- Verified against current source 2026-08-30 -->

From internal/graph/builder.go:
- buildNode(entry *view.Entry, opts RenderOpts) *Node — label construction site for units (post-38-02 signature)
- buildCluster / buildNestedCluster / buildBoundaryCluster — cluster label construction sites
- createEdge — edge Label site (Edge.Label *graph.EdgeLabel)
- From 37-04: ShapeForType drives the plain default shape — under NoLabels the node keeps shape, drops label content

From internal/render/converter.go:
- setNodeLabel(cn, node, opts) / setClusterLabel — dispatch: HTML by default, buildRecordLabel under plain; NoLabels branch goes here (emit empty SetLabel, skip HTML dispatch entirely)
- createEdge (converter) — HTML rectangle emission `buildEdgeLabel` branch; NoLabels → no edge label
- g.Opts.Plain threading (post-38-02) — NoLabels rides the same struct

From cmd/c4drill/root.go: --plain/--no-* registration block (post-38-02); processView/processExpandedView assignment sites

Planner pins (from ROADMAP notes + RESEARCH):
- Suppression at GRAPH layer: builder drops Label CONTENT before emission (converter then naturally emits empty labels) — never SVG post-processing
- Legend default STAYS under --no-labels; its own properties.legend setting governs (document in 38-05)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): label-absence + composition tests</name>
  <files>internal/graph/builder_test.go, internal/render/converter_test.go, cmd/c4drill/root_test.go</files>
  <read_first>
    - internal/render/converter_test.go (phase 37 plain-label test patterns — TestConverter_Plain* are the analogs)
    - internal/graph/builder_test.go, cmd/c4drill/root_test.go (TestPlainFlag* E2E patterns; plain.toml fixture)
    - internal/render/converter.go (setNodeLabel/setClusterLabel/createEdge label branches)
    - internal/graph/builder.go (buildNode/buildCluster label construction)
  </read_first>
  <behavior>
    - TestNoLabelsNodesAreBareShapes: node.Label empty/nil under NoLabels; shape/type retained (ShapeForType path)
    - TestNoLabelsEdgesHaveNoLabel: Edge.Label nil under NoLabels
    - TestNoLabelsClustersUnlabelled: cluster label content dropped; cluster STRUCTURE (nesting, wrappers from 38-01) retained
    - TestNoLabelsLegendStays: legend cluster/entries present under NoLabels (pinned default)
    - TestNoLabelsDOTEmitsNoLabelMarkup (converter): emitted DOT has no HTML-table node/edge labels and no record-label text — empty label= attributes only for elements (legend/table markup exempt — use the phase 37 lowercase-markup discriminator pattern)
    - TestNoLabelsAllGenerationsAndFormats (E2E): --no-labels applies to C1, drill-downs, and --no-labels --expanded; dot/svg/html generated non-empty
    - TestNoLabelsComposesWithPlainAndSwitches: --plain --no-labels and --no-colors --no-labels both work (union semantics)
    - TestNoLabelsOptIn: without the flag, labels identical to default (HTML tables present)
  </behavior>
  <action>
    Write tests against the target API (View.NoLabels / Graph Opts.NoLabels). Converter-level tests assert on rendered DOT strings; builder tests assert on Graph fields; E2E in root_test.go over plain.toml. Run — MUST fail. Commit test(38-03).
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/render/ -run 'NoLabels' -v # RED</automated>
  </verify>
  <done>Tests compile against target API and fail on missing implementation; opt-in lock may pass (pin it green)</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): NoLabels threading + graph-layer suppression</name>
  <files>internal/view/view.go, internal/graph/graph.go, internal/graph/builder.go, internal/render/converter.go, cmd/c4drill/root.go</files>
  <read_first>
    - 38-02 landed code: RenderOpts struct, flag registration block, converter opts threading (read current source, not summaries)
    - internal/graph/builder.go (buildNode label construction, buildCluster/buildNestedCluster labels, createEdge Label)
    - internal/render/converter.go (setNodeLabel/setClusterLabel/createEdge — add NoLabels branch beside the plain branch)
    - cmd/c4drill/root.go, internal/view/view.go
    - Task 1 tests
  </read_first>
  <behavior>
    All Task 1 tests pass; default path untouched.
  </behavior>
  <action>
    Add NoLabels bool to RenderOpts and View. In builder.go, guard label construction: under opts.NoLabels, buildNode leaves Label nil (keep Icon/type/shape semantics — the node's ShapeForType default shape comes from type, retained), buildCluster/buildNestedCluster/buildBoundaryCluster skip cluster label content (cluster IDs retained — structure only), createEdge leaves Label nil. Add the converter-side NoLabels branch at setNodeLabel/setClusterLabel (emit SetLabel(""), skip HTML dispatch) and the converter createEdge edge-label branch (no label emission) as defense-in-depth so the DOT is clean even for hand-built graphs. Register `--no-labels` cobra flag beside the 38-02 switches; assign onto every view in processView AND processExpandedView (expanded composition is LBL-01's explicit requirement). Legend untouched — it is built by its own builder path and stays by default.
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/render/ ./cmd/c4drill/ -count=1 -run 'NoLabels\|Plain\|NoColors' -v && gofmt -l internal/ cmd/ && go vet ./...</automated>
  </verify>
  <done>All NoLabels tests green; 38-02's guard tests and plain goldens still green; no testdata modified; gofmt/vet clean</done>
</task>

<task type="auto">
  <name>Task 3: full-suite calibration</name>
  <files>internal/render/converter_test.go</files>
  <read_first>
    - 38-01-SUMMARY.md expected-failure census; current go test ./internal/... output
  </read_first>
  <behavior>
    - Failure set over go test ./internal/... and ./cmd/c4drill/ is exactly 38-01's census — LBL/KEY added zero new default-path failures
  </behavior>
  <action>
    Run full suite; compare failures to 38-01's census; any new failure is a regression in this plan — fix before done. Note in SUMMARY that cmd/c4drill golden E2E tests referencing boundary views now fail pending 38-04's re-baseline (expected).
  </action>
  <verify>
    <automated>go test -count=1 ./... 2>&1 | grep -c '^--- FAIL\|^FAIL' # matches census from 38-01 SUMMARY</automated>
  </verify>
  <done>Zero unattributed failures; census delta recorded in SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → render pipeline | boolean flag; no new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Denial of Service | empty labels causing degenerate dot layouts on huge models | accept | layout degeneration is a cosmetic concern; dot still terminates; user opt-in |
| T-38-SC | Tampering | no package installs | not-applicable | zero dependencies added |
</threat_model>

<verification>
- go test ./internal/render/ -run 'NoLabels' -v green
- go test -count=1 ./... — failure set == 38-01 census
- Legend-present and expanded-composition assertions green
</verification>

<success_criteria>
- LBL-01: --no-labels silences all element labels, layout re-flows (graph-layer suppression)
- LBL-02: works on drill-down AND --expanded, all formats, composes with --plain and granular switches
- LBL-03: legend stays by default under labels-off (pinned, to be documented in 38-05)
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-03-SUMMARY.md` when done
</output>
