---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 04
type: execute
wave: 4
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - cmd/c4drill/testdata/multilevel.expanded.dot
  - cmd/c4drill/testdata/plain.dot
  - cmd/c4drill/testdata/plain.expanded.dot
  - cmd/c4drill/root_test.go
  - cmd/c4drill/testdata/nolabels.dot
  - cmd/c4drill/testdata/nolabels.expanded.dot
autonomous: false
requirements: [KEY-03, BC-01]

must_haves:
  truths:
    - "Every switch and the labels key works across every generation (C1, all drill-downs, --expanded) and every format (dot/svg/html), and composes with --plain and each other (E2E matrix)"
    - "ONE consolidated golden re-baseline: every diff corresponds ONLY to the documented WRAP cluster-structure deltas"
    - "Full suite green after re-baseline; new additive goldens for --no-labels committed"
    - "Visual evidence that wrapper clusters render legibly and labels-off layout is readable"
  artifacts:
    - path: "cmd/c4drill/root_test.go"
      provides: "KeyComposition E2E matrix test"
      contains: "KeyComposition"
    - path: "cmd/c4drill/testdata/nolabels.dot"
      provides: "additive --no-labels golden"
    - path: "cmd/c4drill/testdata/plain.dot"
      provides: "re-baselined plain golden (WRAP structure)"
  key_links:
    - from: "cmd/c4drill/root_test.go"
      to: "generated output dirs"
      via: "flag-combination renders asserted per generation × format"
      pattern: "KeyComposition|generateFixture"
---

<objective>
Prove every key composes everywhere (KEY-03) and perform the ONE consolidated golden re-baseline for the WRAP deltas (BC-01) — real re-baselining this time, every diff audited as cluster-structure-only — plus additive goldens for the new flags.
Purpose: the compatibility gate before docs and release.
Output: green full suite, audited re-baselined goldens, composition matrix test, additive nolabels goldens.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-01-SUMMARY.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-02-SUMMARY.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-03-SUMMARY.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-05-SUMMARY.md

<interfaces>
<!-- Golden-audit pattern from 37-05 -->
- Render fixture to tmp dir with exact production flags (the root_test.go generatePlainFixtureOutput/collectGeneratedFiles helpers from 37-04 are the analogs)
- Compare with canonical.Canonical (internal/testutil/canonical — strips geometry, sorts statements)
- Attribute EVERY diff to a documented delta before re-baselining; never absorb unrelated churn
- Expected-failure census from 38-01 SUMMARY defines the pre-known breakage set
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KEY-03 composition matrix E2E</name>
  <files>cmd/c4drill/root_test.go</files>
  <read_first>
    - cmd/c4drill/root_test.go (TestPlainFlagAppliesToAllGeneratedFiles / TestPlainFlagAllFormats patterns)
    - 38-02/38-03 SUMMARYs (flag names, semantics)
  </read_first>
  <behavior>
    - TestKeyComposition: for each switch {--no-colors, --no-styles, --no-length, --no-rank, --no-labels} × generation {C1, a drill-down, --expanded} × format {dot, svg, html}: output generated non-empty AND the switch's suppression observable in dot output; pairwise compositions --plain --no-labels and --no-colors --no-labels included; flat + multilevel fixtures
  </behavior>
  <action>
    Implement the matrix as a table-driven E2E test following the 37-04 helper pattern (render to tmp dir, walk generated files, assert). Dot-level assertions reuse the per-switch discriminators from 38-02/38-03 unit tests; svg/html asserted for existence/non-emptiness plus one marker each (e.g. author hex absent under --no-colors in svg). Do not add goldens here — behavior assertions only.
  </action>
  <verify>
    <automated>go test ./cmd/c4drill/ -run 'KeyComposition' -count=1 -v</automated>
  </verify>
  <done>Matrix green across all switch × generation × format cells including compositions</done>
</task>

<task type="auto">
  <name>Task 2: ONE consolidated golden re-baseline (BC-01)</name>
  <files>cmd/c4drill/testdata/multilevel.expanded.dot, cmd/c4drill/testdata/plain.dot, cmd/c4drill/testdata/plain.expanded.dot, plus any in-package canonical expectation files in 38-01's census</files>
  <read_first>
    - 38-01-SUMMARY.md (expected-failure census — THE authoritative scope list)
    - 37-05-SUMMARY.md (golden-audit pattern: byte-compare, attribute, re-baseline)
    - internal/testutil/canonical/canonical.go (comparator contract)
  </read_first>
  <behavior>
    - After re-baseline: go test -count=1 ./... fully green (zero failures)
    - Every regenerated golden's diff vs the old file contains ONLY: added cluster_* wrapper statements/subgraphs, changed cluster nesting containment, and corresponding graph-statement reordering — zero node-ID changes, zero edge-endpoint changes, zero attribute-value changes outside cluster structure
  </behavior>
  <action>
    Follow 37-05's audit pattern: for each golden in 38-01's census, render the fixture to tmp with exact production flags, diff fresh vs committed, and audit each hunk against the documented WRAP delta (wrapper clusters). Re-baseline by regenerating committed goldens via the production CLI (the tests' own generation path). If ANY hunk is not explainable as cluster structure, STOP — that is a 38-01 regression; fix there, not here. Additive goldens: generate cmd/c4drill/testdata/nolabels.dot and nolabels.expanded.dot from plain.toml with --no-labels / --no-labels --expanded and add their canonical golden tests (mirroring TestPlainFlagC1Golden). Flat-model tests must already be green — no flat golden changes permitted.
  </action>
  <verify>
    <automated>go test -count=1 ./... && git diff --stat -- cmd/c4drill/testdata/ # census files only</automated>
  </verify>
  <done>Full suite green; re-baseline scope == census exactly; additive nolabels goldens committed; audit table in SUMMARY (golden × consumed-by × diff-attribute)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: visual approval checkpoint</name>
  <files>[]</files>
  <action>Human verifies rendered SVGs produced by the automated tasks — no code changes in this task.</action>
  <what-built>
    WRAP wrapping (nested boundary entries + expanded-unit ancestor skeleton), the four granular switches, and --no-labels, all rendering end to end.
  </what-built>
  <how-to-verify>
    1. go run ./cmd/c4drill generate -i cmd/c4drill/testdata/multilevel.toml -o /tmp/p38-visual && open /tmp/p38-visual/multilevel.svg — boundary/sibling entries now sit inside their ancestor container clusters; wrapper clusters labelled with pretty names, no 🔍 on wrappers.
    2. go run ./cmd/c4drill generate -i cmd/c4drill/testdata/plain.toml -o /tmp/p38-plain --no-labels && open /tmp/p38-plain/plain.svg — shapes only, no label text, legend still present, layout re-flowed.
    3. Same with --no-colors: output monochrome, legend kind rows gone.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed goldens ↔ current output | goldens are the compatibility contract; unaudited re-baselining could mask regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Tampering | golden re-baseline masking non-WRAP regressions | mitigate | audit every diff hunk against the documented WRAP delta; stop-and-fix on unexplainable hunks; audit table recorded in SUMMARY |
| T-38-SC | Tampering | no package installs | not-applicable | zero dependencies |
</threat_model>

<verification>
- go test -count=1 ./... fully green
- TestKeyComposition green
- git diff scope on testdata == census files + new nolabels goldens only
</verification>

<success_criteria>
- KEY-03: composition matrix proven across generations, formats, and flag combinations
- BC-01: only documented WRAP deltas in re-baselined goldens; suite green; additive flag goldens committed
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-04-SUMMARY.md` when done — include the golden audit table
</output>
