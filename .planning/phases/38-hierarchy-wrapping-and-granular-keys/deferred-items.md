| bug | CI `Validate Examples` fails on master: plugin tree tracks `examples/11-nesting-context/` drill-down SVGs but `skill/` gitignores them, so `diff -r` is asymmetric in pristine checkouts. Pre-existing (identical failure on 37-07 commit 33317601241, 2026-08-14). Release workflow v1.22.0 unaffected (green). Fix in a follow-up: either track the dir in skill/ or drop it from both plugin trees. | open | 38-06 |
