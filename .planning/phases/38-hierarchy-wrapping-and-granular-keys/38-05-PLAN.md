---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 05
type: execute
wave: 5
depends_on: [38-04]
files_modified:
  - README.adoc
  - skill/SKILL.md
  - plugins/c4drill/skills/c4drill-toml/SKILL.md
  - plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md
  - skill/examples/
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "README.adoc documents the wrapping behavior (boundary/sibling entries render inside ancestor chains) and every new key (--no-colors, --no-styles, --no-length, --no-rank, --no-labels) including composition with --plain and the pinned boundaries (kind colours under --no-colors; legend stays under --no-labels; properties.edges tied to --plain only)"
    - "skill/SKILL.md and BOTH plugin copies are updated in ONE task — byte-identical (CI diff -r parity holds)"
    - "New example fixtures demonstrate wrapping and the new keys and render cleanly through the full pipeline (CI-validated)"
  artifacts:
    - path: "README.adoc"
      provides: "v1.15 wrapping + keys documentation"
      contains: "no-labels"
    - path: "skill/SKILL.md"
      provides: "skill documentation for wrapping + keys"
      contains: "no-labels"
    - path: "skill/examples/"
      provides: "fixture demonstrating wrapping/keys"
  key_links:
    - from: "skill/SKILL.md"
      to: "plugins/c4drill/skills/c4drill-toml/SKILL.md"
      via: "byte-identical copies (CI diff -r)"
      pattern: "."
    - from: "skill/examples/"
      to: "CI example validation workflow"
      via: "fixtures render through the pipeline"
      pattern: "."
---

<objective>
Document the v1.15 behavior deltas and keys across README.adoc, skill/SKILL.md, and both plugin copies (single-task sync for CI diff -r parity), with example fixtures proving the docs render.
Purpose: users can discover wrapping behavior and every key; CI parity gate stays green.
Output: updated docs + synced 3-copy skill + new validated examples.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-04-SUMMARY.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md

<interfaces>
<!-- From 37-PATTERNS.md: the binding constraint is mechanical -->
- CI enforces `diff -r skill plugins/c4drill/skills/c4drill-toml` and `diff -r skill plugins/c4drill/opencode/skills/c4drill-toml` parity (.github/workflows/validate-skill-examples.yml) — sync ALL 3 copies in the same task
- skill/examples/ follow the existing .toml (+ .c4d twin where applicable) convention; CI renders them through the full pipeline
- Pinned boundaries to document (from planning): --no-colors suppresses kind-derived colours too and legend kind rows adjust emergently; legend STAYS under --no-labels (metadata, governed by properties.legend); properties.edges stays --plain-only; --plain remains the union of all switches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.adoc — wrapping + every key</name>
  <files>README.adoc</files>
  <read_first>
    - README.adoc (existing --plain / nesting sections from v1.14 — extend, don't restructure)
    - 38-04-SUMMARY.md (exact flag names, semantics, goldens)
    - cmd/c4drill/root.go (flag help strings — quote them verbatim in the flag table)
  </read_first>
  <behavior>
    - README contains: a wrapping section (what changed vs v1.14, fully-external-stays-top-level rule), a key reference for all five new flags with one-line semantics, composition notes (--plain = union; switches combine), the three pinned boundaries, and an --expanded note for --no-labels
  </behavior>
  <action>
    Add/extend sections per the behavior spec. Update the flags table with the five new BoolVar registrations and their help text. Note the v1.15 change log entry (boundary/sibling wrapping reversal). Keep the existing --plain documentation accurate (union semantics unchanged).
  </action>
  <verify>
    <automated>grep -c 'no-labels\|no-colors\|no-styles\|no-length\|no-rank' README.adoc # >= 5 distinct flags documented; asciidoc renders: explicitly spot-check syntax</automated>
  </verify>
  <done>All five keys + wrapping documented with pinned boundaries; --plain section consistent</done>
</task>

<task type="auto">
  <name>Task 2: skill 3-copy sync + example fixtures</name>
  <files>skill/SKILL.md, plugins/c4drill/skills/c4drill-toml/SKILL.md, plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md, skill/examples/</files>
  <read_first>
    - skill/SKILL.md (v1.14 --plain wording — the insertion pattern)
    - .github/workflows/validate-skill-examples.yml (parity + render validation contract)
    - skill/examples/ (existing fixture conventions)
  </read_first>
  <behavior>
    - diff -r skill plugins/c4drill/skills/c4drill-toml → empty; diff -r skill plugins/c4drill/opencode/skills/c4drill-toml → empty
    - New example fixture(s): a model exercising boundary/sibling wrapping plus at least one new key invocation documented; renders cleanly through the pipeline
  </behavior>
  <action>
    Edit skill/SKILL.md ONLY, then copy byte-identical to both plugin paths (per project convention the copies must stay identical; one task, one commit). Add concise v1.15 coverage: wrapping rule + the five keys with the pinned boundaries. Add example fixture(s) to skill/examples/ following the existing .toml/.c4d twin convention, render them through the production CLI to prove clean output, and reference them from SKILL.md if the existing examples are referenced. Run the CI validation workflow locally if a make target exists; otherwise run the two diff -r commands and the example render command.
  </action>
  <verify>
    <automated>diff -r skill plugins/c4drill/skills/c4drill-toml && diff -r skill plugins/c4drill/opencode/skills/c4drill-toml && go run ./cmd/c4drill generate -i <new-example>.toml -o /tmp/p38-skill-check</automated>
  </verify>
  <done>Both diff -r checks empty; new fixtures render cleanly; skill documents wrapping + keys</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/examples → CI | example fixtures are rendered by CI; malformed fixtures break the parity/render workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Denial of Service | example fixtures drifting from CI validation | mitigate | render new fixtures locally through the production CLI in this task before commit |
| T-38-SC | Tampering | no package installs | not-applicable | zero dependencies |
</threat_model>

<verification>
- diff -r skill plugins/c4drill/skills/c4drill-toml and skill vs opencode copy — both empty
- New examples render via production CLI
- go test -count=1 ./... still green (docs-only, but guards against accidental source edits)
</verification>

<success_criteria>
- DOC-01: README documents wrapping + every key + pinned boundaries
- DOC-02: skill/SKILL.md + both plugin copies synced byte-identical
- DOC-03: example fixtures demonstrate wrapping/keys and render cleanly
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-05-SUMMARY.md` when done
</output>
