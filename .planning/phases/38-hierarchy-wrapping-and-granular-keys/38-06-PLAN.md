---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 06
type: execute
wave: 6
depends_on: [38-05]
files_modified: []
autonomous: false
requirements: [REL-01]

must_haves:
  truths:
    - "Product release v1.22.0 is tagged on the milestone's final commit and the release workflow runs green"
  artifacts: []
  key_links:
    - from: "git tag v1.22.0"
      to: "release workflow"
      via: "tag push triggers release pipeline"
      pattern: "v1\\.22\\.0"
---

<objective>
Tag and ship product release v1.22.0 (REL-01) — the milestone's final task.
Purpose: land v1.15 as a shipped product release per milestone contract.
Output: tag v1.22.0 pushed, release workflow green, STATE/ROADMAP progress updated.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Precedent: phases 36/37 closed the same way (v1.18.0, v1.21.0 release tasks) -->
- Bump the version constant in cmd/c4drill (root.go `version = "dev"` is ldflags-injected — check how prior releases bumped; if version is ldflags-only, the tag alone carries the number)
- Full suite green BEFORE tagging (go test -count=1 ./...)
- gh release workflow precedent from v1.21.0 (phase 37 release task)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-release gate</name>
  <files>[]</files>
  <read_first>
    - How v1.21.0 was tagged: git tag --list 'v1.21*' && git show v1.21.0 --stat
    - .github/workflows/ release workflow definition
  </read_first>
  <behavior>
    - go test -count=1 ./... green; gofmt/vet clean; working tree clean of uncommitted planning artifacts
  </behavior>
  <action>
    Run the full uncached suite, gofmt -l, go vet ./.... Verify version bump mechanics match the v1.21.0 precedent (ldflags vs constant). Commit any outstanding planning artifacts (SUMMARYs, ROADMAP progress). Do NOT tag yet.
  </action>
  <verify>
    <automated>go test -count=1 ./... && gofmt -l . | wc -l # 0 failing, 0 unformatted</automated>
  </verify>
  <done>Suite green, tree clean, release mechanics confirmed</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: release decision checkpoint</name>
  <decision>Tag and publish release v1.22.0</decision>
  <action>Human selects tag-now or hold; no code changes in this task.</action>
  <context>REL-01 ships the milestone. Tag push triggers the public release workflow — this is the one irreversible step of the phase.</context>
  <options>
    <option id="tag-now">
      <name>Tag v1.22.0 now</name>
      <pros>Milestone completes; release notes from SUMMARYs</pros>
      <cons>Public and irreversible</cons>
    </option>
    <option id="hold">
      <name>Hold — I want changes first</name>
      <pros>Nothing public until reviewed</pros>
      <cons>Milestone stays open</cons>
    </option>
  </options>
  <resume-signal>Select: tag-now or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 2: tag v1.22.0 + verify release</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - v1.21.0 release commit/tag precedent (git log --grep 'v1.21')
  </read_first>
  <behavior>
    - git tag v1.22.0 exists on the final commit; `gh release view v1.22.0` succeeds (or workflow run green per precedent); ROADMAP/STATE mark Phase 38 complete
  </behavior>
  <action>
    Tag the final commit as v1.22.0 and push the tag (annotated, matching the v1.21.0 style). Verify the release workflow starts and completes green (gh run watch or gh release view). Update ROADMAP Phase 38 progress (plans complete, shipped date, tag) and STATE.md progress. Commit planning updates.
  </action>
  <verify>
    <automated>git tag --list | grep -x v1.22.0 && gh release view v1.22.0 --json name,isDraft -q '.name'</automated>
  </verify>
  <done>v1.22.0 tagged, release published/green, planning records updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tag → public release | tag push publishes publicly; irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | Repudiation | tagging a red suite | mitigate | Task 1 pre-release gate is blocking before the human-action checkpoint |
| T-38-SC | Tampering | no package installs | not-applicable | zero dependencies |
</threat_model>

<verification>
- go test -count=1 ./... green before tagging
- v1.22.0 tag present; release workflow green
</verification>

<success_criteria>
- REL-01: product release v1.22.0 tagged and published; milestone closed
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-06-SUMMARY.md` when done
</output>
