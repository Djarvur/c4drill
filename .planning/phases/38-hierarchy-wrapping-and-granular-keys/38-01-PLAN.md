---
phase: 38-hierarchy-wrapping-and-granular-keys
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - internal/view/scope_test.go
autonomous: true
requirements: [WRAP-01, WRAP-02, WRAP-03]

must_haves:
  truths:
    - "On C2/C3 drill-downs, a boundary/sibling entry with an in-model ancestor renders inside its complete chain of ancestor wrapper clusters"
    - "A fully external boundary entry (no in-model ancestor) stays top-level"
    - "The expanded unit renders inside wrapper clusters for its own ancestor chain (root → parent), then its boundary cluster"
    - "Wrapper clusters carry pretty names from AncestorNames and NO ExploreURL drill affordance"
    - "The depicted node set (node IDs) is unchanged by wrapping — containers only"
  artifacts:
    - path: "internal/graph/builder.go"
      provides: "ancestor wrapper-cluster construction in buildBoundaryViewGraph/buildBoundaryCluster"
      contains: "wrapper"
    - path: "internal/graph/builder_test.go"
      provides: "boundary wrapping tests over the multilevel fixture"
      contains: "Wrap"
    - path: "internal/view/scope_test.go"
      provides: "node-set invariance test"
      contains: "NodeSetInvariant"
  key_links:
    - from: "internal/graph/builder.go buildBoundaryViewGraph"
      to: "view.View.AncestorNames"
      via: "wrapper cluster label lookup"
      pattern: "AncestorNames\\["
    - from: "internal/graph/builder.go"
      to: "buildNestedCluster"
      via: "reuse of existing nesting machinery conventions"
      pattern: "buildNestedCluster|buildCluster"
---

<objective>
Wrap boundary/sibling entries and the expanded unit in their complete ancestor-container chains on every generated view (WRAP-01/02), locking the depicted node set (WRAP-03). This reverses v1.14's scoping decision; it is the only plan in this phase allowed to change default-path output.
Purpose: nothing depicted may hang in the air; v1.15's core correction.
Output: wrapping implemented in internal/graph/builder.go, RED→GREEN tests, node-set invariance test. EXPECTED: existing boundary-view golden tests fail after this plan — record them in the SUMMARY as the expected-failure list for 38-04's re-baseline.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-RESEARCH.md

<interfaces>
<!-- Fix points verified against current source 2026-08-30 -->

From internal/graph/builder.go:
- func buildBoundaryViewGraph(v *view.View, g *Graph) — C2/C3 dispatch; currently: `if entry.IsBoundary || entry.IsExternal { node := buildNode(entry, v.Plain); g.Nodes = append(...); continue }` (the top-level placement to reverse)
- func buildBoundaryCluster(v *view.View) *Cluster — the expanded unit's top-level boundary cluster (ID = expanded unit path)
- func buildNestedCluster(entry *view.Entry, path string, v *view.View) *Cluster — the v1.14 recursive nesting machinery to mirror conventions from
- func buildCluster(entry *view.Entry, v *view.View, plain bool) *Cluster
- func buildNode(entry *view.Entry, plain bool) *Node

From internal/view/view.go:
- Entry{FullPath, IsBoundary, IsExternal, IsExpanded, UnfoldChain, HasSubunits, Unit}
- View.AncestorNames map[string]string — dotted prefix path → pretty display Name (buildAncestorNames, scope.go)

From internal/view/scope.go:
- createExternalBoundaryNode (L221), addResolvedBoundaryNode (L810), resolveBoundaryByDivergence (L855) — boundary paths returned are ≤ common-prefix+1 depth, so wrap chains are 1–2 clusters and typically MERGE with the expanded unit's ancestor skeleton
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): boundary wrapping + invariance tests</name>
  <files>internal/graph/builder_test.go, internal/view/scope_test.go</files>
  <read_first>
    - internal/graph/builder.go (buildBoundaryViewGraph, buildBoundaryCluster, buildNestedCluster, buildC1ViewGraph)
    - internal/view/view.go (Entry/View fields, AncestorNames)
    - internal/view/scope.go (createExternalBoundaryNode, addResolvedBoundaryNode, resolveBoundaryByDivergence, buildAncestorNames)
    - cmd/c4drill/testdata/multilevel.toml (4-level fixture with boundary/sibling entries)
    - internal/graph/builder_test.go (existing golden/canonical patterns, canonical.Canonical usage)
    - .planning/phases/38-hierarchy-wrapping-and-granular-keys/38-RESEARCH.md (WRAP section)
  </read_first>
  <behavior>
    - TestBoundaryEntryWrappedInAncestorChain: on a C2 drill-down of a nested unit (e.g. multilevel `mainSystem.storages.localStorage`), a boundary entry resolving to `mainSystem.sshAuth` appears inside a wrapper cluster for `mainSystem` — not in g.Nodes top level
    - TestFullyExternalBoundaryStaysTopLevel: an entry with no in-model ancestor (e.g. external actor at model root) remains a top-level node in g.Nodes
    - TestExpandedUnitAncestorSkeleton: a C3 view of `mainSystem.storages.localStorage` shows wrapper clusters `mainSystem` ⊃ `storages` containing the expanded unit's boundary cluster; wrapper labels use AncestorNames pretty names
    - TestWrapperClustersHaveNoExploreURL: wrapper clusters carry no drill URL affordance; the expanded unit's own cluster keeps its affordances
    - TestBoundaryViewNodeSetInvariant (WRAP-03, in scope_test.go or builder_test.go): before/after wrapping, the multiset of node IDs over all multilevel views is identical — wrapping adds cluster IDs only, never node IDs
    - TestEdgeEndpointsUnchangedByWrapping: edge source/target IDs identical before/after (dedup/multiplicity keys untouched)
  </behavior>
  <action>
    Write these tests against BuildGraphWithPath / the boundary-view graph path over the multilevel fixture (construct the view via the view generators, then inspect Graph.Nodes/Graph.Clusters recursively). Use canonical.Canonical for any DOT-string assertions. Run them — they MUST fail (current code places boundary entries top-level and renders the expanded unit with no ancestor skeleton). Commit as test(38-01).
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/view/ -run 'Wrap\|AncestorSkeleton\|NodeSetInvariant\|ExternalBoundary' -v # new tests RED</automated>
  </verify>
  <done>New tests compile and fail for the wrapping reasons (not compile errors); boundary locks (external-stays-top-level may already pass — mark which)</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): ancestor wrapper-cluster construction</name>
  <files>internal/graph/builder.go</files>
  <read_first>
    - internal/graph/builder.go in full (buildBoundaryViewGraph, buildBoundaryCluster, buildNestedCluster conventions, cluster-label conventions, ExploreURL affordance sites)
    - internal/view/view.go (AncestorNames, Entry)
    - internal/view/scope_test.go + Task 1's tests (the spec being implemented)
  </read_first>
  <behavior>
    All Task 1 RED tests now pass. Top-level g.Nodes in boundary views shrink to only fully external entries.
  </behavior>
  <action>
    One uniform rule in buildBoundaryViewGraph + buildBoundaryCluster: (1) build the expanded unit's ancestor wrapper skeleton — one wrapper cluster per proper prefix of the expanded unit's FullPath, outermost first, nested inside each other, the existing buildBoundaryCluster result nested innermost; (2) for each boundary/external entry whose dotted path has an in-model ancestor (resolve via existing model lookup / Unit entries), wrap its node in its own ancestor-chain wrappers, MERGING into already-present wrapper clusters by path (a wrapper cluster for path P exists at most once per graph); (3) fully external entries stay top-level exactly as today. Wrapper clusters: ID = wrapper path (namespaced to avoid collision with unit clusters), label = AncestorNames[prefix] pretty name, NO ExploreURL/URL affordance — affordances stay only on the expanded unit's own cluster. Do NOT touch node IDs, edge endpoints, dedup keys, or view-layer scope.go — this is cluster-structure only in the graph builder. Preserve SubunitOrder-with-fallback and deterministic ordering (definition order) to avoid golden flake.
  </action>
  <verify>
    <automated>go test ./internal/graph/ ./internal/view/ -count=1 -run 'Wrap\|AncestorSkeleton\|NodeSetInvariant\|ExternalBoundary\|Boundary' -v && gofmt -l internal/graph/ && go vet ./internal/graph/</automated>
  </verify>
  <done>All Task 1 tests green; boundary views nest every in-model-ancestor entry in its full chain; node IDs and edge endpoints unchanged; gofmt/vet clean. Record in SUMMARY which pre-existing golden tests now fail (expected-failure list for 38-04) — expect boundary-view and plain goldens; C1/flat-model tests must stay green.</done>
</task>

<task type="tdd">
  <name>Task 3 (calibration): flat-model stability + expected-failure census</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>
    - internal/graph/builder_test.go, internal/view/scope_test.go (current failures after Task 2)
    - .planning/phases/37-nesting-context-and-plain-rendering/37-05-SUMMARY.md (golden-audit pattern: attribute every diff to a documented delta)
  </read_first>
  <behavior>
    - Every non-boundary-view test (C1 tests, flat models, expanded-pipeline tests via buildNestedCluster) is UNCHANGED green
  </behavior>
  <action>
    Run the full quick suite (go test ./internal/...). Categorize every failure: it must be a boundary-view golden/structure test whose diff is purely cluster-structure (new wrapper clusters) — assert this by inspecting each diff. If any failure shows a changed node ID, edge endpoint, or a non-boundary view breaking, FIX the wrapping implementation (Task 2 scope), not the test. Record the final expected-failure list (exact test names) in the plan SUMMARY as the load-bearing handoff to 38-04's consolidated re-baseline.
  </action>
  <verify>
    <automated>go test ./internal/... -count=1 2>&1 | tail -20 # only census'd boundary-view failures remain</automated>
  </verify>
  <done>Every failure attributed to the documented WRAP delta; census recorded in SUMMARY; zero unattributed failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author TOML → graph structure | boundary paths from author models drive cluster construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | wrapper cluster IDs from author-controlled dotted paths | mitigate | namespace wrapper cluster IDs (prefix) so author unit names cannot collide with or shadow existing cluster/node IDs; asserted by node-set invariance test |
| T-38-02 | DoS | deeply nested ancestor chains inflating cluster recursion | accept | model nesting already bounded by validator; same recursion depth as buildNestedCluster since v1.14 |
</threat_model>

<verification>
- go test ./internal/graph/ ./internal/view/ -count=1 — all green except census'd boundary-view goldens
- go test -count=1 ./... — failures match the recorded expected-failure list exactly
- Node-set invariance test standing over multilevel fixture
</verification>

<success_criteria>
- WRAP-01: boundary/sibling entries with in-model ancestors render inside complete ancestor chains on drill-downs
- WRAP-02: expanded unit shows its ancestor wrapper skeleton; fully external entries stay top-level
- WRAP-03: depicted node set locked by test — cluster-structure-only change proven
</success_criteria>

<output>
Create `.planning/phases/38-hierarchy-wrapping-and-granular-keys/38-01-SUMMARY.md` when done — must include the expected-failure census for 38-04
</output>
