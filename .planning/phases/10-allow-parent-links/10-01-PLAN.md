---
phase: 10-allow-parent-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/validator/rules.go
  - internal/validator/rules_test.go
autonomous: true
requirements:
  - PLNK-01
  - PLNK-02

must_haves:
  truths:
    - "A unit with subunits can have Links field without validation error"
    - "A unit with subunits can have LinksFrom field without validation error"
    - "Other units can link to units with subunits without validation error"
    - "Existing valid TOML files continue to validate successfully"
    - "Orphan detection still works (units with Links/LinksFrom are not orphans)"
  artifacts:
    - path: "internal/validator/rules.go"
      provides: "Link validation rules"
      contains: "func ValidateLinkRules"
    - path: "internal/validator/rules_test.go"
      provides: "Test coverage for link rules"
  key_links:
    - from: "internal/validator/rules.go"
      to: "ValidateOrphanUnits"
      via: "shared Links/LinksFrom fields"
      pattern: "info.Unit.Links"
---

<objective>
Remove validation restrictions that prevent linking to/from units with subunits.

Purpose: Enable users to create more flexible architecture diagrams where parent containers can have their own connections.
Output: Simplified ValidateLinkRules function with updated tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-allow-parent-links/10-CONTEXT.md

<interfaces>
From internal/validator/rules.go:
```go
type ValidationError struct {
    Message string
    Path    string
}
type ValidationErrors []*ValidationError

func ValidateLinkRules(index map[string]*UnitInfo) ValidationErrors
func ValidateOrphanUnits(index map[string]*UnitInfo) ValidationErrors
```

From internal/validator/index.go:
```go
type UnitInfo struct {
    Unit      *model.Unit
    FullPath  string
}
func BuildIndex(units map[string]*model.Unit, prefix string) map[string]*UnitInfo
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove link restrictions from ValidateLinkRules</name>
  <files>internal/validator/rules.go</files>
  <behavior>
    - Test: Unit with subunits AND Links → no error
    - Test: Unit with subunits AND LinksFrom → no error
    - Test: Link targets unit with subunits → no error
    - Test: Multiple violations (old behavior) → 0 errors (new behavior)
  </behavior>
  <action>
Remove lines 96-115 in ValidateLinkRules function (internal/validator/rules.go).

Specifically, remove:
1. Lines 96-105: The check that errors when a unit with subunits has Links or LinksFrom
2. Lines 107-115: The check that errors when a link targets a unit with subunits

After removal, ValidateLinkRules should:
- Keep the hasSubunits map construction (lines 87-93) if still needed, or remove entirely if unused
- Return empty ValidationErrors (the function becomes a no-op)

The function can be simplified to:
```go
func ValidateLinkRules(index map[string]*UnitInfo) ValidationErrors {
    return nil
}
```

Or keep the structure for future extensibility but remove the actual checks.

Update the function comment (lines 78-82) to reflect the new behavior.
</action>
  <verify>
    <automated>go test ./internal/validator/... -run TestValidateLinkRules -v</automated>
  </verify>
  <done>
ValidateLinkRules function no longer restricts links on units with subunits. All existing tests pass.
</done>
</task>

<task type="auto">
  <name>Task 2: Update tests to reflect new behavior</name>
  <files>internal/validator/rules_test.go</files>
  <action>
Update the following tests in internal/validator/rules_test.go:

1. TestValidateLinkRules_RejectsLinksOnParent (lines 261-288):
   - Rename to TestValidateLinkRules_AllowsLinksOnParent
   - Change assertion: expect 0 errors instead of 1
   - Remove error message check

2. TestValidateLinkRules_RejectsLinksFromOnParent (lines 290-317):
   - Rename to TestValidateLinkRules_AllowsLinksFromOnParent
   - Change assertion: expect 0 errors instead of 1
   - Remove error message check

3. TestValidateLinkRules_RejectsTargetWithSubunits (lines 319-348):
   - Rename to TestValidateLinkRules_AllowsTargetWithSubunits
   - Change assertion: expect 0 errors instead of 1
   - Remove error message check

4. TestValidateLinkRules_CollectsAllViolations (lines 371-399):
   - Rename to TestValidateLinkRules_NoViolationsForParentLinks
   - Change assertion: expect 0 errors instead of 2
   - Update comment to reflect new behavior

Keep TestValidateLinkRules_AllowsValidLinks unchanged (it should still pass).
</action>
  <verify>
    <automated>go test ./internal/validator/... -run TestValidateLinkRules -v</automated>
  </verify>
  <done>
All TestValidateLinkRules tests pass with new behavior. No tests expect errors for parent links.
</done>
</task>

</tasks>

<verification>
- All tests in internal/validator pass
- ValidateLinkRules returns no errors for units with subunits that have links
- ValidateOrphanUnits still works correctly (unchanged)
- Full test suite passes: `go test ./...`
</verification>

<success_criteria>
1. `go test ./internal/validator/... -run TestValidateLinkRules -v` passes with 0 failures
2. `go test ./...` passes with 0 failures
3. ValidateLinkRules function in rules.go no longer contains subunit link restrictions
4. All test names reflect the new positive behavior (Allows* instead of Rejects*)
</success_criteria>

<output>
After completion, create `.planning/phases/10-allow-parent-links/10-01-SUMMARY.md`
</output>
