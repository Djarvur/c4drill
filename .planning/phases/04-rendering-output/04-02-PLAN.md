---
phase: 04-rendering-output
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - internal/render/integration_test.go
  - internal/output/integration_test.go
autonomous: true
requirements:
  - REND-01
  - REND-02
  - OUTP-01
  - OUTP-02
  - OUTP-04
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
must_haves:
  truths:
    - "Full pipeline from graph to rendered files works end-to-end"
    - "All Phase 4 requirements have passing integration tests"
    - "Test coverage meets 75% threshold for both packages"
    - "Lint passes with zero issues"
  artifacts:
    - path: "internal/render/integration_test.go"
      provides: "End-to-end rendering tests"
      contains: "TestFullPipeline"
    - path: "internal/output/integration_test.go"
      provides: "End-to-end output tests"
      contains: "TestWriteAndRead"
  key_links:
    - from: "internal/render/integration_test.go"
      to: "internal/graph"
      via: "import and BuildGraph"
      pattern: "graph\\.BuildGraph|view\\.GenerateC1View"
    - from: "internal/output/integration_test.go"
      to: "internal/render"
      via: "import RenderDOT/RenderSVG"
      pattern: "render\\.(RenderDOT|RenderSVG)"
---

<objective>
Verify the complete rendering pipeline works end-to-end with integration tests, ensure quality gates pass (lint, coverage), and confirm all Phase 4 requirements are met.

Purpose: Provide confidence that the render and output packages work correctly together and meet all quality standards.
Output: Integration tests, quality verification report, and confirmed requirement coverage.
</objective>

<execution_context>
@/Users/nil/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rendering-output/04-CONTEXT.md
@.planning/phases/04-rendering-output/04-VALIDATION.md

<interfaces>
<!-- Types available from prior phases and Plan 01 -->

From internal/graph/graph.go (Phase 3):
```go
type Graph struct {
    Title     string
    Direction string
    EdgeStyle string
    Nodes     []*Node
    Edges     []*Edge
    Clusters  []*Cluster
    Legend    *Legend
}
```

From internal/graph/builder.go (Phase 3):
```go
func BuildGraph(v *view.View) *Graph
```

From internal/view/scope.go (Phase 3):
```go
func GenerateC1View(m *parser.Model) *View
func GenerateC2View(m *parser.Model, systemPath string) *View
func GenerateC3View(m *parser.Model, containerPath string) *View
```

From internal/render/render.go (Plan 01):
```go
func RenderDOT(g *graph.Graph) ([]byte, error)
func RenderSVG(g *graph.Graph) ([]byte, error)
```

From internal/output/writer.go (Plan 01):
```go
type Writer struct { ... }
func NewWriter(baseDir string) *Writer
func (w *Writer) Write(basename, unitPath, format string, data []byte) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration tests for full rendering pipeline</name>
  <files>internal/render/integration_test.go</files>
  <behavior>
    - Test 1: Full pipeline from model -> view -> graph -> DOT produces valid output
    - Test 2: Full pipeline from model -> view -> graph -> SVG produces valid output
    - Test 3: Graph with nodes renders all node IDs in output
    - Test 4: Graph with edges renders all edge relationships in output
    - Test 5: Graph with clusters renders cluster structure in output
    - Test 6: Empty graph renders without error (minimal valid DOT/SVG)
  </behavior>
  <action>
    1. Create internal/render/integration_test.go:
       - Package render_test
       - Import internal/graph, internal/view, internal/parser, internal/render
       - Create helper function to build test model:
         ```go
         func buildTestModel() *parser.Model {
             return &parser.Model{
                 Units: map[string]*model.Unit{
                     "user": {Type: model.TypePerson, Name: "User"},
                     "system": {Type: model.TypeSystem, Name: "Main System"},
                 },
             }
         }
         ```

    2. Test full pipeline:
       - GenerateC1View(model) -> BuildGraph(view) -> RenderDOT(graph)
       - Verify DOT output contains "digraph" and node identifiers
       - GenerateC1View(model) -> BuildGraph(view) -> RenderSVG(graph)
       - Verify SVG output contains "<svg" tag

    3. Test with edges:
       - Create model with linked units
       - Verify edge relationships appear in rendered output

    4. Test with clusters:
       - Create model with expanded system containing subunits
       - Verify cluster structure (subgraph cluster_...) appears in DOT

    5. Test edge cases:
       - Empty graph (nil or empty nodes/edges)
       - Graph with single node
       - Graph with external nodes
  </action>
  <verify>
    <automated>go test -run "TestIntegration" ./internal/render/... -v</automated>
  </verify>
  <done>
    - internal/render/integration_test.go exists
    - Full pipeline tests pass
    - DOT output verified to contain expected structure
    - SVG output verified to contain expected structure
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration tests for output writer with rendered content</name>
  <files>internal/output/integration_test.go</files>
  <behavior>
    - Test 1: Write rendered DOT to C1 path and read back matches
    - Test 2: Write rendered SVG to C2 path and read back matches
    - Test 3: Write multiple files with different unit paths
    - Test 4: Nested directory structure is created correctly
    - Test 5: Written files contain valid DOT/SVG content
  </behavior>
  <action>
    1. Create internal/output/integration_test.go:
       - Package output_test
       - Import internal/graph, internal/render, internal/output
       - Use t.TempDir() for test isolation

    2. Test C1 output:
       - Create simple graph
       - Render to DOT
       - Write to "diagram" basename with empty unitPath
       - Verify file exists at {tempdir}/diagram.dot
       - Read file and verify content matches rendered output

    3. Test C2/C3 output:
       - Create simple graph
       - Render to SVG
       - Write to "diagram" basename with "mainapp.api" unitPath
       - Verify file exists at {tempdir}/diagram/mainapp/api.svg
       - Read file and verify content matches rendered output

    4. Test multiple files:
       - Write same content to different paths
       - Verify all files created correctly

    5. Test directory structure:
       - Use deeply nested path like "a.b.c.d"
       - Verify directory hierarchy a/b/c/d.svg created
  </action>
  <verify>
    <automated>go test -run "TestIntegration" ./internal/output/... -v</automated>
  </verify>
  <done>
    - internal/output/integration_test.go exists
    - C1 flat file output works
    - C2/C3 nested directory output works
    - Written content matches rendered output
    - Directory structure created correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify test coverage and run full quality suite</name>
  <files>.planning/phases/04-rendering-output/04-02-SUMMARY.md</files>
  <action>
    1. Run full test suite with coverage:
       ```
       go test ./internal/render/... ./internal/output/... -cover -race -v
       ```

    2. Generate coverage report:
       ```
       go test ./internal/render/... ./internal/output/... -coverprofile=coverage.out
       go tool cover -func=coverage.out
       ```

    3. Verify coverage >= 75% for both packages

    4. Run full project test suite:
       ```
       go test ./... -cover -race
       ```

    5. Run lint:
       ```
       mise run lint
       ```

    6. Document results in SUMMARY.md:
       - Coverage percentage for render package
       - Coverage percentage for output package
       - Lint status
       - Full test suite status
  </action>
  <verify>
    <automated>mise run test && mise run lint</automated>
  </verify>
  <done>
    - Test coverage >= 75% for internal/render
    - Test coverage >= 75% for internal/output
    - Full project test suite passes
    - Lint passes with 0 issues
    - SUMMARY.md documents quality metrics
  </done>
</task>

</tasks>

<verification>
- Run full test suite: `mise run test`
- Check coverage: `go test ./internal/render/... ./internal/output/... -cover`
- Run lint: `mise run lint`
</verification>

<success_criteria>
- All Phase 4 requirements have passing tests (REND-01, REND-02, OUTP-01, OUTP-02, OUTP-04)
- Integration tests verify full pipeline works end-to-end
- Test coverage >= 75% for both render and output packages (QUAL-04)
- Lint passes with 0 issues (QUAL-01)
- Full project test suite passes (QUAL-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-rendering-output/04-02-SUMMARY.md`
</output>
