---
phase: 01-fix-c1-view-scoping
plan: 03
type: tdd
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - internal/view/view.go
  - internal/view/scope.go
  - internal/graph/builder.go
  - internal/view/scope_test.go
  - internal/view/integration_test.go
autonomous: true
requirements: [VIEW-01, VIEW-02, EDGE-01, EDGE-02]
must_haves:
  truths:
    - "C1 links to hidden grandchildren resolve to the deepest VISIBLE ancestor — the visible subunit node inside an expanded cluster (D-07)"
    - "Links originating inside an expanded cluster start at the visible subunit node, not the parent (D-09)"
    - "Links between two visible units of the same expanded cluster render as edges inside the cluster (D-10)"
    - "Parent-level edges are not drawn when the link resolves to a child (no redundant edges) (D-08)"
    - "Box grouping units follow the same resolution rules as systems (D-11)"
  artifacts:
    - path: "internal/view/view.go"
      provides: "View.VisiblePaths map (visible subunit bookkeeping, Pitfall 5)"
      contains: "VisiblePaths map[string]bool"
    - path: "internal/view/scope.go"
      provides: "GenerateC1View visible-subunit population; resolveAndAddBoundary source-side resolution; resolveToTopLevel unified onto the deepest-visible walk"
      contains: "VisiblePaths"
    - path: "internal/graph/builder.go"
      provides: "BuildGraph C1 branch skips VisiblePaths entries (rendered inside parent cluster)"
      contains: "v.VisiblePaths"
    - path: "internal/view/scope_test.go"
      provides: "RED tests: TestGenerateC1View_ResolvesToVisibleSubunit, TestGenerateC1View_SourceResolvesToVisibleSubunit, TestGenerateC1View_WithinClusterEdge, TestGenerateC1View_NoRedundantParentEdge, TestGenerateC1View_BoxResolutionParity"
      contains: "func TestGenerateC1View_ResolvesToVisibleSubunit"
  key_links:
    - from: "GenerateC1View (scope.go:88)"
      to: "v.Units + v.VisiblePaths"
      via: "visible subunit entries appended (UnitOrder too) before addC1BoundaryNodes runs"
      pattern: "VisiblePaths"
    - from: "resolveAndAddBoundary (scope.go:278)"
      to: "v.Units[resolvedSource].ResolvedLinks"
      via: "resolvedSource := resolveToViewAncestor(v, path); append condition resolved != resolvedSource"
      pattern: "ResolvedLinks = append"
    - from: "BuildGraph C1 branch (builder.go:57-67)"
      to: "v.VisiblePaths"
      via: "skip entries rendered inside the parent cluster (prevents duplicate node IDs)"
      pattern: "VisiblePaths"
---

<objective>
Implement D-07..D-11: deepest-visible-ancestor resolution for BOTH link targets and sources in C1, visible-subunit bookkeeping (`View.VisiblePaths` + entries), within-cluster edges, no redundant parent edges, and box parity — unified onto the existing `resolveToViewAncestor` walk (scope.go:716) per the "Don't Hand-Roll" directive.

Purpose: VIEW-02 (deepest visible ancestor), EDGE-01 (source-side resolution), and the D-08/D-10 edge semantics; VIEW-01 preserved (only top-level units + expanded-cluster children are visible nodes).
Output: Modified `GenerateC1View`/`resolveAndAddBoundary`/`resolveToTopLevel` in scope.go, `VisiblePaths` on View, BuildGraph skip, and RED→GREEN tests in scope_test.go/integration_test.go.

Refinement of the existing v1.8 implementation — DELTA edits at the exact line refs, not rewrites. C2/C3 resolution passes (resolveBoundaryNodeLinks, resolveSubunitCrossLinks, resolveDescendantCrossLinks, scope.go:652-859) MUST stay untouched (Pitfall 1).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-c1-view-scoping/01-CONTEXT.md
@.planning/phases/01-fix-c1-view-scoping/01-RESEARCH.md
@.planning/phases/01-fix-c1-view-scoping/01-PATTERNS.md
@internal/view/view.go
@internal/view/scope.go
@internal/graph/builder.go

<interfaces>
Current state of the exact code to modify:

// GenerateC1View — scope.go:88-135. Top-level loop :112-127 builds v.Units/UnitOrder with
//   IsExpanded: isExpandedInC1(m, unit, name) (:122), then calls addC1BoundaryNodes(v, m) (:132).
//   NEW: populate visible subunits BETWEEN the top-level loop and addC1BoundaryNodes (resolution reads v.Units).

// resolveToViewAncestor — scope.go:716-742 — the existing deepest-visible walk:
//   returns peer if in v.Units; else strips trailing segments until an ancestor is in v.Units;
//   returns "" if nothing found. C1 must unify onto it (D-07/D-09).

// resolveToTopLevel — scope.go:368-386 — C1-only (call sites :289/:318). Replace the body with:
//   resolved := resolveToViewAncestor(v, peer); if resolved != "" { return resolved }; return peer
//   (boundary fallback: truly-external peers keep today's behavior — createExternalBoundaryNode path,
//   verified by TestGenerateC1View_ExternalBoundaryNodesForReferencedUnits scope_test.go:160).
//   Update the doc comment to deepest-visible-ancestor semantics. resolveToTopLevel is NOT used by C2/C3.

// resolveAndAddBoundary — scope.go:278-363:
//   sourceAncestor := path; if idx := strings.Index(path, "."); idx > 0 { sourceAncestor = path[:idx] }  // :280-283
//   sourceEntry := v.Units[sourceAncestor]
//   outgoing: resolved := resolveToTopLevel(v, link.Peer); if resolved == "" { continue }
//     boundary-node creation :294-297; append condition :300 `if sourceEntry != nil && resolved != sourceAncestor`
//     copy block :301-311 (with the D-13 Length gate from plan 01-02 — first operand `path == sourceAncestor` must become `path == resolvedSource`)
//   incoming: same shape :317-343 (append condition :329).
//   recursion :346-362 (resolveAndAddBoundary(v, m, path+"."+subName, subUnit)).
//   NEW: replace sourceAncestor with resolvedSource := resolveToViewAncestor(v, path) — the deepest
//   VISIBLE ancestor of the source path (may be a visible subunit entry). Update the skip condition to
//   `resolved != resolvedSource` at :300 and :329 (D-10: edges inside the same expanded cluster are now
//   recorded instead of dropped; D-08: parent edges are not synthesized — automatic).

// buildCluster — builder.go:265-311 renders an expanded top-level unit's DIRECT subunits as nodes
//   (node.ID = entry.FullPath + "." + childName, IsInCluster = true). Visible subunits are NOT in v.Units
//   today (Pitfall 5) — resolution cannot reach them. THIS plan adds them to v.Units + UnitOrder and
//   marks them in VisiblePaths; buildCluster rendering is UNCHANGED.

// BuildGraph — builder.go:15-74. C1 branch :57-67:
//   for _, key := range v.UnitOrder { entry := v.Units[key]; if entry.IsExpanded { cluster := buildCluster(entry) } else { node := buildNode(entry) } }
//   NEW: `if v.VisiblePaths[key] { continue }` — visible subunits are rendered by their parent's
//   buildCluster; skipping prevents duplicate node IDs in DOT. Nil-map reads are safe in Go, so
//   views without VisiblePaths (C2/C3, expanded, hand-built) are unaffected.

// isExpandedInC1 — scope.go:144-150 (both properties-level and per-unit Expanded lists).
// isUnitExpanded — builder.go:324-326 (per-unit child expansion; used by buildCluster).
// Entry — view.go:45-62 (Unit, FullPath, IsExpanded, IsExternal, HasSubunits, ResolvedLinks, ResolvedLinksFrom).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Failing tests for deepest-visible-ancestor resolution</name>
  <files>internal/view/view.go, internal/view/scope_test.go, internal/view/integration_test.go</files>
  <read_first>
    internal/view/scope_test.go (fixture patterns; TestGenerateC1View_ExternalBoundaryNodesForReferencedUnits :160 — boundary fallback contract that must stay green)
    internal/view/integration_test.go (TestIntegrationC1ViewNoNestedBoundaryPollution :346-455 — the 5-node fixture; TestIntegrationC1EdgeResolution :468-519 — resolved-link assertion pattern)
  </read_first>
  <action>
    Add the data-only skeleton field (no behavior) so tests compile: internal/view/view.go View struct: `VisiblePaths map[string]bool` with doc comment "// VisiblePaths tracks paths of subunits rendered as nodes INSIDE an expanded top-level cluster (C1). They are in Units for edge building but must not be rendered as top-level nodes." Per Pitfall 5.

    Write NEW failing tests in internal/view/scope_test.go using a shared expanded-C1 fixture — a model with top-level units: "webUser" (TypePersonExternal, Links: [{Peer: "linuxSystem.sshAuth.sshd"}] and [{Peer: "linuxSystem"}] where needed), "keycloak" (TypeSystemExternal), and "linuxSystem" (TypeSystem, Expanded: ["linuxSystem"], Subunits: "sshAuth" (TypeContainerBox with child "sshd" TypeContainer), "webAPI" (TypeContainer)):
    - `TestGenerateC1View_ExpandedUnitExposesVisibleSubunits`: GenerateC1View → assert.Contains(v.Units, "linuxSystem.sshAuth"), assert.Contains(v.Units, "linuxSystem.webAPI"), assert.True(v.VisiblePaths["linuxSystem.sshAuth"]), assert.False(v.VisiblePaths["linuxSystem.sshAuth.sshd"]) (grandchildren stay hidden), assert.NotContains(v.Units, "linuxSystem.sshAuth.sshd").
    - `TestGenerateC1View_ResolvesToVisibleSubunit` (D-07): webUser link → "linuxSystem.sshAuth.sshd" (hidden grandchild, parent visible): assert webUser.ResolvedLinks[0].Peer == "linuxSystem.sshAuth" (NOT "linuxSystem").
    - `TestGenerateC1View_SourceResolvesToVisibleSubunit` (D-09): sshAuth (with child sshd) has Links: [{Peer: "keycloak"}] → assert v.Units["linuxSystem.sshAuth"].ResolvedLinks[0].Peer == "keycloak" (the edge starts at the visible subunit, not linuxSystem).
    - `TestGenerateC1View_WithinClusterEdge` (D-10): sshAuth has Links: [{Peer: "linuxSystem.webAPI"}] → assert sshAuth.ResolvedLinks[0].Peer == "linuxSystem.webAPI" AND v.Units["linuxSystem"].ResolvedLinks is nil/empty (no parent-level edge — D-08).
    - `TestGenerateC1View_NoRedundantParentEdge` (D-08): webUser has Links: [{Peer: "linuxSystem.sshAuth.sshd"}] ONLY → require.Len(t, webUser.ResolvedLinks, 1) and Peer == "linuxSystem.sshAuth" — no resolved link with Peer "linuxSystem".
    - `TestGenerateC1View_BoxResolutionParity` (D-11): same shape with TypeBox instead of TypeSystem for the expanded unit — same four assertions (visible subunits exposed; target resolves to child; source resolves to child; within-cluster edge recorded).
    - `TestBuildGraphExpandedC1VisibleSubunitEdges` (in internal/view/integration_test.go, graph-level): GenerateC1View + graph.BuildGraph: g.Nodes does NOT contain ID "linuxSystem.sshAuth" (skip logic); the cluster with ID "cluster_linuxSystem" contains a node with ID "linuxSystem.sshAuth"; g.Edges contains edge Source "webUser" Target "linuxSystem.sshAuth" (D-07 drawn); render.RenderDOT(g) succeeds.
  </action>
  <verify>
    <automated>go test ./internal/view/ -run 'TestGenerateC1View_ResolvesToVisibleSubunit|TestGenerateC1View_SourceResolvesToVisibleSubunit|TestGenerateC1View_WithinClusterEdge|TestGenerateC1View_NoRedundantParentEdge|TestGenerateC1View_BoxResolutionParity|TestGenerateC1View_ExpandedUnitExposesVisibleSubunits|TestBuildGraphExpandedC1VisibleSubunitEdges' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - RED: all new tests fail against current code (visible subunits absent from v.Units; resolution returns "linuxSystem"; within-cluster links dropped as internal).
    - View.VisiblePaths field exists with doc comment; no behavior changes yet.
    - Commit: `test(01-03): add failing tests for deepest-visible-ancestor resolution`
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Visible-subunit bookkeeping and unified resolution</name>
  <files>internal/view/scope.go, internal/graph/builder.go</files>
  <read_first>
    internal/view/scope.go (GenerateC1View :88-135, isExpandedInC1 :144-150, resolveAndAddBoundary :278-363, resolveToTopLevel :368-386, resolveToViewAncestor :716-742)
    internal/graph/builder.go (BuildGraph C1 branch :57-67, buildCluster :265-311 — the rendered child-node contract)
    internal/view/view.go (View struct fields — doc comment convention)
  </read_first>
  <action>
    Per D-07..D-11:
    1. scope.go GenerateC1View: initialize `VisiblePaths: make(map[string]bool)` in the View literal (:93-99). AFTER the top-level loop (:112-127) and BEFORE addC1BoundaryNodes (:132): for each top-level name whose entry IsExpanded is true, iterate unit.SubunitOrder (fallback: map keys of unit.Subunits — same fallback pattern as :101-110); for each subName with subUnit non-nil: fullPath := name + "." + subName; add to v.Units an Entry{Unit: subUnit, FullPath: fullPath, IsExpanded: isUnitExpanded(unit, subName), HasSubunits: len(subUnit.Subunits) > 0, IsExternal: IsExternalType(subUnit.Type)} (mirror buildCluster's child-entry construction :294-308); append fullPath to v.UnitOrder; set v.VisiblePaths[fullPath] = true. Direct subunits ONLY — grandchildren stay hidden (buildCluster renders one level).
    2. scope.go resolveAndAddBoundary: replace the sourceAncestor computation (:280-283) with `resolvedSource := resolveToViewAncestor(v, path)` and `sourceEntry := v.Units[resolvedSource]` (D-09 — deepest visible ancestor of the source path; non-empty because every model unit sits under a top-level in v.Units). Update the append conditions (:300, :329) to `sourceEntry != nil && resolved != resolvedSource` (D-10: within-cluster edges pass; D-08: no parent edges synthesized). Update the D-13 Length gate from plan 01-02: first operand `path == sourceAncestor` becomes `path == resolvedSource` (a visible subunit's own links are both-original when the peer also resolves unchanged).
    3. scope.go resolveToTopLevel (:368-386): replace the body with the unified walk: `resolved := resolveToViewAncestor(v, peer); if resolved != "" { return resolved }; return peer`; update the doc comment (deepest visible ancestor incl. visible subunit paths; boundary fallback returns the peer as-is for truly-external units).
    4. builder.go BuildGraph C1 branch (:57-67): add `if v.VisiblePaths[key] { continue }` as the first statement of the loop body (rendered inside the parent cluster by buildCluster — prevents duplicate node IDs).
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/graph/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - GREEN: all Task 1 tests pass, including the graph-level render check.
    - scope.go contains VisiblePaths population and resolveToViewAncestor call for source resolution; resolveToTopLevel delegates to resolveToViewAncestor.
    - builder.go C1 branch skips v.VisiblePaths entries.
    - Existing tests stay green: TestIntegrationC1ViewNoNestedBoundaryPollution (5 nodes — fixture has NO expanded units), TestIntegrationC1EdgeResolution, TestGenerateC1View_ExternalBoundaryNodesForReferencedUnits (boundary fallback intact), TestBuildGraphClusters (cluster_ + 2 child nodes), TestGenerateC1View_ExternalBoundaryFromLinksFrom.
    - Commit: `feat(01-03): deepest-visible-ancestor resolution in C1 with within-cluster edges`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression — C2/C3 untouched, definition order, explore-link side effect</name>
  <files>internal/view/scope_test.go, internal/view/integration_test.go</files>
  <read_first>
    internal/view/scope_test.go (TestGenerateC1ViewDefinitionOrder :924)
    internal/graph/builder_test.go (TestBuildGraphDefinitionOrder :1341, TestBuildGraphDeterministicOrder :1026)
    internal/view/integration_test.go (TestIntegrationC2ViewNestedSystem :75, TestIntegrationC3ViewNestedContainer :133)
  </read_first>
  <action>
    No production changes expected. Verify:
    1. C2/C3 passes untouched (Pitfall 1): TestIntegrationC2ViewNestedSystem, TestIntegrationC3ViewNestedContainer, and all C2/C3 boundary/cross-subunit tests stay green.
    2. Definition-order determinism (D-01/D-03 first-wins with the new resolution): TestGenerateC1ViewDefinitionOrder, TestBuildGraphDefinitionOrder, TestBuildGraphDeterministicOrder green.
    3. Document the explore-link side effect as a new subtest in TestGenerateC1View_ExpandedUnitExposesVisibleSubunits (or a sibling test): a visible subunit WITH subunits (sshAuth) gets an ExploreURL on its cluster node after BuildGraphWithPath (shouldHaveExploreLink lookups now succeed because the entry is in v.Units — drill-down to its C2 diagram, which auto-generation produces since it has subunits). Assert nodeMap["linuxSystem.sshAuth"].ExploreURL non-empty.
    4. Full suite + lint. If any failure is a genuine regression, fix scope.go/builder.go; otherwise commit nothing.
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1 && go test -v -race ./...</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (`go test -v -race ./...`); `mise lint` clean.
    - New explore-link subtest passes (or its absence explained — the ExploreURL behavior is a consequence of visible-subunit entries, do not remove it).
    - C2/C3 tests unchanged and green.
    - If and only if code changed: commit `refactor(01-03): regression fixes`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file → parser/validator | UNCHANGED — validator rejects undefined peers; boundary fallback in resolveToTopLevel only serves pre-validation unit tests |
| view resolution (scope.go) → graph builder | Internal transform; new v.Units entries for visible subunits are derived from already-validated model units |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | DoS | GenerateC1View visible-subunit population | mitigate | Guard `if subUnit == nil { continue }` in the SubunitOrder loop (existing pattern :115-117); VisiblePaths map initialized in the View literal before writes |
| T-01-08 | DoS | resolveAndAddBoundary resolvedSource | mitigate | `sourceEntry != nil` guard retained before appending ResolvedLinks (existing :300); resolveToViewAncestor may return "" only for units outside the model — impossible for recursion paths under top-levels, but the guard keeps the no-panic contract |
| T-01-09 | Tampering | duplicate node IDs in DOT | mitigate | BuildGraph C1 branch skips v.VisiblePaths entries (rendered once inside parent cluster); nil-map reads safe, so other view types unaffected |
| T-01-10 | DoS | resolveToTopLevel unification | mitigate | Delegation to existing resolveToViewAncestor (no new walk); boundary fallback returns peer unchanged — no allocation or recursion added |
| T-01-SC | Tampering | package installs | accept | Zero new dependencies; no package-manager commands in this plan |

Note: ASVS L1 — no BLOCKING threats. CLI/parser/validator untouched (RESEARCH Security Domain).
</threat_model>

<verification>
- RED gate: the seven new tests fail before Task 2, pass after.
- Per-task commits: `go test ./internal/view/ ./internal/graph/ ./cmd/c4drill/ -count=1`.
- Wave gate: `go test -v -race ./...` + `mise lint`.
- Pitfall 1: C2/C3 resolution passes (scope.go:652-859) receive NO edits in this plan — only the C1 entry points (resolveAndAddBoundary, resolveToTopLevel, GenerateC1View).
</verification>

<success_criteria>
- `go test -v -race ./...` green; `mise lint` clean.
- All Task 1 tests + explore-link subtest pass.
- TestIntegrationC1ViewNoNestedBoundaryPollution (5 nodes) and TestIntegrationC1EdgeResolution stay green.
- Expanded-C1 RenderDOT contains no duplicate node IDs (BuildGraph skip works).
</success_criteria>

<output>
Create `.planning/phases/01-fix-c1-view-scoping/01-03-SUMMARY.md` when done
</output>
