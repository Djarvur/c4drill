---
phase: 25-edges-must-be-twice-thicker-than-borders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/render/converter.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Edges are visually thicker than node borders"
    - "Edge line width is 2x the default node border width"
    - "All edges have consistent thickness"
  artifacts:
    - path: "internal/render/converter.go"
      provides: "Edge rendering with penwidth"
      contains: "SetPenWidth"
  key_links:
    - from: "internal/render/converter.go:createEdge()"
      to: "cgraph.Edge"
      via: "SetPenWidth(2.0)"
      pattern: "SetPenWidth\\(2\\.0\\)"
---

<objective>
Set edge penwidth to 2.0 to make edges twice as thick as node borders (which use the default 1.0).

Purpose: Improve diagram readability by making edges visually prominent and easier to follow.
Output: Modified converter.go with edge penwidth set to 2.0.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and methods the executor needs. Extracted from codebase. -->

From internal/graph/graph.go:
```go
type Edge struct {
    Source string
    Target string
    Label *EdgeLabel
    Style string
    ArrowHead ArrowDirection
    Color string
    MinLen int
}
```

From internal/render/converter.go (createEdge function, lines 425-479):
```go
func createEdge(cg *cgraph.Graph, source, target *cgraph.Node, edge *graph.Edge) error {
    // ... creates edge, sets label, style, arrow, color, minlen
    // Currently does NOT set penwidth
}
```

From github.com/goccy/go-graphviz/cgraph:
```go
func (e *Edge) SetPenWidth(v float64) *Edge
```

Key context:
- Node borders use GraphViz default penwidth (1.0)
- Edges currently use GraphViz default penwidth (1.0)
- Setting edge penwidth to 2.0 makes edges 2x thicker than borders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set edge penwidth to 2.0 in createEdge()</name>
  <files>internal/render/converter.go</files>
  <read_first>
    - internal/render/converter.go (lines 425-479, the createEdge function)
  </read_first>
  <action>
    In the `createEdge()` function (internal/render/converter.go, lines 425-479), add a call to `e.SetPenWidth(2.0)` after the edge is created.

    Location: After the edge creation and before the return statement. A good place is after setting the edge color (around line 471) or after setting minlen (around line 476).

    Add this line:
    ```go
    e.SetPenWidth(2.0)
    ```

    This sets the edge line width to 2.0, which is twice the default node border width of 1.0.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/render/... -v -run TestConverter</automated>
  </verify>
  <acceptance_criteria>
    - File internal/render/converter.go contains "SetPenWidth(2.0)" inside the createEdge function
    - All existing tests pass
    - Generated DOT output contains penwidth="2.0" on edge elements
  </acceptance_criteria>
  <done>
    Edge penwidth is set to 2.0, making edges twice as thick as node borders (which use default 1.0).
    The change is verified by grep pattern "SetPenWidth(2.0)" in converter.go.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `go build ./...`
2. Tests pass: `go test ./...`
3. Code contains the change: `grep -n "SetPenWidth(2.0)" internal/render/converter.go`
4. Visual verification: Generate a diagram and confirm edges are thicker than borders
</verification>

<success_criteria>
- Edge lines render with 2x thickness of node borders
- All tests pass
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/25-edges-must-be-twice-thicker-than-borders/25-01-SUMMARY.md`
</output>
