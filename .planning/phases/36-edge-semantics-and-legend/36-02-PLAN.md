---
phase: 36-edge-semantics-and-legend
plan: 02
type: tdd
wave: 2
depends_on: ["36-01"]
files_modified: [internal/c4d/grammar/c4d.peg, internal/c4d/grammar/parser_gen.go, internal/c4d/grammar/reserved.go, internal/c4d/tomodel.go, internal/c4d/frommodel.go, internal/c4d/emit_toml.go, internal/c4d/emit_c4d.go, internal/testutil/canonsrc/canonsrc.go, internal/template/expand_test.go, internal/c4d/parse_test.go, internal/c4d/emit_test.go, internal/c4d/parity_test.go]
autonomous: true
requirements: [KIND-03]
user_setup: []

must_haves:
  truths:
    - "kind works in C4D edge option blocks: `a -> b { kind: \"read\" }` parses into Link.Kind and errors with a Levenshtein suggestion when misspelled"
    - "kind round-trips canonically in both formats: convert to-toml/to-c4d and fmt preserve it; canonical option/field order includes kind between rank and color"
    - "kind survives template instantiation value-copy; ${param} does NOT substitute it (enum precedent, pinned by test)"
  artifacts:
    - path: "internal/c4d/grammar/c4d.peg"
      provides: "OptionKey alternation includes kind"
      contains: "\"kind\""
    - path: "internal/c4d/tomodel.go"
      provides: "applyEdgeOption case kind"
      contains: "Kind:"
    - path: "internal/c4d/frommodel.go"
      provides: "edgeStmtFromLink emits kind in canonical order (arrow, rank, kind, color, style, labelPosition, length)"
      contains: "addOpt(\"kind\""
  key_links:
    - from: "internal/c4d/frommodel.go"
      to: "internal/c4d/emit_toml.go"
      via: "D-23 canonical field order stays consistent between C4D and TOML emitters + canonsrc writers"
      pattern: "kind"
---

<objective>
Teach the C4D format (and both canonical emitters) the link `kind` option so the feature has full dual-format parity: grammar admission + regenerated parser, to-model mapping, canonical emission order in both emitters, reserved-word suggestions, canonsrc parity writers, and template round-trip coverage. Purpose: KIND-03.

Output: `a -> b { kind: "read" }` parses, converts, formats, and round-trips canonically; convert/fmt parity corpus stays green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md
@.planning/STATE.md

Source files (read before editing):
@internal/c4d/grammar/c4d.peg
@internal/c4d/tomodel.go
@internal/c4d/frommodel.go
@internal/c4d/emit_toml.go
@internal/c4d/emit_c4d.go
@internal/c4d/grammar/reserved.go
@internal/testutil/canonsrc/canonsrc.go

Decisions honored (research §6, §12.3 — locked): Kind NOT substituted by ${param} (enum precedent — Arrow/Rank/LabelPosition/Length are not substituted); canonical option order arrow, rank, kind, color, style, labelPosition, length; pigeon pinned at v1.3.0 (v1.12 decision carry-forward).
</context>

<interfaces>
From internal/c4d/grammar/c4d.peg: `OptionKey` rule at :458 is the closed alternation `("labelPosition" / "color" / "style" / "rank" / "arrow" / "length")`. Regen: `//go:generate pigeon -o parser_gen.go -nolint c4d.peg` (grammar/doc.go:12); run `go generate ./internal/c4d/grammar/`.
From internal/c4d/tomodel.go: `applyEdgeOption` :701-728 — switch on option key setting link fields.
From internal/c4d/frommodel.go: `edgeStmtFromLink` :183-223 — addOpt calls in canonical order.
From internal/c4d/emit_toml.go: `emitLinkTOML` :203-241 — writes link fields, D-23 canonical order doc comment.
From internal/c4d/emit_c4d.go: `edgeOptionRank` map :44-51.
From internal/c4d/grammar/reserved.go: `fieldKeywords` :33-39 (Levenshtein suggestion list).
From internal/testutil/canonsrc/canonsrc.go: TOML link writer + C4D edge-option writer (:226-227 region).
From internal/model/link.go (36-01): `LinkKind` + `Link.Kind`.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>RED: C4D grammar admits kind + regen parser</name>
  <read_first>
    internal/c4d/grammar/c4d.peg (OptionKey :458)
    internal/c4d/parse_test.go (edge option parse test style incl. Levenshtein suggestion assertions)
    internal/c4d/tomodel.go (applyEdgeOption :701-728)
  </read_first>
  <files>[internal/c4d/grammar/c4d.peg, internal/c4d/grammar/parser_gen.go, internal/c4d/grammar/reserved.go, internal/c4d/tomodel.go, internal/c4d/parse_test.go]</files>
  <behavior>
    - `a -> b { kind: "read" }` parses; Link.Kind == model.KindRead (all three values)
    - misspelled option `kindd:` → parse error suggesting "kind" (reserved.go fieldKeywords += "kind")
    - tomodel applyEdgeOption `case "kind": link.Kind = model.LinkKind(v)`
  </behavior>
  <action>
    In c4d.peg: add `"kind" /` to the OptionKey alternation (:458). Run `go generate ./internal/c4d/grammar/` to regen parser_gen.go (committed generated file — commit it). In reserved.go fieldKeywords: add "kind". In tomodel.go applyEdgeOption: add the kind case casting to model.LinkKind. In parse_test.go: table cases for the three kind values + the kindd→"kind" suggestion assertion.
  </action>
  <verify>
    <automated>go test ./internal/c4d/ -run TestParseEdge -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - c4d.peg OptionKey contains "kind"; parser_gen.go regenerated (git diff shows pigeon header/line changes) and committed
    - parse tests assert kind read/write/read-write land on Link.Kind
    - unknown-option test asserts suggestion list contains "kind" for input "kindd"
  </acceptance_criteria>
  <done>
    Commit: `feat(36-02): C4D grammar + tomodel kind option (pigeon regen)`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Canonical emission: kind in both emitters + canonsrc</name>
  <read_first>
    internal/c4d/frommodel.go (edgeStmtFromLink :183-223)
    internal/c4d/emit_toml.go (emitLinkTOML :203-241 + D-23 comment)
    internal/c4d/emit_c4d.go (edgeOptionRank :44-51)
    internal/testutil/canonsrc/canonsrc.go (TOML link writer + C4D edge-option writer)
    internal/c4d/emit_test.go (canonical order assertions)
  </read_first>
  <files>[internal/c4d/frommodel.go, internal/c4d/emit_toml.go, internal/c4d/emit_c4d.go, internal/testutil/canonsrc/canonsrc.go, internal/c4d/emit_test.go]</files>
  <behavior>
    - C4D edge statement emits `kind: "read"` between rank and color options; absent kind emits nothing
    - TOML link emission writes `kind = "read"` after rank; absent kind emits nothing (byte stability for no-kind models)
    - canonsrc writers mirror both (parity-test source of truth)
    - canonical order: arrow, rank, kind, color, style, labelPosition, length
  </behavior>
  <action>
    In frommodel.go edgeStmtFromLink: `addOpt("kind", string(link.Kind))` positioned after the rank addOpt (omit when empty — same pattern as siblings). In emit_toml.go emitLinkTOML: write kind after rank, update the D-23 canonical-field-order doc comment to include kind. In emit_c4d.go edgeOptionRank: add `"kind": 2` and shift later ranks by one. In canonsrc: add kind to the TOML link writer and C4D edge-option writer following the existing putString-when-nonempty pattern. In emit_test.go: extend canonical-order tests with a kind-carrying link asserting exact option order in both formats, plus an absent-kind byte-stability case.
  </action>
  <verify>
    <automated>go test ./internal/c4d/ -run TestEmit -v && go test ./internal/testutil/canonsrc/ -v</automated>
  </verify>
  <acceptance_criteria>
    - emitted C4D for a kind=read link contains `kind: "read"` positioned between rank and color options
    - emitted TOML contains `kind = "read"` after rank
    - no-kind link emits output byte-identical to pre-change (test asserts)
    - canonsrc writers carry kind (grep canonsrc.go for kind)
  </acceptance_criteria>
  <done>
    Commit: `feat(36-02): canonical kind emission in C4D/TOML emitters + canonsrc`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Round-trips: convert, fmt, parity, template survival</name>
  <read_first>
    internal/c4d/parity_test.go (round-trip corpus style)
    internal/template/expand.go (applySubstitutionLink :348-355 — enum fields NOT substituted)
    internal/template/expand_test.go (instantiation test style)
  </read_first>
  <files>[internal/c4d/parity_test.go, internal/template/expand_test.go, internal/c4d/canonequal.go]</files>
  <behavior>
    - kind-carrying model: to-toml → parse → to-c4d → parse → canonical-equal (both directions, D-22 contract)
    - fmt preserves kind in place (both formats, --check clean)
    - template-body link with literal kind survives Clone + instantiation intact (Link value-copy)
    - `${param}` inside a kind value does NOT substitute (kind is enum-ish; pinned negative test — the literal token stays, which documents the constraint)
    - canonequal needs NO kind normalization (no default) — add a comment-only confirmation if touched
  </behavior>
  <action>
    In parity_test.go: add kind fixtures to the round-trip table (TOML→C4D→TOML and C4D→TOML→C4D asserting canonical equality). In expand_test.go: two cases — (1) template link with kind=read instantiates with Kind preserved; (2) link with kind="${param}" instantiates with the literal token unchanged (assert Kind == "${param}" — documenting non-substitution). Do NOT modify expand.go.
  </action>
  <verify>
    <automated>go test ./internal/c4d/ -run TestParity -v && go test ./internal/template/ -v</automated>
  </verify>
  <acceptance_criteria>
    - parity round-trip tests pass with kind fixtures both directions
    - expand_test asserts Kind preserved through instantiation and NOT substituted (literal ${param} case)
    - internal/template/expand.go untouched (git diff empty for that file)
  </acceptance_criteria>
  <done>
    Commit: `test(36-02): kind round-trips — convert/fmt parity + template survival`
  </done>
</task>

</tasks>

<threat_model>
ASVS L1. T-36-02-01: kind values from C4D source are cast to model.LinkKind and later emitted into DOT attributes and C4D/TOML output. Injection risk is the same class as existing `style`/`rank` free strings — values flow through the attribute SafeSet quoting path; the emission omit-when-empty rule means no new unquoted interpolation. Test: kind containing `"` round-trips through parse+emit without breaking DOT emission (quoted attribute). No high-severity threats.
</threat_model>

<verification>
- `go test ./internal/c4d/ ./internal/template/ ./internal/testutil/...` green
- Full `go test ./...` green (convert/fmt corpus included)
- Generated parser committed (pigeon regen diff present in the grammar commit)
</verification>
