---
phase: 36-edge-semantics-and-legend
plan: 04
type: tdd
wave: 3
depends_on: ["36-01", "36-02", "36-03"]
files_modified: [internal/model/properties.go, internal/graph/graph.go, internal/graph/builder.go, internal/graph/builder_test.go, internal/view/view.go, internal/view/scope.go, internal/view/scope_test.go, internal/render/converter.go, internal/render/converter_test.go, internal/render/navigation.go, internal/parser/parser_test.go]
autonomous: true
requirements: [LEG-01, LEG-02]
user_setup: []

must_haves:
  truths:
    - "Every generated diagram (C1/C2/C3/expanded — including nameless C1/expanded) renders the legend by default; properties legend=false disables it"
    - "Legend shows the three kind colours (sourced from model consts — cannot drift) + three line-style samples, then author custom lines verbatim (HTML-escaped)"
    - "ShowLegend false → no legend rows; nameless + no-legend model → still no label at all (today's shape preserved)"
  artifacts:
    - path: "internal/model/properties.go"
      provides: "Legend *bool (nil=on) + LegendLines []LegendLine + LegendLine{Label,Color,Style}"
      contains: "LegendLine"
    - path: "internal/graph/graph.go"
      provides: "Legend{Entries []LegendEntry}, LegendEntry{Label,Color,Style}"
      contains: "LegendEntry"
    - path: "internal/render/converter.go"
      provides: "legend rows appended to the top HTML label table; guard changed so legend alone triggers label emission"
      contains: "legend"
  key_links:
    - from: "internal/model/colors.go"
      to: "internal/render/converter.go"
      via: "legend default rows consume model.Link*Colour consts — single palette source"
      pattern: "LinkReadColour"
---

<objective>
Implement the legend core end-to-end on the Go side: properties surface (TOML via struct tags), view plumbing to all four generators, builder assembly of default + custom entries, and renderer integration into the top HTML label table (right-aligned rows, BGCOLOR swatches, style glyphs) with the guard change. Purpose: LEG-01, LEG-02 (TOML-side; C4D surface + canonical round-trip + golden re-baseline land in 36-05).

Output: TOML-authored diagrams show the upper-right legend; disabling works; escaping is enforced.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md
@.planning/STATE.md

Source files (read before editing):
@internal/model/properties.go
@internal/graph/graph.go
@internal/render/converter.go
@internal/render/navigation.go
@internal/view/scope.go

Decisions honored (research §5, §12.1/4 — locked): `Legend *bool` nil-defaults-true (canonical normalization lands in 36-05); legend content = fixed default block always shown; position = top label block, legend right-aligned within it (GraphViz cannot absolutely position labels/clusters); every legend row wraps content in explicit FONT POINT-SIZE tags (the mixed-font-size row-drop quirk); swatches via `TD BGCOLOR` 12x8; style samples are text glyphs in #666666 (─── / - - - / · · ·) in one constant block next to navFont* in navigation.go.
</context>

<interfaces>
From internal/model/properties.go: Properties{Name, Description, Color, Style, Border, Edges, LineLength, Expanded}.
From internal/graph/graph.go: `Graph.Legend *Legend` (:48) + placeholder `Legend` struct :151-155.
From internal/view/view.go: View struct (Edges fields pattern) — add ShowLegend bool + LegendLines []model.LegendLine; populate in GenerateC1View (:125 region), GenerateC2View (:373), GenerateC3View (:466), GenerateExpandedView (:19) from m.Properties.
From internal/render/converter.go: configureGraphSettings :183-243 builds the top HTML table (SetLabelHTML + SetLabelLocation(TopLocation)); guard :210 `len(navTDs) == 0 && !hasTitle → no label` — legend must also trigger emission; row-drop quirk documented at :196-206.
From internal/render/navigation.go: navFontOpen const :22, color #666666 :17.
TOML parse: [[properties.legendLine]] is parse-safe (captureDefinitionOrder skips ArrayTable, parser.go:653-658); legend/legendLine ride the rawMap→Marshal→Unmarshal path (parser.go:150-159) via struct tags — no parser change needed.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Model + view plumbing: Legend fields, four generators, TOML parse</name>
  <read_first>
    internal/model/properties.go
    internal/graph/graph.go (Legend placeholder :151-155, Graph.Legend :48)
    internal/view/view.go (View struct + Edges pattern)
    internal/view/scope.go (four generator entry points :125/:373/:466/:19)
    internal/parser/parser_test.go (properties table-test style)
  </read_first>
  <files>[internal/model/properties.go, internal/graph/graph.go, internal/view/view.go, internal/view/scope.go, internal/view/scope_test.go, internal/parser/parser_test.go]</files>
  <behavior>
    - Properties gains `Legend *bool toml:"legend"` + `LegendLines []LegendLine toml:"legendLine"`; `type LegendLine struct{ Label, Color, Style string }` in properties.go
    - graph.Legend becomes `struct{ Entries []LegendEntry }`; `LegendEntry{Label, Color, Style string}`
    - View gains ShowLegend bool + LegendLines []model.LegendLine; ALL FOUR generators set ShowLegend = (m.Properties.Legend == nil || *m.Properties.Legend) and copy LegendLines
    - TOML: `legend = false` parses to *false; absent → nil; [[properties.legendLine]] rows parse (no phantom properties unit)
  </behavior>
  <action>
    Extend properties.go, replace the placeholder Legend struct, add the two View fields, populate them in the four generators (same pattern as the Edges: copies). Parser test: legend=false/absent/true + a two-row legendLine array asserting Label/Color/Style fields.
  </action>
  <verify>
    <automated>go test ./internal/parser/ -run TestProperties -v && go test ./internal/view/ -run TestLegend -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - properties.go contains `Legend *bool` and `LegendLines []LegendLine`
    - all four generators set ShowLegend/LegendLines (grep scope.go shows 4 sites)
    - parser tests assert nil-default + false + legendLine rows
  </acceptance_criteria>
  <done>
    Commit: `feat(36-04): legend model/view plumbing (4 generators)`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Builder assembly + renderer integration (upper-right table rows)</name>
  <read_first>
    internal/graph/builder.go (BuildGraph entry, BuildExpandedGraph)
    internal/render/converter.go (configureGraphSettings :183-243, guard :210, row quirk :196-206)
    internal/render/navigation.go (navFont consts)
    internal/render/converter_test.go (label table tests)
    .planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md (§5.3 position + escaping)
  </read_first>
  <files>[internal/graph/builder.go, internal/render/converter.go, internal/render/navigation.go, internal/render/converter_test.go, internal/graph/builder_test.go]</files>
  <behavior>
    - BuildGraph/BuildExpandedGraph assemble g.Legend when view.ShowLegend: default rows FIRST (Read #2E7D32, Write #C62828, Read-Write #6A1B9A swatch rows labelled "read"/"write"/"read-write"; style rows Solid/Dashed/Dotted with glyphs ─── / - - - / · · · in #666666), then view.LegendLines verbatim
    - renderer: legend rows appended to the top HTML table as right-aligned rows (TD ALIGN="RIGHT" COLSPAN=n nesting a 2-column borderless table; swatch cell TD BGCOLOR WIDTH=12 HEIGHT=8); every row wrapped in explicit <FONT POINT-SIZE="10">; custom Label + Color html.EscapeString'd (colour into the attribute, label into content)
    - guard change: legend present → label emitted even with empty name and no nav (nameless C1/expanded now carry the legend)
    - ShowLegend false → no legend rows; nameless+no-legend model → still no label at all (today's shape preserved)
  </behavior>
  <action>
    In navigation.go: add legendGlyph constants block (solidGlyph/dashedGlyph/dottedGlyph = "───"/"- - -"/"· · ·") + legendFontOpen/Close consts mirroring navFont*. In builder.go: add unexported buildLegend(v *view.View) *graph.Legend consuming model.Link*Colour consts for the three swatch rows. In converter.go configureGraphSettings: consume g.Legend (the graph already carries it — pass entries through the existing label-assembly path), append right-aligned rows after nav/title rows, change the :210 guard to `len(navTDs) == 0 && !hasTitle && legend == nil`. In tests: render-level tests asserting the legend HTML rows (swatch BGCOLOR values equal the model consts — single-source check), custom-line escaping (<, &, | labels), guard behavior for nameless C1, and disabled-legend absence.
  </action>
  <verify>
    <automated>go test ./internal/render/ -run TestLegend -v && go test ./internal/graph/ -run TestBuildLegend -v</automated>
  </verify>
  <acceptance_criteria>
    - legend HTML contains BGCOLOR="#2E7D32"/"#C62828"/"#6A1B9A" sourced from model consts (test references consts, not literals)
    - custom line "a<b" renders escaped (no raw < in label content beyond markup)
    - nameless C1 fixture now emits a graph label containing the legend; with legend=false it emits none
    - every legend row is wrapped in an explicit FONT POINT-SIZE tag (mixed-size row-drop quirk)
  </acceptance_criteria>
  <done>
    Commit: `feat(36-04): legend rendering — default + custom rows in top label table`
  </done>
</task>

</tasks>

<threat_model>
ASVS L1. T-36-04-01 (HIGH, mitigated in-task): legend custom lines are AUTHOR STRINGS interpolated into a GraphViz HTML-like label — unescaped content could inject arbitrary HTML/markup into the rendered SVG (stored XSS when the SVG is published). Mitigation: html.EscapeString on Label (content) and Color/Style (attribute positions) before table assembly, using the existing labels.go escape discipline; test asserts `<`, `>`, `&`, `"` in a custom label render escaped. T-36-04-02 (low): legendLine rows with missing fields render with empty colour/style slots (no panic); C4D-side parse strictness lands in 36-05. No other new surfaces.
</threat_model>

<verification>
- `go test ./internal/parser/ ./internal/view/ ./internal/graph/ ./internal/render/` green
- Full `go test ./...` green
- Rendered DOT of a TOML fixture with a custom legend line shows escaped, right-aligned legend rows
</verification>
