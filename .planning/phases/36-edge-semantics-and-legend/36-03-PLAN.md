---
phase: 36-edge-semantics-and-legend
plan: 03
type: tdd
wave: 2
depends_on: ["36-01"]
files_modified: [internal/view/scope.go, internal/view/scope_test.go, internal/validator/index.go, internal/validator/index_test.go, internal/graph/builder.go, internal/graph/builder_test.go, cmd/c4drill/root_test.go]
autonomous: true
requirements: [GEDGE-01, RANK-02, AGG-01, AGG-02, AGG-03]
user_setup: []

must_haves:
  truths:
    - "properties.edges reaches C2 and C3 views when the drilled unit sets no own edges; per-unit edges still win; C1/expanded unchanged"
    - "Kind and Rank survive all 4 view copiers AND the validator mirror"
    - "Collapsed pairs (2+ links, same logical pair) get kind-derived colour (all-same kind → it; mixed → read-write), style precedence (all-same → it; any solid → solid; else any dashed → dashed; else dotted), and explicit-colour suppression (any constituent link.Color != \"\" → source-border default)"
    - "Single-link pairs and AllExpanded mode keep pure per-edge semantics (BC-safe)"
  artifacts:
    - path: "internal/view/scope.go"
      provides: "cmp.Or edges fallback at C2 (:377) and C3 (:470); Kind copied by all 4 copiers"
      contains: "cmp.Or"
    - path: "internal/validator/index.go"
      provides: "populateIncomingLinks mirror copies Kind"
      contains: "Kind:"
    - path: "internal/graph/builder.go"
      provides: "collectPairAggregates pre-scan + AGG overrides at markSeen-survivor"
      contains: "collectPairAggregates"
  key_links:
    - from: "internal/view/scope.go"
      to: "internal/graph/builder.go"
      via: "view copiers carry Kind/Rank on resolved links; builder aggregates per pair"
      pattern: "ResolvedLinks"
---

<objective>
Plumb the new link semantics through view resolution and implement collapsed-edge aggregation: global edges fallback for C2/C3, Kind/Rank survival through all five copy sites, and the pair-level AGG pre-scan in the builder applying colour and style precedence on collapsed pairs. Purpose: GEDGE-01, RANK-02, AGG-01..03.

Output: collapsed C1/C2/C3 diagrams show kind identity and style precedence; splines setting is global.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md
@.planning/STATE.md

Source files (read before editing):
@internal/view/scope.go
@internal/validator/index.go
@internal/graph/builder.go

Decisions honored (research §1, §4, §8, §12.5 — locked): aggregation computed in BUILDER (not view); partial-kind pairs (any constituent kind unset) do NOT aggregate colour (fall to default path) — BC-safe; AGG-02 applies the solid default to unstyled constituents BEFORE comparing (unstyled forces solid); AGG skips when v.AllExpanded; labels/technology stay first-wins.
</context>

<interfaces>
From internal/view/scope.go: `Edges:` copies at :128 (C1, properties — correct), :377 (C2 — `Edges: systemUnit.Edges`), :470 (C3 — `Edges: containerUnit.Edges`); copiers at :331 (resolveBoundaryLink), :831 (resolveBoundaryLinks), :970 (addResolvedCrossLink), :1003 (addResolvedCrossLinkFrom) — each constructs a new model.Link copying Technology, Description, Style, Arrow, Rank, LabelPosition, Color, Mirror.
From internal/validator/index.go: `populateIncomingLinks` :70-81 — synthesizes the reverse link into target's LinksFrom (Mirror=true), copying the same field set.
From internal/graph/builder.go: `countPairMultiplicity` :421-433 (pre-scan precedent, `v.AllExpanded` opt-out); `processOutgoingLinks` :482-519 / `processIncomingLinks` :522-559 (dedup key `"src->dst"` resolved views, first-wins `markSeen` :614-622); effective style default "solid" applied per-link before use.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Global edges fallback for C2/C3 views + cmd-level E2E</name>
  <read_first>
    internal/view/scope.go (:377 C2 View construction, :470 C3 View construction)
    internal/view/scope_test.go (view generator test style)
    cmd/c4drill/root_test.go (E2E pipeline test style — renderThroughPipeline helper)
  </read_first>
  <files>[internal/view/scope.go, internal/view/scope_test.go, cmd/c4drill/root_test.go]</files>
  <behavior>
    - unit.Edges == "" → View.Edges == m.Properties.Edges (fallback works: C2 and C3)
    - unit.Edges != "" → View.Edges == unit.Edges (per-unit wins)
    - C1 (:128) and expanded (:22) unchanged (regression assert: still properties-driven)
    - cmd-level E2E: a model with `properties.edges = "straight"` and a subunit-bearing system yields generated C2 DOT containing `splines=false`
  </behavior>
  <action>
    In scope.go: change :377 to `Edges: cmp.Or(systemUnit.Edges, m.Properties.Edges)` and :470 to `Edges: cmp.Or(containerUnit.Edges, m.Properties.Edges)` (import "cmp"). In scope_test.go: table-driven test per generator covering fallback, per-unit-wins, and empty-both. In cmd/c4drill/root_test.go: add an E2E test rendering a small fixture (temp dir) with properties.edges="straight" + a system with subunits, asserting the generated C2 .dot file contains `splines=false`.
  </action>
  <verify>
    <automated>go test ./internal/view/ -run TestEdgesFallback -v && go test ./cmd/c4drill/ -run TestGlobalEdgesE2E -v</automated>
  </verify>
  <acceptance_criteria>
    - scope.go contains `cmp.Or(systemUnit.Edges, m.Properties.Edges)` and `cmp.Or(containerUnit.Edges, m.Properties.Edges)`
    - tests assert all three branches for C2 and C3
    - cmd-level E2E test asserts `splines=false` appears in the generated C2 DOT for properties.edges="straight"
  </acceptance_criteria>
  <done>
    Commit: `feat(36-03): properties.edges fallback into C2/C3 views + E2E`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Kind/Rank survival: 4 view copiers + validator mirror</name>
  <read_first>
    internal/view/scope.go (copiers :331, :831, :970, :1003)
    internal/validator/index.go (populateIncomingLinks :70-81)
    internal/view/scope_test.go, internal/validator/index_test.go (table-test style)
  </read_first>
  <files>[internal/view/scope.go, internal/view/scope_test.go, internal/validator/index.go, internal/validator/index_test.go]</files>
  <behavior>
    - every copier's constructed Link carries Kind and Rank from the source link (Rank already copied — pin it in the same table)
    - validator-synthesized incoming link (Mirror) carries Kind and Rank
  </behavior>
  <action>
    In scope.go: add `Kind: src.Kind,` to all four Link constructions (Rank is already present — leave as-is). In validator/index.go: add `Kind: link.Kind,` to the mirror construction (:70-81). In both test files: one table-driven test per copier asserting Kind+Rank survival.
  </action>
  <verify>
    <automated>go test ./internal/view/ ./internal/validator/ -v</automated>
  </verify>
  <acceptance_criteria>
    - grep scope.go shows 4 `Kind:` additions in the copier constructions
    - validator index.go mirror carries Kind
    - copier tests assert kind=read survives each resolution path; rank=reverse survives too (RANK-02)
  </acceptance_criteria>
  <done>
    Commit: `feat(36-03): Kind/Rank survive view copiers + validator mirror`
  </done>
</task>

<task type="tdd" tdd="true">
  <name>AGG: pair aggregation pre-scan + collapsed colour/style precedence</name>
  <read_first>
    internal/graph/builder.go (countPairMultiplicity :421-433, processOutgoingLinks :482-519, processIncomingLinks :522-559, markSeen :614-622)
    internal/graph/builder_test.go (hand-built view test style with ResolvedLinks)
    .planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md (§4 — copy the precedence table into the test)
  </read_first>
  <files>[internal/graph/builder.go, internal/graph/builder_test.go]</files>
  <behavior>
    - `collectPairAggregates(v *view.View) map[string]pairAggregate` walked beside countPairMultiplicity (same key `"src->dst"`, skipped when v.AllExpanded); pairAggregate = {kinds []model.LinkKind, styles []string (post-solid-default), anyExplicitClr bool}
    - at the markSeen-true survivor with 2+ constituents:
      - AGG-01 colour: all kinds set AND all equal → that kind colour (kindColour); all kinds set AND mixed → model.LinkReadWriteColour; any kind unset → no kind colour
      - AGG-03 suppression: any constituent link.Color != "" → no kind colour at all (fall to today's source-border default of the drawn edge)
      - AGG-02 style: all styles equal → that style; else any "solid" → solid; else any "dashed" → dashed; else "dotted" (unstyled constituents defaulted to "solid" first)
    - single-link pairs: pure per-edge semantics (36-01 precedence), untouched
    - AllExpanded mode: aggregation skipped (every parallel link renders separately, v1.7 keys)
    - labels/technology unchanged (first-wins)
  </behavior>
  <action>
    In builder.go: add the pairAggregate struct + collectPairAggregates function (walking the same resolved-link lists countPairMultiplicity walks, building the same edgeKey), call it in buildEdges before the process* loops (skip when v.AllExpanded), and in each process* function at the markSeen-true point apply the override to the surviving edge's Color/Style per the precedence above. In builder_test.go: table-driven tests — all-read pair → #2E7D32; all-write → #C62828; read+write → #6A1B9A; read+read-write → #6A1B9A; one unset kind → default; explicit colour on ANY constituent → source-border default; style matrix rows (all-dashed→dashed, solid+dashed→solid, dashed+dotted→dashed, dotted+dotted→dotted, solid+unset→solid); single-link unaffected; AllExpanded unaffected.
  </action>
  <verify>
    <automated>go test ./internal/graph/ -run TestPairAggregate -v</automated>
  </verify>
  <acceptance_criteria>
    - builder.go contains collectPairAggregates + pairAggregate, gated on AllExpanded
    - colour matrix tests assert exact hex values from model consts
    - style precedence tests assert solid > dashed > dotted with the all-equal fast path
    - explicit-colour suppression test asserts source-border default (not #6A1B9A, not the custom colour)
    - single-link and AllExpanded regression asserts unchanged semantics
  </acceptance_criteria>
  <done>
    Commit: `feat(36-03): collapsed-edge kind colour + style precedence (AGG pre-scan)`
  </done>
</task>

</tasks>

<threat_model>
ASVS L1. No new input surfaces — aggregation consumes already-validated model data; cmp.Or is pure. T-36-03-01 (informational): a many-parallel-link model grows the aggregate slices O(n) per pair — bounded by model size, no unbounded growth. No high-severity threats.
</threat_model>

<verification>
- `go test ./internal/view/ ./internal/validator/ ./internal/graph/` green
- Full `go test ./...` green
- BC spot-check: no-kind, no-override fixture renders identical edges (regression asserts inside tests)
</verification>
