---
phase: 36-edge-semantics-and-legend
plan: 05
type: tdd
wave: 4
depends_on: ["36-04"]
files_modified: [internal/c4d/grammar/c4d.peg, internal/c4d/grammar/parser_gen.go, internal/c4d/tomodel.go, internal/c4d/frommodel.go, internal/c4d/emit_toml.go, internal/c4d/emit_c4d.go, internal/c4d/canonequal.go, internal/c4d/parse_test.go, internal/c4d/emit_test.go, internal/c4d/parity_test.go, internal/testutil/canonsrc/canonsrc.go, cmd/c4drill/testdata/multilevel.expanded.dot, cmd/c4drill/testdata/expanded.dot, cmd/c4drill/testdata/expanded/, cmd/c4drill/testdata/multilevel/, internal/graph/builder_test.go]
autonomous: true
requirements: [LEG-03, BC-01]
user_setup: []

must_haves:
  truths:
    - "legend/legendLine authorable in C4D (`legend: false` bareword; `legendLine: [\"Label|Color|Style\"]` pipe-split list) and round-trip canonically: nil legend == true normalization in canonequal + canonsrc; legend=false emitted only when explicitly false"
    - "BC-01: models using no new feature render identical DOT except the graph label block; multilevel.expanded.dot re-baselined in a dedicated commit; full suite green"
    - "no-feature regression test: valid.toml → EmitTOML byte-equal to pre-phase output; DOT differs from pre-phase only in the graph label statement"
  artifacts:
    - path: "internal/c4d/tomodel.go"
      provides: "applyPropertyField cases legend (bareword true/false, error otherwise) + legendLine (pipe-split list)"
      contains: "legendLine"
    - path: "internal/c4d/canonequal.go"
      provides: "nil Legend normalized to true in canonicalModelForCompare"
      contains: "Legend"
    - path: "cmd/c4drill/testdata/multilevel.expanded.dot"
      provides: "re-baselined asserted golden (legend-only delta)"
      contains: "label="
  key_links:
    - from: "internal/c4d/canonequal.go"
      to: "internal/testutil/canonsrc/canonsrc.go"
      via: "nil==true legend normalization must match on both compare and emit sides or convert round-trips fail"
      pattern: "legend"
---

<objective>
Complete the legend's C4D surface and the canonical round-trip story, then land the BC-01 golden re-baseline: grammar admission + regen, to-model parsing (bareword bool, pipe-split custom lines), emission slots in both formats, nil==true normalization in canonequal/canonsrc, and the dedicated multilevel.expanded.dot re-baseline with a no-feature regression test. Purpose: LEG-03, BC-01.

Output: legend fully dual-format; the one accepted golden delta lands documented; convert/fmt stay green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md
@.planning/STATE.md

Source files (read before editing):
@internal/c4d/grammar/c4d.peg
@internal/c4d/tomodel.go
@internal/c4d/frommodel.go
@internal/c4d/canonequal.go
@internal/testutil/canonsrc/canonsrc.go

Decisions honored (research §5.4/5.5, §12.1/2 — locked): C4D has no boolean literals — legend parses as bareword "true"/"false" with hard error otherwise; custom lines = pipe-split list literal (label must not contain |); legend emitted ONLY when explicitly false (nil/true omitted → byte stability); nil==true normalization in canonequal (:37 clone point) + canonsrc writer; [[properties.legendLine]] emission after expanded (D-23 slot); propertyFieldRank updated.
</context>

<interfaces>
From internal/c4d/grammar/c4d.peg: `PropertyKey` closed alternation :666; regen via `//go:generate pigeon -o parser_gen.go -nolint c4d.peg` → `go generate ./internal/c4d/grammar/`.
From internal/c4d/tomodel.go: `applyPropertyField` :285-312.
From internal/c4d/frommodel.go: `propertiesBlockFromModel` :64-99; emit order via internal/c4d/emit_c4d.go `propertyFieldRank` :37-46.
From internal/c4d/emit_toml.go: `emitPropertiesTOML` :64-105.
From internal/c4d/canonequal.go: `canonicalModelForCompare` :37 copies Properties by value — add nil→&trueVal for Legend.
From internal/testutil/canonsrc/canonsrc.go: property writer — emit nothing when Legend nil-or-true, `legend = false` when explicitly false; legendLine rows after expanded.
Golden regen: `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded` → cmd/c4drill/testdata/multilevel.expanded.dot (consumed by internal/graph/builder_test.go:1209 + :2385).
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>C4D legend surface: grammar + parse + emission + nil==true normalization</name>
  <read_first>
    internal/c4d/grammar/c4d.peg (PropertyKey :666)
    internal/c4d/tomodel.go (applyPropertyField :285-312)
    internal/c4d/frommodel.go (propertiesBlockFromModel :64-99)
    internal/c4d/emit_c4d.go (propertyFieldRank :37-46), internal/c4d/emit_toml.go (emitPropertiesTOML :64-105)
    internal/c4d/canonequal.go (:37 clone), internal/testutil/canonsrc/canonsrc.go (property writer)
    internal/c4d/parse_test.go, internal/c4d/emit_test.go, internal/c4d/parity_test.go (existing styles)
  </read_first>
  <files>[internal/c4d/grammar/c4d.peg, internal/c4d/grammar/parser_gen.go, internal/c4d/tomodel.go, internal/c4d/frommodel.go, internal/c4d/emit_c4d.go, internal/c4d/emit_toml.go, internal/c4d/canonequal.go, internal/testutil/canonsrc/canonsrc.go, internal/c4d/parse_test.go, internal/c4d/emit_test.go, internal/c4d/parity_test.go]</files>
  <behavior>
    - C4D: `legend: false` (bareword) parses to *false; `legend: true` to *true; `legend: garbage` → hard error naming the allowed values; `legendLine: ["Label|#2E7D32|dashed", "Plain|#C0392B"]` parses rows (pipe-split: label, colour, optional style); a row with <2 pipe segments → hard error
    - emission both formats: legend emitted ONLY when explicitly false; legendLine rows emitted after expanded (TOML [[properties.legendLine]] blocks / C4D composed list); absent → byte-stable output
    - canonequal + canonsrc: nil Legend normalized to true on both sides (convert/parity green when one side states legend=true explicitly)
    - propertyFieldRank/emit order updated
    - round-trip parity fixtures: legend set/unset, TOML↔C4D both directions
  </behavior>
  <action>
    In c4d.peg PropertyKey: add "legend" / "legendLine"; run `go generate ./internal/c4d/grammar/`; commit regen. In tomodel.go applyPropertyField: case "legend" (bareword true/false, error otherwise) + case "legendLine" (list → rows via pipe-split; error when a row has <2 segments). In frommodel.go propertiesBlockFromModel: emit legend only-when-false + composed legendLine list. Update emit_c4d.go propertyFieldRank + emit_toml.go emitPropertiesTOML slots + doc comments. In canonequal.go canonicalModelForCompare: normalize clone.Properties.Legend nil→&trueVal; mirror in canonsrc property writer. Tests: parse matrix (true/false/garbage/rows + short-row error), round-trip parity with legend set/unset, explicit-true-vs-nil equality, byte-stability of no-legend emission.
  </action>
  <verify>
    <automated>go test ./internal/c4d/ -v && go test ./internal/testutil/canonsrc/ -v</automated>
  </verify>
  <acceptance_criteria>
    - c4d.peg PropertyKey contains "legend"/"legendLine"; parser_gen.go regen committed
    - legend: garbage → hard error (test asserts message mentions true/false)
    - convert to-toml and to-c4d round-trips green with legend=false and legendLine rows
    - nil-vs-explicit-true canonical equality test passes both directions
    - no-legend-model emission byte-stable (asserted)
  </acceptance_criteria>
  <done>
    Commit: `feat(36-05): legend C4D surface + canonical round-trip normalization`
  </done>
</task>

<task type="execute">
  <name>BC-01: golden re-baseline + no-feature regression</name>
  <read_first>
    internal/graph/builder_test.go (:1209 TestBuildExpandedGraphBaselineDOT, :2385 REF-05)
    .planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md (§9 golden inventory)
  </read_first>
  <files>[cmd/c4drill/testdata/multilevel.expanded.dot, cmd/c4drill/testdata/expanded.dot, cmd/c4drill/testdata/expanded/, cmd/c4drill/testdata/multilevel/, internal/graph/builder_test.go]</files>
  <behavior>
    - multilevel.expanded.dot regenerated via `go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml --format dot --expanded` — the ONLY asserted golden; diff shows legend-only label delta
    - stale decorative artifacts (expanded.dot, expanded/, multilevel/) regenerated in the same commit for hygiene
    - NEW no-feature regression test: parse cmd/c4drill/testdata/valid.toml → EmitTOML → byte-equal to a captured pre-phase string (source-level byte stability); render DOT differs from pre-phase ONLY in the graph label statement (legend) — assert via canonical compare excluding the label attr
  </behavior>
  <action>
    Regenerate the golden + decorative artifacts with the CLI. Add the no-feature regression test in builder_test.go. Run the full suite.
  </action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - git diff of multilevel.expanded.dot shows changes confined to the graph label attribute (legend rows)
    - no-feature regression test present and green
    - `go test ./...` fully green
  </acceptance_criteria>
  <done>
    Dedicated commit: `test(36-05): re-baseline multilevel.expanded.dot for default-on legend (BC-01)`
  </done>
</task>

</tasks>

<threat_model>
ASVS L1. T-36-05-01 (low, mitigated): C4D legendLine rows with <2 pipe segments → hard parse error, no panic (tested); pipe-in-label constraint documented in 36-06. Legend content escaping itself landed in 36-04 (html.EscapeString + tests). No high-severity threats.
</threat_model>

<verification>
- `go test ./...` green (incl. c4d parity corpus + cmd convert/fmt)
- BC-01: exactly one asserted golden re-baselined, legend-only delta documented in the commit message
</verification>
