---
phase: 36-edge-semantics-and-legend
plan: 06
type: execute
wave: 5
depends_on: ["36-04", "36-05"]
files_modified: [README.adoc, skill/SKILL.md, skill/examples/03-links.toml, skill/examples/03-links.c4d, skill/examples/04-styling.toml, skill/examples/04-styling.c4d, skill/examples/10-edge-kinds.toml, skill/examples/10-edge-kinds.c4d, skill/examples/10-edge-kinds.svg, skill/examples/06-templates.svg, skill/examples/06-templates-expanded.svg, examples/cloud-system/cloud-system.svg, examples/overflow-test/overflow-test.svg, examples/rank-for-better-layout/rank-for-better-layout.toml, examples/rank-for-better-layout/rank-for-better-layout.svg, plugins/c4drill/skills/c4drill-toml/, plugins/c4drill/opencode/skills/c4drill-toml/, .github/workflows/validate-skill-examples.yml]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, REL-01]
user_setup: []

must_haves:
  truths:
    - "README.adoc documents: working unit styling, global edges fallback + square=ortho, rank=reverse idiom, kind colours, legend setting + custom lines (both formats), and the kind/${param} non-substitution constraint"
    - "skill/SKILL.md and BOTH plugin copies are byte-synced; skill examples render cleanly through the full pipeline and validate in CI (incl. .c4d)"
    - "Release v1.18.0 tagged and pushed as the FINAL action; CI release workflow picks it up"
  artifacts:
    - path: "README.adoc"
      provides: "rank/kind/legend/unit-styling/edges documentation"
      contains: "rank = \"reverse\""
    - path: "skill/examples/10-edge-kinds.toml"
      provides: "kind + aggregation + rank=reverse + legend custom lines fixture (with .c4d twin)"
      contains: "kind"
  key_links:
    - from: "skill/SKILL.md"
      to: "plugins/c4drill/skills/c4drill-toml/SKILL.md"
      via: "byte-sync across all plugin copies (diff -r clean)"
      pattern: "c4drill-toml"
---

<objective>
Document and ship: README sections for the whole new surface, skill + plugin-copy sync, example fixtures (extended 03/04, new 10-edge-kinds with twins + regenerated SVGs), CI example validation extended to .c4d, and the v1.18.0 release tag as the final action. Purpose: DOC-01..03, REL-01.

Output: docs match behavior; examples render; release is cut.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md (§11 docs inventory — the section-by-section map)
@.planning/PROJECT.md

Source files (read before editing):
@README.adoc
@skill/SKILL.md
@.github/workflows/validate-skill-examples.yml

Decisions honored: old `<-` + arrow=reverse idiom keeps working — document rank=reverse as the CONVENIENT form (research §2.5); purple #6A1B9A is the read-write blend; legend default-on documented in Generation Guidelines/Common Mistakes (advise disabling only for final-polish minimalist diagrams); kind is NOT substituted by ${param} in templates (pinned in 36-02) — document this constraint explicitly so the interpretation is user-visible; sync-all plugin copies (resolve the pre-existing 04-styling drift by taking skill/ as source of truth).
</context>

<interfaces>
README.adoc touch map (research §11): `=== Properties Section` (~195) — legend + legendLine rows, edges value clarification (square = ortho routing); `==== Link Attributes` (~520) — kind row + rank reverse semantics annotation; NEW subsection after Links `==== Edge Kinds and the Legend`; `=== Styling` (~602) — note the triple now renders (incl. dotted); `=== Effect of Rank` (~155) — rank=reverse replacement narrative; `== C4D Format` `=== Fields` (~800) + `=== Edges` (~847) + `=== Side by Side with TOML` (~945) — kind/legend/legendLine rows.
skill/SKILL.md touch map: `### C4D Syntax Cheat-Sheet` (~101), `### [properties] Section` (~172), `### Link Syntax` (~419), `### Link Attributes` (~464), `### Examples` (~771), `## Generation Guidelines` (~805), `## Common Mistakes` (~827).
Plugin copies: plugins/c4drill/skills/c4drill-toml/ + plugins/c4drill/opencode/skills/c4drill-toml/ (SKILL.md + examples/ each).
SVG regen commands: `go run ./cmd/c4drill <fixture> -f svg -o <path>` per examples/README.md.
</interfaces>

<tasks>

<task type="execute">
  <name>Example fixtures: extend 03/04, add 10-edge-kinds, regenerate SVGs</name>
  <read_first>
    skill/examples/03-links.toml, 03-links.c4d, 04-styling.toml, 04-styling.c4d
    examples/rank-for-better-layout/rank-for-better-layout.toml
    examples/README.md (regen command table)
  </read_first>
  <files>[skill/examples/03-links.toml, skill/examples/03-links.c4d, skill/examples/04-styling.toml, skill/examples/04-styling.c4d, skill/examples/10-edge-kinds.toml, skill/examples/10-edge-kinds.c4d, skill/examples/10-edge-kinds.svg, skill/examples/06-templates.svg, skill/examples/06-templates-expanded.svg, examples/cloud-system/cloud-system.svg, examples/overflow-test/overflow-test.svg, examples/rank-for-better-layout/rank-for-better-layout.toml, examples/rank-for-better-layout/rank-for-better-layout.svg, examples/README.md]</files>
  <behavior>
    - 03-links gains a kind-carrying link pair (read + write) in BOTH formats
    - 04-styling gains a [[properties.legendLine]] custom row (its unit styling now visibly renders)
    - NEW 10-edge-kinds.toml/.c4d: kind edges incl. a collapsing pair (subunit-to-same-target), rank="reverse" edge, legend custom line; header comments explaining each feature
    - every touched fixture renders cleanly: `go run ./cmd/c4drill <fixture> -f dot` exits 0; 10-edge-kinds.svg committed
    - fmt --check clean on all fixtures (both formats)
  </behavior>
  <action>
    Edit/extend the fixtures as described; generate 10-edge-kinds.svg via the CLI; keep .c4d twins model-equal to their .toml (TestExampleTwins-style parity). Regenerate stale SVGs listed in files_modified (06-templates, cloud-system, overflow-test, rank-for-better-layout) so README image refs match behavior; update examples/README.md table rows for 10-edge-kinds.
  </action>
  <verify>
    <automated>go run ./cmd/c4drill skill/examples/10-edge-kinds.toml -f dot > /dev/null && go run ./cmd/c4drill skill/examples/10-edge-kinds.c4d -f dot > /dev/null && go run ./cmd/c4drill fmt --check skill/examples/ && go test ./cmd/c4drill/ -run TestExample</automated>
  </verify>
  <acceptance_criteria>
    - 10-edge-kinds.toml and .c4d both render (exit 0) and carry kind + rank=reverse + legendLine
    - all fixtures pass fmt --check
    - regenerated SVGs committed; examples/README.md table has a 10-edge-kinds row
  </acceptance_criteria>
  <done>
    Commit: `docs(36-06): edge-kinds examples + regenerated SVGs`
  </done>
</task>

<task type="execute">
  <name>README.adoc + skill/SKILL.md documentation sweep</name>
  <read_first>
    README.adoc (touch map in context)
    skill/SKILL.md (touch map in context)
    .planning/phases/36-edge-semantics-and-legend/36-RESEARCH.md (§11)
  </read_first>
  <files>[README.adoc, skill/SKILL.md]</files>
  <behavior>
    - every README/SKILL snippet actually runs: extract each new TOML/C4D snippet to a temp file and render (exit 0)
    - documented values match implementation: kind colours #2E7D32/#C62828/#6A1B9A; legend default on; square = ortho; rank=reverse arrow-preservation rule; collapsed-style precedence sentence (solid > dashed > dotted); custom-colour suppression note
    - old idiom documented as legacy-compatible; rank=reverse presented as the convenient form
    - kind/${param} constraint documented: kind is NOT substituted by template params (enum fields are not substituted; other link fields are)
  </behavior>
  <action>
    Apply the section touch maps from research §11 to README.adoc and skill/SKILL.md. Keep the established doc voice (terse tables + runnable snippets). Add the new `==== Edge Kinds and the Legend` README subsection, the SKILL.md attribute-table rows, and the template-substitution constraint note in both templates and C4D sections.
  </action>
  <verify>
    <automated>go run ./cmd/c4drill skill/examples/10-edge-kinds.toml -f dot > /dev/null && grep -c "rank = \"reverse\"" README.adoc</automated>
  </verify>
  <acceptance_criteria>
    - README.adoc contains: kind attribute row, legend/legendLine properties rows, rank=reverse section, square=ortho note, unit-styling-works note, kind/${param} constraint
    - skill/SKILL.md contains the same surface in cheat-sheet/properties/link-attributes/examples/guidelines sections
    - all new snippets render via the CLI (manual extraction check recorded in summary)
  </acceptance_criteria>
  <done>
    Commit: `docs(36-06): README + skill documentation for edge semantics and legend`
  </done>
</task>

<task type="execute">
  <name>Plugin copy sync + CI .c4d example validation</name>
  <read_first>
    plugins/c4drill/skills/c4drill-toml/ (tree)
    plugins/c4drill/opencode/skills/c4drill-toml/ (tree)
    .github/workflows/validate-skill-examples.yml
  </read_first>
  <files>[plugins/c4drill/skills/c4drill-toml/, plugins/c4drill/opencode/skills/c4drill-toml/, .github/workflows/validate-skill-examples.yml]</files>
  <behavior>
    - `diff -r skill plugins/c4drill/skills/c4drill-toml` and `diff -r skill plugins/c4drill/opencode/skills/c4drill-toml` both clean (source-of-truth skill/ wins, resolving the pre-existing 04-styling comment drift; 06-templates files now present in copies)
    - validate-skill-examples.yml also validates skill/examples/*.c4d (render via `go run ./cmd/c4drill <file> -f dot`)
  </behavior>
  <action>
    Sync both plugin copy trees from skill/ (cp skill/SKILL.md + skill/examples/ into each). Extend validate-skill-examples.yml with a .c4d validation step mirroring the TOML one.
  </action>
  <verify>
    <automated>diff -r skill plugins/c4drill/skills/c4drill-toml && diff -r skill plugins/c4drill/opencode/skills/c4drill-toml</automated>
  </verify>
  <acceptance_criteria>
    - both diffs exit 0 (byte-synced)
    - workflow yml contains a .c4d validation step
  </acceptance_criteria>
  <done>
    Commit: `docs(36-06): sync plugin skill copies + CI .c4d example validation`
  </done>
</task>

<task type="execute">
  <name>REL-01: tag and push v1.18.0 (FINAL action)</name>
  <read_first>
    .github/workflows/release.yml (triggers on v* tags; builds 6 platforms + gh release create --generate-notes)
    .planning/MILESTONES.md (milestone summary style for release notes context)
  </read_first>
  <files>[]</files>
  <behavior>
    - master is green (`go test ./...`) and all prior commits pushed
    - `git tag v1.18.0 && git push origin v1.18.0` as the LAST action of the phase
    - CI release workflow triggers (verify the workflow file is unchanged and the tag is on the release commit)
  </behavior>
  <action>
    Run the full suite one final time, push master, then tag v1.18.0 on the final commit and push the tag. Record the release URL pattern in the phase summary.
  </action>
  <verify>
    <automated>git describe --tags --exact-match && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - v1.18.0 tag exists on the final commit and is pushed (git ls-remote --tags origin v1.18.0 shows it)
    - full suite green at the tagged commit
  </acceptance_criteria>
  <done>
    Release tagged; GitHub Actions release build kicks off (gh run list --workflow=release.yml)
  </done>
</task>

</tasks>

<threat_model>
ASVS L1. Docs-only + release surface. T-36-06-01 (informational): documented snippets become user copy-paste surface — kept accurate to implementation to avoid users disabling escaping assumptions (no code change). No high-severity threats.
</threat_model>

<verification>
- `go test ./...` green; fmt --check clean on skill/examples
- plugin diffs clean
- v1.18.0 tagged + pushed; release workflow running
</verification>
