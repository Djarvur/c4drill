---
phase: 41-check-command
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - README.adoc
  - skill/SKILL.md
autonomous: true
requirements: [CHECK-04]
must_haves:
  truths:
    - "A reader of README.adoc can run c4drill check knowing the usage, both accepted file formats, the exit codes, and the no-output guarantee (D-06)"
    - "skill/SKILL.md's command usage block lists check alongside convert and fmt (D-06)"
    - "Documented behavior matches the shipped check implementation (no invented flags or outputs)"
  artifacts:
    - path: "README.adoc"
      provides: "`=== check` AsciiDoc command section beside `=== fmt` and `=== serve`"
      contains: "=== check"
    - path: "skill/SKILL.md"
      provides: "check usage line in the command examples block"
      contains: "c4drill check"
  key_links:
    - from: "README.adoc === check"
      to: "cmd/c4drill/check.go"
      via: "documented usage string matches the cobra Use line and exit codes match errValidationFailed path"
      pattern: "c4drill check <file\\.toml\\|file\\.c4d>"
---

<objective>
Document `c4drill check` in the project's CLI surfaces (CHECK-04, D-06).

Purpose: A command users cannot discover is a command that does not exist; the docs must state usage, accepted formats, exit codes, and the no-output guarantee so CI authors adopt it.
Output: README.adoc `=== check` section; skill/SKILL.md usage entry.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-check-command/41-CONTEXT.md
@README.adoc
@skill/SKILL.md
@cmd/c4drill/check.go

<interfaces>
<!-- Facts to document — verified against the implementation shipped by plan 41-01 -->

From cmd/c4drill/check.go (shipped in 41-01):
  Use line: "check <file.toml|file.c4d>"
  behavior: runs parse -> includes -> templates -> peers -> validation (same front-half as render); prints validation errors to stderr; exits 0 valid / exit 1 invalid; writes NOTHING

From README.adoc structure (AsciiDoc — NOT markdown):
  command sections use "=== <name>" H3 headings with [source,text] usage blocks and "*" bullets
  existing order: === convert (line ~1417), === fmt (line ~1452), === serve (line ~1474)

From skill/SKILL.md (lines ~150-166):
  "### Converting and Formatting (convert / fmt)" section holds a ```bash usage block ending with the fmt CI-gate example
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.adoc `=== check` section</name>
  <files>README.adoc</files>
  <read_first>README.adoc lines 1440-1480 (=== fmt and === serve sections — the exact AsciiDoc style to mirror), cmd/c4drill/check.go (the Use line, Long help, and behavior being documented), .planning/phases/41-check-command/41-CONTEXT.md decision D-06</read_first>
  <action>Insert a new `=== check` AsciiDoc section between `=== fmt` (ends ~line 1472) and `=== serve` (starts ~line 1474), matching the sibling sections' style exactly — AsciiDoc headings, [source,text] block, "*" bullets, [source,bash] example; no markdown syntax (RESEARCH Pitfall 4). Content per D-06: usage line `c4drill check <file.toml|file.c4d>`; bullet that it validates without rendering — running the same pipeline front-half as render (includes resolved, templates expanded, relative peers resolved, then validation); bullet that it accepts both authoring formats (.toml and .c4d); bullet for exit codes — 0 when valid, 1 when invalid with the same validation errors render reports (e.g. orphan-unit rules); bullet for the no-output guarantee — writes nothing, no output directory needed, safe for CI gates; [source,bash] example `c4drill check architecture.c4d   # exit 0 valid / exit 1 + errors invalid` plus a CI-flavored example pairing `c4drill fmt --check .` with `c4drill check` as the format+validate gate. Do not document flags — check defines none and hides inherited render flags (D-05).</action>
  <verify>
    <automated>grep -c '^=== check$' README.adoc && grep -c 'c4drill check' README.adoc && grep -n '=== fmt' README.adoc | head -1</automated>
  </verify>
  <acceptance_criteria>
    - README.adoc contains a line exactly `=== check` positioned after the `=== fmt` section and before the `=== serve` section
    - the section contains the usage string `c4drill check <file.toml|file.c4d>`, the strings `exit` (exit codes), and the no-output guarantee wording
    - the section uses AsciiDoc syntax only ([source,text] / [source,bash] blocks, * bullets) — no markdown headings in the diff
    - documented behavior matches check.go (usage string equals the cobra Use line; no flags documented)
  </acceptance_criteria>
  <done>README.adoc documents check alongside the existing command surface, AsciiDoc-consistent</done>
</task>

<task type="auto">
  <name>Task 2: skill/SKILL.md usage entry</name>
  <files>skill/SKILL.md</files>
  <read_first>skill/SKILL.md lines 140-175 (the "Converting and Formatting (convert / fmt)" section and its bash block), README.adoc === check section from Task 1 (consistency reference), cmd/c4drill/check.go</read_first>
  <action>Extend the command examples so the skill's users discover check: retitle the section heading from "### Converting and Formatting (convert / fmt)" to "### Converting, Formatting, Validating (convert / fmt / check)", and append to the section's bash block two lines with comments in the file's established comment style: `c4drill check architecture.c4d` commented as render-free validation (same checks as render, nothing written) and `c4drill check architecture.toml && c4drill fmt --check .` commented as the CI gate pair (validate + format-check, both exit 1 on failure). Wording stays consistent with D-06 and the README section from Task 1.</action>
  <verify>
    <automated>grep -c 'c4drill check' skill/SKILL.md && grep -c 'convert / fmt / check' skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skill/SKILL.md contains at least one `c4drill check` usage line inside the command examples bash block
    - the section heading reads "### Converting, Formatting, Validating (convert / fmt / check)"
    - the check line documents exit semantics consistent with README (valid 0 / invalid 1)
  </acceptance_criteria>
  <done>skill/SKILL.md command surface includes check (CHECK-04 complete across both docs targets)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | documentation-only plan; no code, no input handling, no installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-DOC-01 | Repudiation | docs drifting from behavior | mitigate | Task acceptance criteria pin documented usage string to check.go's cobra Use line and exit codes to the errValidationFailed path shipped by 41-01 |
</threat_model>

<verification>
- grep confirms `=== check` in README.adoc and `c4drill check` in skill/SKILL.md
- documented usage string equals cmd/c4drill/check.go's Use line
- go build ./... still clean (docs-only plan, but confirm nothing accidental)
</verification>

<success_criteria>
- CHECK-04 satisfied: README documents check (usage, both formats, exit codes, no-output guarantee) and skill/SKILL.md lists the command
- Documentation is AsciiDoc-consistent and matches shipped behavior exactly (D-06)
</success_criteria>

<output>
Create `.planning/phases/41-check-command/41-02-SUMMARY.md` when done
</output>
