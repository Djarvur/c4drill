---
phase: 37-nesting-context-and-plain-rendering
plan: 07
type: execute
wave: 5
depends_on: [37-05, 37-06]
files_modified: []
autonomous: true
requirements: [REL-01]
tags: [release, tag, ci]

must_haves:
  truths:
    - "Git tag v1.21.0 exists on the final phase commit and is pushed to origin (REL-01)"
    - "The CI release workflow runs on the tag, builds all six platform artifacts, and creates the GitHub release v1.21.0 with artifacts attached"
    - "The tagged tree is fully green: full test suite, vet, and the skill/example validation workflow all pass before tagging"
  artifacts:
    - path: "git tag v1.21.0"
      provides: "Product release marker (milestone v1.14)"
  key_links:
    - from: "git tag v1.21.0 (push)"
      to: ".github/workflows/release.yml"
      via: "on: push: tags: v* triggers build + GitHub release creation"
      pattern: "v1\\.21\\.0"
---

<objective>
Ship milestone v1.14 as product release v1.21.0: pre-tag validation (full suite green, CI checks clean), annotated tag v1.21.0 on the final phase commit, push, and verify the release workflow builds artifacts and creates the GitHub release.

Purpose: REL-01 is the phase's final task (STATE.md carry-forward decision) — the tag is the release mechanism (CI release.yml injects the version via -ldflags "-X main.version=${GITHUB_REF_NAME}" and creates the GitHub release; v1.18.0 precedent).

Output: Tag v1.21.0 pushed; GitHub release v1.21.0 published with platform artifacts.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.github/workflows/release.yml
@.github/workflows/pr-sanity.yml
@.github/workflows/validate-skill-examples.yml

<interfaces>
<!-- Release mechanics (verified against current source 2026-08-30): -->

Release trigger (.github/workflows/release.yml): on push of tags matching v*. Build matrix: linux/amd64+arm64, darwin/amd64+arm64, windows/amd64+arm64 (.exe); build command: go build -trimpath -ldflags "-s -w -X main.version=${GITHUB_REF_NAME}" -o dist/c4drill-<os>-<arch><ext> ./cmd/c4drill; a release job downloads artifacts and creates the GitHub release (GH_TOKEN: github.token). No version constant in the repo to bump — the version comes from the tag name. No CHANGELOG file exists (none to update).

Pre-tag local gates (mirror pr-sanity.yml): go build ./..., go test -race ./..., golangci-lint (if installed locally; CI lint otherwise — do not block on missing local lint, CI pr-sanity covers it on the branch).

Tag precedent: v1.18.0 was tagged on the final milestone STATE commit; the tag is annotated.

Remote verification via gh CLI: gh run list --workflow=release.yml, gh release view v1.21.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-tag validation gate</name>
  <files>(no file changes — verification only)</files>
  <read_first>.github/workflows/pr-sanity.yml, go.mod</read_first>
  <behavior>
    - Working tree clean (all phase work committed, including plan summaries and STATE updates the orchestrator produced).
    - go build ./... exits 0; go test -race ./... fully green; go vet ./... clean.
    - git log confirms the docs/release-pending commits are on the branch and pushed.
  </behavior>
  <action>
    Run the full local gate: git status (must be clean — commit anything pending first), go build ./..., go test -race ./..., go vet ./.... Confirm the branch is pushed (git status shows up to date with origin). If ANY gate fails, stop — the failure belongs to an earlier plan, not the release.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && git status --porcelain | wc -l | grep -q '^0$' && go build ./... && go vet ./... && go test -race ./... 2>&1 | tail -8</automated>
    Expected: clean tree; build+vet silent; test suite all OK.
  </verify>
  <done>
    Tree clean, build/vet/tests green, branch pushed — safe to tag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tag v1.21.0, push, verify release workflow (REL-01)</name>
  <files>(no repo file changes)</files>
  <read_first>.github/workflows/release.yml</read_first>
  <behavior>
    - Annotated tag v1.21.0 exists on HEAD and is pushed to origin.
    - gh run list shows the release workflow triggered by the tag completing successfully.
    - gh release view v1.21.0 shows the published release with the six platform artifacts (linux/darwin/windows x amd64/arm64).
  </behavior>
  <action>
    1. Create the annotated tag on HEAD with a message summarizing the milestone (nesting context + plain rendering, v1.14): git tag -a v1.21.0 -m "v1.21.0: Nesting Context and Plain Rendering".
    2. Push the tag: git push origin v1.21.0.
    3. Monitor with gh: gh run list --workflow=release.yml --limit 3, then gh run watch <run-id> (or poll) until completion. If the workflow fails, inspect logs with gh run view <run-id> --log-failed, fix, delete+re-tag only if the fix requires a new commit (git tag -d v1.21.0 && git push origin :refs/tags/v1.21.0, then re-tag) — never leave a red release tag.
    4. Verify the release: gh release view v1.21.0 — confirm it exists and lists the expected assets (c4drill-linux-amd64, c4drill-linux-arm64, c4drill-darwin-amd64, c4drill-darwin-arm64, c4drill-windows-amd64.exe, c4drill-windows-arm64.exe).
    5. Record the release URL in the plan summary.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && git tag -l v1.21.0 && git ls-remote --tags origin v1.21.0 && gh release view v1.21.0 --json name,assets --jq '.name + " assets=" + (.assets | length | tostring)'</automated>
    Expected: tag exists locally and on origin; release v1.21.0 with assets count >= 6.
  </verify>
  <done>
    v1.21.0 tagged, pushed, CI release green, GitHub release published with all platform artifacts (REL-01 satisfied).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag push → CI release publication | The tag is the release authority — a premature or wrong tag ships broken artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-13 | Tampering | release tag content | mitigate | Task 1 blocks tagging on an unclean tree or red suite; the tag is annotated (auditable message) and the release artifacts are built by CI from the tagged tree, not local state. |
| T-37-14 | Denial of Service | release workflow failure | mitigate | Workflow failure is monitored via gh run watch; recovery procedure (delete + re-tag after fix) is specified in the task action. |
| T-37-SC | Tampering | supply chain | mitigate | No installs; artifacts built by the existing pinned-action CI workflow (actions/checkout@v7, setup-go@v7) — unchanged from prior releases. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `git tag -l v1.21.0` and `git ls-remote --tags origin v1.21.0` → present both sides.
- `gh run list --workflow=release.yml` → successful run for the v1.21.0 tag.
- `gh release view v1.21.0` → published with >= 6 assets.
- Pre-tag gates: clean tree, go build/vet/test -race green.
</verification>

<success_criteria>
- REL-01: milestone ships as product release v1.21.0 — tag on the final phase commit, CI-built artifacts, published GitHub release.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-07-SUMMARY.md` when done.
</output>
