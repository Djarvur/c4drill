---
phase: 37-nesting-context-and-plain-rendering
plan: 05
type: execute
wave: 5
depends_on: [37-01, 37-02, 37-03, 37-04]
files_modified:
  - internal/view/scope_test.go
  - cmd/c4drill/testdata/multilevel.dot
  - cmd/c4drill/testdata/multilevel.expanded.dot
  - cmd/c4drill/testdata/expanded/mainsystem.dot
autonomous: false
requirements: [CTX-01, BC-01]
tags: [goldens, canonical, invariant, bc, go]

must_haves:
  truths:
    - "Every depicted element in every non-expanded view of the multilevel fixture sits inside its complete ancestor chain — the invariant test proves CTX-01 falls out of CTX-02+CTX-03"
    - "ALL committed-golden re-baselines happen in THIS ONE task (RESEARCH Pitfall 1) and every golden diff corresponds to a documented CTX delta (nested clusters and/or deep-link chains)"
    - "Flat models (fixtures without deep-link/expanded-nesting scenarios) render unchanged — their goldens/tests need no re-baseline (BC-01)"
    - "Full `go test ./...` is green after re-baseline (BC-01)"
    - "Human confirms the rendered SVGs: nested clusters + drill affordances read well, --plain output is legible"
  artifacts:
    - path: "internal/view/scope_test.go"
      provides: "CTX-01 ancestor-chain invariant test over the multilevel fixture views"
      contains: "TestViewAncestorChainInvariant"
    - path: "cmd/c4drill/testdata/multilevel.dot"
      provides: "Re-baselined C1 golden reflecting documented CTX deltas"
    - path: "cmd/c4drill/testdata/multilevel.expanded.dot"
      provides: "Re-baselined expanded golden (also consumed by internal/graph COMPAT-02 and REF-05 baseline tests)"
    - path: "cmd/c4drill/testdata/expanded/mainsystem.dot"
      provides: "Re-baselined C2/C3 drill-down golden"
  key_links:
    - from: "internal/view/scope_test.go (invariant test)"
      to: "cmd/c4drill/testdata/multilevel.toml"
      via: "generate all non-expanded views and assert ancestor closure of VisiblePaths entries"
      pattern: "multilevel|VisiblePaths"
    - from: "cmd/c4drill/testdata/*.dot (re-baselined)"
      to: "internal/testutil/canonical"
      via: "all baseline tests compare order-insensitively with geometry stripped (DI-1)"
      pattern: "canonical\\.Canonical"
---

<objective>
Prove CTX-01 (every depicted element renders inside its complete chain of ancestor containers on non-expanded views) with an invariant test, then absorb ALL documented CTX golden deltas in ONE consolidated re-baseline task and prove BC-01: without `--plain`, models that do not exercise the new nesting scenarios render unchanged and the full suite is green. Close with a single human visual checkpoint.

Purpose: plans 37-01/37-02 deliberately deferred every golden re-baseline here so the goldens are regenerated exactly once against final code (RESEARCH Pitfall 1). This plan is the phase's backward-compatibility gate.

Output: Passing invariant test, re-baselined multilevel + expanded goldens (only where CTX deltas apply), full-suite green, one approved visual checkpoint.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-01-SUMMARY.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-02-SUMMARY.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-03-SUMMARY.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-04-SUMMARY.md
@internal/view/scope_test.go
@cmd/c4drill/testdata/multilevel.toml
@cmd/c4drill/root_test.go

<interfaces>
<!-- Contracts and expected-failure state carried in from prior plans. -->

Expected pre-existing failures before this plan (documented by 37-01/37-02 summaries): EXACTLY two golden-baseline tests, both in internal/graph, both reading cmd/c4drill/testdata/multilevel.expanded.dot — TestBuildExpandedGraphBaselineDOT (COMPAT-02, builder_test.go L1231) and the REF-05 baseline test (builder_test.go L2398). There are NO cmd-level golden-baseline tests in root_test.go: root_test.go renders into tmp dirs (root_test.go L679, L695-699, L760) and never compares committed golden files, so updating the committed dot/svg artifacts requires NO test-code changes and breaks no root_test.go test.

Invariant mechanics: for every non-expanded view generated from multilevel.toml (C1 + each C2/C3 drill-down), every entry registered in View.VisiblePaths (deep-link chains) and every depicted descendant inside expanded clusters must have all its ancestor path prefixes (within the view's scope) present in View.Units — that is the view-level definition of "renders inside its complete chain" once plan 37-01's recursive clusters render nested entries as nested clusters.

Boundary-node exclusion (RESEARCH scoping): entries with IsBoundary or IsExternal are deliberately top-level — their out-of-scope position IS their context. The invariant must NOT require ancestors for them.

Canonical comparator: github.com/Djarvur/c4drill/internal/testutil/canonical — strips geometry (bb/pos/lp/lheight/lwidth/height/width) and sorts statements/attributes; ALL baseline comparisons already use it, so re-baselined files must be regenerated OUTPUT (never hand-edited).

Regeneration command pattern: go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml -f dot -o <tmpdir> then copy the produced files over the committed paths (preserving each file's role: multilevel.dot = C1, multilevel.expanded.dot = --expanded, expanded/mainsystem.dot = drill-down).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CTX-01 ancestor-chain invariant test over the multilevel fixture (test-first)</name>
  <files>internal/view/scope_test.go</files>
  <read_first>internal/view/scope_test.go, cmd/c4drill/testdata/multilevel.toml, internal/view/scope.go, .planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md</read_first>
  <behavior>
    - Test name: TestViewAncestorChainInvariant. Parse cmd/c4drill/testdata/multilevel.toml (same fixture-loading pattern the view tests use for the multilevel model), then for EACH non-expanded view — GenerateC1View, GenerateC2View for every system, GenerateC3View for every container (enumerate from the model like existing integration tests do) — assert: for every path in v.VisiblePaths and every entry in v.Units that is neither IsBoundary nor IsExternal, every strict ancestor prefix of the path that lies within the view's scope (i.e., prefixes longer than the view root being depicted) is also present in v.Units.
    - The invariant MUST pass against the post-37-01/37-02 code. If it fails, a real nesting gap surfaced — fix the view code (scope.go), not the test.
  </behavior>
  <action>
    Add TestViewAncestorChainInvariant to internal/view/scope_test.go: load the multilevel fixture, iterate all non-expanded views, and assert ancestor closure as specified in the behavior block. Implement the prefix walk with strings.LastIndex-based segmentation (strings is already imported in the package). Explicitly skip IsBoundary/IsExternal entries per the RESEARCH scoping decision. If the invariant exposes a gap in the CTX implementation, stop and fix scope.go (or escalate if the fix changes a 37-01/37-02 contract) before proceeding.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/view/ -run 'TestViewAncestorChainInvariant' -v && go test ./internal/view/ -count=1</automated>
    Expected: invariant PASSES on first run against the completed CTX code; full view package green.
  </verify>
  <done>
    CTX-01 is enforced by a standing invariant test over the 4-level fixture; view package green.
  </done>
</task>

<task type="auto">
  <name>Task 2: ONE consolidated golden re-baseline for documented CTX deltas (BC-01)</name>
  <files>cmd/c4drill/testdata/multilevel.dot, cmd/c4drill/testdata/multilevel.expanded.dot, cmd/c4drill/testdata/expanded/mainsystem.dot</files>
  <read_first>cmd/c4drill/root_test.go, internal/graph/builder_test.go, cmd/c4drill/testdata/multilevel.toml, .planning/phases/37-nesting-context-and-plain-rendering/37-01-SUMMARY.md, .planning/phases/37-nesting-context-and-plain-rendering/37-02-SUMMARY.md, internal/testutil/canonical/canonical.go</read_first>
  <behavior>
    - After regeneration: go test ./... is fully green — zero failing tests across all packages.
    - Every regenerated golden's diff vs the old committed file shows ONLY documented CTX deltas: nested sub-clusters where flat child lists were (CTX-03) and/or deep-link chain entries with edges terminating at true targets (CTX-02). No unrelated attribute/label/formatting churn.
  </behavior>
  <action>
    1. Run go test ./... and collect the COMPLETE list of failing golden-baseline tests (expected: EXACTLY internal/graph TestBuildExpandedGraphBaselineDOT + the REF-05 baseline test, both reading multilevel.expanded.dot — builder_test.go L1231 and L2398; there are no cmd-level golden comparisons in root_test.go, so root_test.go must be GREEN before re-baselining — if root_test.go or any other package fails, that is an unexpected regression: fix the code, not the golden).
    2. Regenerate the affected outputs by running the CLI into a temp dir: go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml -f dot -o /tmp/c4drill-p37-rebaseline (and the --expanded run for multilevel.expanded.dot; identify the drill-down run that produces expanded/mainsystem.dot the same way the original golden was produced — check root_test.go for how each golden is generated). Copy regenerated files over the committed paths listed in this task's files. Never hand-edit a golden.
    3. For each regenerated file, diff against git HEAD and VERIFY the diff is exclusively nested-cluster structure and/or deep-link chain/edge changes. If any diff shows unrelated changes (label formatting, colours, legend, ordering), stop and investigate — that signals an unintended behavior change in 37-01..37-04.
    4. Flat models stay clean: fixtures without the new scenarios (e.g. cmd/c4drill/testdata/expanded.toml-era goldens and any in-package expectations on flat models) must require NO re-baseline. If a flat-model golden fails, that is a BC-01 violation — fix the code.
    5. Re-run go test ./... — must be fully green. Commit the re-baselined goldens as one atomic commit (e.g. "test(37-05): re-baseline multilevel goldens for documented CTX nesting deltas (BC-01)").
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./... 2>&1 | tail -12</automated>
    Expected: every package OK — full suite green, zero failures.
  </verify>
  <done>
    All golden deltas absorbed in one atomic re-baseline; diffs audited as CTX-only; flat models untouched; full go test ./... green (BC-01).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual approval of nesting-context and --plain rendering</name>
  <files>(no file changes — human verification of automated renders)</files>
  <read_first>cmd/c4drill/testdata/plain.toml, cmd/c4drill/testdata/multilevel.toml</read_first>
  <action>
    Claude has already automated everything: render both SVGs, then pause for human visual confirmation. What was built: nesting-context rendering (CTX-01..03) and --plain rendering (PLAIN-01..04), verified visually on real SVGs after the golden re-baseline. Verification steps:
    1. Run: go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml -f svg -o /tmp/c4drill-p37-visual
    2. Open /tmp/c4drill-p37-visual/multilevel.svg (C1). Confirm: expanded units show nested containers as nested clusters (not flat lists); any deep-link target appears inside its chain of ancestor containers; collapsed containers keep the magnifier affordance; unfolded container clusters keep their explore/drill-down link affordance on the cluster label.
    3. Open one drill-down SVG under /tmp/c4drill-p37-visual/multilevel/<...> and confirm the drill-down nesting picture matches what C1 shows for the same subtree.
    4. Run: go run ./cmd/c4drill cmd/c4drill/testdata/plain.toml --plain -f svg -o /tmp/c4drill-p37-visual-plain
    5. Open the generated SVG. Confirm: labels are readable plain text (no HTML-table artifacts), no custom colours from the fixture leak through, kind-derived edge colours and the legend are still visible, layout spacing looks default (no stretched minlen gaps, no reversed edge directions).
  </action>
  <verify>
    <human-check>Human opened both SVGs and confirmed: nested clusters + deep-link chains legible with drill affordances intact; --plain SVG readable with legend and kind colours present. Resume signal: type "approved" or describe issues.</human-check>
  </verify>
  <done>
    Human approved both features' rendered output; checkpoint cleared.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Regenerated golden files → committed baselines | A wrong regeneration would silently enshrine buggy output as the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering | golden re-baseline | mitigate | Goldens are regenerated by the CLI only (never hand-edited) and every diff is audited against the documented CTX delta list before committing; unrelated diff content halts the task. canonical.Canonical keeps comparisons order-insensitive so regeneration noise cannot hide semantic changes. |
| T-37-10 | Repudiation | invariant test scope | accept | The invariant's boundary-node exclusion follows the documented RESEARCH scoping decision (sibling/external nodes are top-level by design) — recorded here and in the test comments for traceability. |
| T-37-SC | Tampering | go module / toolchain | mitigate | No new third-party packages; no installs. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `go test ./internal/view/ -run 'TestViewAncestorChainInvariant' -v` → PASS (CTX-01).
- `go test ./...` → fully green after ONE atomic golden re-baseline (BC-01).
- Golden diffs audited: nested clusters + deep-link chains only; flat-model goldens untouched.
- Visual checkpoint approved (nested clusters + affordances legible; plain SVG legible with legend + kind colours intact).
</verification>

<success_criteria>
- CTX-01 enforced by a standing invariant test: every depicted element inside its complete ancestor chain on all non-expanded views.
- BC-01: full suite green; goldens re-baselined exactly once, only for documented CTX deltas; models without the new scenarios render unchanged.
- Human visual approval of both features' rendered output.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-05-SUMMARY.md` when done.
</output>
