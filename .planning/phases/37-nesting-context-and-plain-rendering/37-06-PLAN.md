---
phase: 37-nesting-context-and-plain-rendering
plan: 06
type: execute
wave: 4
depends_on: [37-03, 37-04]
files_modified:
  - README.adoc
  - skill/SKILL.md
  - plugins/c4drill/skills/c4drill-toml/SKILL.md
  - plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md
  - skill/examples/11-nesting-context.toml
  - skill/examples/11-nesting-context.c4d
  - skill/examples/12-plain.toml
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
tags: [docs, readme, skill, examples]

must_haves:
  truths:
    - "README.adoc documents the nesting-context behavior (what changed about container chains, deep links, expanded clusters) and the --plain key — listing exactly what is ignored and what deliberately stays (DOC-01)"
    - "skill/SKILL.md and BOTH plugin copies are byte-identical (CI runs diff -r on all three) with the same --plain / nesting surface documented (DOC-02)"
    - "New example fixtures demonstrate both features and render cleanly through the full pipeline (CI validates every skill/examples/*.toml and .c4d twin) (DOC-03)"
    - "No other README/skill content regressed: existing sections keep their anchors; examples numbering continues the existing sequence"
  artifacts:
    - path: "README.adoc"
      provides: "Nesting-context section + --plain CLI reference (ignored vs stays)"
      contains: "--plain"
    - path: "skill/SKILL.md"
      provides: "--plain and nesting-context documentation synced to plugins"
      contains: "--plain"
    - path: "plugins/c4drill/skills/c4drill-toml/SKILL.md"
      provides: "Byte-identical plugin copy 1"
    - path: "plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md"
      provides: "Byte-identical plugin copy 2"
    - path: "skill/examples/11-nesting-context.toml"
      provides: "Nesting-context demo fixture (+ .c4d twin)"
    - path: "skill/examples/12-plain.toml"
      provides: "Custom-formatting fixture for --plain before/after demonstration"
  key_links:
    - from: "skill/SKILL.md"
      to: "plugins/c4drill/skills/c4drill-toml/SKILL.md + plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md"
      via: "byte-identical copies enforced by CI diff -r (.github/workflows/validate-skill-examples.yml)"
      pattern: "diff -r skill plugins"
    - from: "skill/examples/*.toml"
      to: "full render pipeline"
      via: "CI job renders every example; new fixtures must exit 0"
      pattern: "for file in skill/examples/\\*\\.toml"
---

<objective>
Document both v1.14 features where users and agents read: README.adoc gets the nesting-context behavior and the `--plain` key (what is ignored, what deliberately stays); the c4drill-toml skill documents the same surface and all THREE copies stay byte-identical; new example fixtures demonstrate both features and render cleanly through the full pipeline.

Purpose: DOC-01..03 are ship-gates for v1.21.0 — the skill copies are CI-enforced parity surface, and example fixtures are CI-validated renders, so docs and fixtures land together in one plan (v1.12/v1.13 precedent).

Output: Updated README.adoc, three synced skill copies, two new example fixtures (11-nesting-context.toml + .c4d twin, 12-plain.toml) with rendered SVG artifacts where the existing convention commits them.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@README.adoc
@skill/SKILL.md
@skill/examples/04-styling.toml
@skill/examples/10-edge-kinds.toml
@/Users/nil/.zcode/skills/c4drill-toml/SKILL.md

<interfaces>
<!-- Binding mechanical constraints (PATTERNS.md "No Analog Found"): -->

CI parity check (.github/workflows/validate-skill-examples.yml, runs FIRST on pristine checkout):
    diff -r skill plugins/c4drill/skills/c4drill-toml
    diff -r skill plugins/c4drill/opencode/skills/c4drill-toml
Any file added under skill/ (including examples) must appear byte-identical in BOTH plugin copies in the SAME commit.

CI example validation: every skill/examples/*.toml is rendered through the full pipeline; .c4d twins are validated too (added v1.13). Examples that fail to render break CI.

Existing example numbering: 01-minimal, 02-nested, 03-links, 04-styling, 05-ecommerce, 06-templates, 07-relative-peer, 08-include, 09-composed, 10-edge-kinds. Next: 11, 12. Convention: .toml + .c4d twin where the feature is format-neutral; committed .svg artifacts where prior examples commit them (06-templates.svg, 06-templates.expanded.svg, 10-edge-kinds.svg).

Plain-mode semantics to document (REQUIREMENTS.md PLAIN-01..04 + Out of Scope):
IGNORED under --plain: unit color/style/border (fall back to type-palette defaults, including expanded-unit clusters); link color/style; link length (default spacing); link rank (forward ranking — no endpoint swap, no constraint suppression); properties.edges (default splines); custom label formatting (plain-text labels; name/technology/description preserved).
DELIBERATELY KEPT: kind-derived edge colours (semantic, from link kind) and the legend; queue-pipe SVG shapes (type-default rendering); collapsed subtrees stay collapsed (chains render only for depicted elements).
CLI-only: no TOML/C4D model keys, no granular flags; convert/fmt unaffected.

Nesting-context semantics to document (CTX-01..03): depicted elements always render inside their complete ancestor-container chains on non-expanded views; deep-link targets keep their container context (edge terminates at the true target); expanded units show nested clusters instead of flat lists; sibling/external boundary nodes remain top-level by design; collapsed subtrees are not restructured.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.adoc — nesting-context behavior + --plain CLI reference (DOC-01)</name>
  <files>README.adoc</files>
  <read_first>README.adoc, .planning/REQUIREMENTS.md, .planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md</read_first>
  <behavior>
    - README contains a nesting-context section describing: full ancestor chains on non-expanded views, deep-link targets keeping container context, expanded units rendering nested clusters, and the explicit boundary (sibling/external boundary nodes stay top-level; collapsed subtrees are not restructured).
    - README CLI reference documents --plain with an ignored-vs-stays list exactly matching the semantics in the context block above, including a usage example command line.
    - Existing README sections/anchors are unmodified except where the new sections insert.
  </behavior>
  <action>
    Edit README.adoc: (1) add a nesting-context subsection in the features area — follow the existing section heading style/level; describe the three CTX behaviors and the boundary note; reference the multilevel example if the README already references example fixtures. (2) Add --plain to the CLI reference next to --expanded: boolean flag, one-paragraph semantics, the ignored list (unit color/style/border, link color/style/length/rank, properties.edges, label formatting) and the stays list (kind-derived edge colours, legend, queue pipes), a note that it is CLI-only (no model-file keys, works with --expanded), and an example invocation. Use the exact flag spelling --plain. Do not document granular flags or model-level ignore keys (Out of Scope).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && grep -c '\-\-plain' README.adoc && grep -c -i 'nesting context\|ancestor' README.adoc</automated>
    Expected: both grep counts >= 1 (flag documented; nesting behavior documented).
  </verify>
  <done>
    README documents both features with the exact ignored/stays boundary; no existing content regressed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Skill + both plugin copies synced (DOC-02)</name>
  <files>skill/SKILL.md, plugins/c4drill/skills/c4drill-toml/SKILL.md, plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md</files>
  <read_first>skill/SKILL.md, plugins/c4drill/skills/c4drill-toml/SKILL.md, plugins/c4drill/opencode/skills/c4drill-toml/SKILL.md, .github/workflows/validate-skill-examples.yml</read_first>
  <behavior>
    - skill/SKILL.md documents --plain (same ignored/stays boundary as README, phrased for the authoring-skill audience) and the nesting-context behavior authors can rely on.
    - diff -r skill plugins/c4drill/skills/c4drill-toml → identical; diff -r skill plugins/c4drill/opencode/skills/c4drill-toml → identical.
  </behavior>
  <action>
    Edit skill/SKILL.md: add the --plain key and nesting-context notes in the sections where formatting/CLI behavior is already described (match the existing voice and heading structure; keep it authoring-focused — when to expect default styling, that the model file gains NO new keys). Then copy the FULL skill/ tree to both plugin locations so every file (SKILL.md and any examples mirrored there) is byte-identical: cp -R skill/SKILL.md into plugins/c4drill/skills/c4drill-toml/ and plugins/c4drill/opencode/skills/c4drill-toml/, and mirror any other changed files. Run both diff -r commands locally before committing — CI runs them on a pristine checkout.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && diff -r skill plugins/c4drill/skills/c4drill-toml && diff -r skill plugins/c4drill/opencode/skills/c4drill-toml && grep -c 'plain' skill/SKILL.md</automated>
    Expected: both diffs silent (exit 0); skill mentions plain (count >= 1).
  </verify>
  <done>
    All three skill copies byte-identical with the new surface documented (DOC-02).
  </done>
</task>

<task type="auto">
  <name>Task 3: Example fixtures for both features, pipeline-clean (DOC-03)</name>
  <files>skill/examples/11-nesting-context.toml, skill/examples/11-nesting-context.c4d, skill/examples/12-plain.toml</files>
  <read_first>skill/examples/10-edge-kinds.toml, skill/examples/10-edge-kinds.c4d, skill/examples/04-styling.toml, skill/SKILL.md, README.adoc</read_first>
  <behavior>
    - 11-nesting-context.toml (and its .c4d twin, canonically equivalent) demonstrates: a deep container hierarchy where a link targets a deeply nested unit (deep-link chain demo) and an expanded unit containing a subunit-container (nested-cluster demo). It renders cleanly through the full pipeline (CI job exits 0).
    - 12-plain.toml carries explicit formatting (unit color/style/border, link color/style/length/rank, properties.edges) so the documented `c4drill --plain` command visibly strips it. It renders cleanly too (CI renders it without --plain; the README/skill text carries the --plain invocation).
    - Both fixtures respect the authoring rules in the c4drill-toml skill (naming, types, ordering keys).
  </behavior>
  <action>
    1. Write skill/examples/11-nesting-context.toml: 3-level nesting (system → container → component, one deeper branch), a cross-branch link whose target is a level-3 unit, one expanded unit that contains a container with children. Render it (go run ./cmd/c4drill skill/examples/11-nesting-context.toml -f svg -o /tmp/p37-ex11) to confirm it validates and renders; commit the SVG next to the fixture only if the sibling examples commit SVGs (10-edge-kinds.svg precedent) — if yes, commit 11-nesting-context.svg generated from the final fixture.
    2. Write skill/examples/11-nesting-context.c4d as the canonical equivalent of the .toml (verify equivalence: go run ./cmd/c4drill convert to-toml / to-c4d round-trip or the parity tooling the repo provides — the internal/c4d parity tests define canonical equivalence; the twin must render the same structure). Follow the .c4d twin convention of examples 02/03/04/07/10.
    3. Write skill/examples/12-plain.toml: small model with recognizable custom formatting values (documented in README as the --plain demo fixture; reference it from the README --plain section if a fixture reference fits the existing style). Render once with and once without --plain to /tmp to confirm both paths work; commit per the same SVG convention as step 1.
    4. Mirror any new example files into BOTH plugin copies (the CI diff -r covers the whole skill/ tree, not just SKILL.md) and re-run the two diff -r checks.
    5. Validate the full example sweep the way CI does: render every skill/examples/*.toml through the pipeline (and .c4d twins) — all must succeed.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go run ./cmd/c4drill skill/examples/11-nesting-context.toml -f dot -o /tmp/p37-ex11 && go run ./cmd/c4drill skill/examples/12-plain.toml --plain -f dot -o /tmp/p37-ex12-plain && go run ./cmd/c4drill skill/examples/12-plain.toml -f dot -o /tmp/p37-ex12-default && diff -r skill plugins/c4drill/skills/c4drill-toml && diff -r skill plugins/c4drill/opencode/skills/c4drill-toml && echo EXAMPLES-OK</automated>
    Expected: all renders exit 0; both diffs silent; EXAMPLES-OK printed. Additionally: /tmp/p37-ex12-default contains the fixture's custom colour while /tmp/p37-ex12-plain does not (spot-check with grep on a chosen hex).
  </verify>
  <done>
    Both demo fixtures exist (.toml + .c4d twin), render cleanly through the pipeline, all three skill copies in parity (DOC-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/examples → CI validation | Doc claims and fixtures are enforced by CI (diff -r parity; example renders) — the trust boundary is the CI gate itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11 | Tampering | skill copy drift | mitigate | CI runs diff -r on all three copies on pristine checkout — the task requires running the same diffs locally before committing, so drift is caught pre-push and blocked post-push. |
| T-37-12 | Repudiation | documented semantics drift from code | mitigate | The ignored/stays lists are copied verbatim from REQUIREMENTS.md PLAIN-01..04 and verified against implemented behavior in plans 37-03/37-04 (this plan depends on them); example 12 exercises the documented boundary end to end. |
| T-37-SC | Tampering | no installs | mitigate | Documentation-only plan; no package installs, no new tooling. Package legitimacy gate N/A. |
</threat_model>

<verification>
- README.adoc greps: --plain and nesting-context sections present (DOC-01).
- `diff -r skill plugins/c4drill/skills/c4drill-toml` and `diff -r skill plugins/c4drill/opencode/skills/c4drill-toml` → silent (DOC-02).
- New examples render through the pipeline; .c4d twin present; parity holds after mirroring (DOC-03).
- CI workflow validate-skill-examples.yml would pass: parity checks + example renders.
</verification>

<success_criteria>
- DOC-01: README documents nesting-context behavior and --plain with the exact ignored/stays boundary.
- DOC-02: three byte-identical skill copies carrying the same surface.
- DOC-03: example fixtures demonstrate both features and render cleanly end to end.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-06-SUMMARY.md` when done.
</output>
