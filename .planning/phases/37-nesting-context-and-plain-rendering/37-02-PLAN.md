---
phase: 37-nesting-context-and-plain-rendering
plan: 02
type: tdd
wave: 2
depends_on: [37-01]
files_modified:
  - internal/view/scope.go
  - internal/view/scope_test.go
  - internal/view/view.go
  - internal/graph/builder.go
  - internal/graph/builder_test.go
autonomous: true
requirements: [CTX-02]
tags: [view, scope, deep-links, go, tdd]

must_haves:
  truths:
    - "A C1 link whose target is a deeply nested unit adds the target's ancestor chain (depicted ancestor → target) as visible entries and the resolved link points at the TRUE target, not the collapsed top-level ancestor (CTX-02)"
    - "The chain-bearing depicted ancestor renders as a RECURSIVE CLUSTER (new Entry.UnfoldChain flag honored by BOTH buildC1ViewGraph and buildBoundaryViewGraph dispatch guards), so the target exists as a real node inside its container chain — no graphviz implicit nodes"
    - "Chain entries are registered Units + UnitOrder + VisiblePaths (C1 AND C2/C3) so the builder skips them as top-level entries while the ancestor-cluster recursion renders them at their proper depth"
    - "C2/C3 cross-subunit links to deep targets get the same treatment: chain entries under the viewed unit, edge terminates at the true target"
    - "Links to external top-level peers keep today's boundary-node behavior — no chain, no pollution"
    - "New UnitOrder appends are deterministic (model definition order), not link-scan order"
  artifacts:
    - path: "internal/view/scope.go"
      provides: "Deep-link ancestor-chain insertion in resolveBoundaryLink (C1) and addResolvedCrossLink/addResolvedCrossLinkFrom (C2/C3); UnfoldChain marking on the depicted ancestor"
      contains: "resolveBoundaryLink"
    - path: "internal/view/view.go"
      provides: "Entry.UnfoldChain field"
      contains: "UnfoldChain"
    - path: "internal/graph/builder.go"
      provides: "Dispatch guards honor UnfoldChain; buildBoundaryViewGraph skips VisiblePaths entries"
      contains: "UnfoldChain"
    - path: "internal/view/scope_test.go"
      provides: "Deep-link chain tests: chain entries present, true-target resolution, VisiblePaths registration, external-peer unchanged"
      contains: "TestGenerateC1View_DeepLinkTargetKeepsContainerChain"
    - path: "internal/graph/builder_test.go"
      provides: "Graph-level proof: chain-bearing collapsed ancestor renders as recursive cluster containing the true target; edge terminates at it"
      contains: "TestBuildGraph_DeepLinkChainRendersInsideUnfoldedCluster"
  key_links:
    - from: "internal/view/scope.go (resolveBoundaryLink + chain helper)"
      to: "internal/view/view.go (Entry.UnfoldChain)"
      via: "depicted ancestor marked UnfoldChain when a chain is inserted beneath it"
      pattern: "UnfoldChain = true"
    - from: "internal/view/scope.go (chain entries)"
      to: "internal/graph/builder.go (buildC1ViewGraph + buildBoundaryViewGraph)"
      via: "VisiblePaths skip + UnfoldChain cluster dispatch"
      pattern: "UnfoldChain"
    - from: "internal/graph/builder.go (buildCluster recursion, from plan 37-01)"
      to: "internal/view/scope.go (chain entries in v.Units)"
      via: "recursion renders chain entries at proper depth inside the unfolded cluster"
      pattern: "buildNestedCluster\\(childEntry, childPath, v\\)"
---

<objective>
CTX-02: when a link's true target lies under a depicted ancestor but is not itself depicted, the view generator adds the target's ancestor chain (from the depicted ancestor down to the target) as visible entries and resolves the edge to the TRUE target — the deep-link target keeps its container context instead of silently collapsing to an anonymous top-level ancestor.

Purpose: `resolveBoundaryLink` (internal/view/scope.go L308-351) resolves every link peer via `resolveToTopLevel` to the nearest VISIBLE ancestor, so a link to `linuxSystem.sshAuth.sshd` in C1 becomes an edge to `linuxSystem` with no trace of the deeper target. The collapse was a deliberate anti-pollution choice — CTX-02 reverses it for link TARGETS only (sibling/external boundary nodes stay top-level per RESEARCH scoping).

Output: Chain insertion in `resolveBoundaryLink` (C1) and `addResolvedCrossLink`/`addResolvedCrossLinkFrom` (C2/C3), deterministic ordering of new entries, and scope tests (RED first).

Scope guard (RESEARCH, Out of Scope): container chains render only for DEPICTED link targets reached by this mechanism; collapsed (non-depicted) subtrees are NOT restructured wholesale. CTX-01 (the invariant that every depicted element sits inside its chain) is proven in plan 37-05 once this plan and plan 37-01 (recursive clusters that render the chains) have both landed.

Known golden impact (BY DESIGN): C1/C2/C3 output changes for models with deep links (multilevel.toml among them). Committed-golden baseline failures after GREEN are the documented CTX-02 delta — re-baselined in ONE consolidated task in plan 37-05. Do NOT re-baseline goldens here.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md
@internal/view/scope.go
@internal/view/scope_test.go
@internal/view/view.go
@cmd/c4drill/testdata/multilevel.toml

<interfaces>
<!-- Contracts the executor needs. Verified against current source 2026-08-30. -->

From internal/view/scope.go — the C1 fix point (resolveBoundaryLink, CURRENT, abbreviated):
```go
func resolveBoundaryLink(v *View, m *parser.Model, path, resolvedSource string, sourceEntry *Entry, link model.Link) (model.Link, bool) {
    resolved := resolveToTopLevel(v, link.Peer)            // L357: nearest VISIBLE ancestor
    if resolved == "" { return model.Link{}, false }
    if _, exists := v.Units[resolved]; !exists {           // external boundary creation
        v.Units[resolved] = createExternalBoundaryNode(m, resolved, path)
        v.UnitOrder = append(v.UnitOrder, resolved)
    }
    ...
    resolvedLink := model.Link{ Peer: resolved, ... }      // L336-348: COLLAPSED peer
}
```
Caller (L282-290): resolved links append to `sourceEntry.ResolvedLinks` / `sourceEntry.ResolvedLinksFrom`.

The C2/C3 fix points — addResolvedCrossLink / addResolvedCrossLinkFrom (L968-1028):
```go
    resolvedPeer := resolveToViewAncestor(v, link.Peer)    // L862: nearest visible in view
    if resolvedPeer == "" || resolvedPeer == sourcePath { return }
    // D-13: minlen only when BOTH drawn endpoints are the original units
    length := 0
    if originalSource == sourcePath && link.Peer == resolvedPeer { length = link.Length }
    subunitEntry.ResolvedLinks = append(..., model.Link{ Peer: resolvedPeer, ... })
```

Triple registration analog (GenerateC1View visible-subunit population, L156-184):
```go
    v.Units[fullPath] = &Entry{ Unit: subUnit, FullPath: fullPath, IsExpanded: ..., HasSubunits: ..., IsExternal: ... }
    v.UnitOrder = append(v.UnitOrder, fullPath)
    v.VisiblePaths[fullPath] = true
```
Consumer: buildC1ViewGraph skips VisiblePaths entries as top-level nodes (builder.go L90-94).

Entry-construction analog with real unit data: createExternalBoundaryNode (L221-248) wraps a found model unit (via findUnitByPath) vs synthesizing a placeholder. Chain entries ALWAYS have real units — use the found-unit branch; IsBoundary and IsExternal must stay FALSE for in-scope chain entries.

Ordering analog: sortBoundaryNodesByModelOrder (L595-623) sorts only the boundary tail by index in m.UnitOrder — chain entries are internal (not IsBoundary/IsExternal), so out-of-order appends persist. Insert chain entries in deterministic model-definition order or extend the sort to cover internal entries.

From internal/view/view.go — fields used by tests:
```go
    Units map[string]*Entry; UnitOrder []string; VisiblePaths map[string]bool
    // entry.ResolvedLinks / ResolvedLinksFrom []model.Link (view.go L83-88)
```
This plan ADDS `Entry.UnfoldChain bool` (view.go) — set on the DEPICTED ANCESTOR when a deep-link chain is inserted beneath it and the ancestor is not already IsExpanded. Deliberately a separate flag from IsExpanded: it must not disturb author-intent semantics, the D-07 expanded-but-empty guard, or 🔍 logic.

Builder dispatch facts (verified against source 2026-08-30):
- `buildC1ViewGraph` (builder.go L90-107): skips VisiblePaths entries (L92-94), renders `entry.IsExpanded && len(entry.Unit.Subunits) > 0` via buildCluster else plain node. After plan 37-01, buildCluster is RECURSIVE (renders the full container skeleton from v.Units).
- `buildBoundaryViewGraph` (builder.go L50-83): same dispatch for C2/C3 internal entries but has NO VisiblePaths skip — chain entries would double-render as standalone nodes inside the boundary cluster unless this plan adds the skip.
- CRITICAL (checker blocker 2): without the UnfoldChain dispatch, a chain under a COLLAPSED depicted ancestor produces a true-target edge to a node ID that exists nowhere in the graph — graphviz materializes an anonymous implicit top-level node. The dispatch guard change is what makes CTX-02 render at all.
</interfaces>
</context>

<tasks>

<task type="tdd" phase="red">
  <name>Task 1 (RED): Deep-link chain tests for C1/C2 (view layer) and the unfolded-cluster proof (graph layer)</name>
  <files>internal/view/scope_test.go, internal/graph/builder_test.go</files>
  <read_first>internal/view/scope_test.go, internal/view/scope.go, internal/view/view.go, internal/graph/builder_test.go, cmd/c4drill/testdata/multilevel.toml, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - Test 1 — TestGenerateC1View_DeepLinkTargetKeepsContainerChain: minimal model (follow the model-construction pattern of existing scope_test.go C1 boundary tests): top-level `webApp` (container) with `Links: {{Peer: "linuxSystem.sshAuth.sshd"}}`; top-level `linuxSystem` (system, NOT expanded) containing `sshAuth` (container) containing `sshd` (component). Call GenerateC1View. Assert:
      (a) v.Units contains "linuxSystem.sshAuth" and "linuxSystem.sshAuth.sshd";
      (b) both chain entries have VisiblePaths true;
      (c) chain entries have IsBoundary == false and IsExternal == false and carry the real model units (Unit.Name matches);
      (d) webApp's entry ResolvedLinks contains a link whose Peer == "linuxSystem.sshAuth.sshd" (the TRUE target, not "linuxSystem");
      (e) the linuxSystem entry has UnfoldChain == true (it was NOT author-expanded).
    - Test 2 — TestGenerateC1View_ExternalLinkNoChain (locks the boundary): same model but webApp links to top-level `externalSys` (no nesting, not under any depicted ancestor). Assert externalSys is added as today (present in v.Units, IsBoundary/IsExternal per the existing external-boundary behavior) and NO chain entries appear (no new Units keys beyond the external peer itself) and no entry gains UnfoldChain.
    - Test 3 — TestGenerateC2View_DeepCrossLinkTargetChain: system `mainSystem` with subunit `api` linking to `mainSystem.worker.pool.runner` (worker→pool→runner nested under the same system, worker NOT expanded). Call GenerateC2View(m, "mainSystem"). Assert the api subunit entry's ResolvedLinks Peer == "mainSystem.worker.pool.runner" and v.Units contains "mainSystem.worker" and "mainSystem.worker.pool" and "mainSystem.worker.pool.runner" with IsBoundary == false; the worker entry has UnfoldChain == true.
    - Test 4 — TestBuildGraph_DeepLinkTargetRendersInsideUnfoldedCluster (GRAPH layer — this is the assertion that pins checker blocker 2): build the Test 1 model's view, call graph.BuildGraph(v), then walk the returned *graph.Graph and assert: (i) there is NO top-level node with ID "linuxSystem" (it must NOT render as a plain node); (ii) there IS a cluster with ID "linuxSystem" containing a nested cluster "linuxSystem.sshAuth" which contains node "linuxSystem.sshAuth.sshd"; (iii) some edge in g.Edges has Target (or Source, per orientation) == "linuxSystem.sshAuth.sshd" — the true-target endpoint EXISTS in the graph, so graphviz cannot materialize an implicit node. Use the same graph-building helpers TestBuildGraph_ExpandedClusterRendersNestedSubClusters (plan 37-01) uses.
    - Use require/assert already imported in scope_test.go; no new imports; literal paths exactly as above.
  </behavior>
  <action>
    Append Tests 1-3 to internal/view/scope_test.go in the corresponding GenerateC1View / GenerateC2View test groups, and Test 4 to internal/graph/builder_test.go next to the plan 37-01 nested-cluster test. Do NOT modify production code. Tests MUST fail against current code: today (a)-(e) fail because resolveBoundaryLink collapses Peer to "linuxSystem" and never adds chain entries; Test 3 fails because addResolvedCrossLink collapses Peer to "mainSystem.worker" (deepest visible); Test 4 fails because linuxSystem renders as a plain top-level node and the true-target edge dangles. Test 2 documents existing behavior and SHOULD pass already — if it fails, the model setup is wrong, fix the test not the code.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/view/ -run 'TestGenerateC1View_DeepLinkTargetKeepsContainerChain|TestGenerateC2View_DeepCrossLinkTargetChain' -v && go test ./internal/graph/ -run 'TestBuildGraph_DeepLinkTargetRendersInsideUnfoldedCluster' -v</automated>
    Expected: the chain tests and Test 4 FAIL (collapse behavior). TestGenerateC1View_ExternalLinkNoChain PASSES (behavior lock).
  </verify>
  <done>
    Chain tests compile and FAIL against current code for the collapse reason (view layer AND graph layer); the external-behavior lock passes. No production code changed.
  </done>
</task>

<task type="tdd" phase="green">
  <name>Task 2 (GREEN): Insert ancestor-chain entries, mark the ancestor UnfoldChain, and teach the builder to unfold it (CTX-02)</name>
  <files>internal/view/scope.go, internal/view/view.go, internal/graph/builder.go</files>
  <read_first>internal/view/scope.go, internal/view/scope_test.go, internal/view/view.go, internal/graph/builder.go, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - The four Task 1 tests: chain tests now PASS including the graph-level unfolded-cluster proof; external lock still PASSES.
    - Existing scope/view/graph tests stay green EXCEPT the documented committed-golden baselines from plan 37-01 (TestBuildExpandedGraphBaselineDOT + REF-05 alias) — view-level tests make no golden assertions, so the whole internal/view package must pass.
    - Deep-link targets that are ALREADY depicted (visible subunits) produce no duplicate entries and no behavior change; an ancestor that is already IsExpanded gains no UnfoldChain flag (it already renders as a cluster).
  </behavior>
  <action>
    In internal/view/view.go:
    1. Add field `UnfoldChain bool` to Entry (documented: set by the view generator when a deep-link ancestor chain was inserted beneath this depicted ancestor; the graph layer renders such entries as recursive clusters exactly like IsExpanded ones).
    In internal/view/scope.go:
    2. Add a helper (name it explicitly, e.g. ensureDeepLinkChain(v *View, m *parser.Model, resolved, peer string) bool) that returns true when peer is a real model path strictly under resolved: peer has prefix resolved+".", and findUnitByPath(m, peer) != nil. When true: (a) walk every path prefix of peer strictly below resolved (including peer itself); for each path not already in v.Units, create an Entry carrying the REAL unit from findUnitByPath with FullPath set, HasSubunits from the unit's Subunits, IsExternal from IsExternalType, and IsBoundary FALSE — register it in Units + UnitOrder + VisiblePaths (VisiblePaths is the top-level skip contract for BOTH C1 and C2/C3 builders); (b) set the resolved ancestor entry's UnfoldChain = true when it is not already IsExpanded.
    3. In resolveBoundaryLink: after `resolved := resolveToTopLevel(v, link.Peer)` and the existing external-boundary creation, call the helper with resolved and link.Peer; when it returned true, set resolvedLink.Peer = link.Peer (the TRUE target). When false (external or already-depicted peer), keep today's collapsed resolution exactly.
    4. In addResolvedCrossLink and addResolvedCrossLinkFrom: after resolvedPeer := resolveToViewAncestor(v, link.Peer) and the early returns, call the same helper (same registration shape); when true, set the appended link's Peer to link.Peer. Keep the D-13 minlen rule untouched — its `link.Peer == resolvedPeer` comparison now also covers the case where the true target is depicted (minlen survives), which is correct.
    5. Deterministic ordering: guarantee chain entries land in model definition order, not link-scan order. Either insert each chain path at the position adjacent to its depicted ancestor in v.UnitOrder, or extend sortBoundaryNodesByModelOrder (L595-623) so internal (non-boundary/non-external) entries are also sorted by their index in m.UnitOrder. Pick ONE approach and apply it to both C1 and C2/C3 insertion paths.
    6. Do NOT set IsBoundary or IsExternal on chain entries; do NOT touch resolveToTopLevel/resolveToViewAncestor themselves; do NOT add chain entries for sibling/external boundary nodes.
    In internal/graph/builder.go:
    7. buildC1ViewGraph (L97-105) and buildBoundaryViewGraph (L69-79): extend the cluster-dispatch guard from `entry.IsExpanded && len(entry.Unit.Subunits) > 0` to `(entry.IsExpanded || entry.UnfoldChain) && len(entry.Unit.Subunits) > 0` — plan 37-01's recursive buildCluster then renders the full container skeleton, so the true target exists as a node inside its chain and the edge lands on a real node.
    8. buildBoundaryViewGraph (L50-83): add the VisiblePaths skip at the top of the UnitOrder loop (mirror buildC1ViewGraph L92-94) so C2/C3 chain entries do not double-render as standalone nodes inside the boundary cluster — the ancestor-cluster recursion renders them.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/view/ -count=1 && go test ./internal/graph/ -run 'TestBuildGraph_DeepLinkTargetRendersInsideUnfoldedCluster|TestBuildGraph_ExpandedClusterRendersNestedSubClusters' -v</automated>
    Expected: full internal/view package PASSES; both graph-layer tests PASS.
  </verify>
  <done>
    Deep-link targets resolve to the true target with registered ancestor chains in C1 and C2/C3; chain-bearing collapsed ancestors unfold as recursive clusters so the target renders inside its chain (no implicit nodes); external/sibling boundary behavior unchanged; internal/view package fully green.
  </done>
</task>

<task type="auto">
  <name>Task 3 (VERIFY): Cross-package suite + multilevel fixture renders without duplicates</name>
  <files>(no source edits — verification only)</files>
  <read_first>internal/view/scope.go, cmd/c4drill/testdata/multilevel.toml</read_first>
  <action>
    Run go test ./internal/render/ and go build ./... to confirm no cross-package regression (internal/graph may still show the documented CTX-03 golden-baseline failures from plan 37-01 — those are expected and NOT caused by this plan; any NEW failure is a regression, investigate). Then run the CLI on the multilevel fixture to /tmp: go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml -f dot -o /tmp/c4drill-p37-02-verify. Inspect the C1 dot with STRUCTURAL assertions, not grep counts: (i) every deep-link target identifier (e.g. the multilevel fixture's deep unit path) appears BOTH as a node statement AND as an edge endpoint; (ii) the chain-bearing ancestor appears as `subgraph cluster_<ancestor>` (unfolded), not as a bare top-level node; (iii) no unit appears twice as both a top-level node and a chain entry (the VisiblePaths contract). Record observations in the summary for plan 37-05's re-baseline. Do not modify any testdata files.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/render/ && go build ./... && go run ./cmd/c4drill cmd/c4drill/testdata/multilevel.toml -f dot -o /tmp/c4drill-p37-02-verify && grep -c 'subgraph cluster_' /tmp/c4drill-p37-02-verify/multilevel.dot</automated>
    Expected: render package PASS; build OK; CLI runs clean; subgraph cluster_ count >= 1 (unfolded chains/clusters present in C1 — cross-check the specific deep-link chain identifiers from the fixture in the file contents).
  </verify>
  <done>
    No cross-package regressions; multilevel C1 dot shows deep-link targets inside their chains without top-level duplicates; observations recorded for 37-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML/C4D author link peers → resolveBoundaryLink / resolveToViewAncestor | Untrusted peer paths drive the new chain-walk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Denial of Service | chain-walk over peer path prefixes | mitigate | The walk iterates the dot-separated prefixes of a single validated link peer — length bounded by model nesting depth, which the validator already accepts. Guard: the helper only fires when findUnitByPath resolves the peer (real model path), so crafted junk paths fall through to the existing external-boundary path. |
| T-37-04 | Information Disclosure | chain entry labels | accept | Chain entries carry real model unit Names already present in the author's TOML — the requirement explicitly asks for them to be visible. |
| T-37-SC | Tampering | go module / toolchain | mitigate | No new third-party packages (stdlib-only changes to existing functions). Package legitimacy gate N/A. |
</threat_model>

<verification>
- `go test ./internal/view/ -count=1` → PASS (chain tests green, external lock green, package green).
- `go test ./internal/render/` → PASS; `go build ./...` → OK.
- CLI run on multilevel.toml succeeds; C1 dot shows deep-link chains without duplicated top-level nodes.
- No testdata/golden files modified (re-baseline deferred to 37-05).
</verification>

<success_criteria>
- C1 and C2/C3 links to deeply nested units keep the target's container chain and terminate at the TRUE target (CTX-02).
- Chain-bearing collapsed ancestors unfold as recursive clusters (Entry.UnfoldChain dispatch in BOTH builders) — the target renders as a real node inside its chain; no graphviz implicit nodes (proven at graph layer).
- External/sibling boundary resolution is behavior-identical (locked by test).
- Chain entries registered via the triple-registration contract (Units + UnitOrder + VisiblePaths, both view kinds) so they render nested via the ancestor-cluster recursion, never as duplicate top-level nodes.
- New entries ordered deterministically by model definition order.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-02-SUMMARY.md` when done.
</output>
