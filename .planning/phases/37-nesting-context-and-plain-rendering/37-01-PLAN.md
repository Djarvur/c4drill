---
phase: 37-nesting-context-and-plain-rendering
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - internal/graph/graph.go
  - internal/render/converter.go
  - internal/render/converter_test.go
autonomous: true
requirements: [CTX-03]
tags: [graph, clusters, nesting, go, tdd]

must_haves:
  truths:
    - "A child unit that itself has subunits renders as a nested sub-cluster inside an expanded cluster (CTX-03) — not as a flat collapsed box"
    - "Grandchildren render recursively: a container skeleton inside an expanded cluster unfolds fully, leaf units render as nodes"
    - "Collapsed containers keep the 🔍 affordance: on leaf nodes via buildNode (existing), and on nested container clusters via buildClusterLabel (entry HasSubunits && !entry.IsExpanded)"
    - "Expanded-but-empty units still render as plain nodes (D-07 guard preserved at both caller sites)"
    - "Unfolded container clusters carry the drill affordance: Cluster gains ExploreURL, BuildGraphWithPath assigns it via a RECURSIVE walk into cluster.Clusters, and setClusterLabel emits it via subgraph.SafeSet(\"URL\", ...) — the same mechanism nodes use (cn.SetURL)"
  artifacts:
    - path: "internal/graph/builder.go"
      provides: "Recursive buildCluster: children with subunits dispatch to nested clusters, leaves to nodes; buildClusterLabel 🔍 guard; recursive explore-URL walk"
      contains: "buildNestedCluster"
    - path: "internal/graph/graph.go"
      provides: "Cluster.ExploreURL field"
      contains: "ExploreURL"
    - path: "internal/render/converter.go"
      provides: "Cluster label URL emission in setClusterLabel"
      contains: "SafeSet"
    - path: "internal/graph/builder_test.go"
      provides: "Nested-cluster structure + affordance test for expanded units with subunit-containers"
      contains: "TestBuildGraph_ExpandedClusterRendersNestedSubClusters"
  key_links:
    - from: "internal/graph/builder.go (buildCluster child loop)"
      to: "internal/graph/builder.go (buildNestedCluster)"
      via: "child dispatch on HasSubunits"
      pattern: "buildNestedCluster\\(childEntry, childPath, v\\)"
    - from: "internal/graph/builder.go (buildC1ViewGraph, buildBoundaryViewGraph)"
      to: "internal/graph/builder.go (buildCluster)"
      via: "both callers pass the view so recursion can resolve entries"
      pattern: "buildCluster\\(entry, v\\)"
    - from: "internal/graph/builder.go (BuildGraphWithPath recursive walk)"
      to: "internal/render/converter.go (setClusterLabel)"
      via: "Cluster.ExploreURL assigned in graph, emitted as subgraph URL attribute in render"
      pattern: "SafeSet\\(\"URL\""
---

<objective>
CTX-03: make `buildCluster` recursive so expanded units render depicted nested elements through their intermediate containers (nested clusters, not flat lists), so the nesting picture on a non-expanded scheme matches the drill-down views.

Purpose: today `buildCluster` (internal/graph/builder.go L610-658) computes `IsExpanded`/`HasSubunits` for each child and then IGNORES both, appending every child as a flat node — a nested container inside an expanded cluster renders as a flat collapsed box and grandchildren never appear. The recursion already exists one function over (`buildNestedCluster`, L212-272); this plan copies its child-dispatch into `buildCluster`.

Output: Recursive `buildCluster`, a new structure test in builder_test.go (RED first), and the D-07 expanded-but-empty guard + 🔍 affordance preserved.

Known golden impact (BY DESIGN): this change alters output for any model with expanded clusters containing subunit-containers. The committed cmd goldens (`cmd/c4drill/testdata/multilevel.expanded.dot` and friends) will fail their baseline tests after GREEN. Those failures are the documented CTX-03 delta and are re-baselined in ONE consolidated task in plan 37-05 (RESEARCH Pitfall 1) — do NOT re-baseline goldens in this plan.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md
@internal/graph/builder.go
@internal/graph/builder_test.go
@internal/view/view.go

<interfaces>
<!-- Contracts the executor needs. Verified against current source 2026-08-30. -->

From internal/graph/builder.go — the flattening fix point (buildCluster, CURRENT):
```go
func buildCluster(entry *view.Entry) *Cluster {          // L610 — no view param
    ...
    applyUnitOverrides(style, entry.Unit)                 // L619
    for _, childName := range childOrder {                // L641-655: computes
        childEntry := &view.Entry{                        //   IsExpanded + HasSubunits
            ..., IsExpanded: isUnitExpanded(entry.Unit, childName),
            HasSubunits:  len(childUnit.Subunits) > 0, ...
        }
        node := buildNode(childEntry)                     // then IGNORES both —
        node.IsInCluster = true                           // flat node append
        cluster.Nodes = append(cluster.Nodes, node)
    }
}
```

The recursive analog to copy — buildNestedCluster child loop (L244-268):
```go
    childEntry, exists := v.Units[childPath]              // entry lookup w/ defensive fallback
    if !exists { childEntry = &view.Entry{...} }
    if childEntry.HasSubunits {
        nestedCluster := buildNestedCluster(childEntry, childPath, v)   // recurse
        cluster.Clusters = append(cluster.Clusters, nestedCluster)
    } else {
        node := buildNode(childEntry); node.IsInCluster = true
        cluster.Nodes = append(cluster.Nodes, node)
    }
```
Note: `buildNestedCluster` initializes `Clusters: make([]*Cluster, 0)` (L223-231); `buildCluster` currently does NOT — recursion requires it. Child order: SubunitOrder with map-key fallback (L233-242).

Caller sites of buildCluster (both inside buildC1ViewGraph/buildBoundaryViewGraph where `v *view.View` is in scope):
```go
cluster := buildCluster(entry)   // L73 and L100
```

Guards to preserve:
- D-07 expanded-but-empty: callers L72-79 and L97-105 keep `if entry.IsExpanded && len(entry.Unit.Subunits) > 0` before dispatching to buildCluster.
- 🔍 affordance: buildNode L284-286 `if entry.HasSubunits && !entry.IsExpanded { label.Name += " 🔍" }` — collapsed containers keep it.

From internal/graph/graph.go — Cluster shape (assertion target; L114-129):
```go
type Cluster struct {
    ID string; Label *Label; Nodes []*Node; Clusters []*Cluster; Style *NodeStyle; ... }
// NO ExploreURL today — this plan adds: ExploreURL string (the drill affordance carrier)
```

Drill-affordance facts (verified against source 2026-08-30 — the mechanism this plan CREATES):
- `Node.ExploreURL` exists (graph.go L69-70) and is emitted via `cn.SetURL(node.ExploreURL)` (converter.go L374-375). Assignment happens ONLY in BuildGraphWithPath (builder.go L1165-1180), which walks `g.Nodes` and FIRST-LEVEL `cluster.Nodes` — it never descends into `cluster.Clusters`.
- `setClusterLabel` (converter.go L475-486) emits only the HTML label (`subgraph.SetLabelHTML`) — no URL.
- `buildClusterLabel` (builder.go L661-678) adds only the 📖 glyph — no 🔍.
- `createCluster` (converter.go L434-472) ALREADY recurses into cluster.Clusters when rendering — nested clusters render once the graph layer produces them.
- Subgraphs are `*cgraph.Graph` and accept attributes via SafeSet (converter.go L491: `subgraph.SafeSet("style", "rounded", "")`) — GraphViz supports the `URL` attribute on clusters, so `subgraph.SafeSet("URL", cluster.ExploreURL, "")` is the cluster-side analog of the node's SetURL.
</interfaces>
</context>

<tasks>

<task type="tdd" phase="red">
  <name>Task 1 (RED): Nested-sub-cluster structure test that fails against the current flat buildCluster</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>internal/graph/builder_test.go, internal/graph/builder.go, internal/view/view.go, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - Test name: TestBuildGraph_ExpandedClusterRendersNestedSubClusters (place near TestBuildExpandedGraphBaselineDOT).
    - Model: a top-level system `mainSystem` (type system) marked expanded (or via view with the unit expanded using the same mechanism existing expanded-graph tests use — follow the setup pattern of the nearest existing expanded-cluster unit test in builder_test.go), containing child container `auth` (type container) which itself has subunits `authApi` (type component) and `authDb` (type component), plus a direct leaf child `auditLog` (type component).
    - Generate the view (GenerateC1View or the expanded view the analog test uses), call graph.BuildGraph (or BuildExpandedGraph — match the analog), then walk the returned `*graph.Graph`.
    - Assert: the cluster for `mainSystem` contains a NESTED cluster whose ID is the path of `auth` (g.Clusters[...].Clusters non-empty; walk by ID).
    - Assert: the nested `auth` cluster contains nodes for `authApi` and `authDb` (leaf grandchildren render inside the nested cluster).
    - Assert: `auditLog` renders as a direct node of the `mainSystem` cluster (leaf dispatch unchanged).
    - Assert: the `auth` nested cluster node set does NOT contain a flat node for `auth` itself (the old behavior — this is the assertion that fails RED).
    - Assert (drill affordance): the `auth` nested cluster's Label.Name contains "🔍" (auth is NOT author-expanded) while the `mainSystem` cluster label does NOT (mainSystem IS expanded) — buildClusterLabel must grow the same HasSubunits && !IsExpanded guard buildNode has.
    - Assert (drill affordance): after BuildGraphWithPath(v, "", basename, "svg"), the `auth` nested Cluster has a non-empty ExploreURL equal to ComputeExploreURL("", "mainSystem.auth", basename, "svg") — the recursive walk must reach nested clusters.
  </behavior>
  <action>
    Append the test described in the behavior block to internal/graph/builder_test.go, following the model-construction and graph-building pattern of the nearest existing expanded-cluster test in that file (reuse existing helpers/constants; import only what the file already imports). Do NOT modify any production code in this task. The test MUST fail against current buildCluster because `auth` today renders as a flat node inside the mainSystem cluster (no nested cluster exists, and a flat node for auth IS present); the affordance assertions fail for the same structural reason.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestBuildGraph_ExpandedClusterRendersNestedSubClusters' -v</automated>
    Expected: FAIL (compile OK). Failure must show the missing nested cluster / present flat node. If it PASSES, the test does not capture the flattening — revise it.
  </verify>
  <done>
    Test compiles and FAILS against current code with assertions tied to nested-cluster-vs-flat-node structure. No production code changed.
  </done>
</task>

<task type="tdd" phase="green">
  <name>Task 2 (GREEN): Make buildCluster recursive AND implement the cluster drill affordance (CTX-03)</name>
  <files>internal/graph/builder.go, internal/graph/graph.go, internal/render/converter.go, internal/render/converter_test.go, internal/graph/builder_test.go</files>
  <read_first>internal/graph/builder.go, internal/graph/builder_test.go, internal/graph/graph.go, internal/render/converter.go, internal/render/converter_test.go, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - The RED test from Task 1 PASSES: subunit-containers inside expanded clusters render as nested clusters containing their children recursively.
    - Drill affordance on nested container clusters: nested cluster labels carry 🔍 for non-expanded containers, and Cluster.ExploreURL is assigned by a recursive BuildGraphWithPath walk and emitted as the subgraph URL attribute in the DOT output.
    - D-07 guard preserved: an expanded unit with zero subunits still renders as a plain node (existing expanded-but-empty tests stay green).
    - Collapsed leaf nodes keep the 🔍 marker (buildNode branch unchanged); child ordering stays deterministic: SubunitOrder honored, map-key fallback for test models.
    - Existing view/graph behavior for leaf-only expanded clusters is unchanged (their tests stay green).
  </behavior>
  <action>
    In internal/graph/builder.go:
    1. Change buildCluster signature to accept the view: buildCluster(entry *view.Entry, v *view.View). Update both call sites (builder.go L73 and L100, both have v in scope) to pass v.
    2. Initialize cluster.Clusters = make([]*Cluster, 0) alongside the existing Nodes init (mirror buildNestedCluster L223-231).
    3. Replace the flat child loop (L641-655) with the buildNestedCluster dispatch pattern (L233-268): compute childOrder from entry.Unit.SubunitOrder with map-key fallback; for each child build childPath := entry.FullPath + "." + childName; look up childEntry in v.Units with the defensive inline-construction fallback (preserving IsExpanded: isUnitExpanded(entry.Unit, childName), HasSubunits, IsExternal from the current code); if childEntry.HasSubunits recurse into buildNestedCluster(childEntry, childPath, v) and append to cluster.Clusters; else build a node (IsInCluster = true) and append to cluster.Nodes.
    4. IMPLEMENT the cluster drill affordance (no existing mechanism to reuse — buildNestedCluster applies none; this plan creates it):
       a. internal/graph/graph.go: add field `ExploreURL string` to the Cluster struct (next to the existing navigation-related Node field ExploreURL at graph.go L69-70).
       b. internal/graph/builder.go buildClusterLabel (L661-678): after the 📖 glyph block, add the same affordance guard buildNode uses (L284-286): if entry.HasSubunits && !entry.IsExpanded { label.Name += " 🔍" }. The top-level expanded unit's own cluster stays 🔍-free (its entry IsExpanded=true).
       c. internal/graph/builder.go BuildGraphWithPath (L1165-1180): replace the two one-level loops with a recursive assignment — extract a helper assignExploreURLs(nodes []*Node, v *view.View, currentPath, basename, format string) wrapping the existing shouldHaveExploreLink+ComputeExploreURL body, call it for g.Nodes, then recurse over clusters: assign cluster.ExploreURL when the entry at v.Units[cluster.ID] satisfies the same shouldHaveExploreLink predicate (HasSubunits && !IsExpanded, container-capable types), then walk cluster.Nodes with the helper and recurse into cluster.Clusters.
    5. internal/render/converter.go setClusterLabel (L475-486): after subgraph.SetLabelHTML(htmlLabel), add: if cluster.ExploreURL != "" { subgraph.SafeSet("URL", cluster.ExploreURL, "") } — mirroring the node-side cn.SetURL (converter.go L374-375).
    6. Do NOT touch the D-07 guards at caller sites L72-79 / L97-105.
    In internal/render/converter_test.go: add/extend a unit test asserting the DOT output for a nested-container cluster contains the URL attribute (grep-level assertion on RenderDOT output, following the existing converter test style).
    If an existing internal/graph unit test asserts the OLD flat-child behavior for subunit-container children, update that assertion to the recursive structure — do not weaken the new test.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'TestBuildGraph_ExpandedClusterRendersNestedSubClusters' -v && go build ./...</automated>
    Expected: new test PASSES; build clean. Package-wide `go test ./internal/graph/` passes EXCEPT the committed-baseline tests that read cmd/c4drill/testdata/multilevel.expanded.dot (TestBuildExpandedGraphBaselineDOT and the REF-05 baseline test) — those failures are the documented CTX-03 golden delta, re-baselined in plan 37-05. Run the package suite and confirm the ONLY failures are golden-baseline tests; any other failure means a regression — fix it.
  </verify>
  <done>
    buildCluster recurses: container children render as nested clusters, leaves as nodes; D-07 and 🔍 preserved; RED test GREEN; the only failing tests in the repo are the documented committed-golden baselines deferred to plan 37-05.
  </done>
</task>

<task type="auto">
  <name>Task 3 (VERIFY): Package suite green except documented golden baselines</name>
  <files>(no source edits — verification only)</files>
  <read_first>internal/graph/builder_test.go, internal/graph/builder.go</read_first>
  <action>
    Run go test ./internal/graph/ and go test ./internal/view/ ./internal/render/ and go build ./.... Categorize every failure: (a) the two committed-baseline tests reading cmd/c4drill/testdata/multilevel.expanded.dot — expected, deferred to 37-05; (b) anything else — a regression introduced by the recursion; fix before finishing. Record the exact list of expected-failing test names in the plan summary so plan 37-05 and the phase verifier can distinguish documented deltas from regressions. Do not modify any file under cmd/c4drill/testdata/.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/view/ ./internal/render/ && go build ./... && go test ./internal/graph/ 2>&1 | tail -20</automated>
    Expected: view+render packages PASS; build OK; graph package failures limited to golden-baseline tests.
  </verify>
  <done>
    Non-golden tests all green across internal/graph, internal/view, internal/render; expected-failure list recorded in the summary; no testdata files touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML/C4D model → view entries → graph clusters | Author-defined nesting depth now flows into recursive cluster building. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Denial of Service | buildCluster recursion | mitigate | Recursion depth is bounded by validated model nesting depth (validator gatekeeper); Go stack handles realistic depths. Recursion follows existing buildNestedCluster pattern (already recursive in production since v1.8). No unbounded recursion introduced. |
| T-37-02 | Information Disclosure | nested cluster labels | accept | Labels render unit Names/Technology already present in the author's model — no new data surfaces. |
| T-37-SC | Tampering | go module / toolchain | mitigate | No new third-party packages (pure refactor of existing stdlib-only code). Package legitimacy gate N/A — no npm/pip/cargo installs. |
</threat_model>

<verification>
- `go test ./internal/graph/ -run 'TestBuildGraph_ExpandedClusterRendersNestedSubClusters' -v` → PASS (CTX-03 structure + affordances).
- `go test ./internal/render/` → PASS (cluster URL emission test).
- `go test ./internal/view/` → PASS (no cross-package regression).
- `go build ./...` → OK.
- internal/graph package: only golden-baseline tests fail (documented CTX-03 delta, re-baselined in 37-05).
- D-07 expanded-but-empty tests + node-side 🔍 affordance tests unchanged and green.
</verification>

<success_criteria>
- Expanded units render subunit-containers as nested clusters with grandchildren inside (CTX-03), matching how drill-down views nest clusters.
- Nested container clusters carry the drill affordance: 🔍 on their label and ExploreURL assigned via the recursive walk, emitted as the subgraph URL attribute.
- Leaf dispatch, child ordering, and D-07 guard preserved.
- New structure + affordance test exists and passes; expected golden-delta failure list documented for 37-05.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-01-SUMMARY.md` when done.
</output>
