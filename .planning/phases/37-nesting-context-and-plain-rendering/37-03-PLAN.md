---
phase: 37-nesting-context-and-plain-rendering
plan: 03
type: tdd
wave: 2
depends_on: [37-01]
files_modified:
  - internal/view/view.go
  - internal/graph/graph.go
  - internal/graph/builder.go
  - internal/graph/builder_test.go
  - cmd/c4drill/root.go
autonomous: true
requirements: [PLAIN-01, PLAIN-02]
tags: [plain, cli, graph, go, tdd]

must_haves:
  truths:
    - "`c4drill --plain` is a registered boolean cobra flag that sets View.Plain on every generated view (processView AND processExpandedView) (PLAIN-01)"
    - "With Plain set, unit color/style/border are skipped — units fall back to type-palette defaults, including expanded-unit clusters (PLAIN-01)"
    - "With Plain set, link color/style fall back to defaults, length/rank are ignored (no minlen, no endpoint swap, no constraint suppression), and properties.edges is ignored (EdgeStyle cleared) (PLAIN-02)"
    - "Kind-derived edge colours and the legend are NOT suppressed — they derive from semantic kind (PLAIN-02 boundary)"
    - "Without the flag (default false), builder output is byte-identical to today — no default-path behavior change"
  artifacts:
    - path: "internal/view/view.go"
      provides: "View.Plain bool field"
      contains: "Plain"
    - path: "internal/graph/graph.go"
      provides: "Graph.Plain bool field"
      contains: "Plain"
    - path: "internal/graph/builder.go"
      provides: "Plain guards: applyUnitOverrides skip, createEdge neutralization, EdgeStyle clear, LabelPosition ignore; Plain copied in BuildGraph AND BuildExpandedGraph"
      contains: "applyUnitOverrides"
    - path: "cmd/c4drill/root.go"
      provides: "--plain PersistentFlag + v.Plain assignment in processView and processExpandedView"
      contains: "plain"
    - path: "internal/graph/builder_test.go"
      provides: "Plain-guard unit tests (overrides skipped, edge attrs defaulted, both entry points copy Plain)"
      contains: "Plain"
  key_links:
    - from: "cmd/c4drill/root.go (--plain flag)"
      to: "internal/view/view.go (View.Plain)"
      via: "v.Plain = plain in processView (L295-344) and processExpandedView (L346-383)"
      pattern: "v\\.Plain = plain"
    - from: "internal/view/view.go (View.Plain)"
      to: "internal/graph/graph.go (Graph.Plain)"
      via: "copied at graph construction in BOTH BuildGraph and BuildExpandedGraph"
      pattern: "Plain:\\s*v\\.Plain"
    - from: "internal/graph/graph.go (Graph.Plain)"
      to: "internal/graph/builder.go (guards)"
      via: "applyUnitOverrides / createEdge branch on plain"
      pattern: "if plain|if v\\.Plain|if g\\.Plain"
---

<objective>
PLAIN-01 + PLAIN-02: thread a `--plain` CLI key from cmd/c4drill through View into Graph and suppress author-custom formatting at the builder level — unit color/style/border fall back to type-palette defaults, link color/style fall back to defaults, length/rank are ignored, properties.edges is ignored — while kind-derived edge colours and the legend stay.

Purpose: canonical default-styled output (diffing structure, consistent publishing). Per RESEARCH's recommended design this is EXPLICIT FLAG THREADING (View.Plain → Graph.Plain → guards), NOT a model sanitizer and NOT a package global (the LabelRatio counter-pattern is explicitly rejected).

Output: `--plain` flag registered and set on every generated view; builder-level suppression guards; unit tests RED-first. Render-side label simplification (PLAIN-03) and the E2E goldens (PLAIN-04) land in plan 37-04; the LabelPosition-ignore guard lands HERE (builder-owned, one line in createEdge) so plan 37-04 stays render-only.

Scope guard (REQUIREMENTS.md Out of Scope): NO granular per-aspect flags, NO properties-level ignore keys, kind colours and legend always survive plain mode.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md
@internal/view/view.go
@internal/graph/graph.go
@internal/graph/builder.go
@internal/graph/builder_test.go
@cmd/c4drill/root.go

<interfaces>
<!-- Contracts the executor needs. Verified against current source 2026-08-30. -->

Threading analog — View.AllExpanded (view.go L41-44) copied into both graph entry points (builder.go L22-29 BuildGraph, L168-175 BuildExpandedGraph):
```go
    g := &Graph{ Title: v.Title, Direction: "TB", EdgeStyle: v.Edges, ... }
```
Guard-shape analog (builder.go L736-738): `if v.AllExpanded { return pairCounts }`.

Suppression points in builder.go (verified lines):
- applyUnitOverrides(style *NodeStyle, unit *model.Unit) — L320-339; call sites: buildBoundaryCluster L127, buildNestedCluster L221, buildNode L302, buildCluster L619 (buildCluster/buildNode currently lack a view param — the plain flag must be threaded as a parameter).
- createEdge(source, target string, link model.Link, sourceEntry *view.Entry, penWidth float64) — L1097; call sites L986, L1033 (both have v in scope). The fields to neutralize under plain:
```go
    if link.Color != "" { edge.Color = link.Color } else if colour := kindColour(link.Kind); ... // kind colours STAY
    edge.MinLen = link.Length                       // plain: leave 0
    edge.NoConstraint = link.Rank == model.RankEqual    // plain: leave false
    edge.RankReverse = link.Rank == model.RankReverse   // plain: leave false
```
- LabelPosition default: createEdge L1116-1118 already defaults empty to "middle" — under plain force the position to "" so the default applies (ignore link.LabelPosition).
- EdgeStyle: `EdgeStyle: v.Edges` at both graph constructions — plain must set "" (configureGraphSettings switch in converter.go L236-244 then falls through to default splines).

Cobra flag analog (root.go L49-56 var block with //nolint:gochecknoglobals; L94-95):
```go
    cmd.PersistentFlags().BoolVar(&expanded, "expanded", false, "Generate all-expanded diagram showing all units")
```
Dispatch (L188-191): `if expanded { return processExpandedView(...) }`; per-view processors: processView L295-344 (GenerateC1View/C2View/C3View at L299-306, BuildGraphWithPath at L313), processExpandedView L346-383 (GenerateExpandedView at L350).

model.Link fields (internal/model/link.go): Color, Style, Length, Rank (RankForward/RankReverse/RankEqual), LabelPosition (LabelMiddle/LabelTail/LabelHead), Kind.
</interfaces>
</context>

<tasks>

<task type="tdd" phase="red">
  <name>Task 1 (RED): Plain-guard tests that fail because View/Graph lack the Plain field and guards</name>
  <files>internal/graph/builder_test.go</files>
  <read_first>internal/graph/builder_test.go, internal/graph/builder.go, internal/model/link.go, internal/view/view.go</read_first>
  <behavior>
    - Test 1 — TestBuildGraph_PlainSkipsUnitOverrides: build a model whose unit carries explicit Color, Style, Border (use the exact field names on model.Unit as used by existing styling tests in builder_test.go). Generate the view, set v.Plain = true (this ALONE must fail to compile RED — the field does not exist yet; after Task 2 adds it, the assertion is: the built graph's node/cluster styles equal the type-palette defaults, i.e. applyUnitOverrides produced no effect — assert the node style fields match the default style the type would receive with no overrides, using the same assertions the existing override tests use).
    - Test 2 — TestBuildGraph_PlainNeutralizesEdgeFormatting: two linked units where the link carries Color, Style, Length: 3, Rank: model.RankReverse, LabelPosition: model.LabelTail, and the view sets properties edges style (View.Edges = "straight"). With v.Plain = true assert: edge.Color equals the kind-derived default path result (kindColour applied, custom color NOT present); edge style is the default (custom style absent); edge.MinLen == 0; edge.RankReverse == false; edge.NoConstraint == false; edge label position resolves to middle (builder default, not tail); g.EdgeStyle == "".
    - Test 3 — TestBuildGraph_PlainKeepsKindColourAndLegend: links WITHOUT custom color but with Kind set (pick a kind whose kindColour is non-empty — see builder.go L1077-1088). With v.Plain = true assert: edge.Color == the kind colour (STAYS), and g.Legend != nil (legend survives).
    - Test 4 — TestBuildGraph_PlainCopiedFromView / expanded variant: with v.Plain = true, BuildGraph(...).Plain == true AND BuildExpandedGraph(...).Plain == true (Pitfall 5: --plain x --expanded).
    - Test 5 — TestBuildGraph_DefaultPathUnchanged: v.Plain == false (zero value) reproduces today's behavior — custom unit overrides applied and custom link color/minlen/rank present. (Locks BC-01 at builder level.)
  </behavior>
  <action>
    Append the five tests to internal/graph/builder_test.go following the existing model-construction patterns in that file (reuse helpers; no new imports beyond what exists). The tests reference v.Plain and g.Plain which do not exist yet — the package must FAIL TO COMPILE or fail assertions at this stage (acceptable RED for a field-addition feature). Do NOT modify production code.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'Plain' -v 2>&1 | tail -15</automated>
    Expected: FAIL — compile error on missing Plain fields (or assertion failures). If it compiles and passes, the tests do not capture the feature — revise.
  </verify>
  <done>
    Plain-guard tests exist and fail for the right reason (missing field/guards). No production code changed.
  </done>
</task>

<task type="tdd" phase="green">
  <name>Task 2 (GREEN): Thread Plain through View/Graph and add builder guards (PLAIN-01, PLAIN-02)</name>
  <files>internal/view/view.go, internal/graph/graph.go, internal/graph/builder.go, cmd/c4drill/root.go, internal/graph/builder_test.go</files>
  <read_first>internal/view/view.go, internal/graph/graph.go, internal/graph/builder.go, internal/graph/builder_test.go, cmd/c4drill/root.go, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - All five Task 1 tests PASS.
    - Default path (Plain false) unchanged: all pre-existing builder tests stay green.
    - --plain flag shows in cobra help and compiles (full CLI E2E lands in plan 37-04).
    - --plain x --expanded works: processExpandedView also assigns v.Plain.
  </behavior>
  <action>
    1. internal/view/view.go: add `Plain bool` to View next to Edges (L33), doc-commented in the AllExpanded style ("Plain indicates author-custom formatting is ignored (--plain): unit color/style/border, link color/style/length/rank, properties.edges, and custom label formatting fall back to defaults").
    2. internal/graph/graph.go: add `Plain bool` to Graph next to EdgeStyle with a matching doc comment.
    3. internal/graph/builder.go:
       a. In BuildGraph (L22-29) and BuildExpandedGraph (L168-175): set `Plain: v.Plain` in the Graph literal; for EdgeStyle use a local (`edgeStyle := v.Edges; if v.Plain { edgeStyle = "" }`) so properties.edges is ignored under plain.
       b. applyUnitOverrides: add a `plain bool` parameter; when true, return immediately (skip every override). Update all four call sites: buildBoundaryCluster L127 and buildNestedCluster L221 pass v.Plain (both have v); buildNode and buildCluster gain a `plain bool` parameter passed from their callers — buildNode callers L63/L76/L103/L197/L265 and buildCluster callers L73/L100 all sit where v is in scope, so pass v.Plain; the recursive/nested child call inside buildCluster passes the same plain value down.
       c. createEdge: add a `plain bool` parameter; call sites L986 and L1033 pass v.Plain. Inside, when plain: treat link.Color as unset (custom colour branch skipped — the kindColour else-branch still runs), treat link.Style as unset, leave edge.MinLen = 0, leave edge.NoConstraint and edge.RankReverse false, and force the label position to "" so the existing L1116-1118 default-to-middle logic applies (ignore link.LabelPosition).
       d. Do NOT add any plain branch to kindColour, buildLegend, or legendKindEntries — kind colours and legend must survive plain mode untouched.
    4. cmd/c4drill/root.go: add `plain bool` to the //nolint:gochecknoglobals var block (L49-56); register `cmd.PersistentFlags().BoolVar(&plain, "plain", false, "Ignore author-custom formatting: default unit/edge styling, spacing and ranking, plain-text labels")` directly after the --expanded registration (L94-95). In processView, after each view.Generate*View call (L299-306) and before graph building, add `v.Plain = plain`; do the same in processExpandedView after GenerateExpandedView (L350). No generator signature changes.
    5. If any existing builder_test.go test constructs Graph literals positionally (unkeyed fields), fix those literals to keyed form — do not weaken tests.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/graph/ -run 'Plain' -v && go build ./... && go run ./cmd/c4drill --help 2>&1 | grep -c 'plain'</automated>
    Expected: all five Plain tests PASS; build clean; `--help` output contains the --plain flag (grep >= 1).
  </verify>
  <done>
    Plain threaded end to end (flag → View → Graph → guards) in both graph entry points; unit/edge/EdgeStyle/LabelPosition suppression in place; kind colours + legend untouched; default path unchanged; package suite green except the documented golden baselines deferred to 37-05.
  </done>
</task>

<task type="auto">
  <name>Task 3 (VERIFY): Default output untouched; suite categorized</name>
  <files>(no source edits — verification only)</files>
  <read_first>internal/graph/builder.go, cmd/c4drill/root.go</read_first>
  <action>
    Run go test ./internal/view/ ./internal/render/ and go test ./internal/graph/ (expecting only the documented CTX golden-baseline failures carried over from plans 37-01/37-02 — the count must NOT grow due to this plan; Plain defaults to false so no new deltas are possible; a new failure is a regression in the default path — fix it). Confirm with go vet ./internal/graph/ ./cmd/c4drill clean. Record the final expected-failure list in the summary. Do not modify testdata.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/view/ ./internal/render/ && go vet ./internal/graph/ ./cmd/c4drill && go test ./internal/graph/ 2>&1 | tail -8</automated>
    Expected: view+render PASS; vet clean; graph failures unchanged from the 37-01/37-02 documented list.
  </verify>
  <done>
    Plain-mode code changes produce zero default-output deltas; expected-failure list unchanged and recorded; vet clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv (--plain bool) → cobra → View.Plain | User-controlled flag crosses into view generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | --plain flag parsing | mitigate | Boolean cobra PersistentFlag — Cobra's parser rejects malformed values; no free-text, no injection surface. Flag only selects between existing code paths (guards), never concatenates into output. |
| T-37-06 | Repudiation | plain output claimed canonical | accept | Output is still deterministic DOT/SVG through go-graphviz; canonicality is enforced by tests (goldens), not a security property. |
| T-37-SC | Tampering | go module / toolchain | mitigate | No new third-party packages — cobra is already a direct dependency; all changes are stdlib/existing-code guards. Package legitimacy gate N/A (no npm/pip/cargo installs). |
</threat_model>

<verification>
- `go test ./internal/graph/ -run 'Plain' -v` → 5/5 PASS (suppression, kind+legend retention, both entry points, default-path lock).
- `go run ./cmd/c4drill --help` → lists --plain.
- `go test ./internal/view/ ./internal/render/` → PASS; default-output failure count unchanged from documented list.
- `go vet ./internal/graph/ ./cmd/c4drill` → clean.
</verification>

<success_criteria>
- `--plain` registered and assigned to every generated view in both processors (PLAIN-01 threading).
- Unit color/style/border, link color/style/length/rank, properties.edges, and label position all suppressed at builder level under Plain (PLAIN-01, PLAIN-02).
- Kind-derived colours and legend proven to survive plain mode (test 3).
- Zero default-path deltas (test 5 + unchanged failure list).
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-03-SUMMARY.md` when done.
</output>
