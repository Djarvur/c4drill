---
phase: 37-nesting-context-and-plain-rendering
plan: 04
type: tdd
wave: 4
depends_on: [37-03]
files_modified:
  - internal/render/converter.go
  - internal/render/converter_test.go
  - cmd/c4drill/root_test.go
  - cmd/c4drill/testdata/plain.toml
  - cmd/c4drill/testdata/plain.dot
  - cmd/c4drill/testdata/plain.expanded.dot
autonomous: true
requirements: [PLAIN-03, PLAIN-04]
tags: [plain, render, labels, e2e, go, tdd]

must_haves:
  truths:
    - "Under Plain, unit and cluster labels render as plain text (record path) — no custom HTML-table formatting; name/technology/description content preserved (PLAIN-03)"
    - "Under Plain, edge labels render as plain text via SetLabel — never the HTML rectangle SetLabelHTML path (PLAIN-03)"
    - "`c4drill --plain` produces consistent suppressed output for EVERY generated file — C1 context diagram, all drill-down views, in svg/html/dot formats (PLAIN-04)"
    - "`--plain --expanded` produces plain output too (Pitfall 5)"
    - "Plain-mode goldens are NEW files — no existing golden is re-baselined by this plan"
  artifacts:
    - path: "internal/render/converter.go"
      provides: "Plain label branches in setNodeLabel / setClusterLabel / edge-label emission"
      contains: "buildRecordLabel"
    - path: "internal/render/converter_test.go"
      provides: "Plain label unit tests (no HTML tables, plain edge labels, content preserved)"
      contains: "Plain"
    - path: "cmd/c4drill/testdata/plain.toml"
      provides: "Styled fixture exercising every ignored input: unit color/style/border, link color/style/length/rank/labelPosition, properties.edges, technology+description text, one expanded container"
    - path: "cmd/c4drill/testdata/plain.dot"
      provides: "Committed --plain C1 golden (canonical-compared)"
    - path: "cmd/c4drill/testdata/plain.expanded.dot"
      provides: "Committed --plain --expanded golden (canonical-compared)"
    - path: "cmd/c4drill/root_test.go"
      provides: "E2E --plain tests: golden comparisons + uniformity across all outputs and formats"
      contains: "plain"
  key_links:
    - from: "internal/render/converter.go (setNodeLabel/setClusterLabel)"
      to: "internal/render/labels.go (buildRecordLabel)"
      via: "plain branch routes labels to the existing plain-text path"
      pattern: "buildRecordLabel\\("
    - from: "cmd/c4drill/root_test.go (--plain E2E)"
      to: "cmd/c4drill/testdata/plain.dot"
      via: "canonical.Canonical comparison against committed golden"
      pattern: "canonical\\.Canonical"
---

<objective>
PLAIN-03 + PLAIN-04: complete the `--plain` surface at the render layer — labels become plain text (HTML-table formatting and HTML edge rectangles suppressed, content preserved) — and prove with E2E tests plus NEW committed goldens that `--plain` applies uniformly to every generated file (context diagram, drill-downs, svg/html/dot) including `--plain --expanded`.

Purpose: builder-level suppression (plan 37-03) clears the formatting DATA; this plan switches the label FORMATTING code paths, which are render-side only (RESEARCH: HTML label formatting needs render-side switches regardless). The E2E layer proves PLAIN-04's "every generated output file" contract.

Output: Plain branches in converter.go, unit tests, a styled fixture `plain.toml`, two new committed goldens, and E2E uniformity tests. Existing goldens are NOT touched (flag default false keeps default output unchanged; the CTX golden re-baseline belongs to plan 37-05).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/c4drill/.zcode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-RESEARCH.md
@.planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md
@internal/render/converter.go
@internal/render/labels.go
@internal/render/converter_test.go
@cmd/c4drill/root_test.go
@cmd/c4drill/testdata/multilevel.toml

<interfaces>
<!-- Contracts the executor needs. Verified against current source 2026-08-30. -->

Label dispatch fix point (converter.go L16-40 buildHTMLLabelForType; called from setNodeLabel L382-393 and setClusterLabel L475-486):
```go
func buildHTMLLabelForType(label *graph.Label, t model.UnitType) string {
    if label == nil { return "" }
    switch { case graph.IsPersonType(t): return buildPersonHTMLLabel(label); ... }
}
```
Plain-text label already exists (labels.go L13-42): `func buildRecordLabel(label *graph.Label) string` — builds "{name|technology|description}" parts. NOTE (PATTERNS): today even the record path is emitted via SetLabelHTML (setNodeLabel's default case, converter.go L37-38). Under plain the decision is SetLabel (TRUE plain text) — this plan pins that choice and the new goldens lock it.

Edge-label HTML rectangle emission (converter.go L567-580):
```go
    if htmlLabel := buildEdgeLabel(edge.Label); htmlLabel != "" { e.SetLabelHTML(htmlLabel) } else { e.SetLabel("") }
```
Plain: emit e.SetLabel(...) with the plain text (technology/description content) — never SetLabelHTML.

Rank sites (assert-only here — neutralized at builder in 37-03, all three render sites become inert):
1. endpoint swap converter.go L553-557 (`if edge.RankReverse { tail, head = target, source }`),
2. dir inversion in setEdgeDir L604-621,
3. constraint suppression L652-654 (`if edge.NoConstraint { e.SetConstraint(false) }`).

labelPosition: no converter change needed — builder forces "" under plain (37-03) and createEdge defaults "" to middle (builder.go L1116-1118).

Stay-unchanged under plain: legend (buildLegend reads FINAL edge colours — with custom colours suppressed, kind rows auto-adjust; do NOT special-case, ASSERT this emergent behavior), queue pipes (render/pipe.go).

E2E harness analog: existing root_test.go golden tests use canonical.Canonical (L1315, L1453): `require.Equal(t, canonical.Canonical(t, string(expected)), canonical.Canonical(t, string(dotData)))`. Import: github.com/Djarvur/c4drill/internal/testutil/canonical.
</interfaces>
</context>

<tasks>

<task type="tdd" phase="red">
  <name>Task 1 (RED): Plain label tests that fail because the converter has no plain branch</name>
  <files>internal/render/converter_test.go</files>
  <read_first>internal/render/converter_test.go, internal/render/converter.go, internal/render/labels.go</read_first>
  <behavior>
    - Test 1 — TestConverter_PlainNodeLabelsArePlainText: construct a graph.Graph{Plain: true, ...} containing a node whose Label has Name, Technology, Description set (follow the graph-construction pattern of the nearest existing converter_test.go label test). Convert to DOT (the converter's exported entry point used by existing tests). Assert: the emitted DOT does NOT contain the HTML table markers ("<TABLE" case-insensitive) for that node; the label content (name, technology, description strings) IS present in the DOT.
    - Test 2 — TestConverter_PlainClusterLabelsArePlainText: same with a cluster (expanded unit) label — no HTML markers, content present.
    - Test 3 — TestConverter_PlainEdgeLabelsArePlainText: graph with an edge carrying Label text. Assert: no HTML rectangle markers in the emitted DOT for the edge; the label text present via SetLabel (plain), not SetLabelHTML.
    - Test 4 — TestConverter_NonPlainLabelsUnchanged: identical graph with Plain: false still emits the HTML label path (HTML markers present) — locks the default path.
    - Test 5 — TestConverter_PlainKeepsLegendAndKindColour: graph with Plain: true, a legend, and a kind-coloured edge. Assert: legend statements and the kind colour value still present in the emitted DOT (semantic surface survives plain mode).
  </behavior>
  <action>
    Append the five tests to internal/render/converter_test.go following the existing construction/assertion patterns in that file (reuse helpers/imports). Tests must FAIL against current converter.go (no plain branch exists — plain graphs still emit HTML labels). Do NOT modify production code.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/render/ -run 'Plain' -v</automated>
    Expected: FAIL — plain graphs currently emit HTML labels. If any passes, revise the test to capture the actual gap.
  </verify>
  <done>
    Plain label unit tests exist and fail for the right reason; default-path lock (Test 4) passes. No production code changed.
  </done>
</task>

<task type="tdd" phase="green">
  <name>Task 2 (GREEN): Plain label branches in the converter (PLAIN-03)</name>
  <files>internal/render/converter.go</files>
  <read_first>internal/render/converter.go, internal/render/labels.go, .planning/phases/37-nesting-context-and-plain-rendering/37-PATTERNS.md</read_first>
  <behavior>
    - All five Task 1 tests PASS.
    - All pre-existing converter tests stay green (default path unchanged).
  </behavior>
  <action>
    In internal/render/converter.go:
    1. setNodeLabel (L382-393): when the graph carries Plain (the function's receiver/graph parameter — thread g.Plain or pass it, matching how the function already accesses graph context), set the node label via SetLabel with the output of buildRecordLabel (labels.go L13-42) instead of calling buildHTMLLabelForType/SetLabelHTML. Skip the icon/type-specific HTML dispatch entirely.
    2. setClusterLabel (L475-486): same plain branch — buildRecordLabel via SetLabel.
    3. Edge-label emission (L567-580): under plain emit e.SetLabel with the plain label text (derive from edge.Label the same fields buildEdgeLabel uses — technology/description content) and never call SetLabelHTML; keep the existing empty-label fallback behavior.
    4. Do NOT modify labels.go (buildRecordLabel already provides the plain-text path), buildLegend, or pipe.go.
    5. Rank/minlen/colour sites (L553-557, L604-621, L639-664, L652-654) need NO code change — builder neutralization (37-03) makes them inert; they are covered by the E2E assertions in Task 3.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./internal/render/ -count=1</automated>
    Expected: full internal/render package PASSES (new plain tests green, all existing tests green).
  </verify>
  <done>
    Plain node/cluster/edge labels render as plain text with content preserved; HTML label path untouched for default mode; render package fully green.
  </done>
</task>

<task type="auto">
  <name>Task 3 (E2E): Styled fixture, new --plain goldens, uniformity across every output (PLAIN-04)</name>
  <files>cmd/c4drill/testdata/plain.toml, cmd/c4drill/testdata/plain.dot, cmd/c4drill/testdata/plain.expanded.dot, cmd/c4drill/root_test.go</files>
  <read_first>cmd/c4drill/root_test.go, cmd/c4drill/testdata/multilevel.toml, skill/examples/04-styling.toml, skill/SKILL.md, .github/workflows/validate-skill-examples.yml</read_first>
  <behavior>
    - E2E: running the CLI with --plain on the styled fixture generates C1 + drill-down views in dot/svg/html; every dot file contains NO custom colour hex from the fixture, NO minlen, NO dir=back from rank=reverse links, NO constraint=false, and plain-text labels (no HTML table markers); kind-derived colours and the legend ARE present where kinds are used.
    - Committed goldens plain.dot and plain.expanded.dot match CLI --plain output under canonical.Canonical.
    - Default mode on the same fixture (no --plain) still emits the custom colours (opt-in proven at E2E level).
  </behavior>
  <action>
    1. Create cmd/c4drill/testdata/plain.toml — a small styled fixture exercising every ignored input (follow the authoring format in the c4drill-toml skill): properties edges = "straight"; one person/unit with explicit color, style, and border set to recognizable hex/keyword values; one expanded container with a nested subunit; two links — one with color, style, length = 3, rank = "reverse", labelPosition = "tail", technology and description text; one with rank = "equal" and a link Kind set (so a kind colour exercises the stays-rule).
    2. Generate the goldens with the CLI: run c4drill (go run ./cmd/c4drill) on plain.toml once with --plain -f dot into a temp dir and copy the C1 output to cmd/c4drill/testdata/plain.dot; once with --plain --expanded -f dot and copy to cmd/c4drill/testdata/plain.expanded.dot. These are NEW files — do not touch any existing golden.
    3. In cmd/c4drill/root_test.go add tests following the existing golden-test harness pattern (L1315/L1453 canonical style):
       a. TestPlainFlagC1Golden + TestPlainFlagExpandedGolden: run generation with --plain (and --expanded for the second), compare against the committed goldens with canonical.Canonical.
       b. TestPlainFlagAppliesToAllGeneratedFiles: run --plain -f dot into a temp dir; for EVERY generated .dot file (C1 and each drill-down), assert: none of the fixture's custom hex values appear; "minlen" absent; "dir=back" absent; "constraint=false" absent; "<TABLE" absent; the legend block marker present; the kind colour value present.
       c. TestPlainFlagAllFormats: run --plain with -f svg and -f html; assert every expected output file exists and is non-empty (svg/html exact formatting is graphviz-owned — existence + non-empty is the honest assertion; dot is the precise one).
       d. TestPlainFlagOptIn: run WITHOUT --plain on plain.toml (-f dot); assert the fixture's custom colour hex IS present in the C1 output (proves the flag is opt-in and default output keeps author formatting).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/c4drill && go test ./cmd/c4drill/ -run 'Plain' -v && go test ./internal/... 2>&1 | tail -6</automated>
    Expected: all new E2E tests PASS; internal packages unchanged (graph still shows only the documented CTX golden-baseline failures deferred to 37-05).
  </verify>
  <done>
    --plain proven uniform across every generated file and format (PLAIN-04); plain-text labels proven at E2E (PLAIN-03); new goldens committed; opt-in behavior locked; no existing golden modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author label text (TOML/C4D) → DOT emission under plain path | Untrusted label strings flow into the NEW SetLabel plain path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Tampering | plain-text label emission (SetLabel) | mitigate | SetLabel routes through the same go-graphviz escaping as every existing plain label in the codebase (the record path already used by setNodeLabel's default case) — no new string concatenation into DOT source is introduced; the plain branch reuses buildRecordLabel output through the existing API. E2E assertions on emitted DOT catch escaping regressions. |
| T-37-08 | Information Disclosure | plain output | accept | Plain mode renders a SUBSET of default formatting with identical content — no new data exposure. |
| T-37-SC | Tampering | go module / toolchain | mitigate | No new third-party packages (go-graphviz, testify, cobra all existing). Package legitimacy gate N/A. |
</threat_model>

<verification>
- `go test ./internal/render/ -run 'Plain' -v` → 5/5 PASS; `go test ./internal/render/ -count=1` → package green.
- `go test ./cmd/c4drill/ -run 'Plain' -v` → golden + uniformity + formats + opt-in tests all PASS.
- `cmd/c4drill/testdata/plain.dot`, `plain.expanded.dot`, `plain.toml` committed; zero modifications to pre-existing goldens.
- Default-mode HTML label path locked by TestConverter_NonPlainLabelsUnchanged and TestPlainFlagOptIn.
</verification>

<success_criteria>
- PLAIN-03: labels render as plain text under --plain with name/technology/description preserved, at node, cluster, and edge sites.
- PLAIN-04: --plain output verified uniform across the context diagram, all drill-downs, and svg/html/dot formats, including --plain --expanded.
- Legend + kind colours proven present in plain output (semantic boundary held).
- New goldens only — BC-01 discipline respected.
</success_criteria>

<output>
Create `.planning/phases/37-nesting-context-and-plain-rendering/37-04-SUMMARY.md` when done.
</output>
